# 代码与架构分析报告

- 日期：2026-08-24
- 范围：当前工作树（`src/**`、`scripts/**`、`docs/**`、`README.md`、`AGENTS.md`、`package.json`）+ 数据文件
- 方法：逐文件核对 + 静态引用 grep + `npm run test`（**229 passed**）
- 目的：在已修复 CODE_REVIEW.md(08-23) 的基础上，复核当前状态，找出仍存在的错误 / 不合理 / 可改进 / 过度设计

---

## 0. 与上次评审（CODE_REVIEW.md）的关系

本轮是 CODE_REVIEW.md 之后的状态复核。该报告列出的 P0（B1 计时重置 / B2 `useAI` 门控 / B3 `rubric.required` 注入）与 P1（B4 自适应 `profile` 断线 / B5 提前结束计 0 分 / A1 README pi-agent-core / nodeTypes 清理）**均已确认修复**：

- `interviewEngine.ts:133` 已有 `if (!def.useAI) return null;`
- `provider.ts:84` 已把 `requiredPoints` 注入评分（`mergeQuestionRubric` → `evalOpen`）
- `App.tsx` 倒计时锚定 `sessionStartedAt`、`handleFinishEarly` 先评分再入账
- `adaptive.ts:126` 已用 `recommendWeakTopics(profile, 5)` 兜底
- `nodeTypes` / `prerequisitesOf` / `InterviewSession.variants` 已删除

本轮**不再重复**上述内容，只报当前仍存在的问题。

---

## 1. 错误 / 不合理（按严重度）

### [中] 1.1 选择题错题污染 Learner Memory 的「薄弱要点」
- 现象：`gradeChoice` 判错时返回 `gaps: ['答案不正确，请参见解析']`（`domain/evaluation.ts:42`）。`sessionFromQuiz` 原样写入 `QuestionResult.gaps`（`domain/learner.ts:183`），`updateLearner.aggregateGaps` 据此统计 `commonWeaknesses`，`recommendationText` 又把它当作"你反复遗漏的要点"展示。
- 后果：用户只要选择题答错，掌握画像就会累积字面串「答案不正确，请参见解析」，并最终在首页/结果页的 AI 建议里被当成真实薄弱要点输出。这与 `types.ts:327` 对 `QuestionResult.gaps` 的定义（"开放题的遗漏/错误要点"）直接冲突——选择题本不应贡献 gaps。
- 修法（单行，不动结构）：`sessionFromQuiz` 改为
  ```ts
  gaps: format === 'choice' ? [] : (g?.gaps ?? []),
  ```

### [中] 1.2 package.json 仍声明已移除的 pi-agent-core 依赖 —— ⚠️ 已撤销
- **撤销说明（2026-08-24）**：原判断为"死依赖"是误报。`src/agent/`（Agent 面试运行时）现已正式引用 `@earendil-works/pi-agent-core`（`interviewAgent.ts` 构建 `Agent`、`tools.ts` 定义 `AgentTool`、运行时接线 `buildAgentRuntime`）。该依赖**保留且为必要依赖**，不再视为死代码。原"删一行"建议作废。

### [中] 1.3 `domain/knowledge.ts` 两个导出是生产死代码
- `knowledgeCoverage()`（`knowledge.ts:50`）：仅被自身测试引用；UI（ProgressPage）与 CLI（`scripts/question-coverage.ts` 用的是 `domain/coverage.ts`）都未使用。ADR-029 规定"节点必须有题目支撑"，所以它产出的 `gaps` 永远为空，纯属摆设。
- `KNOWLEDGE_AREA_LABELS`（`knowledge.ts:7`）：仅被自身测试引用，生产 UI 未消费。
- 修法（二选一，符合 AGENTS 原则 2）：删除这两个导出及对应测试断言；或若想保留 area 展示，让 ProgressPage 真正消费 `KNOWLEDGE_AREA_LABELS`。当前建议直接删——area 维度展示未列入产品计划。

---

## 2. 不合理的冗余 / 可简化（低）

### [低] 2.1 薄弱阈值 `WEAK_AVG` 与 `WEAK_MASTERY` 重复
- `isMastered` 用 `mastery >= WEAK_MASTERY || avgScore >= WEAK_AVG`（`learner.ts:35`），`recommendWeakTopics` 用 `mastery < WEAK_MASTERY && avgScore < WEAK_AVG`（`learner.ts:201`）。但 `mastery = round(avgScore/100*100)/100 ≈ avgScore/100`，两阈值本质同一条件（仅 84.5–85 的 rounding 带内有细微差异，且两处处理方式还不统一）。
- 修法：保留单一阈值 `WEAK_MASTERY`，去掉 `avgScore` 比较；`WEAK_AVG` 若无其他用途一并删除。

### [低] 2.2 README 残留 "Agent" 措辞
- `README.md:12`「开放题 Agent 多维评分」——pi-agent-core 已移除，开放题评分走 one-shot `ai/evaluate.ts`。改为「开放题四维评分」即可。

### [低] 2.3 `evaluate.ts` 重复定义 `DEFAULT_RUBRIC`
- `src/ai/evaluate.ts:22-27` 自带一份 `DEFAULT_RUBRIC`，与 `domain/evaluation.ts:13` 完全重复，仅作 prompt 提示兜底。建议从 domain 导入，避免两处漂移。

---

## 3. 仓库卫生（低，但符合 AGENTS「删死代码」）
- 根目录散落两份明显是工作草稿的文件：`缺口.json`（25KB，即 `npm run question:blueprint` 的产物 dump）与 `ttt/`（12 个日期命名 md 的会话笔记）。它们放在仓库根、且非构建所需，建议移出跟踪树（删除或进 `.gitignore` / 单独 scratch 目录）。`dist/` `node_modules/` 应已在 `.gitignore`，确认即可。

---

## 4. 设计合理性 / over-design 评估（你特别关心的点）

**总体结论：未过度设计，基本符合 AGENTS.md。**

- 分层清晰（`domain` 纯函数 / `ai` 适配层 / `application` 编排 / `storage` / `components`），且 `ai → domain` 只允许依赖纯计算函数，边界成文。✅
- 没有引入状态管理库、图数据库、Agent loop 等 AGENTS 明确反对的东西。✅
- LLM 严格藏在 `LLMProvider` 后，分数所有权在 domain，变体安全靠"收窄权限"而非"靠校验兜底"——这些 ADR 原则都落实且有测试。✅
- 我曾怀疑 taxonomy 合并后 `KnowledgeArea` 类型会残留已删 area；但核对 `knowledge/*.json` 的实际 `area` 取值，确认 8 个 area 全部仍在使用，类型与数据一致——**不是问题**，撤回。
- 唯一"重"的地方是 knowledge / coverage / blueprint 这套慢速生产管线。它体量不小，但属于 ADR-029/032 的刻意设计（题库建设工具），且 CLI 真实可用（`question:coverage` / `question:blueprint`）。问题不在"该不该有"，而在其中 `knowledgeCoverage` / `KNOWLEDGE_AREA_LABELS` 这一段没被接上（见 1.3）。**建议：要么消费、要么按 AGENTS 删，不要在"已设计未接线"状态停留。**

---

## 5. 修复优先级建议

| 优先级 | 条目 | 改动量 |
| --- | --- | --- |
| 中 | 1.1 选择题 gaps 污染 | 1 行 |
| 中 | 1.2 删 pi-agent-core 依赖 | ~~已撤销~~（已被 Agent 层启用，保留为必要依赖） |
| 中 | 1.3 删 `knowledgeCoverage` / `KNOWLEDGE_AREA_LABELS`（或接线） | 删导出 + 对应测试断言 |
| 低 | 2.1 阈值收敛 | 删冗余比较 |
| 低 | 2.2 / 2.3 README 措辞 / 复用 `DEFAULT_RUBRIC` | 小改 |
| 低 | 3 根目录草稿清理 | 删 / ignore |

> 按 AGENTS.md：任何影响架构/行为的改动落地时，同批更新 `docs/CHANGELOG.md`。本分析涉及的 1.1 / 1.2 / 1.3 属行为/契约变更，建议在 CHANGELOG 追加一条。
