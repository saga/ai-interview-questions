# 离线变体池（题库资产）存放处：每个 batch / topic 一个 JSON（VariantPool 形态）。
# 由 `npm run question:variants` 离线生成并提交进仓库；运行时经 variantBank 的 import.meta.glob 合并加载。
# 目录为空时 variantPool 退化为 EMPTY_VARIANT_POOL（Pool-first 自动 no-op，不联网、不报错）。
