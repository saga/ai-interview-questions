# 双引擎统一改造方案

> 创建时间：2026-08-29；状态：已完成（Phase 1-4）
> 背景：`架构分析.md` 指出双引擎（`application/interviewEngine` + `agent/interviewAgent`）并存导致核心逻辑重复，随功能演进维护成本线性增长。本文档描述具体的改造方向与行动 checklist。

---

## 一、问题定位：哪里在重复？

两条路径共享 `domain/` 的纯函数，但在**衔接层**（调用 domain + LLM 的胶水代码）上存在以下三处重复：

| 能力 | 确定性引擎位置 | Agent 工具层位置 | 重复程度 |
|------|---------------|----------------|---------|
| **单题评分**（选择题 gradeChoice + 开放题 LLM） | `interviewEngine.ts::evaluateAnswer` | `tools.ts::evaluateSessionQuestion` | ★★★ 高（逻辑几乎相同，仅入参结构不同：引擎接受 `def.scoringRubric`，Agent 用 `DEFAULT_RUBRIC`） |
| **变体生成 + 回退**（LLM variant → validateVariant 兜底） | `interviewEngine.ts::finalizeQuestion`（私有） | 未实现（Agent 路径不做变体）| ★☆☆ 低（Agent 不需要，但若日后加入需手写一份） |
| **答题判空**（`undefined / null / '' / []`） | `interviewEngine.ts::evaluateAnswer` 内联判断 | `tools.ts::isAnswerEmpty`（模块级私有） | ★★☆ 中（逻辑相同，字面重复） |
| **开放形态概率 / `availableFormats` 过滤** | `interviewEngine.ts::effectiveFormats` | `tools.ts::createAgentTools` 内多处直接 `availableFormats(q, [...])` | ★★☆ 中（同一意图的代码不在同一函数） |

最大的维护隐患是**单题评分**：两处评分口径（`def.scoringRubric` vs `DEFAULT_RUBRIC`）已是一个隐性分歧点——将来调整评分维度时，若只改一处，两个引擎会给出不同分数，而测试不会直接发现它。

---

## 二、目标状态

```
application/
  interviewEngine.ts      ← 不变：确定性会话编排（buildSession / nextAdaptiveStep / evaluateSession）
  sessionEvaluator.ts     ← 新：单题评分核心（isAnswerEmpty + evaluateSingleQuestion）
                             ↑ 被确定性引擎和 Agent 工具层共同 import

agent/
  tools.ts                ← 移除 evaluateSessionQuestion / isAnswerEmpty；改 import sessionEvaluator
  interviewAgent.ts       ← 无需改动（仅依赖 tools.ts）
```

改造不涉及 domain/、schemas/、storage/，也不改变任何对外 API 或 UI 行为。

---

## 三、行动 Checklist

### Phase 1：提取单题评分核心（最高优先级，消除评分口径分歧）

- [x] **新建 `src/application/sessionEvaluator.ts`**
  - 将 `tools.ts::isAnswerEmpty` 移入，改为 `export`
  - 将 `tools.ts::evaluateSessionQuestion` 移入，改为 `export`
  - 参数签名升级：接受可选的 `rubric?: ScoringRubric`（缺省用 `DEFAULT_RUBRIC`，确定性引擎传 `def.scoringRubric`），消除评分口径分歧
  - 补充 `sessionEvaluator.test.ts`（至少覆盖：选择题判分、开放题走 LLM、未作答返回 null、rubric 覆盖生效）

- [x] **修改 `src/agent/tools.ts`**
  - 删除 `isAnswerEmpty` 和 `evaluateSessionQuestion` 的本地实现
  - 改为 `import { isAnswerEmpty, evaluateSessionQuestion } from '../application/sessionEvaluator'`
  - 调用时不传 rubric（使用 DEFAULT_RUBRIC 缺省值），行为与现在一致

- [x] **修改 `src/application/interviewEngine.ts`**
  - `evaluateAnswer` 内的判空逻辑改为 `import { isAnswerEmpty } from './sessionEvaluator'` 
  - 开放题评分（`provider.evaluateOpenAnswer` 调用处）抽取为调用 `evaluateSessionQuestion`（传 `def.scoringRubric`）
  - 确认现有 `interviewEngine.test.ts` 仍全量通过

- [x] **跑全量测试** `npm run test` — 确保无回归

---

### Phase 2：统一答题判空（消除字面重复，Phase 1 完成后处理）

> Phase 1 已把 `isAnswerEmpty` 导出到 `sessionEvaluator.ts`，此阶段只需确认所有调用点都 import 同一份。

- [x] 全局搜索 `answer == null`、`answer.trim() === ''`、`.length === 0` 的判空散落点；确认剩余 `domain/variant.ts` 检查的是 LLM 变体答案结构，不属于用户作答判空
- [x] 各评分调用点统一使用 `sessionEvaluator.isAnswerEmpty`（通过 `evaluateSessionQuestion`），删除引擎与 Agent 的内联实现
- [x] 补充测试覆盖边界值（`null` / `undefined` / `[]` / `''` / 空白串）

---

### Phase 3：对齐 `effectiveFormats` 过滤（消除意图重复，Phase 2 完成后处理）

- [x] 将 `interviewEngine.ts::effectiveFormats` 移入 `sessionEvaluator.ts` 并改为 `export`
- [x] `agent/tools.ts::createAgentTools` 中所有直接写 `availableFormats` 的地方，替换为共享 `availableSessionFormats`
- [x] 更新相关测试，并由既有 Agent 工具测试覆盖题型过滤行为

---

### Phase 4：Agent 路径变体生成

> Agent 运行时已确定作为未来正式方向保留，故执行此阶段。

- [x] 将 `interviewEngine.ts::finalizeQuestion`（变体生成 + 回退）移入 `sessionEvaluator.ts` 并导出
- [x] `agent/tools.ts::getQuestion` 工具在选定题目后调用 `finalizeQuestion`，使 Agent 路径也享受 LLM 变体
- [x] 补充变体生成的 mock 测试

---

### 验收标准

- [x] `npm run test` 全量通过（无跳过、无新 skip）
- [x] `npm run typecheck` 无报错
- [x] `src/agent/tools.ts` 中不再存在 `evaluateSessionQuestion` 或 `isAnswerEmpty` 的本地实现
- [x] 两条引擎路径的单题评分均通过 `sessionEvaluator.ts` 同一函数；rubric 默认值集中在共享函数，确定性引擎可传入 definition rubric

---

## 四、注意事项

1. **不改 domain/**：本次重构只动 `application/` 和 `agent/`，domain 保持纯函数不变。
2. **不改 UI 行为**：用户看到的评分结果、题目选取逻辑不变；评分口径统一后，选择题分数 100% 一致（之前也一致），开放题 rubric 统一后理论上可能有极小差异（现在 Agent 用 DEFAULT_RUBRIC，确定性引擎用 def.scoringRubric），但 DEFAULT_RUBRIC 就是 InterviewDefinition 默认值，正常情况下无感知差异。
3. **测试先行**：Phase 1 新建 `sessionEvaluator.test.ts` 后，再做迁移，确保行为有锚点。
4. **双引擎并存本身不需要消除**：Agent 面试和确定性面试的「会话编排」职责不同（一个是 LLM 驱动决策，一个是规则驱动），并行是合理的；需要消除的只是**衔接层的重复实现**。
