"""把「电报体」解析（…、D称…错误）改写为可读散文，并补齐错误项归因。"""
import json, pathlib, re

ROOT = pathlib.Path("src/data/questions")

NEW = {
# ── ai-architecture.json ────────────────────────────────────────────────
"agent-cot-switch-01":
 "不同供应商的推理模型各自训练出的内部思维链（Reasoning Tokens）在标记、结构与语义约定上并不通用，也没有公开标准，因此跨族切换时旧思维链无法被新模型当作自身推理继续解析，通常只能丢弃——这会损失一部分已建立的推理上下文。"
 "但丢失的是思维链，工具调用历史与返回结果仍以结构化形式留在上下文里，Agent 可以据此继续执行，代价只是后续单步质量轻微下降。"
 "D 错在把「用 JSON 封装 Context」当成了思维链互认：JSON 只统一了外层信封，解决不了不同模型内部推理 token 的序列化差异，更谈不上无损共享推理过程。",
"agent-gateway-01":
 "Gateway 的价值在于把三件事收敛到一处：统一请求/响应格式以屏蔽各家协议与数据结构差异；把「协议适配」与「路由决策」解耦，使换模型、调路由都能靠配置生效而无需改动客户端；"
 "集中托管供应商账号与密钥，避免 API Key 散落在上游代码里，同时让成本核算能按业务维度拆分。"
 "D 错在凭空发明了一个能力：不存在把自然语言 Prompt 编译成「模型字节码」的机制，语义理解的差异只能靠提示工程、评测与模型选型来收敛，不可能被某个编译层消除。",
"agent-stateless-01":
 "把状态外置、让模型调用保持无状态，换来三个工程收益：状态可序列化落盘，于是崩溃后可恢复、长任务可暂停重启；每次调用都从权威数据源重建上下文，模型看到的视图与数据库真实记录同步，不会基于陈旧副本决策；"
 "服务实例本身无状态，可以随意放在负载均衡后扩缩容。D 颠倒了可靠性来源：上下文窗口只是易失的模型输入，既没有事务也没有持久性保证，长周期一致性恰恰只能由外部数据库提供。",
"agentic-world-01":
 "文本大模型做的是「上文 → 下一个 token」的自回归预测；世界模型做的是「当前状态 + 给定动作 → 下一状态」的预测，正因为能前向模拟，它可以在仿真里低成本、零风险地生成现实难采集的边缘场景数据。"
 "VLA 则是把视觉观测与语言指令映射到具体动作，端到端输出控制量。D 错在层级错配：世界模型产出的是状态预测与潜在表征，底层电机控制仍需控制器、动作预测与实时反馈回路，不存在直接驱动硬件的通路。",
"ai-unified-01":
 "统一基座有三项收益：推理与表征能力作为共享 Backbone 被各领域模型复用，省掉重复造轮子；算力与研发集中到单一基础模型，底层能力升级会同时惠及所有业务线；跨领域共享的隐式表征打通了模态与场景，提升未见场景的泛化。"
 "C 错在把「共享基座」等同于「部署规格不可变」：基座本身可以裁剪层数与隐藏维度、可做蒸馏与稀疏化，各领域的任务头也可以独立挂载，轻量化并不只能靠蒸馏出一个新模型。",
"harness-code-01":
 "Code Mode 的关键是把「一次往返调一个工具」改成「一次往返跑一段脚本」：模型直接生成含业务逻辑与多次工具调用的完整脚本，在 Harness 沙箱里执行，工具以本地函数形式暴露；"
 "于是数千行检索结果留在沙箱内存中过滤、拼接，只有提炼后的结论写回 LLM 上下文，省下大量往返与 token。"
 "D 错在因果不对：省掉的是工具调用的往返次数与中间数据的上下文占用，用户意图的解析仍然由 LLM 完成，沙箱运行时不做意图理解。",
"net-napt-01":
 "出站时 NAPT 把源私有 IP 改为公网 IP，并动态分配新源端口写入映射表；入站时转换表是状态化的，只有「目标 IP + 目标端口」命中活跃表项才会转发；未配置端口映射或 UPnP 时，外网主动发起的未邀连接会被默认丢弃。"
 "B 错在忽略了端口这一维：多个内网主机共享同一个公网 IP，仅凭目标 IP 根本无法区分该交给哪台主机，必须靠 NAPT 分配的端口号回查映射表。",
"realtime-bottleneck-01":
 "实时多模态架构有三个硬约束：连续音视频流在时间轴上产生高密度 token，长会话会迅速撑爆上下文窗口，必须依赖流式会话缓存与裁减策略；200ms 级双向流对网络抖动与丢包极其敏感，体验会阶跃式劣化；"
 "严格的延迟预算直接限制单次微轮次能激活的参数量，千亿级全激活单体模型难以直接上前台。"
 "D 错在把延迟预算当成了能力禁区：微轮次放弃的是在响应窗口内同步等待慢工具，外部 API 仍可由后台异步路径或快慢双路架构来承担。",
"realtime-micro-01":
 "微轮次以固定时间片（如 200ms）为基本处理单位，模型在每个时间片内同时吃进多模态输入流并产出输出流，输入输出在时间轴上连续重叠，于是原生支持边听边说、边看边说，可用于实时口译、同传纠错与动作实时计数。"
 "传统轮次制即使把管道压到 200ms 以内，感知与生成在单次执行逻辑里仍然互斥，无法在被打断的瞬间基于视觉事件实时响应。"
 "C 错在混淆了交互模型与计算量：时间切片改变的是调度粒度，复杂长文本推理的算力开销并未消失，延迟仍需通过模型规模与级联设计来权衡。",
"realtime-pipeline-01":
 "管道式语音系统有三处结构性缺陷：VAD 只依据声学特征判断停顿，没有语义理解，因此做不了「语法一错就打断」这类语义级主动打断；LLM 生成期间感知输入是冻结的，用户中途新增的语音或视觉修改只能排队到下一轮；"
 "VAD、Dialog Manager 这类硬编码启发式正是「苦涩的教训」所指——随算力与端到端模型规模增长，终将被学习出来的模型取代。"
 "D 错在定位错了瓶颈：双工听写与视觉打断要求感知与生成在时间轴上重叠，纯串行管道无论 STT/TTS 多快都做不到。",
"redis-cow-01":
 "fork() 时并不物理复制主进程内存，子进程与主进程共享同一批物理页并标记为只读；快照期间主进程一旦写入，触发缺页异常，内核才复制被修改的那一页，主进程在新副本上改，子进程继续读旧页，因此不阻塞。"
 "代价是写放大：并发写密集时会复制大量页，内存占用逼近翻倍，极易触发 OOM。"
 "D 错在把 COW 理解成实时同步：子进程看到的是 fork 那一刻的内存快照，快照期间主进程的后续修改落在新副本上，不会进入子进程写入磁盘的 RDB。",
"selfhost-api-01":
 "自建推理栈在流量规模化后的收益有三类：可针对自身负载模式（输入长度分布、并发特征）调优延迟 Profile，不受公共 API 兼顾通用吞吐的限制；通过专用部署与高可用设计把 SLA 从约 99% 提到 99.99% 以上；"
 "请求量足够大时摊薄硬件与研发投入，单位请求成本显著下降。D 不是收益：TCO 与流量规模强相关，低流量阶段自建要承担固定的硬件与运维成本，反而不如按量付费，所谓「任何阶段 TCO 均更低」不成立。",
"task-ai-05":
 "双边平台的客服纠纷涉及买家、卖家与平台三方状态，单线 Chat 加会话记忆并不足以显式跟踪「谁在等什么、当前处于哪一裁决阶段」，状态机也并非前 LLM 时代的遗产——它承担的是可审计的流程约束，不是记忆。"
 "三种可行范式各有侧重：自主裁决用历史判例训练概率模型，置信度足够时直接执行退款或裁定；赋能范式按订单资产数据生成建议回复，把摩擦消弭在买卖双方之间以减少工单；"
 "全局路由与摘要范式解决大规模跨国团队的多语言无损传递，压降转交后的 AHT。",
# ── inference.json ─────────────────────────────────────────────────────
"agent-cache-routing-01":
 "Prompt Caching 只降低了被缓存前缀那部分的输入单价，而高并发 Agent 每轮都会新增工具输出与递增历史，这些无法缓存的增量累积起来依然是主要成本；"
 "若所有请求默认走最贵的前沿模型，即便命中率很高，未命中部分的绝对单价乘以高频请求量仍构成巨大的成本基数。"
 "两者作用维度不同：Caching 解决重复上下文的传输单价，Smart Routing 解决请求难度与模型算力的匹配，应当配合而非二选一。"
 "D 错在把命中率当成充分条件：命中率与省下的绝对金额之间还隔着「未命中部分的单价 × 请求量」，80% 命中率并不保证降本优于把简单任务路由到廉价模型。",
"ai-infra-001":
 "分页式 KV Cache 借鉴 OS 虚拟内存，把逻辑上连续的 KV 切成固定大小物理 Block 按需分配，消除预分配带来的显存碎片；前缀树复用则按 token 序列组织 KV，跨请求匹配重叠前缀并共享，多轮对话与 Agent 场景下能显著降低 TTFT。"
 "二者并不互斥：分页解决的是物理块的分配粒度，块本身可以被多个请求共享（配合引用计数），前缀复用正是建立在分页之上。"
 "D 错在认为分页只能请求内分配——可共享的物理块恰恰是前缀复用得以实现的前提。",
"ai-quant-fp4-01":
 "4-bit 预训练首先要硬件原生支持（如 FP4 张量计算单元），否则低精度只能靠反量化模拟，拿不到收益；相比先高精度训练再后量化，从预训练阶段就引入低精度能让权重主动适应有限的数值范围，从而缩小后量化的精度落差；"
 "其收益主要是省内存带宽与数据传输开销，并加快矩阵乘。D 错在因果倒置：反向传播确实常用高精度影子权重与补偿累加器，但权重本体是低精度的，这部分额外开销远小于省下的显存与带宽，总体上仍是显存下降、吞吐上升。",
"harness-api-01":
 "API 层对 TTFT 的影响主要来自分词与安全审查两处：无状态请求每次都要对全量历史重新分词，CPU 耗时随上下文长度线性增长；改为会话保持后可在内存里维护已分词序列，只对客户端新增的 delta 增量分词并追加，单次耗时接近 O(1)。"
 "安全审查则可与 LLM 的 Prefill 并发启动，用首包生成前的等待窗口把校验延迟掩盖掉。"
 "D 错在把并发当成浪费：审查不通过时确实会损失一部分 GPU 算力，但换来的是绝大多数请求下 TTFT 的显著下降，这是标准的延迟隐藏取舍，不是反模式。",
"harness-router-01":
 "缓存感知路由要解决的是「KV Cache 在哪」与「节点还有多少余量」的联合决策：路由到未持有该会话缓存的节点，新节点必须为全部历史 token 重做 Prefill，代价极高；"
 "但如果只盯命中率，把请求持续压给已超载节点，队列延迟又会吃掉缓存收益，因此必须同时看队列深度。KV Cache 还占用大量显存，需要按真实访问痕迹制定淘汰与分层策略（下沉 Host 内存或驱逐）。"
 "D 错在把会话粘性当成硬约束：负载失衡时应允许打散会话，并用扩容、迁移与共享前缀缓存来兜底，绝对粘性会让热点节点直接成为瓶颈。",
"pd-disagg-01":
 "PD 分离的动机是 Prefill 与 Decode 的瓶颈不同——前者算力受限、后者带宽受限——因此可以分别选型：Prefill 集群上高算力卡，Decode 集群上高带宽卡。"
 "代价是 Prefill 产出首 token 与 KV Cache 后，必须经高速网络把 KV 传给 Decode 节点，传输开销真实存在；所以实践中会用条件分离，让短 Prompt 或前缀命中的请求跳过解耦，在 Decode 节点本地完成以省掉传输。"
 "D 错在宣称零开销：KV 的传输量与延迟正是 PD 分离能否划算的关键变量，两阶段还需要在调度、批处理与故障恢复上协同，绝不是两组互不相关的无状态节点。",
# ── agent-fundamentals.json ────────────────────────────────────────────
"agent-control-01":
 "生产级 Agent 的控制流应当「代码为主、模型为辅」：主循环由状态机与条件分支这类确定性代码掌控，模型只在需要非结构化推理或开放式决策的节点被调用；"
 "任何循环都必须配硬性逃生机制（最大迭代数、超时、确定性完成判定），否则死循环会直接变成账单；模型输出尽量约束成 JSON 等结构化数据，外层代码才能精确解析并驱动下游。"
 "D 错在把协调问题归因为「不够内聚」：让元 Agent 独占全局状态机、子 Agent 无状态，只是把不确定性集中到单点——状态越集中，一次误判的影响面越大，而且失去了外层代码的确定性校验，可靠性反而下降。",
"guard-inout-01":
 "输入护栏与输出护栏守的是两个方向：输入侧防数据泄露与非法注入——检索权限控制、敏感信息脱敏、边界隔离；输出侧防 Agent 产出幻觉动作或虚构事实——工具校验、接地校验与内容安全过滤。"
 "二者不可互相替代：Agent 是否调用了未注册或越权的工具，只有在它输出调用意图的那一刻才能判定，因此必须由输出侧的工具校验拦截。"
 "D 错在把日志审计当成阻断手段：审计只能事后追溯，越权调用一旦执行就已经产生副作用，输入侧再严也拦不住模型「自己想出来」的非法动作。",
# ── context-engineering.json ───────────────────────────────────────────
"token-rightsize-01":
 "把 100K token 的原始返回直接塞进上下文，代价是三重的：Prefill 变长直接推高 TTFT，且这部分 token 每轮都要重新付费；长上下文里的注意力被稀释，模型提取「订单状态」这类关键字段的精度下降（Needle in a Haystack）；"
 "无关字段越多，模型张冠李戴、误用字段的概率越高。D 错在把「上下文丰富」当成无条件收益：上下文的价值取决于信噪比，全量字段只会降低信噪比。正确做法是在工具层做字段裁剪与摘要，只把结构化后的必要字段交给模型。",
# ── cost-performance.json ──────────────────────────────────────────────
"agent-cost-01":
 "Agent 的 token 消耗之所以远超单次对话，根源是 LLM API 无状态：每一轮都要把系统指令、工具 Schema、历史调用与返回结果整体重发，上下文随轮次单调递增，而每轮计费又按全量输入计，因此总量随轮次呈超线性累积。"
 "此外 Agent 没有人类的输入与阅读节奏限制，可以在 API 允许的速率下连续高频发起请求，「自然制约」被消除了。"
 "D 错在假设服务商暴露了隐状态写入接口：外部系统既读不到也写不了模型的 hidden states，要复用历史只能靠重传上下文，或改用服务端会话、前缀缓存这类由供应商提供的机制。",
"agent-jevons-01":
 "这是典型的杰文斯悖论：单次成本下降后，原本因为太贵而不可行的场景被释放出来，调用量暴涨，最终账单不降反升。因此降单价不等于控预算，必须把硬约束放在架构与管理层——设定团队或全局的月度预算上限，并在接近阈值时降级模型或限流。"
 "优化目标也应从「降低单次调用价格」换成「在固定预算内交付最大业务价值」。"
 "D 错在把反弹效应当成路由失灵：调用量上升恰恰说明路由生效、更多场景变得划算；切回旗舰模型只是把单价提回去，既压不住被激发出来的需求，也会直接砍掉已经跑通的业务。",
# ── evaluation.json ────────────────────────────────────────────────────
"eval-grader-01":
 "评测架构必须随系统形态变化。RAG 要把评估拆成两段：检索阶段看 Document Recall/Relevance，生成阶段看 Faithfulness/Hallucination——只看最终答案，永远分不清是没召回还是编了。"
 "Coding Agent 有可执行的产物，最客观高效的 Grader 是跑单元/集成测试，而不是让另一个模型给文本打分。Multi-Agent 除了任务完成度，还要评任务路由是否准确、角色契约是否被遵守、节点协作是否高效。"
 "D 错在把输出端分数当成整体质量的充分条件：LLM-as-Judge 只看结果文本，架构层面的检索漏召、路由错配、协作冗余都可能被一个「看起来不错」的答案掩盖。",
# ── llm-fundamentals.json ──────────────────────────────────────────────
"mem-longctx-01":
 "把全部历史拼进 Context Window 有三个递增的代价：token 计费与处理开销随历史线性增长，直接推高成本；Prefill 变长使 TTFT 上升，响应变慢；长文本下注意力分布不均，出现 Lost-in-the-Middle——放在 Prompt 中间的关键信息反而最容易被忽略。"
 "D 错在把「容量」当成「检索」：扩大窗口只是让更多 token 进入注意力，注意力本身并不提供精确定位能力，窗口越长反而越稀释。任意历史细节的可靠命中，仍要靠外部检索与结构化记忆。",
# ── memory.json ────────────────────────────────────────────────────────
"mem-retrieval-01":
 "检索策略必须在相关性与时效性之间取平衡：过度偏向相关性，Agent 会被高相似度但已过时的历史反复吸引；过度偏向时效性，又会丢掉早期对话里关键但时间久远的约束条件。"
 "组装上下文时还要考虑 Lost-in-the-Middle——把最重要的召回内容放在 Prompt 开头或结尾，比塞在中间更容易被模型注意到。"
 "D 错在假设模型能自动降噪：召回内容一旦进入上下文就会被同等对待，无关或陈旧信息会挤占注意力、干扰判断，模型并没有可靠的「自动忽略」机制。",
"mem-risk-01":
 "压缩与持久化都会引入新的失效模式。摘要是有损压缩，日期、精确名称、具体承诺这类可核查细节最容易被平滑掉，只剩泛化主题；长期记忆若缺少失效与淘汰机制，用户偏好在现实中变了，库里仍会高置信度地把旧事实喂给模型；"
 "持久化层还可能被记忆毒化——恶意指令一旦写入，会在后续每次检索中反复污染 Prompt。"
 "D 错在把「意识到不确定」的责任推给模型：压缩丢失的细节在记忆里不留痕迹，模型看到的只是一句通顺的概括，无从判断缺了什么，也就不会主动发起确认。",
# ── model-architecture.json ────────────────────────────────────────────
"gqa-swa-01":
 "GQA 的做法是让多个 Query 头共享同一组 Key-Value 头，KV Cache 的量级因此按 KV 头数成比例下降，直接省下显存占用与解码时的内存带宽。"
 "注意它并没有改变 KV Cache 随上下文长度线性增长这一关系，只是显著压低了增长斜率；端侧还可以把 GQA 与滑动窗口注意力或跨层 KV 共享叠加，进一步给 KV Cache 设上限。"
 "C 错在混淆了两类显存：GQA 优化的是运行时的 KV Cache，与权重参数无关；把权重从 16-bit 压到 4-bit 是量化做的事，两者作用对象不同。",
# ── multi-agent.json ───────────────────────────────────────────────────
"agent-proto-01":
 "两类协议的定位不同。MCP 解决 Agent 与工具/上下文服务的对接：调用基本是单向、无状态的，由宿主 Client 负责格式化与路由，Server 执行后返回。"
 "A2A 解决 Agent 之间的委托与协作：双方都是具备自主推理能力的主体，需要服务发现（Agent Card/Manifest 暴露能力与元数据）、任务状态机，以及子 Agent 发现信息不足时进入 Input-Required 状态、把控制权与上下文回抛给上游。"
 "D 错在把「能封装成工具」等同于「能协作」：MCP Tool 的调用是一次性的请求-响应，无法表达异步流式协商、多轮澄清与跨 Agent 的状态机同步。",
"agentic-67":
 "用确定性规则替代真实 LLM，换来的是评测基础设施本身的可信：100% 可审计、比特级可复现、零 API 开销，且能准确测出拓扑几何分布、消息路由规则、链路利用率这类纯结构与工程指标。"
 "代价也很明确：真实 LLM 的推理噪声、指令遵循偏差与幻觉对通信量和重试链的影响完全消失了。"
 "C 错在两头都占：确定性规则并不复现 LLM 的输入输出分布——它省掉的恰恰是 LLM 特有的方差，因此结论只能外推到结构性指标，不能外推到生产环境的语义行为；人工抽检可以补充，但补不回被系统性移除的那部分分布。",
# ── observability.json ─────────────────────────────────────────────────
"agent-observability-01":
 "LLM 系统的成本驱动力是 token 数量而不是请求次数：一次携带长上下文的调用，其 Input Token 可能抵得上成千上万次短调用，这正是「低频模块吃掉 70% 支出」的成因。"
 "因此成本监控必须下沉到 token 粒度，记录每次请求的 Input/Output token，再按任务类型与功能标签聚合，才能定位到真正的成本中心。全库代码规划、长文档分析这类任务调用频次低但上下文极大，总消耗完全可能远超高频但短上下文的拼写检查。"
 "D 错在用单价替代用量：Output 单价高不等于它主导账单，长上下文场景下 Input token 的绝对量往往才是大头，只盯 Output 会把成本归因完全做反。",
# ── prompt-injection.json ──────────────────────────────────────────────
"guard-input-mask-01":
 "脱敏不是免费的：一旦把字段替换掉，模型依赖的关联信息与相似度线索也随之消失，关联分析、相似度匹配这类任务会直接失败。"
 "ZDR（零数据保留）提供了另一条路径——由供应商以合规合约承诺不存储、不训练输入数据，从而在保留完整上下文的前提下满足隐私要求；对上下文依赖度高的复杂推理，私有化部署加 ZDR 通常比全局开启脱敏更合适。"
 "D 错在把两件事混为一谈：脱敏管的是「进入模型的数据长什么样」，输出护栏管的是「模型想做的动作是否合法」，前者无论做得多严都拦不住一次越权的工具调用。",
# ── rag.json ───────────────────────────────────────────────────────────
"graphrag-01":
 "向量检索的机制决定了它不擅长全局聚合：它按查询与 chunk 的语义距离召回，倾向于找回含「频繁」「重复」这类同义词的片段，而不是语料背后的统计分布；"
 "而「出现频率最高的根本原因」这类答案本就以分布式形态散落在整个语料里，不存在少数几个可被距离精确定位的文本块。扩大窗口只增加了接收量，没有改善召回的全局代表性，模型仍会在有偏的召回上做表面推理。"
 "D 错在过度泛化：向量检索在局部点查（Local Query）上通常表现良好，图谱的优势集中在需要跨实体关系与全局聚合的问题，不能说点查「必然落后」。",
"graphrag-03":
 "社区层级本质上是一个详尽度与成本的旋钮：层级越深、社区越小，报告越细，回答的详尽度（Comprehensiveness）越高，但需要 Map-Reduce 汇总的报告数量剧增，token 消耗与端到端延迟随之飙升；"
 "反过来选最宏观的 Level 0，成本最低、响应最快，代价是可能漏掉子领域的关键细节。"
 "D 错在把索引期产物当成查询期产物：社区划分与社区报告都是在索引阶段一次性构建好的，查询阶段只是在既有层级中挑选；选哪一层既决定了查询成本，也反过来决定了索引要为多深的层级付费。",
"graphrag-04":
 "两条路径的执行逻辑不同。Local Search 从与查询最匹配的实体出发，在图上向外扩展，收集关联的文本块、社区报告、邻居实体、关系与断言（Claims），统一排序筛选后填进一个上下文窗口。"
 "Global Search 则不在查询时做实时图遍历，而是直接用预先生成的社区报告做 Map-Reduce：Map 阶段把分批且打乱顺序的报告并行交给 LLM 打分并产出中间答案，Reduce 阶段挑选高分中间点合成最终回答。"
 "D 说反了：Local 是从种子实体出发的局部扩展，不需要扫描全图；真正要处理全量社区报告、token 消耗更高的恰恰是 Global Search。",
"graphrag-05":
 "LazyGraphRAG 的核心是把 LLM 的工作从索引期挪到查询期：索引阶段尽量少调甚至不调 LLM（用传统 NLP 抽实体词、借社区检测做划分），构建成本因此降到接近传统向量 RAG 的量级，"
 "代价是被推迟的抽取与推理要在查询时补上，查询延迟与 token 上升。标准 GraphRAG 则相反，索引期一次性投入换来查询期的稳定，并且它产出的实体描述与社区摘要本身就是可独立阅读、分享的人类资产。"
 "D 错在把「推迟建图」理解成「取消索引」：Lazy 仍然依赖向量索引做候选定位，图结构也只是推迟而非取消，索引从来不是可以整体下掉的构建期负担。",
# ── training.json ──────────────────────────────────────────────────────
"chinchilla-overtrain-01":
 "Chinchilla 的 1:20 是 compute-optimal，其目标函数只有一个——在固定预训练算力预算下最小化训练 loss，完全没有把上线后按请求反复计费的推理成本算进去。"
 "而实际生产中 serving 成本是按请求累加的长期 recurring cost，规模化后通常远超一次性的预训练投入。因此对高频场景，用小模型 + 数万亿 token 过度训练是划算的：多付一次性的预训练算力，换一个更小、更快、单请求更便宜且精度足够的模型。"
 "D 错在把「缩小差距」说成「消除限制」：过度训练提升的是单位参数的知识密度，长链条多步推理与分布外泛化仍受模型容量与推理时算力（如 CoT、采样预算）约束。",
}

# 定位每题所属文件
files = sorted(ROOT.glob("*.json"))
applied = []
missing = set(NEW)
for p in files:
    d = json.loads(p.read_text(encoding="utf-8"))
    changed = False
    for q in d:
        if q["id"] in NEW:
            q["explanation"] = NEW[q["id"]]
            missing.discard(q["id"])
            changed = True
            applied.append((q["id"], p.name))
    if changed:
        p.write_text(json.dumps(d, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")

# 自检：不应再命中电报体
left = []
for p in files:
    for q in json.loads(p.read_text(encoding="utf-8")):
        if re.search(r"[A-D]\s*称", q.get("explanation") or ""):
            left.append(q["id"])
print(f"已改写 {len(applied)} 条解析；未找到 {sorted(missing)}")
print(f"剩余电报体解析：{len(left)} {left}")
