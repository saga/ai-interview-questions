[
  {
    "id": "agentic-03",
    "target": "能区分 Agent 的运行时组件与只存在于模型训练期的概念",
    "reasoningGoal": "先明确「运行时 Agent 组件」的边界——参与「感知→决策→执行」闭环的模块；再据此确认规划、记忆、工具调用三项都落在该边界内；并排除 Loss 函数这种只在训练期出现、不参与推理期运行闭环的项。"
  },
  {
    "id": "hil-vs-auto-loop",
    "target": "能判断在高吞吐自动化下，哪些工程取舍能同时保住交付并发与生产级代码质量",
    "reasoningGoal": "先确定取舍发生在「交付并发」与「质量可控」两个维度之间；再逐项判断人类主导架构决策、异模型族对抗审查、真实环境动态校验是否在这两个维度上同时成立；并排除「放弃 PR 合并权限、全自动合入主干」这种只满足前者而击穿后者的做法。"
  },
  {
    "id": "subagent-vs-inline",
    "target": "能依据模型的子 Agent 调度与长上下文保持能力，判断该走子 Agent 驱动还是单层内联执行",
    "reasoningGoal": "先对齐比较维度——模型的多层调用调度能力与长上下文抗漂移能力；再据此判断强调度模型适合拆 Spec 交给子 Agent、弱调度模型适合单层内联配增量验证，以及小粒度增量提交对抑制代码漂移的作用；并排除「不论模型能力一律深度无限嵌套」这种忽略前提的绝对化方案。"
  },
  {
    "id": "longhorizon-control-modes-01",
    "target": "能区分自主模式、目标模式与心跳模式各自的推进与终止依据，并判断外部测试失败时的正确处理",
    "reasoningGoal": "先分别复现三种控制模式的推进与终止机制——自主模式依赖预算内的外部校验反馈循环、目标模式依赖 Agent 的完成声明、心跳模式依赖定时/周期事件触发；再据此确认外部测试失败会把有界错误写回上下文并开启下一轮，而不是终止并清空 L2/L3 持久化状态。"
  },
  {
    "id": "envharness-stage-01",
    "target": "能判断 Stage 组件只作用于初始化阶段的 reset()，并据此划清它与 Contract 的职责边界",
    "reasoningGoal": "先复现 Stage 的执行时序——在 reset() 内部跑完预设动作序列 δ 之后才把新初始状态 s0' 暴露给 Agent；再据此推出它既能通过前置子任务缩短 Horizon、也能通过注入干扰提高起始难度；并排除「Stage 修改 step() 并限制后续动作空间」——那是 Contract 的职责。"
  },
  {
    "id": "envharness-contract-01",
    "target": "能按 r=(f_A, f_T, f_O) 的映射职责，把限制快捷动作、隐藏上下文、注入结构化提示三个诉求分配到正确接口",
    "reasoningGoal": "先识别场景给出的三个约束各自作用在哪个空间——动作空间、观察空间、转移函数；再据此把 f_A 对应动作过滤、f_O 对应观察掩码与裁剪、f_T 对应转移拦截与提示注入；并排除把 Stage 的初始化动作、Chain 的组合职责错配进 Contract 映射的选项。"
  },
  {
    "id": "envharness-chain-01",
    "target": "能判断 Chain 在接口层组合复合环境时做了什么，以及它对子环境实现不作什么要求",
    "reasoningGoal": "先复现 Chain 的组合逻辑 g(E, E_ext)：动作空间取各子环境的并集、满足条件后动态把交互焦点转移到下一环境、奖励端到端统一判定；再据此确认只要子环境遵循标准交互接口，底层实现与数据结构可以各自独立，从而排除「必须基于完全相同的底层实现代码」这一说法。"
  },
  {
    "id": "selfharness-definition-01",
    "target": "能按「非参数化控制层」这一边界，把 Harness 与模型内部机制、基础设施层区分开",
    "reasoningGoal": "先明确 Harness 的定义特征——介于基座模型与外部环境之间、不改变模型权重的控制层（提示词构建、工具封装、状态管理、校验、编排、错误恢复）；再据此确认只有该描述落在边界内；并排除注意力掩码（模型推理引擎内部）、权重增量（参数化微调）、负载均衡与 API 限流（基础设施网络层）。"
  },
  {
    "id": "selfharness-signature-01",
    "target": "能判断归因签名 φ(r)=(c,q,m) 在失败轨迹聚类中的作用是结构化归类，而不是过滤噪声",
    "reasoningGoal": "先拆出签名三个分量各自承担什么——终端校验原因是外部判定、因果状态是 Agent 行为、抽象机制是可复用根因；再据此推出同签名样例可映射到同一类 Harness 级干预、外部判定与内部行为得以解耦、提议模块能基于群体模式而非单次失败生成修改；并排除「过滤掉所有非确定性异常、只保留随机错误」——签名用于确定性归类，不用于筛噪声。"
  },
  {
    "id": "selfharness-parallel-edits-01",
    "target": "能说明 Harness 提议阶段采用「并行多元最小修改」而非整体重构的三重取舍",
    "reasoningGoal": "先确定设计要同时满足的三个目标——不破坏已有有效行为、单轮搜索命中率、候选之间可合并；再据此确认最小有界修改对应第一个、并行多元候选对应第二个、模块化正交对应第三个；并排除「全局重构反而降低 Token 消耗」这种因果倒置的描述。"
  },
  {
    "id": "skillevo-scaling-complementarity-01",
    "target": "能判断演进技能与模型规模之间是互补而非替代关系，并识别两个方向的极端化误判",
    "reasoningGoal": "先明确取舍发生在「参数规模带来的泛化」与「显式流程与避坑知识」两个维度之间；再据此确认基座越强从高质量技能获得的增益越大、小模型配技能可在特定场景反超无技能的更大模型；并排除「超大模型自动涵盖领域工作流」与「大模型加载技能反而性能下降」两个方向的绝对化结论。"
  },
  {
    "id": "agent-constitution-design",
    "target": "能按操作的可逆性与影响面划出 Agent 自主执行的边界，判断哪些操作必须回到人类授权",
    "reasoningGoal": "先确定判据是操作可逆性与影响面——本地单体模块改写、跑测试属可逆，可自主推进不打断人类；再据此确认破坏性命令与重大架构变更必须暂停并请求明确授权，以及汇报须结果导向、人可读；并排除「遇到困难就自动执行 git reset --hard 等破坏性命令」这种把恢复手段置于安全底线之上的做法。"
  },
  {
    "id": "agtool-guard-01",
    "target": "能说明两阶段级联如何用「前级高召回 + 后级降误报」换取安全拦截的延迟与成本",
    "reasoningGoal": "先明确级联要平衡的两个维度是漏过风险（FNR）与推理成本/延迟；再据此确认 Stage 1 只输出单 Token 以低成本放行绝大多数常规操作、与 Stage 2 共享上下文以命中前缀缓存、以及配置为高召回把可疑项推给 Stage 2 甄别；并排除「彻底消除假阳性、通过 Stage 2 即 100% 无越权」——概率模型做不到绝对零风险。"
  },
  {
    "id": "agtool-guard-02",
    "target": "能说明分类器对 Agent 推理与工具结果「双盲」换来的是什么，又牺牲了什么",
    "reasoningGoal": "先复现裁剪规则——只保留用户原始输入与工具调用指令；再据此推出剥离 Assistant Prose 可防「用户已隐式授权」这类说服性辩解、剥离 Tool Output 可阻断外部不可信数据的二次注入劫持；并确认代价是无法校验工具参数来源真实性；同时排除「完全无法评估写文件与脚本执行的危害」——Payload 本身仍写在工具调用文本里。"
  },
  {
    "id": "agtool-guard-03",
    "target": "能在自主模式下正确处理静态通配规则与安全分类器的优先级，避免分类器被宽泛 Allowlist 架空",
    "reasoningGoal": "先确定约束是「减少审批弹窗」与「高风险命令仍须审查」不可偏废；再据此推出必须剥离任意代码执行类的宽泛通配规则、只保留风险明确的窄域规则，使 python * 之类不再直接命中 Allowlist 绕过分类器；并排除「静态规则一律优先」与「一律禁用静态规则」两个极端。"
  },
  {
    "id": "agtool-guard-04",
    "target": "能说明输入层注入探测与输出层动作审计为何构成互补而非冗余的两道边界",
    "reasoningGoal": "先拆出两道防线各自看什么——输入层扫描进入上下文的工具返回结果，输出层独立审查工具调用的真实 Payload；再据此确认输入层命中后靠植入安全警告引导 Agent 重新锚定用户意图、输出层即使上下文已被注入也能按动作危险性拦截、数据外泄必须同时突破两层；并排除「单靠输入层即可解决所有注入风险」这一单层依赖。"
  },
  {
    "id": "agtool-guard-05",
    "target": "能说明多 Agent 委派链上「委派时」与「返回时」两次检查各防什么，以及告警时的正确处置",
    "reasoningGoal": "先复现委派链的身份塌陷问题——子 Agent 会把主 Agent 的指令当作初始用户 Prompt 从而掩盖越权本质，故委派前必须结合主 Agent 全局历史校验是否超出原始授权；再确认返回阶段审查的是子 Agent 是否被外部注入并带回污染结果；并排除「告警即销毁子 Agent、禁止回传」——那会让工作流死锁，正确做法是附安全警告降级返回。"
  }
]
