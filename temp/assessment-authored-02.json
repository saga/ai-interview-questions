[
  {
    "id": "agtool-guard-06",
    "target": "能说明「拦截后继续」与阈值熔断如何让低比例误报不中断任务，同时仍能止住失控 Agent",
    "reasoningGoal": "先确定自主系统要同时满足「不因单次误报中断」与「不让被注入的 Agent 无限重试」；再据此确认拒绝原因写回工具结果可驱动 Agent 换更安全的替代路径、误报由此降级为可接受的重试开销、以及连续与累计拦截上限用于熔断死循环；并排除「Headless 模式下降级等待控制台输入」——无交互终端时正确处置是直接终止进程。"
  },
  {
    "id": "agentic-subagent-03",
    "target": "能判断子 Agent 架构带来的真实收益是上下文隔离与并行，而非共享窗口或天然防死循环",
    "reasoningGoal": "先对齐子 Agent 与主 Agent 的核心差异维度——是否拥有独立 System Prompt/工具集与独立上下文；再据此确认独立精简配置提升子任务成功率、子 Agent 内部消化检索日志只回传摘要可防主上下文膨胀、并行启动可降低整体等待时间；并排除「父子共享完整历史」与「解耦即防死循环」这两种把收益张冠李戴的说法。"
  },
  {
    "id": "agentic-safety-05",
    "target": "能在 Shell/Git 权限设计中选出既保留自主性又控住破坏风险的工程手段，并识别两个极端",
    "reasoningGoal": "先确定权衡的是「执行自主性」与「破坏性风险」；再据此确认高危命令黑名单加人工确认、沙盒/容器隔离、只读免审批而写操作审批三项都同时满足两端；并排除「每次改动直接 push 到主干」（污染主干且不可逆）与「移除 Shell 工具退回手动复制」（用零风险换掉全部自主性）两个极端。"
  },
  {
    "id": "agentic-context-drift-06",
    "target": "能从长程 Agent 的遗忘与重复失败现象中定位到注意力稀释这一根因，而不是归咎于权重或限流",
    "reasoningGoal": "先定位现象发生在上下文中接近 128k token、轮次 40+ 之后，属于长上下文而非权重或网络层问题；再据此确认「Lost in the Middle」与噪点 token 稀释注意力能同时解释遗忘早期架构约束和重复执行已失败命令；并排除权重退化、API 限流丢响应、前缀缓存自动删 System Prompt 这些与现象不匹配的解释。"
  },
  {
    "id": "task-ai-03",
    "target": "能沿置信度阈值这一个变量，推出自动化率与资金风险的同向/反向变化",
    "reasoningGoal": "先明确阈值调低直接放宽了自动执行的准入条件；再据此推出更多低置信样本被自动执行，于是自助解决率上升、而高风险场景的误判率与资金损失风险同步上升；并排除「人工负载增加且错误率降低」（那是抬升阈值的结果）与「整体准确率提升且转人工率上升」（两者方向矛盾）。"
  },
  {
    "id": "local-first-tooling",
    "target": "能在高并发 Agentic 协作场景下选出真正缓解中心化 API 瓶颈的本地优先设计",
    "reasoningGoal": "先识别场景瓶颈是网络延迟、Rate Limit 与可用性波动；再据此确认本地缓存视图（毫秒级 PR 状态与 Diff）、本地 Daemon 配异步联邦同步、终端复用配隔离工作树三项都把读写路径从中心化 API 上移开；并排除「同步阻塞等待中心化 REST API」——它正是要被本地优先架构消掉的反模式。"
  },
  {
    "id": "prompt-cache-affinity-routing-01",
    "target": "能解释相同静态前缀仍 Cache Miss 的物理成因，并选出工程上可行的命中率优化手段",
    "reasoningGoal": "先确定 KV Cache 的物理约束——它驻留在具体节点的显存/内存里，且工程上不可跨节点实时同步；再据此确认节点过载触发溢出路由会打到未持缓存的节点、显式 Prompt Cache Key 可做哈希分组与亲和性路由、以及缓存边界限定在同租户同 Region；并排除「用 RDMA 批量搬运 KV 做跨节点同步」——GB 级张量同步的带宽与延迟代价高过重算。"
  },
  {
    "id": "longhorizon-daemon-session-01",
    "target": "能判断守护进程架构真正提供的是 Session 生命周期与前端解耦，而不是状态共享或免干预",
    "reasoningGoal": "先明确守护进程解决的是「客户端连接不可靠、而长程任务必须持续」这一矛盾；再据此确认独立持有 Session 生命周期使断连不影响后台工具执行与子 Agent 协作；并排除「子 Agent 复用同一热状态线程」（破坏隔离）、「人工干预只能走异步审批队列」（守护进程实际暴露 Agents View 支持随时 attach）、「提供共享 L1」（各子 Agent 有独立 L1）三项。"
  },
  {
    "id": "aws-ai-practitioner-02",
    "target": "能按任务目标匹配 LLM 应用形态，判断「从长文档提取关键点」对应的是摘要而非抽取或生成类任务",
    "reasoningGoal": "先识别业务诉求是「读长文档并提炼关键点」，即压缩后保留要义；再据此确认摘要聊天机器人直接对应该诉求；并排除命名实体识别（只抽实体不提炼要点）、推荐引擎（与文档要点无关）、翻译系统（面向语种转换）。"
  },
  {
    "id": "aws-ai-practitioner-05",
    "target": "能在不换模型的前提下，选出控制输出长度与语种这类生成约束的正确手段",
    "reasoningGoal": "先判断「输出简短」与「使用特定语言」都属于可在提示层直接声明的生成约束；再据此确认调整提示词成本最低且最直接；并排除换模型尺寸（成本高且不确定）、提高 temperature 与 Top-K（增加随机性，反而不利于稳定服从格式约束）。"
  },
  {
    "id": "aws-ai-practitioner-07",
    "target": "能识别「复用预训练模型适配新任务」对应的策略是迁移学习，而非训练轮数或无监督范式",
    "reasoningGoal": "先抓住题干的两个关键条件——避免从零新建、把已有预训练模型适配到相关新任务；再据此确认迁移学习通过复用预训练权重并微调来同时满足两者；并排除增减 epoch（只调训练轮数，不改变是否从零）与无监督学习（与复用预训练权重无直接关系）。"
  },
  {
    "id": "aws-genai-developer-pro-06",
    "target": "能在「最低实现成本 + 按复杂度自动路由」的约束下选出托管路由方案，而不是自建分类链路",
    "reasoningGoal": "先确定约束是同时压低实现成本与延迟、且要按复杂度自动分流；再据此确认托管智能提示路由无需自建分类器与路由基础设施即可逐条预测并动态分发；并排除自建小模型分类加 Lambda 路由（工程量大）、单中等模型兼顾（无法兼顾成本与强推理）、关键词规则路由（脆弱且运维重）。"
  },
  {
    "id": "context-2026-101",
    "target": "能区分「按任务相关性做选择」与「相似度 top-K 检索」，并选出让 context 随任务与反馈演进的设计",
    "reasoningGoal": "先明确题干要避免的是把语义相似度当作有用性的代理；再据此确认召回后用 reranker 按任务相关性/近期性/预测有用性重排并按执行反馈迭代、query-aware 按阶段动态选源、以及用离线日志对真正影响成败的片段加权三项都建立在任务相关性上；并排除「相似度 top-K 再按时间倒序」——时间加权并不等于任务相关性。"
  },
  {
    "id": "context-2026-102",
    "target": "能按生命周期与信任等级两个维度，说明四类 context 为什么必须分仓而不是拼成单一 blob",
    "reasoningGoal": "先拆出两个判据——生命周期（历史短暂易变、任务状态随步骤更新、长记忆持久、工具结果临时且体量大）与信任等级（系统指令可信、工具结果与抓取内容不可信）；再据此确认分仓存储、不可信内容与指令隔离、以及按体积与访问模式采用不同存储与淘汰策略；并排除「只按信任等级分两仓、同级内统一拼串压缩」——那会抹平生命周期差异并埋下间接注入风险。"
  },
  {
    "id": "context-2026-106",
    "target": "能选出在压缩 token 的同时保住关键事实、约束与可恢复状态的 compaction 策略",
    "reasoningGoal": "先明确压缩的风险不是丢字数而是丢不可重建的信息；再据此确认结构化 compaction 保留约束与未完成任务并带可回溯指针、do-not-compact 锚点让关键事实常驻、压缩前把可恢复状态抽成结构化字段以便重建；并排除「以 token 预算为硬约束、越短越好」——那正是会摘要掉关键约束的过度压缩。"
  },
  {
    "id": "context-2026-107",
    "target": "能判断评估 Context Manager 应看端到端任务结果与失败模式，而不是输入 token 减少量",
    "reasoningGoal": "先明确省 token 是手段而非目的；再据此确认应以端到端成功率与质量为主指标、用消融实验对比有无 Context Manager 的质量成本与失败模式、并检查关键信息被错删或不可信内容被提升优先级这类退化；并排除「仅比较输入 token 数即可」——该口径会奖励把关键信息裁掉的劣质策略。"
  },
  {
    "id": "context-2026-108",
    "target": "能说明 context 应随规划/检索/执行阶段动态装配，并指出固定 prompt 的具体代价",
    "reasoningGoal": "先确定三个阶段各自需要的信息不同——规划要目标与计划模板、检索要检索工具与查询上下文、执行要当前步骤与工具结果；再据此确认按阶段路由注入、用 context assembler 动态组装、阶段切换时清空无关上下文三项都服务于该差异；并排除「全生命周期固定 prompt」——无关信息会占用 budget 并干扰决策。"
  }
]
