# 2026-09-03

## ACTION_CHECKLIST 复核审计（用户："重新检查action checklist，还有什么没有完成的或者做的不完整的"）

逐项对 16 条 checklist 做代码级验收，不信任打勾。结论：**15 项真完成，3 项部分完成，0 项纯阻塞**。

### 已验证通过（实测命令，非口头声称）
- `tsc --noEmit` EXIT=0；`vitest` 721 passed / 52 files（基线 705）；
  `validate:questions` 1308 题 / 123 节点、带 angle 100.0%
- A-2：`question.test.ts` 5 个 Zod 契约单测在位（越界 answer / misconceptionMap 长度 /
  下标越界 / 正确项被标注 / 合法标注）
- A-3 `add-question.ts:74` 已改 errors；A-4 `variant-bench` 双 main → 1；
  A-5 `lint-bias.ts:60` 已改；A-6 `ARCHITECTURE.md:9` + `:533-535` 两级 Invariant 已重写；
  A-7 `ANGLE_GENERATION_HINTS` + 报告提示行在位
- B-11：5 个 skill 全部改 `spec §N` 引用（`check` 10/11/36-45/105，`add` 73/77-83，
  `article` 16/32/36/49/56，`fill` 10-11/47/49-53/64/83，`curate` 21/60-68/88/111）
- B-12 `reuseCandidateIds`（blueprint.ts:41 + fill:19）；B-13 映射表在 check:91-94；
  B-14 fill:59-61；B-15 article:26+；B-16 curate skill 存在

### 新发现的缺口（原清单没写）
1. **文档严重滞后，违反 AGENTS.md §2。** 本批含数据契约变更（angle optional→required）、
   导出重命名、两个新模块、一条新规范、一个 skill 语义改名，但 CHANGELOG 与 DECISIONS
   **一个字都没有**。`ARCHITECTURE.md` 的 ADR-069 段也完全没提 `--oversample` 与 challenger。
   → 已补 CHANGELOG 条目 + **ADR-070**（超采+质询）+ **ADR-071**（三层分层+契约收口），
   并给 ARCHITECTURE ADR-069 段补了「离线超采 + 质量质询」小节。
2. **A-8「立基线」只做了一半。** 脚本 + Level 3 接线完成（214 命中 / 16.4%），
   但精确率从未人工抽检 → 基线不存在。已在清单里写明「候选集 ≠ 基线」。
3. **B-11 验收命令指向错文件名。** 原文 grep `添加题库prompt.md`，实际实现引用的是
   `question-content-spec.md`，按原文 grep 永远得 0。已按最终实现修订。

### 🔴 最重要的发现：第 9 项打了勾但自己验收没过
并行 session 在审计期间完成了量产，池子从 0 → **98 题 / 196 条变体、stale 0**。
但 `validate-variants` 报 **近重复 79 对**（阈值 ≥88）——81% 的题，两条变体互相 88–96% 相似，
且全部是同题 `surface-options ⇄ context-options` 配对，系统性而非偶发。
本项验收白纸黑字要求「无 stale、**无近重复**」→ 未过。

后果：按 `question-variants.ts` 的 ≥88 去重，这 79 对的第二条会被直接筛掉，
98 题名义 2 条变体、实质 1 条；扩产扩了个容量寂寞。
根因：两变体**共用同一套选项改写、只换题干**；纯中文选项无空白分词，
`token_set_ratio` 易把合理改写判为漂移（<45 拒），assemble 路径退回 canonical 原选项，
越保守越重复。已在清单里给出二选一（改产出去做选项差异化 / 改口径降到 1 条并修订验收）。

### A-10 状态更新（依赖口径变了，不是 API key 问题了）
第 9 项走的是**新建的 `scripts/assemble-variants.ts`**（模型产出文本 + 复用
validateVariant/computeVariantSourceHash/variantPoolSchema 组装，无需 API key），
**完全绕开 `--oversample` 与 `challengeVariant`**。
所以 `src/ai/variantChallenger.ts` 目前是**已接线的死代码**——唯一调用方没被量产用过，
留存率/质询否决数至今零测量。按 AGENTS.md §3，要么真跑一批回填数字，要么删掉，不能留着不管。

## 踩坑
- **本仓库 Bash `grep` 会静默返回空**（模式明明命中，exit 0 无输出），MD 文件和 package.json 都中招。
  改用 **Grep 工具**即可，不要浪费轮次怀疑文件内容。
- `npm run question:validate-variants` 报 `vite-node: command not found`（`.bin` 被之前
  rename 失败搞坏了）。绕过：直接 `node node_modules/vite-node/dist/cli.mjs scripts/xxx.ts`。
  同理 vitest 用 `node node_modules/vitest/dist/cli.js run`。
