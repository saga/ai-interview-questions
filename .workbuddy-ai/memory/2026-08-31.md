# 2026-08-31

## 复核：model_error telemetry reason 判断 bug 已修（无需我动）

用户重新报了一个 bug：`interviewAgent.ts` 的 `agent_end` 处理里 `lastErrorMessage` 先被置 `undefined` 再据此算 `reason`，导致 `fallbackReason` 永远是 `'agent_no_action'`、`'model_error'` 永不入账。用户给了精确修复（先 `const reason = lastErrorMessage ? 'model_error' : 'agent_no_action'`，再 `lastErrorMessage = undefined`）。

**核实结果：该 bug 在当前工作树已不存在。** `git blame` 显示 `src/agent/interviewAgent.ts:318-320,324` 已由 commit `02b8789f`（2026-08-31 07:18，"adddd"）改写为正确版本（与用户建议逐字一致），并带注释「先定格兜底原因」。原始 bug 代码来自 `4cbbd695`（2026-08-27）。另外两个 `ensureQuestionDelivered` 调用点（watchdog→`'timeout'`、fallbackAdvance→默认 `'agent_no_action'`）均无此问题。

- 当前 `HEAD` = `ace6e30`（= `origin/main`，并行会话已 push）。commit 链：`928f871`(ADR-060) → `f468a51` → `02b8789`(telemetry 修复) → `ace6e30`。
- 验证：`npx vitest run src/agent/interviewAgent.test.ts` **16 passed**。
- **本回合零代码改动**：这是查证而非修复。用户可能是看了过期编辑器缓冲（旧版 `4cbbd695` 代码）才误以为仍存在。结论：本 bug 已闭合，无需再修。

### 提醒（给下次会话）
用户这一轮倾向「我已改了大部分你提的建议，再review一遍」——但这次具体报的 telemetry bug 实际已被他自己的并行提交修掉。复查类任务要先 `git blame`/读当前文件确认「是否真还存在」，避免对已知修复做重复改动。

---

## 任务十二：新增 7 道 AI Agent 评估题（接入知识图谱）

用户 @long-text 给了 7 道 Agent 评估题（pass@k/pass^k 指标、轨迹 vs 状态评估器、代码/LLM/人工评估器选型、能力评估 vs 回归评估、Computer Use 感知架构权衡、User Simulator、Flakiness 调试）。

### 关键判断：复用既有 `evaluation` 节点，不新建节点
- 排查发现 `src/data/questions/evaluation.json` 里已有 3 道 agent 评估题（`llm-judge-vs-agent-judge` / `judge-memory-cascade` / `trajectory-bias-pitfall`），其 `topic` 都是 `"evaluation"`。
- 知识节点 `evaluation` 存在于 `src/data/knowledge/system-design.json`（area ai-systems）。→ **我的 7 题直接映射到 `topic: "evaluation"`、`category: "evaluation"`**，与既有 agent 评估题同族，无需新建节点，也避免引入与 `evaluation` 重叠的 `agent-evaluation` 节点。
- 避坑：最初 grep `"agent-evaluation"` 命中是因为那是 `tags` 里的词，不是 `topic` 字段；别被 tag 误导。

### 产物
- 新文件 `src/data/questions/agent-evaluation.json`：7 道题（避免改动并行会话持有的大文件 `evaluation.json`）。题型 6 多选 + 1 单选（86% 多选，满足 §4.2 ≥2/3）。每题含 choice + open 双形态与 `misconceptions`。

### 验证（全绿）
- `question:add --check`：通过（topic 有节点、无长度泄题、无重复选项、单选占比达标）。同样：草稿先放仓库根 `agent-evaluation-draft.json` 校验，再 `--write --output` 落到 questions/（注意 `--write` 会覆盖目标文件且要求目标不存在，故用新文件而非追加到 evaluation.json）。
- `validate:questions`：1177 题 / 89 节点全过。
- `knowledge.test.ts` 9/9（节点 `evaluation` 早已被既有题支撑，本批只是扩充）。
- 覆盖报告：7 题全落在既有 `evaluation` 节点下（新增覆盖格「无」，因该节点对应 angle 已被既有题覆盖）。

### 状态
未提交（隔离于并行会话改动）。可选用法：可单独提交这 1 个新文件；或若用户想要更细的知识图粒度，可将 7 题拆到独立 `agent-evaluation` 子节点（但会与 evaluation.json 里 3 道 agent 题的 topic 不一致，需一并迁移）。

---

## 任务十三：新增 9 道安全 Agent 评估题（接入知识图谱 security-eval 节点）

用户 @long-text 给了 8 道「网络安全/漏洞利用能力评估」题（端到端 vs 子任务导向、Sanitizer 差分校验、多主机渗透架构、Zero-Day/One-Day、数据污染控制、防御机制影响、抗奖励哈克 Harness、PTY vs 隔离 Bash）。题型：1/3/4/6/7 多选，2/5/8 单选。

### 产物
- 新建知识节点 `src/data/knowledge/security-eval.json`（1 节点，area ai-security，topic security-eval，P1）。summary/required/misconceptions/angles 齐全。
- 新建题库 `src/data/questions/security-eval.json`：9 道题（用户 8 道 + 我补 1 道）。

### 关键决策
- **题型门禁冲突（§4.2 单选 ≤1/3）**：用户明确指定 2/5/8 为单选，但 3/8=37.5% > 1/3，且这几题解析里其余选项均明确错误，无法在不背离答案键前提下把某一单选改成多选。经 AskUserQuestion，用户选「补一道多选」稀释到 3/9=33.3%。
- 补的第 9 题 `sec-eval-09`（design 角度，多选，答案 [1,2,3]）：评测度量体系设计——同时报成功率+奖励哈克率、按攻击链阶段给分层部分分、人工标注交叉校验 Grader。补齐 6 个 angle 全覆盖。
- **长度泄题（sec-eval-05）**：原 B（正确答案）最长 87、C（干扰项）最短 47=1.85×，且正确项=全局最长=典型「正确项更长」暗示。重新平衡后 B=60，max/min=1.28，正确项不再鹤立鸡群。草稿阶段在仓库根 `security-eval-draft.json` 校验（避免 `question:add` 把 questions/ 当存量 id 误报），通过后再 `--write --output` 落到 questions/。

### 验证（全绿）
- `question:add --check`：通过，新增覆盖格 6 个 angle 全中。题型 单选 3 · 多选 6（67% 多选）。
- `validate:questions`：1192 题 / 90 节点全过（较上一轮 1177/89 +9 题 +1 节点）。
- `knowledge.test.ts` 9/9（security-eval 节点现由 9 题支撑）。

### 状态
未提交，隔离于并行会话改动（git status 仅 `?? src/data/knowledge/security-eval.json` 与 `?? src/data/questions/security-eval.json` 是新增）。草稿 `security-eval-draft.json` 已删除。

---

## 任务十四：新增 30 题（HITL 10 + 后训练 10 + 上下文工程 10）

用户 @long-text 给 30 题 stem（仅 id/category/topic/tags/difficulty/angle/question，无 formats/explanation/misconceptions）。按落盘建议生成 3 个新文件。

### 用户给的 angle 含非法值，需映射
全局合法 angle 仅 10 个。用户原 angle 中 `failure-mode/security/architecture/data/evaluation` 不在枚举内。映射：failure-mode→debugging、security→system-design、architecture→system-design、data→mechanism、evaluation→design。`taxonomy.test.ts` 不强制逐题 angle∈白名单（白名单仅建议性），只需对齐全局枚举。

### 关键决策
- **`human-in-the-loop` 是新 topic**：新建节点 `src/data/knowledge/human-in-the-loop.json`（area agent-engineering, id "human-in-the-loop", P1）。
- **`training` 作为 topic 此前无 node.id="training"**（现有 training.json 节点 id 是 pretraining/sft 等概念 slug，question 用 distillation 等 concept topic）。为让用户的 `topic:"training"` 通过 `question:add` 门禁（topic 必须命中某 node.id），新建节点 `src/data/knowledge/post-training.json`（id "training", area llm, P0）。`context-engineering` 节点（id 同名）已存在，无需新建。
- 草稿放仓库根（`hitl-draft.json`/`training-draft.json`/`context-draft.json`）绕过 add-question 把 questions/ 当存量 id 的检查；`--check` 通过后再 `--write --output` 落到 questions/。
- 用 3 个并行 subagent 各写 10 题（4 选项/答案/explanation/misconceptions/open），各带 `--check` 自迭代到通过；我复核了后训练 10 题答案正确性（DPO/GRPO/RLVR/reward hacking 等均准确），另两批门禁 + 报告 rationale 通过。

### 产物
- `src/data/questions/human-in-the-loop.json`：10 题，全多选。
- `src/data/questions/training-post-2026.json`：10 题，全多选。
- `src/data/questions/context-engineering-2026.json`：10 题，2 单选 + 8 多选。
- 节点：`human-in-the-loop.json`、`post-training.json`。

### 验证（全绿）
- `question:add --check` ×3：通过（HITL 100% 多选；training 100% 多选；context 80% 多选）。
- `validate:questions`：1222 题 / 92 节点（较 1192/90 +30 题 +2 节点）。
- `knowledge.test.ts` 9/9；`taxonomy.test.ts` 7/7（新节点 area/topic 与 taxonomy 自洽）。

### 状态 / 注意
- ⚠️ 并行会话（adddd/dddd 提交）在写入 question 文件前已 `git add -A` 提交，把两个 knowledge 节点文件扫进了 `aa01d6e "dddd"` 这个无意义提交；3 个 question 文件是之后写的，仍是 `??` 未跟踪。
- 3 个 question 文件建议尽快单独提交（避免被并行会话后续 `git add -A` 扫进无意义提交）。未 push。草稿已删除。
