# 2026-08-30

## 深度审查：架构 / Prompt / Context 工程 / 代码

产出根目录 `深度审查报告.md`（独立审查，非既有 `ARCHITECTURE-REVIEW.md` 的续篇）。
基线：`main` @ `5c5c407`；`tsc --noEmit` 0 error，`vitest run` 35 文件 / 349 测试通过。
因此所有发现均为**行为层**问题，编译器与现有测试都拦不住。

### 关键结论

- **Context 工程是最弱环节（5.5/10）**：Agent 被 prompt 要求"用自然语言复述题干+选项"（`agent/prompt.ts:24`），但 `getQuestion` 只回 id（`agent/tools.ts:222`），题干从未进入上下文 → 必然幻觉。这是本次最重要的新发现，既有评审完全没覆盖。
- 既有 `ARCHITECTURE-REVIEW.md`（同日）的 3 个 P0 **全部未修复**：`tools.ts:161` 重复出题、`domain/variant.ts:74` 语义校验是空注释、`tools.ts:319` getCoverageGaps 占位。
- 问题计数：P0 × 8、P1 × 13、P2 × 12。

### 8 个 P0（均已实证，附行号）

1. `ai/evaluate.ts:120` 残缺 LLM 输出 → `{}` → 伪装成真实 0 分污染 topicStats。**`ai/evaluate.test.ts:91` 把这个行为断言成正确行为，会拦住修复，必须先改测试。**
2. `storage/learner.ts:59` + `useTrainingSession.ts:201` 持久化零 try/catch（后者是 try/finally 无 catch）→ 静默丢失整场成绩。
3. `agent/interviewAgent.ts:310` `dispose: unsubscribe` 不调 `clearWatchdog`（只在 `abort():305` 调）→ 90s 后对已卸载组件 setState。
4. Agent 复述未见过的题干（见上）。
5. `interviewAgent.ts:299` 上下文 append-only 无 compaction；全仓库 `transformContext` 零命中（只有 chromeAgent.ts:84 截断，原生路径没有）。
6. `domain/variant.ts:74` 语义校验只有一行注释。
7. `tools.ts:161` `unasked[0] ?? byTopic[0]` 重复出题。
8. `interviewAgent.ts:159` fallbackNextQuestion 与 `interviewEngine.ts:78` nextAdaptiveStep 重复实现且分叉（Agent 侧不应用 categories/difficulties 过滤）。

### 实证成立的架构红线（重构时别破坏）

grep 验证：`schemas/` 零 domain 依赖；`ai/` 只依赖 domain 纯函数不碰 learner/adaptive/quiz；`domain/` 无 React/LLM；Dexie 确为 version(4) 且有 v3→v4 升级路径。

### 其它值得记住的

- `tools.ts:92` validIdPool 兜底回退全题库（1084 题）→ 一次 id 拼错可能把全部 id 永久灌进上下文。
- `docs/ARCHITECTURE.md:487` 描述的 `persistedLearnerSchema`（version/data 包装）在代码中**不存在**；`storage/learner.ts:8` 声称的 Zod 读出校验也没实现 → 文档与代码不一致（违反 AGENTS.md §2）。
- git log 近 15 条全是 `adddd`/`ddddd`，决策不可追溯。

### 待用户决策

报告已给出 P0/P1/P2 分级修复清单，但**未改动任何源码**（审查性质）。是否动手修、按什么顺序修，等用户确认。
建议：先修 #1（连带改测试）+ #2 + #3，这三个是真实数据丢失/污染，成本都在 50 行内。

---

## 修复 P0 #7：getQuestion topic 兜底重复出题（ADR-048）

用户指定只修这一项。已完成，代码改动落在 `src/agent/tools.ts` + `src/agent/tools.test.ts`。

### 改了什么

1. 新增 `isDelivered(session, id)` = 已作答 ∪ 已评分 ∪ 正是当前题，作为「是否已交付」单一出处。
   **关键细节**：`evaluations[id]` 用 `in` 而非真值判断——`null`（未作答/评估失败）也算已交付，与 `countEvaluated` 同口径，否则「答不上来的题」会被反复追问。
2. topic 兜底 `unasked[0] ?? byTopic[0]` → `byTopic.find(x => !isDelivered(...))`，不存在时宁可不出题。
3. 主题耗尽返回新错误码 `topic_exhausted` + 回带全题库中未交付的题号，**不擅自换主题**（换主题是 Agent 的选题决策，工具不越权）。
4. `validIdPool` → `deliverableIds`：not_found 自纠正回带的题号同样过滤已交付项（此前会把正在答的当前题也列进建议，等于诱导重复选题），并剔除已不在题库的陈旧 id。
5. 全题库考察完 → 明确提示 `finishInterview`。
6. 同步 `getQuestion` 的 tool description（该描述即 LLM 可见提示，必须与代码一致）。

### 测试方法（值得复用）

新增 7 例（tools.test.ts 15 → 22）。**做了反向验证**：临时还原旧实现后跑测试，7 例中 5 例失败，证明用例真实捕获 bug 而非只断言「不崩」。分两轮还原（先只还原 `?? byTopic[0]` → 3 例失败；再完整还原旧实现 + 旧 `validIdPool` → 5 例失败），顺带确认了每个用例各自盯的是哪一处改动。
结论：`npx vitest run` **365 passed**（35 文件），`npx tsc --noEmit` 0 error。

### ⚠️ 重要：仓库存在并发工作流

审查期间（13:46–14:29）**另一条并行工作流在同一个 main 分支上活动**：
- `d0b68a7`（14:05）修了 `validateVariant`，接入 `hasConceptEvidence`（精确 token + fuzzball 模糊兜底），即 ADR-047。
  → **我报告里的 P1「语义校验是空注释」因此过时**，已就地更正并降级为 P1（难度/实质差异/干扰项正确性仍未校验）。
- `974af83`（14:23:59）把我这次的 tools.ts / tools.test.ts 改动连同对方未提交的 docs 改动、以及 `架构分析.md` → `temp/` 的删除一起提交了，commit message 仍是 `adddd`。

**教训**：
- 写文档前先看 `git status`，本次差点在对方未提交的 docs 改动上叠写（改用 Edit 做定点插入规避了覆盖）。
- 报告类文档要写明基线 commit，否则并行改动一进来就失真。
- 两个会话共用一个分支 + 无意义 commit message（`adddd`）非常危险，建议提交前先 `git status` 确认改动归属。

### 当前状态

- 代码修复：已提交（`974af83`），工作区 `src/agent/` 与 HEAD 一致。
- 文档：CHANGELOG（新增 ADR-048 变更条目）、DECISIONS（ADR-048）、`深度审查报告.md`（更正 P1 + 〇节对照表 + 计数）**均未提交**，等用户确认。

---

## 任务三：`getCoverageGaps` 重定义为 coverage-based 事实查询（用户指定设计，非报告建议）

用户给了明确设计约束，逐条落地：**不删除工具、不做成推荐系统、`recommendWeakTopics()` 不参与、它应该是事实查询工具、不需要测复杂 ranking**。改动范围：`src/agent/tools.ts` + `src/domain/learner.ts` + 两个测试文件。

### 实现

- `domain/learner.ts` 新增 `CoverageGapReason = 'uncovered' | 'prerequisite'`、`CoverageGap {topic, reason, prerequisites?}`、`findCoverageGaps(topicRefs, profile, {threshold?, limit?})`、`describeCoverageGap(gap, profile)`。
- 语义正交：`getUserWeaknesses` = **mastery-based**（已练但薄弱）；`getCoverageGaps` = **coverage-based**（还没练到）。判据：**已练但未掌握、前置完备的 topic 不算覆盖缺口**。
- 缺口只两类；前置缺口排序在前（基础没打就练上层是无效投入），同档内 `topoRankOf` + 字典序保证稳定可测。
- 复用既有能力：`collectTopicRefs` / `prerequisiteClosure` / `isAttempted` / `isMastered` / `WEAK_AVG`。**没有发明 `hasEvidence`**——"练过没有"用既有的 `TopicStats.attempts`。
- 只统计题库中也存在的前置：用户无从练习的前置不是可闭合缺口（那是题库生产问题，归 `domain/coverage.ts` 视角）。
- `recommendWeakTopics()` 完全移出该工具；description 写明分工（description 即 LLM 可见提示）；补 `session.log` 条目。

### 偏离用户方案的一处（已在 ADR-049 记录理由）

用户建议放 `src/domain/coverage.ts`，我放在了 **`src/domain/learner.ts`**。理由：`domain/coverage.ts` 是 ADR-032「两速分离」下**题库生产侧**的 topic×angle 矩阵，不感知 `LearnerProfile`、运行时不加载；而本函数依赖 `isAttempted`/`isMastered`/`WEAK_AVG`/`prerequisiteClosure`，全在 `learner.ts`。放进 `coverage.ts` 会让它反向依赖 learner 概念。

### 反向验证（两轮，都有效）

1. 还原旧的 `recommendWeakTopics` 实现 → **4 例工具测试全失败**。
2. 破坏 `findCoverageGaps`（去掉前置优先排序 + 去掉 `isAttempted` 守卫）→ **3 例 domain 测试失败**。

第一次反向验证**失败了**：用 python `str.replace` 注入破坏代码时字符串没匹配上（目标是多行格式），测试全绿但没有真正改动文件。教训：**注入破坏性改动后必须 `diff` 确认真的改到了**，否则会得出"测试没覆盖"的错误结论。

### 一个自测断言写错

`describeCoverageGap` 的用例我最初断言 `WEAK`（`attempts: 3`）的前置缺口描述为「未练习，前置…」，但 `isAttempted` 为 true 时应该只说「前置…尚未掌握」。**实现是对的，测试写错了**，已改为覆盖三个分支：未练习 / 前置未掌握 / 未练习+前置未掌握。

### 结论

`npx vitest run` **378 passed**（35 文件；learner 35→44 +9、tools 22→26 +4。基线 365 + 13 = 378，与上一轮记录自洽），`npx tsc --noEmit` 0 error，`npm run build` 通过。
文档：CHANGELOG 新增条目、DECISIONS 新增 **ADR-049**、`深度审查报告.md` 的 P7 标记为已修复（含新旧方案对照表）。`docs/ARCHITECTURE.md` 无需改——那里只列了工具名，没描述语义，且本次没有模块/分层/依赖方向变化。
**全部未提交**，等用户确认。

---

## 任务四：按用户建议复核并更正两份架构分析文档（纯文档改动，代码零改动）

用户的核心判断：**「选择题完全跳过 Agent 决策」是把 Agent 决策能力与 LLM 调用绑太紧了**。给的验收标准只有一条——
> 选择题答完以后，下一题是不是根据当前 learner state 做了 adaptive decision？

### 核实结果：答案是「是」，原判定为误报

逐跳跟读确认的调用链：

```
choiceAdvance()               interviewAgent.ts:212
  ├─ evaluateSessionQuestion()  → gradeChoice（确定性判分）
  └─ fallbackNextQuestion()     interviewAgent.ts:160
       ├─ pool    = 传入 Agent 的 bank（已排除 disabledCategories）− 已问题
       ├─ signals = 从 session.evaluations 实时重建（topic/score/difficulty）
       └─ pickNextAdaptive(pool, signals, profile, Math.random)   adaptive.ts:114
```

`pickNextAdaptive` 是真自适应：上一题得分 → `decideStrategy`（<60 gap-probe / ≥80 broaden / 否则 deep-dive）、难度升降、`relatedOf`、`prerequisiteClosure`、`recommendWeakTopics(profile)`，以及每个策略子集内按 (topic, angle) 掌握度细选（`angleWeakRank` + `angleEvidence`）。**不是「取下一道未问题」**。

**一个必须精确说明的边界**：`profile`（跨会话 mastery/angleCoverage）在会话进行中是**会话开始时的快照**，只在会话结束时经 `finalize → onComplete → handleAgentComplete → updateLearner → saveLearner` 落库。**两条路径完全一致**（`useTrainingSession.ts:216/265` 同样只在会话结束调用），所以不是缺陷也不构成双引擎分叉。中途一度以为 Agent 路径根本不落库，追到 `useAgentInterview.ts:148 finalize()` 才发现是经 App.tsx 传下来的 `onComplete` 回调。

### 顺带发现我自己的报告 A1 有一行是错的

`深度审查报告.md` A1 原表写「categories/difficulties/disabledCategories 过滤：确定性引擎应用、Agent 兜底**不应用**」，结论是「Agent 兜底绕过用户设置的分类与难度约束」。核实后**不成立**：
- `disabledCategories` 两边都应用 —— Agent 侧在 `useAgentInterview.ts:193` 和 `:358` 就把 bank 预过滤了。
- `def.categories` / `def.difficulties` 来自 `session.definition`（训练/组卷定义），而 Agent 会话类型是 `InterviewAgentSession`，**根本没这一层**。不是"绕过"，是"该维度不存在"。
- rng 也非差异（`pickNextAdaptive` 默认参数就是 `Math.random`）。

→ A1 由 **P0 降级为 P2**，只剩「pool 组装写了两遍」这个可维护性问题。真实差异仅剩「题数上限来源」和「确定性路径不调 `finalizeQuestion` 所以不出变体」，两者都按设计。

### 改动

- `ARCHITECTURE-REVIEW.md`：第 4 条改写为「✅ 已复核关闭」，补调用链 + 自适应输入表 + 明确不建议塞 LLM；P0 清单两项标为已修复；§四总结同步；末尾加复核记录。
- `深度审查报告.md`：A1 降级并重写；§0 表格新增「选择题快路径」误报行、并说明既有评审 3 个 P0 已全修；第六节 P0 清单 6→5、移除记录写清理由；清单编号重排（原来有断号和两个 26）；计数改 P0 5 / P1 11 待办 / P2 7；§7 第 6 条同步。
- `docs/DECISIONS.md`：新增 **ADR-050**（选择题跳过 LLM Agent loop + 完整「不做」清单 + profile 快照边界 + `fallbackNextQuestion` 更名建议）。
- `docs/CHANGELOG.md`：新增条目记录两处结论更正。

`npx vitest run` **378 passed**，`git status` 确认**只有 4 个 md 文件改动，代码零改动**。全部未提交。

### 教训

评审文档里「跳过 X」这类否定式断言，下笔前必须把调用链逐跳跟完再定性 —— 本次两处更正（既有评审第 4 条、我自己报告的 A1）都是**没跟到最后一跳就下了结论**。特别是「A 应用了某过滤、B 没应用」这种表格行，一定要去 B 的**上游**（本例是构造 Agent 时预过滤 bank）确认，而不是只看函数体。

---

## 任务五：`OPENING_INSTRUCTION` 可配置化（ARCHITECTURE-REVIEW 第 5 条，ADR-051）

### 做法（选了 `config.prompts.agentOpening`，不是 `createInterviewAgent` 参数）

理由：本条诉求是「**用户**能通过设置页修改」；做成构造参数只解决代码侧可注入性，用户照样改不了。

1. `schemas/ai-config.ts` 的 `promptConfigSchema` 加 `agentOpening: z.string().optional()`。
   设置页 Monaco 借已有的 `z.toJSONSchema(aiConfigSchema)` **自动获得校验与补全**，零额外接线。
2. 默认值从 `useAgentInterview.ts` 模块常量**上移到 `agent/prompt.ts`**
   （`INTERVIEW_AGENT_OPENING_INSTRUCTION`），与 `INTERVIEW_AGENT_SYSTEM_PROMPT` 同处。
   关键原因：设置页「恢复默认值」按钮要引用它，留在 hooks 层会让组件反向依赖 hooks 内部常量。
3. 回退逻辑抽成纯函数 `resolveOpeningInstruction(agentOpening)` 放 `agent/prompt.ts`：
   `undefined` 与**空白串**都回退默认。抽函数是因为 hooks 层无测试基建，而「用户清空输入框不能发空指令」这条必须可测。
4. 设置页加编辑区，说明里写明题数硬上限由 `MAX_AGENT_QUESTIONS` 控制（这里是软目标）。
5. `storage/settings.ts` 的 `safeConfigSnapshot` 同步加 `agentOpening`（审计日志只记 present/length）。

`interviewAgent.ts` **未改** —— `handle.start(instruction)` 本来就接受指令参数。

### 反向验证暴露了两个弱测试

第一次反向验证（从 schema 移除 `agentOpening`）只有 **1/3** 新测试失败。另两个本来就过：
- 「往返持久化」—— `loadConfig` 对 `prompts` 是**透传不校验**的，没有 schema 也照样存下来。
- 「审计日志不落正文」—— `safeConfigSnapshot` 本来就只输出 `{present, length}`，永不输出正文。

→ 意识到这两个不是本次改动的守卫，于是补了真正可测的纯函数 `resolveOpeningInstruction`
并新增 `src/agent/prompt.test.ts`（5 例）。第二次反向验证（去掉 `trim()`）→ 2 例失败，确认有效。

**教训**：写完测试就要问「如果我把功能删掉，这个测试会红吗？」。`loadConfig` 透传、`safeConfigSnapshot`
只记元信息，这两个既有设计会让看似相关的测试完全失去守卫作用。

### 两个遗留项（刻意不动，已写进 ADR-051）

1. **默认文案题数自相矛盾**：「约 6–10 题」vs「达到约 8 题」，且与硬上限 `MAX_AGENT_QUESTIONS = 10` 都不同。
   本次**没改文案** —— 改提示词内容是行为变更，应与「可配置化」分开提交，便于单独回退和对比效果。
2. `loadConfig` 对 `prompts` 透传不校验，手改 localStorage 写入非字符串时，新字段与既有 `agentSystem`
   一样会在下游 `.trim()` 抛错。已在 `深度审查报告.md` A3 跟踪。

### 结论

`npx vitest run` **385 passed**（36 文件，基线 378，+7），`npx tsc --noEmit` 0 error，`npm run build` 通过。
改动 8 个文件 + 新增 `src/agent/prompt.test.ts`。文档：CHANGELOG 新条目、**ADR-051**、ARCHITECTURE-REVIEW 第 5 条
标记已修复并补实现说明、§四总结同步。**全部未提交**。

---

## 修复 ARCHITECTURE-REVIEW 第 6、7 条：LLM 每轮输入瘦身（ADR-052）

两条同根因：**把开发者需要的信息放进了 LLM 的每轮输入**。系统前缀每轮重发，Chrome 每轮重建 user prompt，
所以这是「每轮的固定税」而非一次性成本。

### 第 6 条：删掉系统提示里的开发者说明

`INTERVIEW_AGENT_SYSTEM_PROMPT` 里有一段 141 字符（~100 token，原报告估 80）的括号说明，内容是
「`只调一次 searchQuestions` / `不编造 id` / `not_found 回列表挑真 id` 已由工具代码保证，无需在 prompt 中约束」。
删除 → 内容原样移进 `prompt.ts` 文件头注释（紧邻它所描述的工具行为，比埋在模板字符串里好找）。
顺带 `[PROMPT-VERSION v1]` → `v2`，但**不迁移**用户保存的自定义 `agentSystem` 副本（手改过的提示不该被静默覆盖）。

### 第 7 条：Chrome `renderTools` 改紧凑签名

`JSON.stringify(t.parameters)` → `- getQuestion(id: string, format?: "choice" | "open") — <desc>`。
新增内部函数 `renderType()`（`enum`/`const`/`anyOf`+`oneOf` 联合/`array`/`integer`→`number` 塌缩；>40 字符 → `any`）
与 `renderParams()`（必填无 `?`、选填带 `?`；整条签名 >120 字符 → `args: object`）。工具级 description 一字不减。

### 关键实测（推翻了我自己的乐观估计）

| | 完整 schema | 紧凑签名 |
|---|---|---|
| 工具清单总长 | 1312 字符 | **947 字符** |
| schema 部分 | 513（39%） | 0 |

净省 **28%**，不是我一开始在代码注释里写的「省 2/3」。**体积大头是 7 条工具 description（644 字符，占 68%）**，
那是模型选工具的唯一依据，不能砍。所以在文档里显式改写了这条的结论：价值是「确定性信息清零」，不是数量级压缩。
**教训：先测量再写结论，不要凭 schema 看起来很长就推断占比。**

有意取舍：参数级 `description` 被丢弃（`getWeakAngles.topic` 的「要查询的 topic id」）。
将来参数名不自明，应改参数名或写进工具级 description，而非塞回签名。

### 测试与反向验证

+11 例：`prompt.test.ts` +3（5→8）、`chromeAgent.test.ts` +5（6→11）、`tools.test.ts` +3（26→29）。
反向验证（改回旧实现）→ 4 例 + 1 例分别变红，确认有效。
体积门禁用**真实** `createAgentTools` 产物（不是手写 fixture，防漂移）：相对阈值 `< 80%` 等效 JSON
（schema 被塞回来时两边都是 1312 → 直接失败）+ 绝对上限 `< 1100`（防描述逐条变长的温水膨胀）+ 工具名不丢。
`prompt.test.ts` 反向断言「4 条真红线仍在」，防止下次清理提示时误删真规则。

`npx vitest run` **396 passed**（36 文件，基线 385），`tsc --noEmit` 0 error，`npm run build` 通过。

### 文档同步

`ARCHITECTURE-REVIEW.md` 第 6、7 条标记已修复（含实测表 + 估值修正）、P1 节加进度说明、
§三路线图 P1 第 3/5 项划掉、§四总结与页脚复核记录同步；`docs/CHANGELOG.md` 新条目；
`docs/DECISIONS.md` **ADR-052**。**全部未提交。**

---

## 修复深度审查报告 C1（P0）：Agent 被要求复述它没有的题干（ADR-053）

### 用户复核后的表述更正

不是「必然幻觉」，而是 **prompt 要求模型生成它上下文里没有的数据 → 无法可靠复述真实题干 →
存在不必要的重述/幻觉风险**。根因是**职责冲突**，不是模型缺陷——这一点直接决定修复方向。

### 方案：改 prompt，不给 getQuestion 加题干

补 `question`/`options` 会让 Agent 在**选题阶段**拿到 `answer`/`explanation` 等它不需要的数据，
还扩大注入面。用户明确否决了这条路。

### 关键发现：要改的是 5 处措辞，不是报告点的 2 处

1. `prompt.ts` §你的职责 1；2. §呈现方式（整节替换为 §题目呈现（职责边界））；
3. §工具调用铁律 1；4. 开场指令步骤 2 + 结尾；
5. `tools.ts`：`getQuestion` 的 **description** 与**结果正文**（`请呈现给用户`）、`searchQuestions` 的 `nextStep`。

**第 5 项最关键**：那句「请呈现给用户」紧跟在工具结果之后，是触发编题干最直接的指令——
只改 system prompt 会被它覆盖掉。但改的只是**措辞**，`details` 仍严格是 `{id, format, matchedBy}`，
有测试断言 `Object.keys(details)` 守着。

**教训**：这类「prompt 与 UI 职责冲突」的问题，凡是有文字能触发模型动作的地方都要排查一遍——
工具 description、工具结果正文、nextStep 提示，都算 prompt。

### 与 ADR-052 的边界（重要，别搞混）

052 说「不写模型无需执行的说明」，但 053 在禁令后**保留了一句理由**（「题目正文不在你的上下文里」）。
判断标准：**理由是否服务于一条模型需主动执行的规则**——
服务于「无需执行」的一律不写；服务于「必须执行且反直觉」的禁令可留一句短理由
（模型天然想复述题目，不说「你没有这段数据」压不住）。已写进 `prompt.ts` 头注释。

### 保留弹性

不写「绝对禁止提及题目内容」——开放题反馈里引用关键概念有价值（「你解释了 KV Cache 的作用，但…」）。
禁令只针对「重新生成/完整复述」，测试里同时断言「含『可以简短引用题目的关键概念』」且「不含『绝对禁止』」。

### 验证

+6 例（prompt.test.ts 8→12、tools.test.ts 29→31）。反向验证：塞回旧 §呈现方式 → 1 例失败；
改回 `请呈现给用户` → 1 例失败。顺带把 `textOf` 从某个 describe 内提升到模块作用域。
`npx vitest run` **402 passed**（36 文件，基线 396），tsc 0 error，build 通过。

文档：`深度审查报告.md` C1 标记已修复 + 表述更正 + Context 评分 5.5→6.0 + P0 计数 5→4 + 第六节表格更新；
`docs/CHANGELOG.md` 新条目；`docs/DECISIONS.md` **ADR-053**。**全部未提交。**

### 补记（同日稍后）

用户确认「按你的判断修改」→ `tools.ts` 措辞改造保留。
补上了漏掉的 `docs/ARCHITECTURE.md`（AGENTS.md §2 要求架构级变更必须同步）：
「LLM 能力边界」节新增「题目呈现的职责边界（ADR-053）」条目（数据流图注 + 为什么不补题干 + 5 处改动面），
并在 DeepSeek 的 Tool Calls 条目补交叉引用（省下的输出 token 不再重复 UI 已渲染的题干与选项）。

---

## 修复深度审查报告 C2（P1）：评分决策字段未进 LLM 上下文（ADR-054）

### 重要发现：实现已在工作区，非我所写

`git status` 显示 `src/agent/tools.ts` / `src/domain/evaluation.ts` / `src/domain/evaluation.test.ts` 三个文件
**未提交改动**，其中已含完整的 C2 修复（`describeEvaluationSummary` / `describeLevels` + 9 例测试 + ADR-054 引用注释）。
HEAD 里仍是旧代码。判定为**并行工作流写入共享工作区**的结果，不是我写的——本轮我做的是**验证 + 补文档**，
没有重新实现（避免与并行会话冲突）。

### 实现符合用户指定方案

- 保留 `details`（完整 `result`）不变；
- LLM-visible 文本补 `综合评分 / 维度序级（0-4）/ 薄弱点 gaps`；
- **不带** `evidence`/`strengths`/`feedback`（用户复核明确反对全量 stringify）；
- 不删工具描述（反而把描述与行为重新对齐）；
- `describeLevels` 四维相同塌缩成「四维均为 X」（选择题按对错判定，四维必全 0/全 4）。
- 测试锁死核心诉求：两个 overall 相同、维度构成不同的答案摘要必须可区分。

### 提炼出的通用原则（写进了 ADR-053/054）

**Agent Prompt 要求 Agent 使用 X → Tool 是否把 X 放进 LLM-visible 的 text？**
C1（要求复述题干 / Tool 只有 id）与 C2（要求按 levels/gaps 决策 / Tool 只给 overall）都是同类
**Agent context contract 不一致**。`textResult` 的 `details` 给程序/UI/logging，**不**自动进模型上下文。

### 验证

`npx vitest run` **411 passed**（36 文件，基线 402，+9 来自 C2 的 evaluation 测试），tsc 0 error。
文档：深度报告 C2 标记已修复并按同源原则改写、P1 计数 11→10；新增 ADR-054；
`docs/ARCHITECTURE.md` LLM 能力边界节补交叉引用；CHANGELOG 新条目（均注明「实现由并行工作流写入，仅补文档」）。
**全部未提交。**

---

## 复盘修正（同日，重新验证 git 后）

上一轮对话总结里「`af4f72c "ddd"` 是 broken commit（提交了 tools.ts 却没提交 evaluation.ts，build 不过）」**不成立**——实测：
- `git show af4f72c:src/agent/tools.ts | grep describeEvaluationSummary` → 0 命中；`af4f72c:src/domain/evaluation.ts` 同样 0 命中。
- C2 完整修复（tools.ts 调用点 + evaluation.ts 两个纯函数 + 9 例测试）**至今全部未提交**，只躺在共享工作区，且内部自洽（tools 导入 / evaluation 定义齐全）。`af4f72c` 本身是干净的旧代码。

### ⚠️ 工作区混入了非本次范围的破坏性依赖改动

`git diff --stat af4f72c` 显示，除本次 ADR/C2 的文档+源码外，工作区还多了：
- `package.json`：React `^18.3.1`→`^19.2.8`、`react-dom` 同、`@types/react ^18.3.11`→`^19.2.18`、`@types/react-dom`→`^19.2.5`、`antd ^6.6.1`→`^6.6.2`（React 18→19 **主版本跃迁** + antd 次版本升）。
- `package-lock.json`：**被删除**（8246 行移除）。
- `node_modules` 实测：react 19.2.8 已装、`node_modules/vitest` 在，但 `node_modules/.bin` 为空（`npm test` 跑不了，需 `node node_modules/vitest/vitest.mjs run`）。

判定：这俩依赖改动**不是我做的**，是并行会话 / 某次 `npm install` 残留进共享工作区。**绝不能和 ADR/C2 一起提交**——React 19 是主版本迁移，需单独评估（JSX 行为、types 破坏、antd 6 兼容性）。

### 验证（当前工作区）

定向跑 ADR-052/053/054 相关 4 个测试文件 → **68 passed**（evaluation 14 / tools 31 / prompt 12 / chromeAgent 11）。C2 全绿自洽。全量 411 受 `.bin` 空环境影响未重跑，但定向已覆盖本次改动面。

### 决策已定（用户回复）

1. ADR-052/053/054 + C2 修复 → **合成 1 条提交**（`c5e420f`，7 文件 / +202 −10，仅 docs + src/domain/evaluation + src/agent/tools + 测试 + 深度审查报告）。
2. 工作区 React 19 + antd 升版 + 删 lockfile → **先保留，稍后处理**（未纳入 `c5e420f`，仍为未提交改动）。
3. `.workbuddy-ai/memory/2026-08-30.md` 按用户明示范围**未纳入提交**，留作未提交。

⚠️ 提交期间 `package-lock.json` 由 `D`（已删）变 `M`（被重新生成）——印证**并行会话仍活跃**（某次 npm install 在后台跑）。提交只 stage 了 7 个指定文件，未碰 package.json/lock，React 19 改动仍隔离在工作区。

`c5e420f` 仅落本地 `main`，未 push（推送前应先处理 React 19 那坨依赖改动，避免把主版本跃迁混进本次语义修复）。

---

## 任务六：设置 → 日志 tab 改用 antd Table 显示 log（含 error log）

用户要求用 https://ant.design/components/table 显示日志。

### 现状
`src/components/settings/SettingsPanel.tsx` 已有 `logs` tab，原先用 antd `List` 渲染 `getErrorLogs()` 回来的 `ErrorLogEntry[]`（错误/审计/运行三类）。

### 改动（仅此一个文件）
- import：`List` → `Table` + `import type { TableColumnsType }`。
- 新增模块级 `LOG_TABLE_COLUMNS: TableColumnsType<ErrorLogEntry>`：时间(可排序,默认倒序) / 类型(Tag 红=错误 蓝=审计 绿=运行) / 级别(Tag) / 模块 / 事件 / 信息(ellipsis)。
- 用 `<Table<ErrorLogEntry> rowKey={r=>r.id ?? r.createdAt} ... expandable={{ rowExpandable: detail!=null, expandedRowRender: <pre>JSON.stringify(detail) </pre> }}>` 替换原 `<List>`；保留外部 `logKind` Select 过滤、刷新、清空按钮与 Empty/加载态。
- 错误日志满足「包括 error log」：全部 kind 都展示，类型=错误 红色高亮，且 Select 可单独筛「错误」。

### 验证
`npx tsc --noEmit -p tsconfig.app.json` → **0 error**（整个工程，含 node_modules 里未提交的 React 19 + antd 6.6.2）。
→ **修正一个早先的担忧**：React 18→19 主版本跃迁**并未破坏 tsc / 构建**（antd 6 与 React 19 类型在此工程兼容；组件没用到 React 19 移除的 API）。所以 React 19 那坨改动「不破坏构建」但仍是主版本迁移，是否保留仍待用户与并行会话确认。

未提交（与 React 19 改动隔离，沿用 c5e420f 之后「先保留、稍后处理」的决定）。

---

## 任务七：修复深度审查报告 C4（P1）—— id 纠错池不再回退全题库（ADR-055）

用户给的修复 spec（@long-text）要点：保留 P1；「永久灌进上下文」表述要更严谨（实为一条异常历史消息在 replay 中持续占 token，非永久驻留）；
不用 `slice(0, 20)`（太随意、可能截断对的 id）；不机械把 `searchQuestions` 的 `limit` 50→20；
最小修复 = `deliverableIds` 不再回退全题库、只返 `lastSearchIds` 候选（cap ~3-5）或引导 Agent 调 `searchQuestions`；
`fuzzball` 仅当已为 `validateVariant` 导入才复用、不为这题新引；不写死「4.5KB」，改看 token 数 + replay 次数。

### 改动（仅 `src/agent/tools.ts` + `src/agent/tools.test.ts`）
- `validIdPool` → `deliverableIds(session, byId)`：`return session.lastSearchIds.filter(id => byId.has(id) && !isDelivered(session, id))`——**删除全题库回退分支**（`tools.ts:109`）。
- `not_found` / `topic_exhausted` 改为**四路 hint**（`tools.ts:186-207`）：有候选只列 ≤5 个真实 id（`MAX_SUGGEST=5`）；题库全考完→`finishInterview`；主题考完→换 topic 搜索；完全没搜过→先 `searchQuestions`。绝不灌全题库。
- 选题兜底示例 id 改用 `deliverableIds` 并加空集合守卫（`tools.ts:249`）。
- `searchQuestions` 的 `limit` 上限仍是 50（`tools.ts:45`），未动。无 `fuzzball` 导入。

### 验证
新增 2 个 C4 回归测试、修正 2 个依赖旧回退的旧用例（先 `search.execute` 注入 `lastSearchIds` 再断言）。**33 passed**，tsc 0 error。
反向验证：临时还原 `Array.from(byId.keys())` 回退 → 新回归测试失败（吐出 `c4-0…c4-7` 全量），证明测试守住了根因。

### 文档同步（AGENTS.md §2，已完成）
- `docs/DECISIONS.md` 顶部新增 **ADR-055**；
- `docs/CHANGELOG.md` 顶部新增条目；
- `深度审查报告.md` C4 标记已修复、按用户更正改写措辞、记录四项否决点，并注明 `limit` 50 未动属独立维度；
- `docs/ARCHITECTURE.md` LLM 能力边界节补 ADR-055 交叉引用（与 C1/C2 同归「避免把确定性大块数据塞进每轮重放的上下文」）。

### 状态
**代码 + 文档全部未提交**，落在 `c5e420f` 之后工作区（`M src/agent/tools.ts`、`M src/agent/tools.test.ts` + 4 个 md）。

---

## ⚠️ 同期 git 状态重核（提交前务必看清）
- `af4f72c (ddd)`（16:48）其实**包含**大量 C2/ADR-048~054 的 docs + 源码（`docs/DECISIONS.md` +138、`docs/CHANGELOG.md` +83、`src/agent/tools.ts` +9、`深度审查报告.md` +161 等）→ 早先「`af4f72c` 是 broken commit（只提交了 tools 没提交 evaluation）」的判断**是错的**：当初 grep 查了 `tools.ts` 里的 `describeEvaluationSummary`（它其实在 `domain/evaluation.ts`），才误判。该 commit 是干净的旧代码，C2 完整修复至今从未提交、只躺在共享工作区。
- `6250e79 (adddd)`（17:07）提交了 React 19 跃迁（`package.json` `react ^18.3.1`→`^19.2.8`、`react-dom` 同、`@types` 升 19、`antd ^6.6.1`→`^6.6.2`）+ `package-lock.json` 重生成 + memory。
- `be7945e (dddd)`（17:13）提交了**任务六 Table 改造**（`src/components/settings/SettingsPanel.tsx` +120） + memory。
- 仅 **C4 修复（`tools.ts` + `tools.test.ts` + 4 个 md）** 仍在工作区未提交；其余 ADR/C2/Table/React19 已被并行会话分批提交，但 commit message 全是 `adddd`/`dddd` 无意义，且存在重叠提交。

### 待用户决策（提交策略）
现状：主分支上有 3 个无意义 message 的本地 commit（含 React 19 主版本跃迁），C4 修复未提交。需要确认：
1. C4（代码+文档）如何提交——合成 1 条干净 commit（沿用 ADR 工作偏好）？还是先不提交？
2. 是否需要整理/重写 `adddd`/`dddd`/`ddd` 这些无意义 commit message（如 `git rebase -i` reword / 或 reset 后重新聚合）？
3. React 19 主版本跃迁是否保留、何时评估、是否单独成 commit？`c5e420f` 仍**未 push**，推送前应先把 React 19 这坨依赖改动理清。

---

## 任务八：完整 repo 代码 / 架构 / context / prompt 审查（2026-08-30）

新增根目录报告：`完整仓库审查报告-2026-08-30.md`。本轮只读审查，没有修改既有源码。

### 验证基线
- `npm run typecheck`：0 error；`npm test`：36 文件 / 413 tests；顺序重跑 `npm run build` 成功（8780 modules transformed，3.15s），但有大 chunk 警告。
- 当前题库校验（含未跟踪 `src/data/questions/rl-agent-infra-2026-08.json`）：1098 题 / 80 节点 / 196/196 coverage；`question:audit` 349 个 P2；`lint:length` 254 题超 1.8×；`lint:bias` 122 strong。
- 当前工作区有未跟踪：`docs/improvement_plan/plan0830_2.md`、`src/data/questions/rl-agent-infra-2026-08.json`、`temp/draft-rl-infra.json`；未确认归属前不删除/移动/提交。

### 本轮最重要的新结论
- 没有确认到需要整体重写的 P0；但发现公开 Cloudflare 代理 `server/index.js:35-39` / `worker/index.ts:24-36` 的 `new URL('//host', base)` 绕过固定上游，存在 SSRF/open-relay 风险（P1）。
- Agent 结束流程非幂等、`onComplete` 未 await、结束后仍可能 `persistDraft`，并且 dispose 未 abort/clear watchdog；建议统一 `finishSession` + 串行草稿保存。
- `getQuestion` 的 direct id 分支绕过 `isDelivered`，`searchQuestions` 总是返回固定前 N 题；存在重复出题/循环（P1）。
- pi-agent-core 默认 `toolExecution = "parallel"`，而工具共享 `session` 可变状态；必须顺序执行或逐工具守卫（P1）。
- Provider/残缺 JSON 可能被 evaluate 降级为 0 分；`null` evaluation 也被 `countEvaluated` 当成已评，停止计数不一致（P1）。
- Variant 仍未携带当前 Session format；semantic gate 把 explanation 纳入 any-match 证据，和 prompt 的 requiredConcept 实际考察契约不一致（P1）。
- 用户答案原样进入 Agent/Chrome/EVAL prompt，native history 无预算，Chrome 静默截断，存在 prompt injection 与双引擎决策不一致（P1）。
- IndexedDB 读边界尚未兑现架构文档声称的 Zod 校验（P1）；README/ARCHITECTURE 仍写 React 18、1084 题，实际 package 为 React 19、当前校验 1098（P2）。

之后若修复，优先按报告第一批：代理安全 → Agent finish/persistence → sequential tools/去重 → 评估错误语义 → variant contract → context budget/injection → storage validation。

---

## 任务九：落地 P0-1 / P0-2 / P1-1~P1-4（用户 @long-text 复核后指定）

任务八把 variant format / semantic gate 列为 P1，用户后续复核把它们升级为 P0（真 bug）并给了精确改法。本轮全部实现 + 补测试 + 文档同步，已本地提交。

### P0-1（variant 形态对齐，ADR-056）
- 根因：`finalizeQuestion` 调 `provider.generateVariant(sq.question)` 不带 `sq.format`，`buildUser`/`validateVariant`/`applyVariant` 一律按 `canonical.formats.choice` 判形态 → 双形态题（~1078/1084）永远生成选择题变体，open 形态被错误塞 options/answer。
- 改：`LLMProvider.generateVariant(q, format?)` → `ai/variant.generateVariant(q, complete, format?, ...)` → `domain/variant.validateVariant/applyVariant(canonical, v, format?)`；`buildUser` 据 format 输出对应 JSON 契约。`format==='choice'` 才校验/落地 options，否则按 open 跳过；不传 format 回退旧行为。

### P0-2（semantic gate 收紧，ADR-057）
- 旧 `hasConceptEvidence`：把 `explanation` 当证据 + 「任一 token 命中即通过」→ 漂移变体靠解析蒙混通过。
- 改：证据文本仅取「题干+选项」（排除 explanation）；覆盖率门槛 `requiredCoverageMet`：`need=max(1, round(N*2/3))`（3条需≥2、2条需≥1、1条需全中）；保留 fuzzball 兜底。旧函数拆为 `anchorHasEvidence` + `hasMinimalEvidence` + `requiredCoverageMet`。

### P1-1~P1-4（ADR-058）
- P1-1 `searchQuestions` limit 上限 50→20、默认 10→8。
- P1-2 Agent system prompt v3→v4 + 「工具调用节制」段。
- P1-3 `questionResultSchema` 增 `missingConcepts?`；开放题评估识别的缺失概念并入 `commonWeaknesses`（`aggregateGaps`→`aggregateWeaknesses`），选择题不伪造 gap。
- P1-4 EVAL system prompt v2→v3 + 「维度适用性」段（不适用维度给中性 2 而非低分）。

### 测试坑（已解决）
- `domain/variant.test.ts`：原 graph-rag 用例 topic 写 `'graph-rag'` 但节点 `topic` 字段是 `'rag'`、且 anchor0 含「上下文」→ 误触 FORBIDDEN `下文` 假阳性。改用 `realtime-interaction`（3 条干净 required）测 1/3 拒 / 2/3 过；P0-1 块改用 `regularization` 主题（default variant() 已含 required 证据）。
- `ai/variant.test.ts`：变体题干只写 `regularization` 缺 required 证据，P0-2 后不过门 → 前缀补 `L2 正则化`。
- `tools.test.ts`：`generateVariant` 调用断言补第二参 `format`（`'choice'`）。

### 验证 & 提交
- 全量 `vitest` **422 passed**（36 文件），`tsc -p tsconfig.app.json` 0 error。
- 文档同步：ADR-056/057/058、CHANGELOG、ARCHITECTURE 变体操/React19/`server`+`worker` 代理说明、README React18→19。
- **排除并行会话文件**：`src/data/knowledge/llm-architecture.json`（interpretability 节点）、3 个未跟踪 question json、`docs/improvement_plan/plan0830_2.md` 均不纳入本次提交。
- 提交策略：合成 1 条干净 commit（本地 main，未 push，避免和并行会话的 `adddd` 推送冲突；React 19 跃迁由并行会话另管）。

---

## 任务九（续）：复核收口——P0-1 修订 + P1 第4项 telemetry + P1-3 收回（ADR-059，commit f489cda）

用户复核上一轮 `f40c2d5` 后又给了「只做 4 件事、做到这里停手」的收口 spec，本轮全部落地并已本地提交 **f489cda**（未 push）。

### 改了什么
1. **P0-1 修订（真 bug）**：上一轮把 `format` 形参扩成了自造的 `VariantFormat`（`'choice'|'open'|'single'|'multiple'`），`buildUser` 在 `format==='choice'` 时没落到 `q.formats.choice!.type`，导致双形态单选题被误当多选题。
   - 删除 `VariantFormat`（`schemas/common.ts`），`generateVariant/validateVariant/applyVariant` 的 `format` 全部回到 `FormatId`（`'choice'|'open'`）。
   - `buildUser` 改为 `const fmt = format === 'choice' ? q.formats.choice!.type : 'open';`（single/multiple 由题面 `type` 推导）。
2. **P1-1 修正**：`searchQuestions` limit 上限 50→20 保留，但**默认从 8 改回 10**（`tools.ts`，用户明确「默认 10 保持」）。
3. **P1 第4项 兜底 telemetry**：`InterviewAgentSession` 增 `fallbackReason?`（timeout/model_error/agent_no_action）+ `fallbackCount`；`ensureQuestionDelivered(reason)` 接管时写入首次原因、累加次数并 push 一条 `kind:'event'` 日志到 `session.log`。看门狗→timeout、流式报错→model_error、run 静默收尾→agent_no_action。仅观察，不驱动逻辑。
4. **P1-3 收回**：用户撤销「`missingConcepts` → `commonWeaknesses`」。`aggregateWeaknesses` 仅统计 `gaps`；`missingConcepts` 仍落库 `QuestionResult` 但不汇入长期薄弱项（「暂不改动 Learner Memory 数据结构」）。

### 验证 & 提交
- 全量 `vitest` **422 passed**（新增 `interviewAgent.test.ts` 兜底 telemetry 断言：reason=`agent_no_action`、count=1、日志存在），`tsc` 0 error。
- 文档同步：新增 **ADR-059**；CHANGELOG 顶部新条目 + 修正上一轮误记的「默认 8」/「P1-3 生效」两行；ARCHITECTURE 变体段补 single/multiple 子类型推导说明。
- 提交 `f489cda`（本地 main，未 push；origin/main 停在 572c1a4）。改动 13 文件 / +74 −28。

### 关键判断（避免重复踩坑）
- 用户「默认 10 保持」与上一轮我改的 `?? 8` 冲突 → 以用户最新 spec 为准，回退到 10。
- 用户 #8 明确否决了上一轮我实现的 P1-3 → 撤销映射，但**保留** `missingConcepts` 字段与 `sessionFromQuiz` 的捕获（属用户数据契约，删它是破坏 schema 的结构性改动）。
- 这些收口项恰好踩中 AGENTS.md §2「代码+文档同步」——每动一处逻辑就同步 ADR/CHANGELOG/ARCHITECTURE。

---

## 任务十：用户 7 项最小 TODO 收口（ADR-060，commit 928f871）

用户从《完整仓库审查报告》砍掉一半以上建议，收窄为 **7 项最小 TODO**，明确「做完这 7 项，先跑真实使用和测试，不再继续加架构」。`#6 Variant format=SessionQuestion.format` 与 `#7 Variant semantic gate 小幅收紧` 已在 ADR-056/057 + ADR-059（f489cda）完成；本轮落地剩余 5 项，本地提交 **928f871**（未 push）。

### 实际改了什么（5 项）
1. **Proxy SSRF 加固（P0）**：`server/index.js:35-39` 与 `worker/index.ts:24-36` 在 `new URL(stripped, base)` 之后校验 `targetUrl.hostname === TARGET_HOST && protocol === 'https:'`。拒绝协议相对（`//evil.com`）/绝对 URL 改目标主机（否则变开放中继并泄露 Authorization），违规返回 400。
2. **Agent 工具串行（P0）**：`createInterviewAgent` 构造 `Agent` 时显式 `toolExecution: 'sequential'`（SDK 默认 `parallel`；共享 `session` 可变状态在并行工具执行下会竞态）。已 grep node_modules 确认选项名为 `toolExecution?: ToolExecutionMode`。
3. **生命周期幂等（P0）**：`interviewAgent.ts` 的 `dispose` 由「仅 unsubscribe」改为「清看门狗 → `agent.abort()` → unsubscribe」；`useAgentInterview.finalize()` 加 `finalizedRef` 守卫保证 `onComplete` 只落库一次；`start()`/`restart()` 重置守卫；`restart()` + App 卸载清理均走真正 abort 的 `dispose()`。
4. **防重复出题（P0）**：`tools.ts` `getQuestion` 真实 id 命中分支补 `isDelivered(session, q.id)` 守门——已交付 id 降级到自纠正路径另选（新增 `already_delivered` reason + 提示文案）。topic 兜底分支本已排除已交付题。
5. **Provider 错误 ≠ 0 分（P1）**：`evaluate.ts` 新增 `EvaluationParseError`；`parseEvaluation` 遇非空但不可解析/结构不符的模型输出时**抛错**（而非降级全 0 分污染 Learner Memory）。上层 try-catch（`tools.evaluateAnswer` / `interviewAgent.fallbackAdvance` / `choiceAdvance`）捕获后记为 `null`（跳过评分）。空输入仍走 `isAnswerEmpty` 短路 → null，不进评分 0 分。

### 测试与验证
- 全量 `vitest` **423 passed**（36 文件，基线 422 + 本次新增 1 例），`tsc --noEmit` 0 error。
- `evaluate.test.ts` 新增「不可解析 JSON → 抛 `EvaluationParseError`」；原有「残缺 JSON → 缺失维度按 0 级兜底」用例用的是**合法但部分缺字段**的 JSON（safeParse 仍通过），保持不变，确认未退化为 0 分污染。
- `extractJSON` 本身在无法解析时会抛泛型 `Error`，所以 `parseEvaluation` 用 try/catch 包住它统一转成 `EvaluationParseError`，保证上层能识别「评分解析失败」并一致记为 null。

### 排除的并行会话文件（不纳入提交）
`.workbuddy/memory/2026-08-30.md`（并行会话 memory）、`src/data/knowledge/rag.json`（并行会话加了 `sentence-embedding` 节点 +59 行但无对应题，导致 `knowledge.test.ts` 偶发失败——实测隔离运行 9/9 通过，属并行会话数据半成品）、`src/data/questions/multi-vector-retrieval-2026-08.json`、`src/data/questions/sentence-embedding-2026-08.json`（两个未跟踪 question json，并行会话产出）。只 stage 了本次 10 个文件。

### 刻意不做的（用户明确否决）
不做 BKT/IRT、embedding judge、第二个 LLM judge、复杂 context compaction、Agent 架构重设计、语义搜索、KV Cache 抽象层；不大规模改 Learner Memory；不为了题库历史质量债务暂停功能开发。Context budget / IndexedDB+Zod / delivered-attempted-scored 三态计数 列为「可以后做」而非现在做。

---

## 任务十一：新增 12 道 AI Search / Retrieval Engineering 题（接入知识图谱）

用户 @long-text 要求新增 12 道「AI Search / Retrieval Engineering 能力覆盖」题（非名词题库），并给出知识图谱树：`ai-search → information-retrieval / ranking / agentic-search / search-quality / search-infrastructure`。原 verbatim 12 题 spec 不在我的 carried-over 上下文里，我据「框架 + 优先 6 题清单 + 5 子树」重建，全部 12 题均落地（优先 6 题全部包含：① Hybrid vs Dense ② 传统 IR ③ Search Agent 真伪 ⑥ Late Interaction ⑦ 检索评估 ⑨ 停止条件 ⑩ Search as Code）。

### 产物（2 个新文件，未提交，隔离于并行会话改动）
- `src/data/knowledge/ai-search.json`：5 个知识节点（area 分别归 llm-applications / agent-engineering / ai-systems；节点 `topic` 统一设 `"ai-search"` 以便知识图内聚成一族）。
- `src/data/questions/ai-search.json`：12 道题，`topic` 分别映射到上述 5 节点；角度覆盖 comparison/mechanism/debugging/scenario/tradeoff/system-design；题型 10 多选 + 2 单选（83% 多选，满足 AGENTS.md §4.2 ≥2/3）；每题含 choice + open 双形态与 `misconceptions`。

### 验证（全绿）
- `node scripts/add-question.ts --file draft.json --check`：通过（topic 有节点、无长度泄题、无重复选项、单选占比达标）。**关键坑**：draft 不能放在 `src/data/questions/` 里跑 `--check`，否则脚本会把同文件 id 当成 existing 报「id 已存在」——先放仓库根 `ai-search-draft.json` 校验，再用 `--write --output` 落到 questions/。
- `node scripts/validate-questions.ts`：1163 题 / 89 节点全过（题库级多选 47.8% 是历史存量提示，非失败）。
- `npx vitest run src/domain/knowledge.test.ts`：9/9（每个新节点都有题支撑，否则该测试会报「知识点 X 缺少任何题目支撑」）。
- `coverage.test.ts` / `blueprint.test.ts`：19/19。5 个 ai-search 节点已不再出现在 coverage gap。

### 决策与边界
- 未改 `taxonomy.ts`：节点 `topic` 用 `"ai-search"` 字符串即可在知识图内聚，无需动 taxonomy（避免范围蔓延/潜在破坏）。如需 roadmap 集成可后续补。
- 未自动提交：工作区当前有并行会话改动（App.tsx、chrome.ts、pytorch question 等），只动了这 2 个新文件，等用户确认是否单独提交这 2 个。
- Hypencoder / Wormhole Vectors / Semantic IDs 按用户指示**暂不做**独立核心题（前沿主题延后）。
