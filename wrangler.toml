# Cloudflare Pages 部署配置
# 本应用是纯静态 SPA，构建产物在 dist/，使用 HashRouter（无需 Pages Functions / SPA fallback）。
# 发布范围：仅 dist/ —— Python 分析脚本与经 Git LFS 管理的离线模型 models/ 不参与浏览器构建，不会被发布。

name = "ai-interview-trainer"
pages_build_output_dir = "dist"
compatibility_date = "2025-08-30"
