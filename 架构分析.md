# AI 面试训练器 — 架构分析报告

> 生成时间：2026-08-29  
> 分析对象：`saga/ai-interview-questions`（`ai-interview-trainer` v0.1.0）

---

## 一、总体印象

这是一个**设计有明确意图、文档较为完整**的个人前端项目。架构遵循了较严格的分层原则，ADR（架构决策记录）数量可观（已到 ADR-045），说明项目在演化过程中有刻意积累设计上下文。

**总评：合理，有亮点；双引擎共享核心与 Chrome Agent prompt 体积风险已完成针对性收敛。**

---

## 二、分层结构评估

```
schemas/          ← 数据契约（Zod）：Question / LearnerProfile / AIConfig …
domain/           ← 纯逻辑（无 I/O）：选题 / 评分 / 自适应 / 覆盖率 …
application/      ← 引擎编排：buildSession / evaluateAnswer / nextAdaptiveStep
agent/            ← Agent 运行时（pi-agent-core）：实验性并行路径
ai/               ← LLM 适配层：Provider 抽象 / fallback 链 / chrome / pi-ai
storage/          ← Dexie (IndexedDB)：LearnerProfile + Sessions + ErrorLog
data/             ← 静态 JSON 题库 + knowledgeMap + taxonomy（source of truth）
components/       ← 按功能分组的 React 组件（feature-folder 风格）
hooks/            ← useTrainingSession（会话状态机）
App.tsx           ← 页面路由 + 全局布局
```

### ✅ 亮点

**1. 依赖方向严格向下**  
`schemas → domain → application/agent → ai → storage/components`，不存在下层反向引用上层的问题。`LLMProvider` 接口作为 boundary type 定义在 `types.ts`，让 `domain` 层对 AI 完全无感。

**2. domain 层是真正的纯函数**  
`domain/` 内所有文件无 I/O、无 React、无 LLM 调用，逻辑可独立测试。测试覆盖率扎实（每个 domain 文件都有同名 `.test.ts`，总计约 1600 行测试）。

**3. Zod schema 作为唯一数据契约**  
`schemas/` 统一定义所有数据结构，运行时边界（题库加载、LLM 输出、API 配置）全部走 Zod 校验，避免了"数据形状在类型和运行时脱节"的常见问题。

**4. LLM 作为可选增强，不是核心依赖**  
`InterviewEngine` 在 `useAI=false` 或无有效 API Key 时完全降级为确定性逻辑，不崩溃，不返回假分数（返回 `null` 而非 0）。`FallbackProvider` 降级链设计干净。

**5. ADR 文化**  
45 条 ADR 详细记录了关键取舍，让代码里的"为什么"有据可查，后来者（包括 AI Agent）不会重复踩坑。这是工程成熟度的体现。

**6. 静态题库 + 启动时校验**  
题库全量校验在启动时抛出，开发阶段立刻暴露数据问题，不会等到用户答题时才出错。

---

## 三、风险点与问题

### ⚠️ 风险 1：双引擎并存，边界模糊，维护压力增长快

**现状**  
`src/application/interviewEngine.ts`（确定性引擎）和 `src/agent/interviewAgent.ts`（Agent 引擎）是两条**会话编排**路径，在 `App.tsx` 中分别对应不同页面（训练/面试 vs Agent 面试）；评分、题型过滤、变体生成已统一由 `application/sessionEvaluator.ts` 提供。

**问题**  
- 两条路径的选题编排仍有意不同：确定性引擎负责规则流程，Agent 负责自主决策；共享的评分、题型过滤和变体生成不再在 `agent/tools.ts` 重复实现。
- 随着功能演进，同一个 bug 需要在两处同时修，同一个产品需求需要在两处同时实现。  
- `AgentInterviewPage.tsx`（447 行）是项目最大的单文件组件，内部包含了过多的会话状态管理，进一步放大了复杂度。

**建议**  
Agent 作为未来正式方向保留；共用的评分/题型过滤/变体逻辑已提取到 `application/sessionEvaluator.ts`，Agent 只负责决策“问哪道题”，确定性工作由共享核心执行。

**Chrome Built-in AI prompt 体积**

Chrome Prompt API 没有原生 function calling，工具协议仍需通过提示词注入。但 `src/ai/chromeAgent.ts` 已将稳定的 JSON 协议放入 system prompt，user prompt 只发送最近 10 条历史（单条最多 900 字符）与紧凑工具 schema，避免完整历史和长工具描述随轮次线性增长。`src/ai/chrome.ts` 继续通过 system-keyed base session + `clone()` 复用稳定 system；Chrome 不支持 `clone()` 时保留独立 session fallback。该优化以降低输入体积和等待时间为目标，工具 JSON 解析及确定性 fallback 仍是最终护栏。

---

### ⚠️ 风险 2：`App.tsx` + `useTrainingSession` 承担了过多职责

**现状**  
`App.tsx`（333 行）包含：全局路由、全局 layout、learner profile 加载与初始化、训练会话 UI 状态（题目展示、答题、评分动画）以及多个 inline 子组件（`LearnerBound`、页面渲染分支）。`useTrainingSession`（344 行）是一个大型状态机 hook，管理整个会话的生命周期状态。

**问题**  
- `App.tsx` 兼具路由容器和训练页面的职责，测试和维护时边界不清。  
- `useTrainingSession` 作为单一 hook 管理的状态节点过多（当前题目、答案、评分结果、会话步骤、自适应状态……），在会话流程变复杂时可读性会快速下降。

**建议**  
- 将路由层（`App.tsx`）与训练流程页面（`TrainPage.tsx`）拆分；  
- `useTrainingSession` 可按关注点拆为 `useSessionLifecycle`（会话初始化 / 结束 / 持久化）和 `useAnswerFlow`（答题 / 评分 / 下一题）两个更小的 hook，或引入显式状态机（如 `useReducer`）让状态转换更可追踪。

---

### 🔍 次要问题

**问题 3：`src/data/` 入口多样，语义稍模糊**  
`questionBank.ts`、`knowledgeMap.ts`、`taxonomy.ts`、`conceptGraph.json` 分散在 `data/` 下，彼此关系不够直观。`knowledgeMap.ts` 和 `taxonomy.ts` 的职责边界建议在 `data/README.md` 中补充一段说明。

**问题 4：无跨设备同步**  
全量本地存储（Dexie IndexedDB + localStorage）是轻量化决策的合理结果，但对于有多设备需求的用户是硬限制。这是已知取舍，建议在 README 中明确标注，避免用户期望管理失位。

**问题 5：题库全量 eager import**  
`import.meta.glob('./questions/*.json', { eager: true })` 在题目数量较少时没有问题。当题库规模增长到几千题时，启动时间和内存会受影响。`questionBank.ts` 的注释中已预判此问题（"题库规模到达需要按需加载时再引入动态 import"），这是合理的 YAGNI 决策，但需要有明确的触发阈值（比如题目数超过 1000 时再处理）。

---

## 四、总结

| 维度 | 评价 |
|------|------|
| 分层清晰度 | ✅ 高 |
| 依赖方向 | ✅ 严格单向 |
| 纯逻辑可测试性 | ✅ 扎实 |
| LLM 解耦 | ✅ 到位 |
| 文档与决策记录 | ✅ 完整 |
| 双引擎并发维护风险 | ⚠️ 需关注 |
| App/Hook 职责边界 | ⚠️ 需关注 |
| 跨设备同步 | 🔍 已知限制 |
| 题库可扩展性 | 🔍 有预案，尚未触发 |

**核心结论**：架构设计总体合理，分层意图清晰，测试文化健康。主要风险在于双引擎并行带来的代码重复与维护成本，建议在 Agent 模式确定留存后，优先完成两条路径的核心逻辑统一，再推进功能扩展。
