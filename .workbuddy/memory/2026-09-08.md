# 2026-09-08

## 架构文档 vs 代码 审计 + 修复（承 2026-09-07 的审计结论，用户批复「修复」）

只改文档与注释，未改运行时行为。改动集中在三处：

- `docs/ARCHITECTURE.md`：
  - 存量计数 77 文件/1317 题 → **82/1357**；双形态 1237+80 → **1232+125**；
    knowledge 16 文件/123 节点 → **19/124**；变体双形态 1078/1084 → **1232/1357**。
  - 「10 角度」→ **19 角度**；薄弱阈值改为引用常量 `WEAK_AVG(75)/WEAK_MASTERY(0.75)`（不再写死 0.85/85）。
  - `schemas/types.ts` **不存在** → 标注跨层行为契约在根 `src/types.ts`，补漏列模块。
  - `expandWithPrerequisites`/`isMastered`/`isAttempted`/`WEAK_*` 归属 **learner.ts**（原先误写 conceptGraph.ts）。
  - 边界：domain 对 schemas 现为 **type-only**，两处历史值依赖（learner.ts、variantPool.ts）标注为例外。
  - 「Learner memory 参与检索排序」从「未做」移出 → **已实现**（ADR-065 `retrieve.ts` learnerBoost 上限 0.15）。
  - 默认降级链 1 条 → **6 条**；`zod@4.4.3` → **4.5.4**（注明勿写死补丁号）；目录树补齐 16 个漏列文件。
- `README.md`：计数 81/1354/8/100 → **82/1357/9/102**；页面列表补 **Agent 面试**（五页）。
- `src/domain/learner.ts:26` 注释「<0.85 且均分 <85」→ 改为引用 `WEAK_AVG`（与紧邻的 0.75/75 常量一致）。
- `docs/CHANGELOG.md` 顶部新增条目记录全部改动。

回归：`tsc -p tsconfig.app.json` 干净；`learner.test.ts` **64/64**。

### 教训（写进长期笔记）
文档里**任何写死的数字**（题数/文件数/节点数/阈值/版本号/角度数）都会过期。
写法约定：**只写常量名或 CLI 口径，不写数字**；无法避免时加「勿写死数字，以 X 为准」的提示。
`ARCHITECTURE.md` 的目录树是「正文引用但树里没有」式失修的重灾区，改代码时应顺手补树。
