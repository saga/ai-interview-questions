# 2026-09-04

## 全仓复核（代码 / 架构 / prompt / 题库）——在 main 上，非 dev0902

⚠️ 前提：工作区已切到 `main`，HEAD = cffcd38（09-04 05:23，别人的提交）。
我 09-03 那轮 P0/P1 的**代码**都还在（languageSanity.ts / variantSourceOf / validate-variants exit 1），
但**文档契约**被并行工作覆盖：
- `docs/添加题库prompt.md` / `添加题库prompt精简.md` **已被删除**，换成 `prompt_part1.md` + `prompt_part2.md`（两轮 blueprint 流程）
- `question-content-spec.md` §8 被改成 ADR-077 口径，我的 ADR-073 版本被覆盖
- 新东西：`src/domain/questionIdentity.ts`、`scripts/convert-blueprint-output.ts`、`npm run question:convert`、
  questionSchema 新增 `cognitiveTask` / `concepts`（`knowledgeId` 早就有，在 question.ts:114）

### 三个大问题

1. **Variant 契约反转 + 新能力是死的（最严重）**
   - ADR-073（我的）：Variant = 同一 assessment contract 的**表达/情境**变体，不可改 angle/cognitiveTask
   - ADR-077（新的）：Variant = 同一 Knowledge 的不同 **reasoning path 测量**，可改 angle/cognitiveTask
   - 两条同时存在于 DECISIONS.md 且直接冲突；**ADR-077 根本没写进 DECISIONS.md**（spec/ARCHITECTURE/CHANGELOG 三处引用了一个不存在的 ADR）
   - 落地是断的：`src/schemas/variant.ts:41-42` 变体可自声明 angle/cognitiveTask，注释说「applyVariant 优先采用」，
     但 `src/domain/variant.ts:265 applyVariant` 只 `...canonical` + 覆盖 question/formats，**不读这两个字段**；
     全仓 grep 无任何消费者 → 落库即丢（与 plan0903_2 §二-A 记过的「字段被静默丢弃」同一类，换字段重演）

2. **变体资产缩水 52%**：117 题/192 变体 → 61 题/93 变体。evaluation 26 题只有 26 条（每题 1 条，其余文件每题 2 条）；
   wiki-skill 42→6。门禁是绿的，所以不是脏数据被清，像是转换/重生成漏的。**待查是有意还是丢的。**

3. **无 LLM 反证信号在数据上近乎空转**：`misconceptionMap` 仅 97/1311 = 7.4%（misconceptions 149/1311 = 11.4%，
   中间 52 题有 misconceptions 无 map）。`gradeChoice`（evaluation.ts:171-177）无 map 时 misconceptionIds 恒为 []，
   ⇒ 92.6% 选择题答错不产生任何误解信号。有 `backfill:misconceptions` 脚本，长期未跑完。

### 中等

- `convert-blueprint-output.ts:95` 告警说「knowledgeId 无对应 schema 字段」——**错的**，`question.ts:114` 有。
  真正风险是**同名不同义**：prompt 的 knowledgeId = Knowledge 节点 id，schema 的 = 课程知识点 id。
  谁照着告警"修好"它，就会把节点 id 写进课程字段。建议在 prompt 侧改名 `knowledgeNodeId`。
- 多选占比 55.5%，目标 66.7%（validate:questions 每次报警无人处理）。新 prompt §二十一 已写「默认多选」。
- 主包 4.63 MB（gzip 1.48 MB）；monaco 已拆（editor.api 2.65 MB）但 index 仍 4.6 MB。
- `cognitiveTask` 进了 schema 但存量 **0/1311** 有值 ⇒ `questionIdentity.ts` 的 fork 判定对存量题实际只有三维。

### 健康

validate:questions 1311 题通过 · question:validate-variants exit 0（stale/近重复/语言 三项全 0）·
vitest 781 passed / 56 files · typecheck 两个 project 全过。
新的 convert 脚本有自己的 zod schema + errors/warnings 分流 + parseQuestionArray 兜底，比旧「prompt 直接产出入库格式」稳。

---

## 外部评审第 3 轮（13 条）核实 + 收口

评审要我「让代码契约追上 Prompt/ADR 的最终设计」。逐条对代码核实后的结论：

**评审两条 P0 事实过期，未改（不要照做）**
- P0-1「cognitiveTask 不在 questionSchema」→ 已在 `src/schemas/question.ts:96`（optional，cffcd38 加的）
- P1-5「check-question-identity 只审三维」→ `check-question-identity.ts:76` 已在拼四维字符串
- 真正缺的是 **ADR-077 没写进 DECISIONS.md**（spec / ARCHITECTURE / CHANGELOG 三处引用了一个不存在的 ADR）

**唯一真实的代码缺陷（评审 §3）= 死字段第三次复发**
`src/schemas/variant.ts` 声明变体可自带 angle/cognitiveTask 且注释称「applyVariant 优先采用」，
但 `applyVariant` 只 `...canonical` + 覆盖 question → 声明值落库即丢、全仓零消费者。
修法：`VariantMeasurementFace = Partial<Pick<Question,'angle'|'cognitiveTask'>>`，
`applyVariant(canonical, v: GeneratedVariant & VariantMeasurementFace)` 声明即采用、缺省即继承，
一个函数同时服务离线 assessment variant 与 runtime presentation variant（后者结构上声明不了测量面），**无运行时分支**。
补 5 条回归测试。

**改动清单**
- `src/domain/variant.ts` + `.test.ts`：测量面落地 + 5 测试
- `scripts/add-question.ts`：cognitiveTask 检查。**缺失只 warn、非法由 Zod 拦（更早）**——
  三个出题 skill 全仓 grep 无 cognitiveTask（plan0903_3 §二.7 未做），此刻上硬门禁会锁死出题管线；
  升级条件写在代码注释里（warn→error 一行）
- `src/schemas/question.ts:93`：删掉「add --check 对新题按必填拦截」的**不实注释**
- `scripts/convert-blueprint-output.ts:95`：告警措辞的事实错误修正 —— schema 的 `knowledgeId` 是**课程知识点 id**，
  prompt 的是 Knowledge 节点 id（= 本仓库的 `topic`），改为「与 topic 不一致才告警」而非「一律丢弃」
- `docs/DECISIONS.md`：新增 **ADR-077**（Variant = 同 Knowledge 不同 reasoning path；cognitiveTask 入 contract；
  两类 Variant 对照表）；ADR-073 第 1 条标注被取代、第 2 条（sourceHash / variantSourceOf）保留
- `docs/CHANGELOG.md`：新条目

**验证**：tsc app+node 全绿 · vitest 786/786 · build 通过 · validate:questions 1311 题 ·
validate-variants exit 0（61 题/93 变体）· question:add --check 冒烟三态（合法过 / 缺失 warn / 非法 error）。

**方法论（可复用）**
1. 外部评审必须先对代码核实再动手：本轮 2/13 条 P0 是旧快照，照做会改坏
2. 「schema 有字段 + 文档说在用 + 无代码读取」= 死字段，这是本项目**第三次**复发
   （前两次：cognitiveTask/questionRole/variantOf/concepts、variant.angle/cognitiveTask）；
   加字段时同步 grep 消费者是硬要求
3. 门禁升级要看上游产出方：skill 还没产出该字段就上硬门禁 = 锁死管线，先 warn + 注释写清升级条件

---

## 用户拍板（4 项）+ misconceptionMap 回填

**拍板结果**
1. **题型门禁 D3 —— 我的提问是错的。** 我照抄 plan0903_3 §一第 3 行说「part2 默认 single，与门禁冲突」，
   用户当场质疑。核对 `prompt_part2.md` §三 / §四 / §五 / §二十一 原文：**「默认多选。只有天然唯一最佳判断时才单选。」**
   与现行门禁（multiple ≥ 2/3）**同向，无冲突**。教训：plan 文档里的对照表也会过期，引用前必须回原文核。
   已撤销 plan0903_3 §一第 3 行 + §二.5 + D3，并修正 DECISIONS ADR-077 与 CHANGELOG 的同一处误述。
   附带发现的**真**冲突：part2 §三/§二十一「**禁止 open**」vs 库内存量开放题（生成期口径，不追溯存量）。
2. 变体池 192→93 = 有意收缩，不追查。
3. Variant 契约确认 ADR-077，本次 `applyVariant` 改动保留。
4. `misconceptionMap` 全量回填 → 已执行（下）。

**回填执行要点（可复用）**
- 原脚本 `scripts/backfill-misconceptions.ts` 的自研 bigram-Dice + 阈值 0.40 **一直在空跑**：
  对 52 道待回填题的 96 个干扰项命中 0 个（最高分仅 0.391）。**脚本跑出 0 结果不等于没数据，先查分布。**
- 度量标定法：用已有的 168 个人工标注项当标尺测 top-1 准确率。
  bigram-Dice@0.40 → 准确率 98.1% 但命中 0；`cjkDice`@30 → 准确率 95.5% 命中 43。
  阈值 0.30/0.35/0.40/0.45 的准确率 95.5/96.0/96.6/96.3% **是平的** ⇒ 取最低阈值换覆盖。
  并人工复核边界带 21 条（19 对 2 错），与实测一致 —— 度量 + 人审双保险。
- 改用 `cjkDice` 顺带消除「脚本另有一套中文相似度实现」的口径分裂。
- 结果：97 → **133 题**（10.1%），43 个干扰项 / 36 题 / 11 文件，纯新增 228 行零删除。
- **天花板必须讲清**：脚本只处理已有 `misconceptions` 的题（149 道）⇒ `misconceptionMap` 上限 11.4%，
  剩下 1162 道要覆盖必须先由 LLM 生成 misconceptions。已写进脚本头部，避免下次又误判为"回填没跑完"。

**仍未解**
- ~~变体池 192→93 的缩水~~ 已确认是有意收缩，不追查
- ~~`misconceptionMap` 7.4%~~ 已回填至 133 题（10.1%）；更高需 LLM 生成 misconceptions（1162 题零标注）
- ~~D3 冲突~~ 核实为误判，无冲突；真冲突是 part2「禁止 open」vs 存量开放题（不追溯）
- 多选占比 55.5% < 目标 66.7%：门禁方向没问题，是新题产出偏单选，属内容侧待办
