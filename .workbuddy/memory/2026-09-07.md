# 2026-09-07

## 跑 plan0907 的 P1 四项（用户从三选一里挑的「plan0907 的 P1 四项」）

### P1-1 offline/runtime variant 分家（方案 A）
- `src/schemas/variant.ts`：`questionVariantSchema` 加 `.superRefine` → **runtime 条目带 angle/cognitiveTask/assessment 直接拒**；
  新增 `variantModeSchema` + `variantModeOf()`（offline=assessment / runtime=presentation）。
- **真 bug**：`sessionEvaluator.finalizeQuestion` 池命中路径手工拼 `{question, options}`，把池条目的测量面整段丢掉。
  新增 `measurementFaceOf()`（domain/variant.ts）作唯一取面口，`applyVariant` 内部 face 也改用它。
- 教训：schema 允许 + applyVariant 支持 + 衔接处手拼 = 「落库即丢」第三次复发（前两次是 angle/cognitiveTask、assessment）。
  **凡「从变体取测量面」一律走 measurementFaceOf，不要手拼对象。**

### P1-2 Challenger 接进离线管线
- 新脚本 `scripts/challenge-questions.ts` → `npm run question:challenge`；
  纯汇总函数 `summarizeChallenges()` 在 `src/ai/questionChallenger.ts`（已单测）。
- 引擎走环境变量（CHALLENGER_PROVIDER/MODEL/API_KEY/BASE_URL），不读 localStorage（Node 侧没有）。
- **默认不阻断**（退出码恒 0），`--gate` 才卡。分层：deterministic=hard gate / challenger=语义审计 / human=验收。
- 坑：`npm run x -- --topic=rag` 传进来是**单个 token** `--topic=rag`，只认 `--key value` 会静默丢参数。
  新脚本的 parseArgs 两种都支持；老脚本（review-question 等）仍是老写法。

### P1-3 misconceptionMap 覆盖率
- 新脚本 `scripts/misconception-coverage.ts` → `npm run question:misconceptions`（三层口径 + topic 缺口排序）。
  注意：plain `node` 跑的脚本不能 import `src/data/questionBank`（那是 import.meta.glob 浏览器路径），
  必须 fs 读 `src/data/questions/*.json`，且相对导入要带 `.ts` 扩展名。
- `backfill-misconceptions.ts` 新增 `--from-nodes`：用知识节点（question.topic = 节点 id）的 misconceptions 做种。
  124 节点 100% 有（均值 2.14），等于给「无米下锅」找到了米，零 LLM。
- 阈值实测（节点做种，cjkDice）：20→285 道 / 25→140 / 30→84 / 35→59 / 42→32 / **45→19** / 50→14。
  ≥45 的 19 条人工复核**全对**；35~45 带约 10% 误配 ⇒ 默认 45，宁缺毋滥（误配=诊断说错，比不标更糟）。
- 结果：清单 185→204（15.1%）、有绑定 169→188（13.9%）、绑定率 301/2919（10.3%）。
  **离「可用」还远**：剩余 1150 道需 LLM 生成题级 misconceptions，本仓库暂无该脚本。

### P1-4 / P2 文档
- ARCHITECTURE.md 顶部加「文档权威性声明」；修掉残留三维 contract（questionIdentity 条目）+ ADR-076 加「已被 ADR-077 取代」指针。
- README：77 文件/1317 题 → 81 文件/1354 题；angle 可选10种 → 必填19种（前 10 个才进 nodes.test.ts 白名单）。

### 校验
typecheck 过；validate:questions 1354/124；coverage 缺口 0；variants 池健康；`npm test` **809/812**（+7），
3 失败仍是 local/provider/interviewCapability 的 pre-existing 超时。

### 状态速查（供下一批）
- assessment 数据仍为 **0/1354 canonical、0/100 variant** —— 链路通、数据空，等下一批 blueprint 带字段。
- 多选占比 57.2%（目标 ≥66.7%），新题应以多选为主。
- misconception 下一步 = LLM 生成题级 misconceptions 脚本（可复用 question:challenge 的 env 引擎配置写法）。

---

## 存量 assessment 回填（用户点名修 "0/1354 题目、0/100 变体"）

### 诊断结论
- 不是 schema 缺字段，是**缺 producer**。新管线 `convert-blueprint-output.ts` 早已落库 Blueprint 的
  `assessmentTarget/reasoningGoal`（P0-2 已修），但存量 1354 题走旧管线，全空。
- 环境里**没有 LLM**：env 无 API key，localStorage 里那份 Node 读不到，127.0.0.1:8888 未起。
  ⇒ 只能离线抽取，不能生成。

### 做法（新增两个文件）
- `src/domain/assessmentInference.ts`（纯函数 + 10 条单测）：**只抽取不生成**。
  - target = 解析里的核心断言句，分句打分取最优（谓语信号 +2 / 长度 20~90 +2 / 命中正确项 +1 / 首句 +1）。
  - reasoningGoal = angle×cognitiveTask 起手动作 + 实际正确项摘要 + 干扰项摘要。
  - 抽不到断言句 → 返回 null（不用模板文本灌满，理由同 misconception 阈值 45）。
  - 排除：元信息行、`A 项…` / `A、B 项…` 判错句、干扰项复述（cjkDice ≥70 **或** token 覆盖 ≥80%）。
- `scripts/backfill-assessment.ts` → `npm run question:assessment`：默认报告，`--write` 回填，
  `--engine=infer|llm`，`--overwrite` 重建并**剪枝**，审计侧车 `temp/assessment-backfill-<date>.json`。

### 三个踩坑（按重要性）
1. **变体声明的测量意图与 canonical 雷同**：首轮写完 94 条变体后 validate-variants 报 31 条雷同。
   这些是 `surface-options`（选项没改写），推导必然一致 ⇒ 按 ADR-077 证明不了「不同 reasoning path」
   就**不该声明**（不声明=继承 canonical）。加全等比对后降到 0。**最终 52 自声明 / 48 继承。**
2. **cjkDice 被长句稀释**：干扰项复述判定只用 Dice 会漏过「…确实更灵活，但这不是动机」这种长句，
   必须补 token 覆盖率（干扰项 token 被句子覆盖 ≥80%）。
3. **`--overwrite` 必须能剪枝**：回填工具只 set 不 delete，改了推断规则后旧值会残留成矛盾状态。
   改成 `planned: Map<string, Assessment|undefined>`，undefined = 删。改规则后要跑**两遍**才收敛
   （第一遍 canonical 在磁盘上还是旧值，变体比对用的是旧 canonical）。

### 结果
- 题目 **1198/1354（88.5%）**；变体 52 自声明 + 48 继承，**0 雷同 / 0 stale / 0 近重复**。
- 剩余 156 题须 LLM（`--engine=llm` 已留好，用 vite-node 跑，env 复用 CHALLENGER_*）。
- `question:coverage` 新增一行度量（带 assessment / 带 cognitiveTask），数字可回归。
- typecheck 过；`npm test` **822/822**（+10，本次 3 个老 flaky 也通过了）。

### 状态速查（供下一批）
- **cognitiveTask 只有 43/1354** —— 比 assessment 更严重的洞：assessment contract 第四维缺失时，
  adaptive 的 assessment-cell 层整体回退到 angle 粒度（P0-1 白做一半）。angle→cognitiveTask 不是
  单射（mechanism 可为 explain/diagnose/apply），确定性推断约 85% 准，不能静默批量写。
- 多选占比 57.2%（目标 ≥66.7%）。
- 题级 misconceptions 仍需 LLM 脚本（~1150 道）。

---

## 续：剩余 156 题人工撰写收口（题目 1354/1354）

### 用户的关键纠正
> "你不就是llm么？直接使用你的能力，串行操作！"

我原本造了 `--engine=llm` 去调外部 provider（本地无 key、localStorage 读不到、127.0.0.1:8888
拒连，实际也跑不通）。用户点明：**我本身就是 LLM，直接撰写即可**。于是改为 dump→分批撰写→apply
的人工闭环（`scripts/assessment-manual.ts`），共 10 批（前 9 批 17 条 + 末批 3 条）。

### `scripts/assessment-manual.ts` 三个模式
- `--dump [--kind=] [--out=]`：导出待撰写条目（含 canonicalAssessment 供变体对照），
  **自动跳过 runtime/presentation 变体**。
- `--apply --file= [--dry-run]`：`authoredSchema` 校验 + **全有或全无**（一条不合法整批中止），
  再硬拒给 presentation 变体写意图。
- `--status`：题目 1354/1354 · 变体 52 自声明 / 48 继承 / 0 雷同。

### 撰写规约（保持与推断结果同风格，否则两端不可比）
`target` = 「能 + 一个可判定的判断」；`reasoningGoal` = 「先…；再…；并排除…」。
**排除项要点名题面里具体的干扰项**，不能写「其它错误说法」——否则这条意图对审题无用。

### 易错点
1. **变体不是都要写**：33 条 `surface-options` + 15 条 `context-options` 的解析与 canonical 同源，
   推导结果逐字相同 ⇒ 必须继承。**变体 52% 自声明不是缺口**，是「证明得了不同 reasoning path 才声明」。
2. **先数清楚再动手**：`--status` 说题目缺 3 条，就只有 3 条；shard 10 里 3 题 + 14 变体，
   shard 11/12 全是变体 —— 别把变体也顺手写上。
3. 数据结构：`src/data/variants/*.json` 的 `variants` 是**按 canonicalId 分组的对象**，不是数组；
   变体字段叫 `kind`（不是 `variantKind`），`assessment` 写在变体对象上。

### 结果
题目 **1354/1354（100%）**；变体 52 自声明 / 48 继承 / 0 雷同 / 0 stale / 0 近重复。
`validate:questions` 1354 题 · 124 节点 · angle 100%；`npm test` **822/822**；typecheck 过。
覆盖率报告新行：`测量意图（assessment contract）：带 assessment 1354/1354 · 带 cognitiveTask 43/1354`。

### 下一步（未做，需决策）
- **cognitiveTask 43/1354** 是比 assessment 更严重的洞（第四维缺失，assessment-cell 回退 angle 粒度）。
- 多选占比 57.2%（目标 ≥66.7%）；题级 misconceptions 204/1354。

---

## cognitiveTask 第四维收口：43/1354 → 1354/1354（100%）

### 三段式
1. **规则推断** `src/domain/cognitiveTaskInference.ts`（纯函数 + 17 单测）。
   两级判定：① 题面显式提问方式 ② angle 先验。**① 优先于 ②**（写测试时踩过：fixture 题干里含
   「取舍」会被 EVALUATE_ASK 命中，先于 comparison→compare 返回——这是正确行为）。
   推不出**返回 null**，不用模板值灌满。43 条人工标注上校准 37/43 精确，其余全落 null。
2. **规则回填** `scripts/backfill-cognitive-task.ts`（`npm run question:cognitive-task`）
   → 1062/1354（78.4%），292 条留人工（98 system-design + 167 scenario + 27 fundamental）。
3. **人工撰写** `assessment-manual.ts` 加 `--field=cognitive-task`（dump/apply/status 三模式复用
   同一套实现，只换 schema 与写入字段）。dump 291 条分片 5 片，逐条判定后 apply。

### 人工判定口径（下次复核照这个来）
- 「下列说法正确的有」**按陈述性质分流**：定义/职责/机制 → `identify`；价值/考量/优势/缺陷分析 → `evaluate`。
- 「哪些措施 / 哪些设计 / 如何做」→ `design`；「为什么 / 原因 / 原理」→ `explain`。
- 「优先排查 / 最可能的原因」→ `diagnose`；「在哪些情形下会 X」→ `predict`。
- 「哪个方案满足需求」：**单能力层**（AIP 级选服务/指标）→ `apply`；
  **需组合多组件架构**（AWS Pro 级）→ `design`。

### 关键坑：改 cognitiveTask 会让变体变 stale（93 条）
`sourceHash` 对 `cognitiveTask` 是**条件入指纹**（`src/schemas/variant.ts`：未声明不出该键，
声明后变化即判 stale）。所以补写第四维 → 其变体全部 stale。**不是真的漂移**：变体文本没变，
变的是继承来的元数据，而 ADR-077 规定未自声明变体恒取 canonical 当前值。
正确处置 = **重设指纹**，不是重跑 `question-variants.ts`（那要 LLM key 且会改内容）。

新增 `scripts/refresh-variant-hash.ts`（`npm run question:variant-rehash`）：
仅当「旧指纹 == 去掉 cognitiveTask 后的指纹」才改写，其余判真实漂移并列出。
本次 93/93 属前者、0 条真实漂移、0 孤儿。

### 结果
`cognitiveTask` **1354/1354**；变体 **0 stale / 0 近重复 / 0 语言质量**（52 自声明 + 48 继承）；
typecheck 干净；`npm test` **839/839**（新增 17 条推断单测）。

### 遗留（已提出，未处理）
- 多选占比 57.2%（目标 ≥66.7%）；题级 misconceptions 204/1354（15.1%）、misconceptionMap 188/1354。

---

## 评审 skills/verify-question-correctness（用户要求"参考执行"）

### 结论
- skill **有问题**；单靠它**找不到真问题**（脚本只贴关键词标签，方法学要靠人工/LLM 套用 说明.md）。

### 找到的缺陷
1. **SKILL.md 截断**：磁盘文件止于 §1.2 示例（第 104 行，代码块未闭合），缺 §1.3–1.5 + 工作流 3–14 + 裁决/报告格式。完整版仅存在于 说明.md 内嵌。Skill 入口不完整 → 无法被自动正确加载。
2. **脚本是词法分诊器，不是验证器**：`scripts/verify-question-candidates.ts` 仅按正则加权打分产出 worklist，不做任何"独立求解"。
3. **82% 误报**：实跑 109 文件 / 1354 题 → 1115 候选（P0 26 / P1 272 / P2 817）。打分几乎全靠 `high-difficulty`(696) + `提到 AI 词`(677) —— 本仓是 AI 题库，几乎每题都中。
4. **与 Purpose 自相矛盾**：Purpose 称"检测结构校验检测不出的错误"，但权重最高的两项（单选答案数≠1→+20、答案越界→+30）是 `question:validate`(zod) 已强制的结构校验 → 在已验证 bank 上**永不触发**（reason 词频中这三项=0）。
5. **"missing explanation" 规则死权重**：schema 强制 `explanation.min(1)`，该规则 0 触发。
6. **package.json 未接 `question:verify:candidates`**（说明.md 第 2352 行要求加，未落地）；`tsx` 未装，`npx tsx` 需下载，实测 `node --experimental-strip-types` 可跑。
7. 扫描 `src/data/questions` **和** `src/data`（重复且白扫 variants/knowledge 的 json）。

### 实跑验证（抽 3 道 P0 独立求解）
- `llm-14`（SWA 混合注意力，单选）：独立求解答案=[1]，与存键一致，技术宣称正确 → 无误报的误报。
- `ai-moe-tradeoff-001`（MoE，多选）：答案=[0,1] 正确，解析正确 → 误报。
- `agentic-29`（Agent 成功率回归排查，单选）：答案=[1] 正确 → 误报。
- ⇒ 三题全正确，脚本全标 P0 → 分诊失败。

### 若修复的方向
补全 SKILL.md；降/删与 validate 重复的结构性规则；词法信号改"组合触发"或仅作 tie-breaker；只扫 questions/；接 package.json+装 tsx；脚本改"每档抽 N 高 risk 待 LLM 验证"而非全量倾倒。

---

## 落实技能重写（用户给完整新版，照改照执行）

### 改动
1. `skills/verify-question-correctness/SKILL.md`：用用户提供的完整版整体覆盖（原文件在 §1.2 截断，现补全到 §34）。核心口径：**Skill 负责 correctness verification（独立求解/claim 分解/证据/裁决），脚本只做高质量 risk triage**，不重复 schema validation，不因为"AI 题 / hard 题"判高风险。P0/P1/P2 在 triage 里明确是**验证优先级**不是"已确认缺陷"。
2. `scripts/verify-question-candidates.ts`：整体重写为 triage-only。删除 `AI +4` / `hard +3` / `math +6` / `always +5` 这类无差别域信号；只保留窄信号（强断言 + 技术/经验/版本/数量化 claim + 数学/定理/复杂度 + 因果/必要充分/等价 + API/版本/current + explanation↔answer 显式矛盾），且**组合触发**才加分（如 absolute+quant/version/impl）。`#!/usr/bin/env npx tsx` → `node --experimental-strip-types`，不依赖 tsx。
3. `package.json`：加 `"question:verify:candidates": "node --experimental-strip-types scripts/verify-question-candidates.ts"`。

### 实跑结果（--min-risk low --sample 50）
- 1354 题扫描 → **361 候选（26.7%）**，对比旧版 1115/1354（82%）。
- high(P0)=7 / medium(P1)=50 / low(P2)=304；Semantic contradiction=0；calibration=50。
- 7 个 P0 全是真高信号（math+quant+version/absolute 组合，如 ai-eng-045 / flashattn-07 / prompt-cache-billing-tradeoff-01）。
- `llm-14` 从旧版误报 P0 降到 P1(17)——只剩 math+quant 组合信号，不再因"AI 题"冤枉。
- 修复：`getFullText` 对 `concepts.core/supporting` 非数组（老题是 string）做兜底（`toText()` 协助），否则 `.join` 崩。

### 结论
收窄达标（<30%），高优先级 7 题是真正该深审的集；下一步 Agent 走 worklist→独立求解→证据→裁决，不必重读全库。脚本产物 `reports/question-correctness-worklist.jsonl`。

## verify-question-candidates 复审后收尾（2026-09-07 下午）

外部复审结论：SKILL/triage 架构已合格，**只需修 2 处**，不要再扩大脚本。

### 1. 必修：正则里未转义的 `.`
- `/\bv\d+.\d+(?:.\d+)?\b/i` 的 `.` 是「任意字符」，会命中 `v1x2`/`v1-2` 却匹配不准 `v1.2`。
- 同类问题还有 4 处：`quant.percent` / `quant.number-with-unit` / `quant.multiplier` / `quant.times`，
  一律改 `\.`。**以后写数值类正则先想清楚 `.` 是否要转义。**
- 实测影响：误报 2 处（`moe-mixtral-02` 的 `8x7B`、`pp-1f1b-bubble-calc` 的 `1F1B`），
  候选 361 → **360**（high 7 / medium 49 / low 304）。

### 2. 消歧义：triage 不再用 P0/P1/P2
- `priority: "P0"|"P1"|"P2"` → **`verificationPriority: "HIGH"|"MEDIUM"|"LOW"`**。
- 本仓库语义分层固定为：**triage = HIGH/MEDIUM/LOW（验证紧急度）**，
  **verification = P0/P1/P2（已确认缺陷严重度，Skill 产出 `severity`）**。
- SKILL.md 里的 P0/P1/P2 保持不动——那本来就是 severity。

### 其它
- 已同步 `skills/verify-question-correctness/说明v2.md`（内嵌代码 + 输出提示）；
  `说明.md`（v1 设计稿）顶部加「已被 说明v2.md 取代」标注，避免文档/实现再次分叉。
- typecheck 干净、`npm test` 839/839。
- **未补单测**：脚本在模块顶层直接 `main()`，要测就得重构导出，属扩大范围，暂不做。

---

## 首轮题库正确性验证实跑（用户："使用你这个LLM"；report-only）

### 做法
triage worklist（360 条）→ **7 HIGH 全覆盖 + 17 MEDIUM 抽样** → 独立求解 → 逐选项裁定 → 取证 → 报告。
产物：`reports/question-correctness-report-2026-09-07.jsonl`（26 条，§24 schema）+ 同名 `.md` 摘要。
**未动 `src/data/questions/*`**（SKILL.md §30 Do Not Auto-edit），P0 按 §26 Tier D 留人工确认。

### 结果
20 VERIFIED / KEEP，4 CONDITIONAL / REVIEW，**2 INCORRECT / FIX（P0）**。

### 两个 P0（都是同一类：**open 参考答案 = 自家的错误答案**）
1. `inference-vram-budget-calc-01`：`open.referenceAnswer` 写 `正确答案：A、B、C、D`，但其
   A/B/C/D 与当前 `choice.options` **完全对不上**（旧版选项残留）；B 项用的 3.5 GB 正是本题
   要抓的「漏算 K/V 因子 2」陷阱值。正解：每 token `2×48×8×128×2=196,608 B`，32K≈6.4 GB、
   64K≈12.9 GB、总计 15.6 / 22.1 GB。`choice.answer=[0,1]` 与 explanation 都对，只有 open 是错的。
2. `flashattn-07`：`open.referenceAnswer` 是**干扰项 [2] 的原文**（"约 33 倍…"）。
   正解已取证 arXiv 2205.14135v2 Theorem 2：`Θ(Nd+N²)` vs `Θ(N²d²M⁻¹)`，比值 `M/d²` 与 N 无关，
   实测 GPT-2 medium 40.3→4.4 GB = 原文 "up to 9×"。

### 高收益扫描（下次直接复用这两个）
- **open/choice 答案集一致性**：解析 open 参考答案里的「正确答案：X、Y」字母集，映射回选项索引比对。
  全库只命中 2 条，**两个 P0 全是它找出来的** —— 性价比最高的检查。
  局限：只覆盖 open 答案显式写字母集的题，是必要非充分；无法解析的题不在比对范围。
- **结构完整性**（answer 越界 / 空 / 重复选项 / 选项数<2 / misconceptionMap 长度·越界·
  正确项误绑·干扰项未绑）：1354 题 **0 问题**。
- 第三类扫描（open 答案回显干扰项独有数字/复杂度）命中 11 条，**9 条良性** —— 数字出现在
  「驳斥该错误选项」的语境里（如 `llm-13` 逐个走过 24 KiB / 768 KiB / 7.5 GB 陷阱）。
  ⇒ **该扫描噪声大，只能当线索不能当结论**，必须人工看上下文。

### 易混淆点（澄清，别再当缺陷上报）
- `long-context-attn-01` 的「显存占用 O(N²)→O(N)」与 `flashattn-07` 的「访存流量 Θ(N²d²M⁻¹)」
  **不矛盾**：一个是 memory footprint，一个是 HBM IO traffic，是两个不同的量。建议两题加交叉引用。

### 8 条 P2（答案键无误，属口径/术语/单位/来源）
`quant-qah-06`（"每 token 计算量" FLOPs vs 硬件耗时歧义，按 FLOPs 推的考生会被误判）、
`specdec-03`（"offload 约 10x" 缺来源，一手常见 2–3x）、`transformer-comparison-01`
（"受限卷积"非论文术语，Table 1 Convolutional 行是 `O(log_k n)` 指膨胀卷积）、
`vectordb-04`（延迟数字缺测量条件）、`gqa-kv-cache-calc`（`32 GiB` 写成 `32 GB`）、
`ai-eng-051`（open 里"1200 例/表面"乱码 + 统计口径未写明）、`ai-eng-038`（对 B 的驳斥
"注定失败"过度断言，verifier-gated cascade 是真实有效模式）、`quant-qah-02`（实验数值缺出处）。

### 待办
- 2 个 P0 等人工确认后再改。
- 建议把「open/choice 答案集一致性」做成**新增**校验脚本（不是继续扩 verify-question-candidates，
  复审明确说过别再扩大它）—— 需用户拍板。
- LOW 剩余 304 条未逐题验证。

---

## P0 修复落地（用户："修复"）

只改 `formats.open.referenceAnswer`，未动选项文本 / `choice.answer` / `explanation` / `misconceptions`。

- `flashattn-07`：改为定理 2 表述（`Θ(Nd+N²)` vs `Θ(N²d²M⁻¹)`、比值 `M/d²` 与 N 无关、
  40.3→4.4 GB 约 9×），并显式区分「显存占用」与「访存流量」两个量。
- `inference-vram-budget-calc-01`：改为「正确答案：A、B」+ 完整推导
  （196,608 B/token、32K≈6.4 GB、64K≈12.9 GB、总计 15.6 / 22.1 GB、点明漏算因子 2）。

### 回归全绿
open/choice 答案集比对 **637 对 · 0 矛盾**（修复前 2）；结构完整性 1354 题 0 问题；
`validate:questions` 1354/124；`question:validate-variants` **0 stale / 0 近重复 / 0 语言质量**；
typecheck 干净；`npm test` **839/839**。

### 记住
- **`formats.open.referenceAnswer` 不入 `variantSourceOf`** —— 改它不会让变体 stale（本次 0 stale 已验证）。
  同理 `misconceptions` 也不入。
- 答案集比对这次覆盖到 **637 对**（比上次多，因为正则放宽到 `[A-H]` 且允许 `、`/`,`/空格分隔），
  0 矛盾 ⇒ 该检查现在是干净的，适合固化成常驻校验防回归。

### 仍未处理
8 条 P2（口径/术语/单位/来源，答案键无误，需逐条决策）；LOW 304 条未逐题验证。

---

## P2 复核（用户："如果有错误就改，没有错误就不用改"）

**关键教训：先回原文取证再判 P2，别凭语感。** 我上一轮凭「计算量通常指 FLOPs」就判
`quant-qah-06` 的答案键可能错了 —— 一查源文，论文自己用的就是 "compute per token = 8×"，
且题面已限定「原 bf16 权重族」（对应论文 "model families released in bfloat16"）。**误判。**

### 已修 3 处（真错误，均不动答案键）
- `ai-eng-051`（evaluation.json）：open 参考答案「1200 例/表面」乱码 → 「需约千例量级样本」。
- `quant-qah-06`（hf-quantization.json）：`misconceptions[0]` 漏「忽略了」，把**正确事实**写成了
  误区（"以为…约 1 倍，4-bit 权重同样显著降低访存与算力开销"）→ 补回。
  ⇒ **检查 misconception 的通法：读一遍是不是「以为/误以为…」的句式，缺了否定/忽略词就是残缺。**
- `ai-eng-038`（ai-architecture.json）：open 称 B「注定失败」失实 → 「缺少分层与降级链，
  把复杂高风险任务也压给无法胜任的小模型网关」。

### 取证后撤回 5 处（不再改）
- `quant-qah-06` 8× ← arXiv 2608.20953v1 "roughly 8× less compute per token"。
- `quant-qah-02` step700/19 点/7× ← 同论文：QAT 峰值 54.6@step700→step1200 约 36；QAH ~100 步 vs ~700 步。
- `specdec-03` offload 10x ← HF blog `assisted-generation`："relying on memory offloading,
  you can see up to 10x speedups"（`source.materialId` 就是它）。
- `transformer-comparison-01` ← AIAYN 原文 contiguous→O(n/k)、dilated→O(log_k n)，判 D 错正确。
- `vectordb-04`（仅缺测量条件）/ `gqa-kv-cache-calc`（GB/GiB 通行混写）→ 非错误。

### ⚠️ 并发改动（重要）
跑回归时题数从 **1354 → 1357**：另一个会话/进程在并行加
`src/data/questions/dit-2026-09.json` + `src/data/variants/dit-2026-09.json`（3 题 / 2 变体），
并改了 `docs/ARCHITECTURE.md` 与 `src/domain/knowledge/nodes.test.ts`。
**我没碰这些文件。** 以后做全库回归先看 `git status`，别把别人的改动算进自己的数字。

### 本次回归（含并发改动后共 1357 题）
结构完整性 0 问题；open/choice 比对 **637 对 0 矛盾**；`validate:questions` 1357/124；
`question:validate-variants` 0 stale / 0 近重复 / 0 语言质量（70 题 / 102 变体）；
typecheck 干净；`npm test` **839/839**。
⇒ 新增：改 `misconceptions` 与 `open.referenceAnswer` 都**不会**让变体变 stale。

## 架构文档 vs 代码 审计（只报告未修）

核对 `docs/ARCHITECTURE.md`（753 行）+ `README.md` 与代码。结论：**文档里写死的数字几乎全过期**。

### 已确认不一致（A 数值 / B 结构）
- A1 题库：文档 77 文件/1317 题、双形态 1237/choice-only 80 → 实际 **82/1357、1232/125**。
- A2 knowledge：文档 16 文件/123 节点 → 实际 **19/124**。
- A3 角度：文档 :216 写「10 角度」，同文档 :691 写 19 → **内部自相矛盾**，`questionAngleSchema` 实为 19。
- A4 文档 :575「双形态题约 1078/1084」→ 实际 1232/1357。
- A5 薄弱阈值：文档 :531「mastery<0.85 且 avgScore<85」→ 代码 `WEAK_MASTERY=0.75 / WEAK_AVG=75`。
- A6 `src/domain/learner.ts:26` 注释也写 0.85/85，与紧邻的 0.75/75 矛盾（代码内注释漂移）。
- A7 文档 :740 `zod@4.4.3` → package.json `^4.5.4`、安装 4.5.4。
- A8 README:11「81 文件/1354 题/8 变体文件/100 条」→ 实际 82/1357/9 文件/70 canonical 102 条。
- B1 文档 :47 的 `schemas/types.ts` **不存在**；那些类型在根 `src/types.ts`。schemas/ 树还漏
  index/jsonSchema/learner/session/interview/conversation/variant。
- B2 文档 :354-356 说 `expandWithPrerequisites` / `isMastered` / `isAttempted` / `WEAK_*` 定义在
  `conceptGraph.ts` → 实际全在 `learner.ts`（conceptGraph 只有 prerequisiteClosure/relatedOf/
  prerequisitesOf/dependentsOf/topoRankOf）。
- B3 文档 :250 称「domain 不依赖 schemas」→ 除 type-only 外有 2 处越界**值**依赖：
  `learner.ts:14` proficiencyConfigSchema、`variantPool.ts:12` computeVariantSourceHash+variantSourceOf
  （conceptGraph.ts 的两处在文档白名单内，合规）。
- B4 文档 :330 把「Learner memory 参与检索排序」列为 Phase 2/3 未做 → **已实现**：
  `knowledge/retrieve.ts:88-101` learnerBoost（ADR-065，上限 0.15），在 metadataScore:78 生效。
- B5 文档 :226 说 courses「不创建目录」→ `src/data/courses/` 已存在（仅 .gitkeep）。
- B6 文档 :713 默认降级链写成单条 deepseek → 实际 sample-config 是 6 条（chrome/local enabled，
  其余 disabled），只有 `generateOpenQuestions:false` 对。
- B7 目录树整体失修：ai/ 漏 chromeAgent + variantChallenger；agent/ 漏 sessionState；hooks/ 漏
  useIsMobile；components/ 漏 AgentInterviewPage / CopilotSidebar / SessionReplayDrawer；
  domain/ 树漏 adaptive/bias/variantPool/textSimilarity/options/reasoningPath/variantDiversity/
  cognitiveTaskInference/languageSanity（前四个在正文还被引用）。

### 核对一致（不用改）
CHROME_AI_*(4 / 90s / retries 1)、DEFAULT_RUBRIC 0.4/0.2/0.2/0.2、LEVEL_TO_SCORE 0/25/50/75/100、
检索权重 0.40 lexical + 0.25 metadata + 0.20 graph + 0.15 semantic（未接入按比例回填）、
graph 1-hop 1.0/0.8/0.6/0.45、questionSecondaryWeight 0.7、DEFAULT_KNOWLEDGE_LIMIT 5、
bias 阈值 1.8、MAX_OPEN_RATIO 0.3、ADAPTIVE_OPEN_PROBABILITY 0.3、Dexie v1–v4、SESSION_CAP 50、
TREND_EPSILON 2、local 默认 127.0.0.1:8888/v1、providerId 6 / knowledgeArea 6 / VariantKind 4、
面试 30*60s、runtimeVariantEnabled 默认 false、Monaco ^0.56 / antd ^6 / React 19、
build=`tsc -b && vite build`、tsconfig.app.json 排除 *.test.ts、全部 npm 脚本存在、
server/index.js + worker/index.ts 存在、schemas 未反向依赖 domain。

### 教训
文档里**任何写死的数字**（题数/文件数/节点数/阈值/版本号）都会过期，`ARCHITECTURE.md` 的
目录树与正文也存在「正文引用但树里没有」的失修。写文档时应只写常量名，数字留单一出处。
