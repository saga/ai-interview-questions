# 2026-08-25

## QuestionCard 选项竖排 + 行距修复
- `src/components/quiz/QuestionCard.tsx`：Radio/Checkbox.Group 由 `options` 属性（横向内联）改为子组件 + `Space direction=vertical`，每选项独占一行；标签行/题干行距 8→12/16px。typecheck/build 绿。

## QuestionCard 题干标签新增 subtopic 展示
- 同文件：`{question.subtopic && <Tag color="geekblue">{question.subtopic}</Tag>}`。

## Concept×Angle 逐角度学习证据（ADR-037，用户提案"弱 concept → 缺证据 angle"）
- 用户提案核心：Concept 为中心、topic×angle 覆盖矩阵、两阶段 Blueprint→题目——经核查**大部分已在代码**（KnowledgeNode/coverage.ts/blueprint.ts/conceptGraph.ts/adaptive.ts 均存在），真正缺口三处，本次补齐：
  1. `schemas/question.ts` 加 `subtopic` 可选字段（仅分类/展示，不进证据聚合）。
  2. `schemas/common.ts` `questionAngleSchema` 由 6→10：补 `fundamental/comparison/debugging/design`（与既有 definition/system-design 并存，不迁移旧数据）；`coverage.ANGLE_ORDER`/`ANGLE_SUGGESTIONS`、`blueprint.ANGLE_PURPOSE_TEMPLATES` 同步补齐。
  3. `schemas/learner.ts`：`QuestionResult` 加 `angle` 可选；`LearnerProfile` 加 `angleCoverage: Record<topic|angle, AngleStat>` 可选；`domain/learner.ts`：`updateLearner` 并行聚合 angleCoverage、`sessionFromQuiz` 透传 `q.angle`、新增 `angleKey/getAngleStat/weakAnglesOf`；`domain/adaptive.ts`：新增 `angleEvidence/pickLeastCovered`，deep-dive/gap-probe/move-on 各分支优先选 (topic,angle) 证据最少题。
- 测试：`learner.test.ts`+2（angleCoverage 聚合 + weakAnglesOf）、`adaptive.test.ts` 将 `q()` 改为真实 Question 形状 +2（角度感知选题）。修复 `pickPrioritized` 未使用 import。
- 验证：npm run test **270 passed**（原 266+4）、typecheck/build 绿。docs/DECISIONS.md 新增 ADR-037，docs/ARCHITECTURE.md Training Coach 段补角度证据说明。
- 未做：用户顶部贴的 6 道 Agent 多选题未入库（本次按"仅补缺口"选项，未含内容落地）；subtopic 级独立证据未做（防止 over-design）。
