# 2026-08-25

## QuestionCard 选项竖排 + 行距修复
- `src/components/quiz/QuestionCard.tsx`：Radio/Checkbox.Group 由 `options` 属性（横向内联）改为子组件 + `Space direction=vertical`，每选项独占一行；标签行/题干行距 8→12/16px。typecheck/build 绿。

## QuestionCard 题干标签新增 subtopic 展示
- 同文件：`{question.subtopic && <Tag color="geekblue">{question.subtopic}</Tag>}`。

## Concept×Angle 逐角度学习证据（ADR-037，用户提案"弱 concept → 缺证据 angle"）
- 用户提案核心：Concept 为中心、topic×angle 覆盖矩阵、两阶段 Blueprint→题目——经核查**大部分已在代码**（KnowledgeNode/coverage.ts/blueprint.ts/conceptGraph.ts/adaptive.ts 均存在），真正缺口三处，本次补齐：
  1. `schemas/question.ts` 加 `subtopic` 可选字段（仅分类/展示，不进证据聚合）。
  2. `schemas/common.ts` `questionAngleSchema` 由 6→10：补 `fundamental/comparison/debugging/design`（与既有 definition/system-design 并存，不迁移旧数据）；`coverage.ANGLE_ORDER`/`ANGLE_SUGGESTIONS`、`blueprint.ANGLE_PURPOSE_TEMPLATES` 同步补齐。
  3. `schemas/learner.ts`：`QuestionResult` 加 `angle` 可选；`LearnerProfile` 加 `angleCoverage: Record<topic|angle, AngleStat>` 可选；`domain/learner.ts`：`updateLearner` 并行聚合 angleCoverage、`sessionFromQuiz` 透传 `q.angle`、新增 `angleKey/getAngleStat/weakAnglesOf`；`domain/adaptive.ts`：新增 `angleEvidence/pickLeastCovered`，deep-dive/gap-probe/move-on 各分支优先选 (topic,angle) 证据最少题。
- 测试：`learner.test.ts`+2（angleCoverage 聚合 + weakAnglesOf）、`adaptive.test.ts` 将 `q()` 改为真实 Question 形状 +2（角度感知选题）。修复 `pickPrioritized` 未使用 import。
- 验证：npm run test **270 passed**（原 266+4）、typecheck/build 绿。docs/DECISIONS.md 新增 ADR-037，docs/ARCHITECTURE.md Training Coach 段补角度证据说明。
- 未做：用户顶部贴的 6 道 Agent 多选题未入库（本次按"仅补缺口"选项，未含内容落地）；subtopic 级独立证据未做（防止 over-design）。

## Topic Taxonomy 重构为 6 大能力域（ADR-038，"打地基"范围）
- 用户提案：以"面试能力域"组织题库（域→topic→concept→subtopic→angle），批评原 `knowledgeAreaSchema` 8 扁平值把域/主题混用。
- 落地（用户选"打地基"：不动 questions/*.json 大题库）：
  1. `schemas/common.ts` `knowledgeAreaSchema` 8值→6大域：`ai-engineering/llm/llm-applications/agent-engineering/ai-systems/ai-security`。
  2. `schemas/knowledge.ts` `KnowledgeNode` 新增必填 `topic`（`area`=域，`topic`=域下二级主题，如 Inference/RAG/Agents；`id`=Concept slug）。
  3. 新增 `src/data/taxonomy.ts`：6 域→topic 骨架 `TAXONOMY`（含暂无知识点的 topic 占位）、`DOMAIN_LABELS/TOPIC_LABELS/groupNodesByDomain/groupNodesByTopic/domainOfTopic`；单一真理来源。
  4. 脚本重映射 7 个 knowledge/*.json（67 节点）的 `area`→6 域 + 注入 `topic`（id 不变，题库 topic 引用有效）。临时脚本 `scripts/_remap-knowledge.mjs` 已删。
  5. `domain/knowledge.ts` `KNOWLEDGE_AREA_LABELS`→6 域中文；`domain/coverage.ts` `TopicCoverage.area`→`domain`+加 `topic`，报告新增"按能力域汇总"段（注意：CLI 经 `node scripts/*.ts` 原生执行，域汇总 import 必须带 `.ts` 扩展名，否则 ERR_MODULE_NOT_FOUND；Vite/tsc 不受影响）。
- 测试：新增 `src/data/taxonomy.test.ts`（5 例：域=6、topic 不重名、每个知识节点 domain 在 6 域且 topic 在骨架且 domain/topic 自洽、group 正确）；修复引用旧 area 的测试（knowledge.test / coverage.test / blueprint.test / schemas/knowledge.test）。
- 验证：npm run test **275 passed**（270+5）、typecheck/build 绿；`npm run question:coverage` 输出 6 域汇总。docs/DECISIONS.md 新增 ADR-038，ARCHITECTURE.md `data/knowledge/` 段补层级说明。
- 踩坑：Node 24+ 原生跑 TS 脚本，相对 import 必须带 `.ts` 扩展名；且纯 domain 模块（如 coverage.ts）不能 import 含 `import.meta.glob` 的模块（如 data/knowledgeMap），否则 CLI 崩溃——用 taxonomy.ts（只依赖 types）替代。

## 题库 category 重映射 6 大域 + topic 角度白名单 + 6 道 Agent 题（ADR-039，"都做了"范围）
- 用户说"都做了"→执行 ADR-038 列的两项"不做" + 6 道 Agent 题：
  1. **题库 category 重映射**：7 个旧文件（agentic-ai/ai-engineering/deep-learning/llm/machine-learning/mlops/safety-ethics）重组为 6 个域文件（agent-engineering/ai-engineering/llm/llm-applications/ai-systems/ai-security）。每题 `category`=其 topic 所属域，解析优先级：知识节点 area → taxonomy topic → 显式纠正（llm-as-judge→ai-systems, prompt-engineering→llm-applications）→ 源文件域回退。409 题 topic id 不变，零语义破坏。临时脚本 `scripts/_remap-category.mjs` 已删。
  2. **topic 角度白名单**：`src/data/taxonomy.ts` 新增 `ANGLE_WHITELIST: Record<topic, QuestionAngle[]>`（每 topic 合适角度子集）+ `allowedAnglesFor(topic)` 兜底返回全部 10 角度；`coverage.questionCoverageMatrix` 节点未声明 `angles` 时以 topic 白名单作期望角度兜底。`taxonomy.test.ts` 加 2 例守护白名单合法性。
  3. **6 道 Agent 架构单选题入库**：基于用户给的 6 主题（Model vs Harness/System Prompt 运行时/Agentic Loop/Translation Layer/Tools Schema 解耦/本地化 Harness）生成，topic 指向 agent-fundamentals/agent-loop/tool-calling，angle 取 system-design/scenario/mechanism/design/tradeoff。⚠️ 原题正文在上下文已折叠，选项与答案需用户最终核对。临时脚本 `_add-agent-questions.mjs` 已删。
- `domain/categories.ts` 的 `CATEGORY_LABELS` 改为复用 `taxonomy.DOMAIN_LABELS`（UI 分类标签直接显示 6 域中文名）。修复过期测试 `knowledge.test.ts` 的 `ANGLE` 白名单：原为 6 角度硬编码（ADR-032 遗留），改为 10 角度字面量（对齐 ADR-037）。
- 验证：npm run test **277 passed**（275+2 白名单）、typecheck/build 绿；`npm run question:coverage` 6 域汇总正常（Agent 工程 6 概念 P0 5/5 缺口 0）。docs/DECISIONS.md 新增 ADR-039，ARCHITECTURE.md `data/questions/` 段改为 6 域文件说明。
- 待用户确认：6 道 Agent 题的精确选项/答案（我按标准知识生成，非原题）；是否要改多选。

## 6 道 Agent 题改为多选（"多选"指令）
- 用户回"多选"→把 agent-engineering.json 中 6 道 `agent-arch-*` 由单选（`type:single`, 1 正确项）改为多选（`type:multiple`）。
- 设计：每题重写成"3 个正确真命题 + 2 个干扰项"，题干改为"以下哪些说法正确"，`answer`=[0,1,2]。评测链已原生支持多选：`isChoiceCorrect`（quiz.ts）用"选中集==答案集"精确判定；QuestionCard 对 multiple 渲染 Checkbox.Group；`bank.test.ts` 对 multiple 仅校验 answer 无重复且非全选。
- 验证：npm run test **277 passed**、tsc 绿。临时脚本 /tmp/_multi.mjs 已清。
- 注：仍沿用我按标准知识生成的选项/答案，非用户原题，需其最终核对。

## 生成根目录 CHECKLIST.md（"做一个checklist，放到根目录"）
- 基于实查缺口数据（28 topic 中 21 有题、7 空白、87 孤儿题、偏薄 topic）写成 `CHECKLIST.md`，分五节：①结构层已完成 ②内容层缺口（空白 topic / 偏薄 topic / 孤儿题治理）③功能数据流层 ④待用户确认 ⑤建议优先级。数据通过 node 脚本实查（taxonomy.ts 抽 topic + knowledge/questions 映射）。

## 撰写 LLM 替代方案分析报告（根目录 `llm-replacement-analysis.md`）
- 用户要求"写一个完整的分析报告，包括另一个AI的意见都写进去，放在根目录"。
- 产出 `llm-replacement-analysis.md`：含①当前 4 处 LLM 调用点清单（variant/evaluate/interviewAgent/Copilot）与既有 LLM 隔离事实（LLMProvider 接口、分数所有权归 domain、ADR-019/036/037）②逐调用点替代方案（模板变体 / 三层开放题评估 / 确定性 ranking / 嵌入+MMR 去重 / 保留 Copilot）③**完整收录外部 AI critique**：肯定先确认调用点、纠偏三处（变体模板仅 fallback、embedding 是 evidence detector 非 grader、勿立即上 Bandit）、主张 Concept×Angle coverage 为确定性智能层核心、P0 五条 + Phase2 BKT/IRT + Phase3 Bandit ④我方自评：认错三处过强、采纳 coverage 核心与 Agent=interaction intelligence、补拓扑排序/嵌入校验/QuestionResult 地基/难度双字段 ⑤ADR-040 草案路线图 ⑥当前状态：实测 278 测试 276 通过 / 2 失败（adaptive.move-on、learner.suggestNextTopics，均为确定性层、测试漂移非生产回归），数据就绪度风险（87 孤儿题/7 空白 topic/角度标注缺失使 Bandit/IRT 短期不可行）。
- 测试实测：278 例，276 passed / 2 failed（与对话摘要记录的 277 passed 有 1 例漂移；2 失败项根因是测试 fixture 自相矛盾 + reason 断言过约束，非逻辑回归）。
