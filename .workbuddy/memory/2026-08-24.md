# 2026-08-24

## 代码架构复审（REVIEW_2026-08-24.md）
- 复核当前工作树 + CODE_REVIEW.md(08-23) 遗留项。测试 229 passed。
- 已确认修复：B1 计时锚定 / B2 useAI 门控 / B3 required 注入 / B4 自适应 profile / B5 提前结束先评分 / nodeTypes 清理。
- 新发现问题（已写入根目录 REVIEW_2026-08-24.md）：
  1. [中] 选择题错题把 `gaps:['答案不正确，请参见解析']` 写进 Learner Memory，污染 commonWeaknesses/推荐文案；fix: sessionFromQuiz 对 choice 置空 gaps。
  2. [中] package.json 仍声明已移除的 pi-agent-core 依赖（src 零引用）→ 应删。
  3. [中] domain/knowledge.ts 的 knowledgeCoverage / KNOWLEDGE_AREA_LABELS 仅被自身测试引用，生产死代码 → 删或接线。
  4. [低] WEAK_AVG 与 WEAK_MASTERY 阈值冗余；README "Agent" 措辞过时；evaluate.ts 重复 DEFAULT_RUBRIC；根目录 缺口.json / ttt/ 草稿散落。
- 结论：架构未过度设计，符合 AGENTS.md；knowledge/coverage/blueprint 管线属刻意设计，仅其中一段未接线。

## 修复：选择题 gap 污染 Learner Memory（Phase 1 + Phase 2）
- 按用户确认的评审分析落地最小修复：
  - Phase 1：`src/domain/learner.ts` `sessionFromQuiz` 对 `format==='choice'` 置 `gaps:[]`（开放题保留 `g?.gaps`）。
  - Phase 2：`src/domain/evaluation.ts` `gradeChoice` 不再伪造 `gaps:['答案不正确，请参见解析']`，改为 `gaps:[]`（EvaluationResult 要求该字段必须存在，故保留空数组）。
- 测试：新增 4 个回归测试（choice 对错均不写 gaps、open 保留 gaps、全链路 JSON 不含假文案）；并把 `evaluation.test.ts` 原 `g.gaps.length>0` 断言改为 `toEqual([])`。
- 验证：`npm run test` 242 passed；`npm run build` 通过。
- 说明：假 gap 实际只漏进 Learner Memory(recommendationText)，结果页 ResultPanel 已对 choice 特判不渲染 gaps，故 Phase 1 单独即修掉可见 bug，Phase 2 为代码卫生。

## 计划：新增「Agent 面试」页（AGENT_INTERVIEW_PLAN.md）
- 用户要求新增 UI 页，用 `@earendil-works/pi-agent-core@0.84.2` 跑自主决策 loop，现有流程保留。已核实：pi-agent-core 已安装但 src 未引用（纠正 review 中"死依赖"误判）；`Agent.streamFn` 来自 `buildModels().streamSimple`、`model` 来自 `getModel`(src/ai/pi.ts 已就绪)；`AgentTool` 用 TypeBox 参数 schema。
- 计划要点：新增 `src/agent/`（types/tools/prompt/interviewAgent/runtime），复用 domain/learner/evaluation 作工具；Agent=决策中心，评分与持久化仍走现有管线（updateLearner/sessionFromQuiz）；现有 4 页 + 规则式 adaptive.decideStrategy 保留。
- 分阶段：Phase0 接线验证 → Phase1 工具层(最小5工具) → Phase2 Agent运行时+提示词 → Phase3 UI页(第5导航项) → Phase4 文档/ADR-016。待用户确认后开工。

## 落地：Agent 面试 Phase1+2（src/agent/ 运行时层）
- 已完成并验证：types.ts(InterviewAgentSession/createAgentSession/sessionRecordFromAgent/averageOverall) + runtime.ts(buildAgentRuntime) + tools.ts(5工具:searchQuestions/getQuestion/evaluateAnswer/getUserWeaknesses/finishInterview, TypeBox 参数) + prompt.ts + interviewAgent.ts(createInterviewAgent, shouldStopAfterTurn 题数上限/finishInterview, beforeToolCall 拦截无 key 的开放题评估) + 测试(interviewAgent.test.ts 驱动完整 loop / tools.test.ts)。
- 踩坑：`Agent` 构造不收 `tools` 选项，须 `agent.state.tools = tools` 注入；build 阶段修 3 处类型错误(AnswerValue 导入 / beforeToolCall 返回 Promise / formats 非空断言)。
- 验证：npm run test 256 passed；npm run build 通过。

## 落地：Agent 面试 Phase3+4（UI 页 + 收尾）
- Phase3：新增 `src/components/agent/AgentInterviewPage.tsx`（开场 → Agent 选题/追问 → 复用 QuestionCard 作答 → 提交推进 → 结束 persist → 展示推理/工具调用过程）；App.tsx 加第 5 导航项 `agent`(RobotOutlined)，现有 4 项不动；新增 `handleAgentComplete` 复用 updateLearner+saveLearner 落库；schemas 的 mode 枚举加 `'agent'`；agent/types.ts 加 `sessionRecordFromAgent` 桥接(内部按 evaluations 过滤未评估选题，防 0 分污染)。
- Phase4：DECISIONS.md 加 ADR-034( Agent=pip-agent-core 决策运行时)；README 注明第 5 页；撤销 REVIEW 中"pi-agent-core 死依赖"误判(1.2 条)。
- 新增 `src/agent/persistence.test.ts` 锁 Agent→Learner 桥接契约。
- 验证：npm run test 259 passed；npm run build 通过。
- 说明：真实 LLM loop 需 API Key + 浏览器实机，本环境无法跑（无 Chromium）；构建+全量测试已覆盖编译与单元逻辑。
