## embedding 模型原理 + 预选问题清单（用户问询）

- 用户问：embedding 模型工作原理 / 如何生成一个 embedding 模型 / 参数含义；题库有无相关题，无则放入"预选问题清单"文件。
- 实查：题库 `embeddings.json` 已有 6 题（ai-code-002 / ai-fund-010 / emb-03..06，topic=word-embedding）；`search.json` 另有 5+ 题属 vector-db 主题但含 embedding 操作。
- 关键缺口：**无"如何训练/生成模型"题**（对比学习/InfoNCE/in-batch negatives/triplet），**无"参数含义"题**（dimension/temperature/normalize/pooling/similarity），无 Matryoshka。
- 产出 `embedding-questions-preset.md`（根目录）：①现有题索引 ②按 taxonomy 白名单覆盖分析 ③6 道预选草稿 JSON（emb-preset-01..06，符合 questionSchema，可直接并入 embeddings.json，无需改引擎）④落地建议。
- 附带一张 embedding 推理+对比训练架构图（show_widget SVG）。
- 注意：用户关心的两块恰好是之前 v2 报告/ADR-040 里"evidence detector / 算法化生成"涉及的同一能力面，预选清单可作为后续真正落地 embedding 去重/证据检测的训练素材。

## 依据用户长建议建立 action list（知识覆盖驱动演进）

- 用户贴长篇架构建议：题库压缩≠知识压缩；应把 Adaptive Interview 从 `Topic→Question` 升级为 `Concept→Question`，并区分 Knowledge Set / Question Set / Assessment Set 三个数量不相等的集合。
- 产出 `concept-coverage-action-list.md`（根目录）：现状基线（coverage.ts 已有 domain+topic+angle、adaptive.ts 已有四策略+pickLeastCovered、blueprint.ts/source.ts 已存在→本计划是增量扩展）、目标架构、数据模型增量（knowledge 节点加 concepts[]、question 加 tests[]、learner 加 ConceptStats）、PR0–PR6 执行计划（PR0=transformer 节点手工实验）、V1/V2 边界与避免过度设计红线、greedy weighted coverage 算法、落地检查单。
- 锚定真实节点：`transformer` 在 `src/data/knowledge/llm-architecture.json:165`；建议落 ADR-042。
- 核心红线：V1 不引入 Facet/BKT/IRT/prerequisite 图；tests 每题 ≤3 concept（1 primary+≤2 supporting）。

## PR0 实验（已执行，验证通过）

- 执行 action list 的 PR0：对真实 `transformer` 节点 + `transformer.json`（43 题）做概念覆盖试点。
- 改动（最小可逆、零回归，tsc 绿 / 278 test passed）：`knowledgeNodeSchema` 加可选 `concepts[]`；`questionSchema` 加可选 `tests[]`；`llm-architecture.json` 的 `transformer` 节点挂 10 概念面；`tests` 暂存于 `scripts/pilot/transformer-concept-tests.json`（未污染生产题库）；新增 `scripts/pilot/coverage.mjs`（只读，加权覆盖计算）。
- 实测结论（全部验证 PR0 假设）：①43 题但仅 8/10 概念在本题库触达；②覆盖极度不均（self-attention 36 题次 vs residual-normalization 2）；③kv-cache 在 transformer.json 0 题、却在 model-architecture/inference 题库大量存在→概念跨 topic 泄漏，topic 级视图会漏概念；④反向缺口：ffn/residual-normalization/causal-mask/training-objective 被题考察但知识库无节点→Knowledge Set 比 Question Set 还粗，概念应作独立覆盖坐标系；⑤training-objective 仅作 supporting→弱覆盖陷阱。
- 加权覆盖率（本题库范围）81.5%，2 未覆盖（ffn/kv-cache 本库）+1 弱覆盖（training-objective）。
- 报告 `PR0-transformer-pilot.md`（根目录）。建议进入 PR1（补 ffn 等概念节点）/PR2（tests 并回题库+校验）/PR3（coverage.ts concept 级）/PR4（adaptive 接 selectNextConcept）。

## PR1–PR4 实施完成（2026-08-26）

- 执行 concept-coverage-action-list 的 PR1–PR4。**关键偏离（刻意为之）**：原计划 PR1"把 ffn/residual-normalization/causal-mask/training-objective 升为正式知识节点"被放弃——会触发 `knowledge.test.ts` 的"无悬空节点"不变量（每个节点 id 必须作为某题 topic 存在）失败，且违背 PR0 洞察#4（概念应独立于知识节点）。这 4 个概念已作为概念面存在于 transformer 节点 `concepts[]`，无需成为节点。
- PR2：`scripts/pilot/merge-tests.mjs` 把 PR0 映射并回 `src/data/questions/transformer.json` → 43/43 题带 `tests`（≤3 概念、primary 唯一）。新增 `scripts/validate-questions.ts` + npm `validate:questions`（校验 concept 存在性/数量/primary 唯一）。
- PR3：`src/domain/coverage.ts` 加 concept 级纯函数 `conceptFaceOf/buildConceptStats/computeConceptCoverage(加权)/getConceptStatus(unseen≠0分)/getCoverageGaps/conceptPriority/rankConcepts`。
- PR4：`src/domain/adaptive.ts` 加 `selectNextConcept→findQuestionForConcept`、`pickNextConceptAware`；`pickNextAdaptive` 增可选 `conceptCtx` 参数（提供概念面走概念优先，否则回退原 topic/angle 逻辑，向后兼容）。四策略保留，作用对象 topic→concept。
- 共享类型加到 `src/types.ts`：`ConceptRef/ConceptStats/ConceptStatus/ConceptAttemptSignal/QuestionTest`。
- 测试：`src/domain/conceptCoverage.test.ts`（14 例）。验证 `npx tsc --noEmit` 绿；`npm test` **292 passed**（原 278，+14）；`npm run validate:questions` 绿（520 题 / 43 带 tests / 10 概念面）。
- 文档：DECISIONS.md 加 **ADR-042**；根目录报告 `PR1-PR4-concept-coverage.md`。
- 未做（留 V2/后续）：Facet、BKT/IRT、持久化 concept 级 learner 统计、向其他节点推广概念面。

## 接线引擎：nextAdaptiveStep 传 conceptCtx（接续"第 1 项"，2026-08-26）

- 把 PR1–PR4 的 `conceptCtx` 真正接进运行时：`src/application/interviewEngine.ts` 的 `nextAdaptiveStep` 现在每步根据**当前题池话题**取对应知识节点概念面，构建 `ConceptSelectionContext`（`face` 取自 `knowledgeNodes` 中 `concepts[]` 非空节点，按概念 id 去重取较大 importance；`answered` 由已问题目按序与其作答信号 `AnswerSignal[]` 对齐拼出），并作为第 5 参传给 `pickNextAdaptive`。
- 向后兼容：无概念面（题池话题无对应知识节点）时 `conceptCtx` 为 `null`，`pickNextAdaptive` 自动回退原 topic/angle 路径——**非 transformer 会话行为完全不变**；现有引擎测试（d1/d2 假话题）零影响。
- 当前仅 `transformer` 节点挂有 `concepts[]`（10 概念，importance 1.0 起），故概念优先抽题已对 transformer 会话生效（第一步先选最高 importance 概念 `transformer` 的主探题，unseen→move-on，已测未掌握→gap-probe）。向其节点推广概念面即可自然扩展。
- 新增引擎测试 3 例（概念优先命中 / 已作答继续概念覆盖 / 无概念面回退不报错）。验证 `npx tsc --noEmit` 绿；`npm test` **295 passed**（原 292，+3）；`npm run validate:questions` 不受影响。
- 文档：DECISIONS.md 的 ADR-042 把"nextAdaptiveStep 暂未接线"改为"已完成"，并注明默认路径不变 + 仅 transformer 生效。

## PR5 + PR6 落地（Concept-coverage 生成管线前移 + Dynamic Probe，2026-08-26）

- **PR5 生成管线前移**：`src/domain/blueprint.ts` 新增 `blueprintFromConcept` / `conceptBlueprintsFromGaps`（均衡：每概念≤1 张蓝图，消灭 5 题同概念）/ `testsFromBlueprint` / `buildQuestionFromGeneration`；`src/ai/generateQuestion.ts` 新增 `generateQuestionForBlueprint`；`LLMProvider` 接口加 `generateQuestion`（Pi/Chrome/Fallback 实现）。离线入口 `scripts/generate-concept-questions.ts`（`npm run generate:concept-questions -- --node transformer --count 5`）实跑产出 5 张均衡蓝图（multi-head-attention/kv-cache/training-objective/ffn/flash-attention 各 1）。
- **PR6 Dynamic Probe**：`pickNextConceptAware` 加 `allowProbe` 参数——开 AI 且选中概念无题库题时返回 `{question:null, probeConceptId}`；`pickNextAdaptive` 透传；`nextAdaptiveStep` 据此调 `provider.generateQuestion` 组装 **transient 临时题**（不经 QuestionSource 持久化）。`src/domain/probe.ts` 纯函数 `probeFrequency`/`shouldPromoteProbe`（阈值=3）做晋升判定，引擎返回 `probe:{conceptId,promoted}`。无 AI/生成失败回退原 topic/angle 路径（向后兼容铁律）。`Question.transient` 字段加入 schema；`types.ts` 加 `GeneratedQuestion`。
- 新增测试 26 例（blueprint.concept 8 / probe 4 / adaptive.probe 5 / engine.probe 3 / generateQuestion 4 / provider 2）。验证 `tsc --noEmit` 绿；`npm test` **321 passed**；`npm run validate:questions` 绿（520 题/43 带 tests/10 概念面）。
- 文档：ADR-042 更新（PR5/PR6 已落地）；根目录报告 `PR5-PR6-concept-coverage.md`。
- 关键不变量：探针题 `transient=true` 且 `tests` primary 命中目标概念 → 作答后覆盖缺口关闭、概念统计自动回写；`validate:questions` 仅校验正式题库。

## 概念面推广 + PR6 Curation 闭环（2026-08-26 晚）
- 推广 `concepts[]`：`rag.json`(rag/vector-db/rag-pipeline/reranking 共 33 概念面) + `agentic-ai.json`(agent-fundamentals/agent-loop/tool-calling/agent-guardrails 共 35 概念面)，概念 id 加命名空间前缀(vdb-/rag-/rgp-/rk-/af-/aloop-/tc-/grd-)避免与节点 id 碰撞。**零引擎改动**（nextAdaptiveStep 按 concepts[] 非空节点自动取 face）。
- Curation 闭环：引擎 `nextAdaptiveStep` 加可选 `curationSink(e: ProbePromotionEvent)`；`src/domain/curation.ts`(recordProbe/isPromoted/markCurated/curationTasks 纯逻辑) + `src/infra/curationStore.ts`(Node 文件账本, ledgerSink) + `src/types.ts`(ProbePromotionEvent)。`generate-concept-questions.ts` 增 `--from-curation [path]`/`--commit`，npm 别名 `curation:produce`。
- Node 脚本直跑修复：`curation.ts`/`curationStore.ts`/`probe.ts` 内部相对 import 补 `.ts` 扩展名（原生 ESM 不解析 extensionless）。
- 新增测试 17 例（curation 8 / curationStore 7 / 引擎 curation 接线 2）。验证 `tsc --noEmit` 绿；`npm test` **338 passed**；`--from-curation` 实跑：vdb-hnsw@vector-db(计数3晋升)→产出1蓝图，`--commit` 落盘 status=curated。
- 文档：ADR-042 更新（Curation 闭环 + 概念面推广）；`PR5-PR6-concept-coverage.md` 追加后续增强段。
