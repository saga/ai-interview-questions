# 2026-09-03

## 离线变体 corpus（ACTION_CHECKLIST ⑨）收口
- 核对发现 `src/data/variants/wiki-skill-evolution-2026-08.wb-llm-20260902.json` 已存在（21题×2=42变体，0 stale，门禁0失败）——之前的会话已用「模型自身产出改写文本 + 组装脚本」绕过 API key 完成首批量产，⑨ 并非真正阻塞。
- 本会话先重复生成 `wiki-skill-evolution.json`（42变体，过真实门禁），后发现与旧文件近重复（88–98%），已删除冗余文件，保留旧文件。
- 验证通道：`scripts/assemble-variants.ts`（复用真实 `validateVariant`/`computeVariantSourceHash`/`variantPoolSchema`，无 key 手动生成）+ `scripts/validate-variants.ts` 审计。
- ⑨ 已据实改为 `[x]`；评分表「离线生产能力」4→7。
- 已知天花板：同题 surface/context 变体共用选项 → 互相~90%相似，按生成管线≥88去重会塌成1条/题；纯中文选项因 fuzzball 按空白分词，`token_set_ratio` 易把合理改写判为漂移（<45拒），纯中文选项只能以 canonical 原选项兜底。
- 下一步（真正剩余工作）：扩到更多 topic（evaluation / agent-fundamentals 等）+ 改良选项去重/分词以提升单题两变体多样性。

## 2026-09-03 晚间：离线变体 corpus 扩展（续）
- 同一「模型产出改写 + assemble-variants.ts 组装」无 key 通道，新增 3 个 topic 变体语料：
  - `evaluation.wb-llm-20260903.json`：42 题 × 2 = 84 变体
  - `rag.wb-llm-20260903.json`：35 题 × 2 = 70 变体
  - `memory.wb-llm-20260903.json`：19 题 × 2 = 38 变体
- 全池：117 题 / 234 变体 / stale 0 / 近重复 96 对（阈值 ≥88，系统性同题 surface⇄context）。
- 草稿：`temp/variant-draft-{evaluation,rag,memory}.json`（可复用）。assemble 已参数化（草稿路径/输出名/slug 可配；选项省略取 canonical）。
- 踩坑（已固化）：FORBIDDEN_REFERENCES 含「下文」，含「上下文」会误伤；本仓并行发多个同文件 Edit 不可靠，改用整文件 Write 覆盖或串行 Edit。
- 决策：先扩覆盖（更多题有可用变体），单题双变体选项多样性（根治近重复天花板）留下一步，卡点 = 纯中文 fuzzball 空白分词。ACTION_CHECKLIST ⑨ 已据实更新。

## 2026-09-03 深夜：变体相似度门禁根治（ADR-072，#282–#285 完成）
- 根因：drift/dup/生成器去重三处门禁共用 `fuzzball.token_set_ratio`，纯中文无词边界→整句退化 1 token→整串 Levenshtein，同时造成「合法中文改写被 drift 误杀(<45)」与「选项逐字同只换题干被 dup 漏检」两个反向错误。
- 修复：新建 `src/domain/textSimilarity.ts`（CJK 字符级 token 多重集 Dice）：中文按单字、拉丁/数字按词、kebab-case 拆词；`cjkDice=200·|交|/(|A|+|B|)`。三处门禁统一改用 `cjkDice`。
  - drift `optionChangedTooMuch`：`< 35` 拒绝（原 token_set_ratio <45）。
  - dup `VARIANT_DUP_THRESHOLD = 88`，且**改比选项级**（题干不计入），干净分离「照抄=100 / 轻改≈91 / 重述≈54」。
  - 生成器去重 `question-variants.ts` 与审计 `validate-variants.ts` 都走 domain `cjkDice`+阈值，消除「生成拒/组装放」口径分裂。
- 删死脚本：`build-variants-wb.ts` / `variant-draft.ts` / `debug-variant-ratios.ts`（原型链，未进 package.json）。
- v4 prompt（`src/ai/variant.ts`）：`*-options` 风格要求选项做幅度明显改写（换视角/句式/主语），明确「轻改(≈91)仍会被门禁判死」——根治硬约束。
- 测试：`textSimilarity.test.ts`(8) + `variant.test.ts`(42) = 50 全过；改动源文件 tsc 干净。
- 端到端演示：`temp/variant-draft-fixtest.json` 重写 contextOptions 为与 surfaceOptions 真正多样化的重述版（Dice=55.2<88，per-option drift 全>35），assemble 0 门禁失败 0 近重复。
- 文档同步：ARCHITECTURE/CHANGELOG/DECISIONS(ADR-072)。
- 验证结论：现有 117 题 / 234 变体池被新门禁判为**全量近重复（117 对，相似度 100）**——因 promptVersion ≤ v3 选项雷同。
- ⚠ 待办（需用户定夺，未做）：① 现有 234 变体池须用 v4 重新生成（需 LLM key 或复用无 key 组装通道）；② 全部改动未 commit（AGENTS.md 不擅自提交红线）。
- 探针/校准脚本留在 `temp/`（probe-*.mjs / calibrate-*.mjs），仅观测用。
