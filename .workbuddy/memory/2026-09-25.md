# 2026-09-25

## 面试「逐题反馈」（immediate 模式）落地完成

按设计简报把「逐题反馈」做成**会话级节奏**（不是全局设置），跨 Agent 面试页与 Copilot 侧栏两侧共用。
决策依据见 `docs/DECISIONS.md` ADR-083，变更记录见 `docs/CHANGELOG.md` 顶部。

### 核心设计（下次改动前务必先读）

- **暂停点必须放在共享内部缝 `afterEvaluation()`**，不能放在 `evaluateAnswer` 工具里。
  判分有三条路径：`choiceAdvance`（选择题确定性 `gradeChoice`）、`fallbackAdvance`
  （`evaluateSessionQuestion`）、LLM 循环（`evaluateAnswer` 工具）。**只有第三条经过该工具**，
  塞进工具里会让选择题和降级路径静默地不暂停。三条路径现在都收敛到 `afterEvaluation()`。
- **不要用 `agent.abort()` 实现暂停**：运行时会按 `stopReason === 'aborted'` 归类为异常，
  打出 `model_error` 遥测 + 用户看到「模型返回错误」。正确做法是 `finishTurn` 钩子返回
  `{ action: 'end' }` 让模型停在正常轮次边界，并用 `beforeToolCall` 拦掉等待期的
  `getQuestion` / `evaluateAnswer`。已有测试锁住「暂停态 `errors` 为空」。
- **`evaluation === null` 不产生反馈**：否则「评分失败」会被投影成「0 分」，是伪造数据。
- `finishTurn` 返回 `undefined`（不是 `{ action: 'continue' }`）以保留默认调度，避免多一次 provider 请求。

### ★ pi-agent-core 0.87 破坏性变更（本次踩到的坑）

工作区有一份**未提交的依赖升级**（`pi-agent-core` / `pi-ai` 0.85.1 → 0.87.1，
`package.json` 版本 1.1.2 → 1.2.1），它已经先一步弄坏了 HEAD 上的 `tsc`。

- **`shouldStopAfterTurn` / `ShouldStopAfterTurnContext` 在 0.87 被删除**（不是改名，`.d.ts` 里完全没有）。
  继任者是 **`finishTurn?: FinishTurn`**：
  `FinishTurn = (turn: AgentTurnContext, signal?) => AgentTurnDecision | void | Promise<...>`，
  `AgentTurnDecision = { action: 'continue' } | { action: 'end' }`。
  `AgentTurnContext = { message; toolResults; context; newMessages }`。
- 顺带修了 `src/ai/chromeAgent.ts` 一处类型错误（`arguments: args` → `args as JsonObject`，从 `pi-ai` 导入 `JsonObject`）。
- **遗留（未处理，非本特性范围）**：`deepseek-v4-flash` 已从 pi-ai 的 DeepSeek 注册表移除，
  导致 `src/ai/local.test.ts` 3 个用例失败。已确认该 model id **只出现在测试里**
  （`local.test.ts` / `provider.test.ts` / `providerSystem.test.ts` / `settings.test.ts` / `ai-config.test.ts`），
  零运行时引用，故无功能影响。要修只需把测试里的 model id 换成注册表现存值。

### 关键文件

- `src/schemas/interview.ts` — `interviewFeedbackModeSchema = 'standard' | 'immediate'` + `DEFAULT_INTERVIEW_FEEDBACK_MODE`。
- `src/agent/types.ts` — `AgentStatus` 增 `'awaiting_feedback'`；`InterviewAgentSession.feedbackMode`；
  `AgentHandlers.onEvaluation`；`isAwaitingFeedback()`。
- `src/agent/interviewAgent.ts` — `afterEvaluation()`（核心缝）、`advanceDeterministic()`、
  `continueAfterFeedback()`、`beforeToolCall` 暂停闸门。
- `src/domain/interviewFeedback.ts` + `src/components/interview/InterviewFeedbackCard.tsx` — 共享投影 + 共享组件。
- `src/application/conversation/interviewContext.ts` — `ActiveInterviewContext` → `toAnswerContext()` 桥接。
- `src/hooks/useAgentInterview.ts` — 恢复路径已重构为**复用 `buildHandle`**（原先重复配置一份 agent，
  会静默丢掉新 handler）。`clearFeedback()` 配对清 `feedback` + `lastEvaluation`。

### 持久化注意

`storedAgentSessionSchema` 用 `.passthrough()`——它只能**保留**已存在的 key，**不能给缺失 key 补默认值**，
所以新字段必须显式声明。用 `.catch(DEFAULT)` 而非 `.default()`：将来出现非法值时降级为 `standard`，
而不是把整份草稿判废丢弃。

### 门禁结果

- `tsc -p tsconfig.app.json && tsc -p tsconfig.node.json`：**通过**。
- 全量 `vitest run`：**937 passed / 944**。6 个失败全部与本特性无关：
  - 3 个 `local.test.ts` 的 `deepseek-v4-flash`（依赖升级导致，仅测试引用）；
  - 3 个并行负载下的 5s 超时（`local.test.ts` / `provider.test.ts` / `interviewCapability.test.ts` 各 1），
    单独跑这三个文件时**全过**（`provider.test.ts` + `interviewCapability.test.ts` 零失败）。

### 写测试的教训（本仓库特有）

`pickNextAdaptive` 会在同主题的多道题之间**随机**挑，所以断言**不能硬编码题号**。
本次连续三次踩到：`expected 'q-choice-1', received 'q-choice-2'`。
正确写法是断言相对关系：
```ts
const first = res.firstQuestion!.question.id;
const other = first === 'q-choice-1' ? 'q-choice-2' : 'q-choice-1';
// 或
expect(step.question?.question.id).not.toBe(first);
```

### 内存目录（再次确认）

`.workbuddy/memory/` 是实际在维护的那份（日志连续到 2026-09-16）；
`.workbuddy-ai/memory/` 停在 2026-09-03/04。**新笔记写这里**（`.workbuddy/memory/YYYY-MM-DD.md`）。
