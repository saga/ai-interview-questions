# 2026-09-25

## 逐题反馈「收口轮」：3 个 P0 + Agent/Copilot 合并 runtime（ADR-084）

用户逐文件复查后指出 3 个 P0 + 1 个结构性问题，本轮全部改完。

### ★ 最重要的一条经验：runtime 状态与 UI 投影必须分开持有

三个 P0 全是同一个根因的不同表现——**把「UI 投影」当成了「运行时状态」**：

1. **standard 模式误弹反馈卡**：`afterEvaluation` 在 standard 下也会触发 `onEvaluation`
   （评分总得发生），而 UI 用 `feedback !== null` 推断「已暂停」。于是「评分完成 → 下一题交付」
   的窗口里（开放题下数秒）弹出反馈卡、题目只读、提交按钮消失。
   → 修正：运行时**显式下发** `awaitingFeedback` 参数，且**先置 `session.status` 再通知回调**
   （否则回调里读到的 status 还是旧的）。hook 另立 `awaitingFeedback` state 作暂停态真源。
   **注意**：用户建议的改法（在 `onEvaluation` 里读 `session.status`）本身是错的——
   当时 `afterEvaluation` 是先调回调、后置 status，读到的是 `'running'`。
2. **刷新后「让 Copilot 详细解释」消失**：只恢复了 `feedback`（投影），漏了 `lastEvaluation`
   （原始评分），而该入口条件是 `lastEvaluation` 非空。
3. **刷新后节奏 UI 错**：没把 `session.feedbackMode` 同步回 React state。

### ★ 两个 UI 合并为同一场面试（ADR-084）

- Copilot 侧栏不再自建 `startChatInterview` / `ChatInterviewController`，改吃 App 层
  `useAgentInterview` 的 `agentInterview` prop。
- **Copilot 面试模式改为状态直渲染**（用户选的方案）：题目 + 反馈卡由 `agentInterview` 渲染，
  不再推成消息气泡。原因：`useAgentInterview` 是**状态 hook 不是事件流**，用 effect 把状态
  同步成消息会重复追加（StrictMode）且与真源脱节。
- `useAgentInterview.submit(answerOverride?)`：侧栏作答来自聊天框文本解析。
- **新增 `interviewRoutingContext()`（纯函数 + 4 个单测）**：面试中路由上下文必须由共享会话派生，
  否则 `shouldSubmitAsAnswer` 判定「无待作答题」→ 用户输入的「A」被当成提问 → **面试卡死**。
  反馈态下 `pendingAction='feedback'` 关闭答案通道。
- 删死代码：`startChatInterview`/`ChatInterviewController`/`ChatInterviewStep`/
  `rehydrateInterviewAgent`/`projectToConversationSession`（后者失去唯一调用方）。
  `interviewCapability.ts` 只留规则式面试三函数。
- **`ConversationSession` 的 `feedbackMode` / `pendingAction:'feedback'` / `agentSession?` 刻意保留**：
  是持久化契约，旧草稿里已写入，删字段会让 `parse` 失败并丢弃整份草稿。留待独立一步清理。

### `CONTINUE_PATTERN`：前缀匹配是错的

旧实现只有 `^` 没有 `$` → `继续解释一下`、`继续讲刚才那个` 被判成「要下一题」，
与 Copilot「解释考点」的定位直接冲突。

- 改成整句匹配，但**末尾保留**指代填充（`跳过这道题`）与难度修饰（`下一题难一点`）——
  用户给的简单 `$` 版本会**弄坏已有测试** `跳过这道题`。
- 另加 `CONTINUE_EXPLAIN_PATTERN`（`^(继续|接着)(讲|说|解释|…)`）进 `isHelpSeeking`：
  开放题下**任何非空文本**都会走答案通道，不拦的话「让它接着讲」会被当成答案去评分。

### 历史反馈（P1）

- `QuestionResult` 新增可选 `evaluation`（完整 `EvaluationResult`），`sessionFromQuiz` 原样留存
  （同一对象引用，**不重算**——重算会得到与用户当时所见不同的分数，是伪造历史）。
- `SessionReplayDrawer` 复用同一个 `InterviewFeedbackCard`。旧记录无该字段时退化为原视图。

### 其它

- `useTrainingSession`：`g?.overall ?? 0` → `if (g)` 才记自适应信号（两处）。原先评估失败会被
  当成「答了 0 分」喂给自适应引擎，让它降级出题。
- `storage/learner.ts` 补 Zod 校验（注释早就写了「不可信边界」但代码没做）：
  learner 行损坏 → 退回空画像；**session 行只做最小形状检查**，刻意不套完整 schema——
  旧记录形状过时不该整条丢弃用户历史（我第一次写成完整 schema 校验，直接弄坏了已有端到端测试）。
- `storage/db.ts`：`StoredLearner` 补 `assessmentCoverage` / `misconceptionHits` 类型。
  原先靠 `toStoredLearner` 的对象展开「碰巧能用」（Dexie 保留未知字段），但类型缺失 ⇒
  一旦有人把 `toStoredLearner` 改成显式列字段，这两层证据会被静默丢掉。

### 门禁

- `tsc -p tsconfig.app.json && tsc -p tsconfig.node.json`：通过。
- 全量 `vitest run`：**944 passed / 949**。5 个失败全部与本改动无关（3 个 `deepseek-v4-flash`
  测试引用 + 2 个并行负载超时，单独跑全过）。
- `src/application/conversation` + `src/agent`：192 项全绿。

## 面试「逐题反馈」（immediate 模式）落地完成

按设计简报把「逐题反馈」做成**会话级节奏**（不是全局设置），跨 Agent 面试页与 Copilot 侧栏两侧共用。
决策依据见 `docs/DECISIONS.md` ADR-083，变更记录见 `docs/CHANGELOG.md` 顶部。

### 核心设计（下次改动前务必先读）

- **暂停点必须放在共享内部缝 `afterEvaluation()`**，不能放在 `evaluateAnswer` 工具里。
  判分有三条路径：`choiceAdvance`（选择题确定性 `gradeChoice`）、`fallbackAdvance`
  （`evaluateSessionQuestion`）、LLM 循环（`evaluateAnswer` 工具）。**只有第三条经过该工具**，
  塞进工具里会让选择题和降级路径静默地不暂停。三条路径现在都收敛到 `afterEvaluation()`。
- **不要用 `agent.abort()` 实现暂停**：运行时会按 `stopReason === 'aborted'` 归类为异常，
  打出 `model_error` 遥测 + 用户看到「模型返回错误」。正确做法是 `finishTurn` 钩子返回
  `{ action: 'end' }` 让模型停在正常轮次边界，并用 `beforeToolCall` 拦掉等待期的
  `getQuestion` / `evaluateAnswer`。已有测试锁住「暂停态 `errors` 为空」。
- **`evaluation === null` 不产生反馈**：否则「评分失败」会被投影成「0 分」，是伪造数据。
- `finishTurn` 返回 `undefined`（不是 `{ action: 'continue' }`）以保留默认调度，避免多一次 provider 请求。

### ★ pi-agent-core 0.87 破坏性变更（本次踩到的坑）

工作区有一份**未提交的依赖升级**（`pi-agent-core` / `pi-ai` 0.85.1 → 0.87.1，
`package.json` 版本 1.1.2 → 1.2.1），它已经先一步弄坏了 HEAD 上的 `tsc`。

- **`shouldStopAfterTurn` / `ShouldStopAfterTurnContext` 在 0.87 被删除**（不是改名，`.d.ts` 里完全没有）。
  继任者是 **`finishTurn?: FinishTurn`**：
  `FinishTurn = (turn: AgentTurnContext, signal?) => AgentTurnDecision | void | Promise<...>`，
  `AgentTurnDecision = { action: 'continue' } | { action: 'end' }`。
  `AgentTurnContext = { message; toolResults; context; newMessages }`。
- 顺带修了 `src/ai/chromeAgent.ts` 一处类型错误（`arguments: args` → `args as JsonObject`，从 `pi-ai` 导入 `JsonObject`）。
- **遗留（未处理，非本特性范围）**：`deepseek-v4-flash` 已从 pi-ai 的 DeepSeek 注册表移除，
  导致 `src/ai/local.test.ts` 3 个用例失败。已确认该 model id **只出现在测试里**
  （`local.test.ts` / `provider.test.ts` / `providerSystem.test.ts` / `settings.test.ts` / `ai-config.test.ts`），
  零运行时引用，故无功能影响。要修只需把测试里的 model id 换成注册表现存值。

### 关键文件

- `src/schemas/interview.ts` — `interviewFeedbackModeSchema = 'standard' | 'immediate'` + `DEFAULT_INTERVIEW_FEEDBACK_MODE`。
- `src/agent/types.ts` — `AgentStatus` 增 `'awaiting_feedback'`；`InterviewAgentSession.feedbackMode`；
  `AgentHandlers.onEvaluation`；`isAwaitingFeedback()`。
- `src/agent/interviewAgent.ts` — `afterEvaluation()`（核心缝）、`advanceDeterministic()`、
  `continueAfterFeedback()`、`beforeToolCall` 暂停闸门。
- `src/domain/interviewFeedback.ts` + `src/components/interview/InterviewFeedbackCard.tsx` — 共享投影 + 共享组件。
- `src/application/conversation/interviewContext.ts` — `ActiveInterviewContext` → `toAnswerContext()` 桥接。
- `src/hooks/useAgentInterview.ts` — 恢复路径已重构为**复用 `buildHandle`**（原先重复配置一份 agent，
  会静默丢掉新 handler）。`clearFeedback()` 配对清 `feedback` + `lastEvaluation`。

### 持久化注意

`storedAgentSessionSchema` 用 `.passthrough()`——它只能**保留**已存在的 key，**不能给缺失 key 补默认值**，
所以新字段必须显式声明。用 `.catch(DEFAULT)` 而非 `.default()`：将来出现非法值时降级为 `standard`，
而不是把整份草稿判废丢弃。

### 门禁结果

- `tsc -p tsconfig.app.json && tsc -p tsconfig.node.json`：**通过**。
- 全量 `vitest run`：**937 passed / 944**。6 个失败全部与本特性无关：
  - 3 个 `local.test.ts` 的 `deepseek-v4-flash`（依赖升级导致，仅测试引用）；
  - 3 个并行负载下的 5s 超时（`local.test.ts` / `provider.test.ts` / `interviewCapability.test.ts` 各 1），
    单独跑这三个文件时**全过**（`provider.test.ts` + `interviewCapability.test.ts` 零失败）。

### 写测试的教训（本仓库特有）

`pickNextAdaptive` 会在同主题的多道题之间**随机**挑，所以断言**不能硬编码题号**。
本次连续三次踩到：`expected 'q-choice-1', received 'q-choice-2'`。
正确写法是断言相对关系：
```ts
const first = res.firstQuestion!.question.id;
const other = first === 'q-choice-1' ? 'q-choice-2' : 'q-choice-1';
// 或
expect(step.question?.question.id).not.toBe(first);
```

### 内存目录（再次确认）

`.workbuddy/memory/` 是实际在维护的那份（日志连续到 2026-09-16）；
`.workbuddy-ai/memory/` 停在 2026-09-03/04。**新笔记写这里**（`.workbuddy/memory/YYYY-MM-DD.md`）。

---

## 测试用例瘦身（去掉重复 / 低价值用例）

动因：用例里堆了大量重复断言与**永不失败**的断言，白耗 token 与维护注意力。
改 7 个文件，**+87 / −229 行**，用例数 949 → 940（净 −9），失败数不变（仍为那 3 个 `local.test.ts`）。

- `interviewContext.test.ts` 9 → 6；`interviewAgent.test.ts` 22 → 21（两个镜像用例合并为 `it.each`）；
  `interviewFeedback.test.ts` 12 → 11；`learner.test.ts` 65 → 62；`variant.test.ts` 50 → 48。
- `copilot.test.ts` 抽 `capturedPrompt()` 助手，消掉 5 处重复的 `let captured` + fake chat 样板（用例数不变）。
- 顺手清掉的**名不副实**用例：
  - `expect(rec.questions?.[0].question).not.toBe('__stub__')`——对象与字符串比较，恒真；
  - `optionLetter` 单测——`String.fromCharCode` 的封装，且已被 `resolveAnswerText` 的「A. 选项一」覆盖；
  - `指纹把选项计入` 的断言实际只证明了「题干计入」，已补上真正的选项断言。
- 去脆化：`关键知识点取自知识点层 required` 原本硬编码两条要点正文，改为与
  `requiredPointsFor(q)` **同源比对**——要点改词不该弄坏这个测试。

### ★ 教训：断言必须**能失败**（本次抓到 1 个假通过用例）

`learner.test.ts` 原写 `expect(JSON.stringify(profile)).not.toContain(' 答案不正确，请参见解析')`
——**串首多了一个空格**，而 JSON 里数组元素没有前导空格，所以这条断言**永远为真**。
去掉空格后立刻变红：那个假 gap 确实进了 `profile`。

真因不在聚合层（`sessionFromQuiz` 已把选择题的 `gaps` 清空），而是**新加的 `evaluation` 字段**
原样留存了判分器原文——这是刻意的（历史回放要复用实时反馈卡）。
故正确的断言粒度是**只锁聚合信号**，而非整份 profile：

```ts
const profile = updateLearner(emptyProfile(), rec);
expect(JSON.stringify(profile.topicStats)).not.toContain('答案不正确，请参见解析');
expect(JSON.stringify(profile.misconceptionHits)).not.toContain('答案不正确，请参见解析');
```

推论：**「整份对象序列化后不含某串」是很差的断言**——它把「聚合结果」与「忠实原始记录」两种语义
混在一起，且极易被一个多余空格变成永真。要断言什么，就精确指到那个字段。

### 看过但判定要留（别顺手删）

- `variant.test.ts` 的 `各类结构失败都带机器可读 code`：看着像重复，实为**唯一**断言各
  `VARIANT_REJECT_REASON` 码的地方（上面的用例只断言 `.ok === false`）。
- `tools.test.ts`（44 例 / 765 行）逐条看过，无重复，全是各自独立的回归契约。
- `learner.test.ts` 的 `评分为 null…也算已考察` 与 `已作答但尚未评分…也算已考察`：
  分别走 `evaluations` 与 `answers` 两条不同的「已交付」判定，不能合。

---

## 测试瘦身 · 第二轮（7 个文件，再 −14 例）

用例数 **940 → 926**，失败数不变（仍为那 3 个 `local.test.ts`）。全量 923 passed / 926。

| 文件 | 变化 | 说明 |
|---|---|---|
| `agent/prompt.test.ts` | 25 → 21 | `resolveOpeningInstruction` 6→4（未配置/空白合并、配置值/去空白合并）；删 `边界内内容完整保留`（被「恰好等于上限」覆盖）；删 `与旧设计对比…`（只断言 `startsWith(SECURITY)`，被「防降级覆盖」完整包含），注释并入后者 |
| `ai/variant.test.ts` | 25 → 22 | `answer 被丢弃` + `explanation 被丢弃` 合并（同一个白名单契约）；ADR-081 三条「放宽」条款三合一 |
| `agent/interviewAgent.test.ts` | 21 → 19 | 删 `短答案原样返回`（被「恰好等于上限不截断」覆盖）；删 `全为 null…`（被「null 计入已交付但不计入已评分」覆盖） |
| `domain/variant.test.ts` | 48 → 46 | 删「选项照抄 → 近重复」（严格弱于「轻改 → 近重复」，因相似度单调）；warning 组两条合并 |
| `application/sessionEvaluator.test.ts` | 17 → 16 | `Pool miss + 开关关` 两条合并（`variantPool: null` 与 `emptyPool` 是同一断言的两种输入） |
| `domain/bias.test.ts` | 10 → 9 | 删 `passes balanced options`（同类非命中已有两条更具体的） |
| `domain/knowledge/retrieve.test.ts` | 34 → 33 | `answer` / `explain` 两个模式合并为循环 |

### 方法：先程序化找「近似重复」，再人工判定

写了个一次性脚本（`/tmp/dupscan.py` 思路）：按括号配平切出每个 `it` 的 body，
抽 `expect(` 行做归一化集合，同文件内两两算 Jaccard，阈值 0.6 / 交集 ≥2 报警。
**这一轮把所有 ≥0.45 的都捞出来了，剩下的都是误报**（如 `ai/evaluate.test.ts` 两条共用
`dimensions.correctness` 这类通用断言行，但主张完全不同）。
结论：**近似重复已经捞干，再往下就是主观判断，不是机械可查的**。

### ★ 差点误删：互补函数不等于重复

`reasoningPath.test.ts` 里 `isReasoningPathSubstantiallyDifferent`（5 例）与
`isReasoningPathTooSimilar`（4 例）的用例是**一一对应且结果相反**的
（`逐字相同 → false` vs `逐字相同 → true`），看着像一套冗余。
查实现才发现**不是互补**：前者签名是 `AssessmentInference`、只看 `reasoningGoal` 的单一阈值；
后者签名是 `ReasoningPath`、用 `targetDice + goalDice` **双阈值**，还先短路 `isAssessmentIdentical`。
→ **断言相反 ≠ 逻辑互补**，删之前必须读实现。

---

## 评审收口第二轮：8 项（7 改 1 已修），ADR-085

外部评审又给了 8 项。全量 **925 passed / 928**（3 项仍是未提交依赖升级的 `deepseek-v4-flash`）。
本轮净增 2 例回归，**两条都在修复前实测为红**。

### ★ 守卫要按「状态」冻结，不要按「工具名」枚举

P0：`beforeToolCall` 原先只拦 `getQuestion` / `evaluateAnswer`。但
`toolExecution: 'sequential'` ⇒ **同一条 assistant 消息可带多个 tool call**，而 `finishTurn`
只在**整轮工具全部执行完**之后才调用。于是模型一轮返回 `evaluateAnswer + finishInterview` 时，
`evaluateAnswer` 置 `awaiting_feedback`、`finishInterview` 紧随其后**无人看守**，直接把状态推到
`finished` —— 用户跳过反馈卡、直接看到「面试已结束」。

改法：`if (session.status === 'awaiting_feedback') return { block: true }`，**不再判断工具名**。
判据是**状态语义**（暂停态下模型本就不该做任何动作），所以「全部冻结」既是最严的守卫，
也是唯一**不需要随工具集演进而维护**的守卫。停在「把 `finishInterview` 也加进黑名单」＝
下次新增工具（`skipQuestion` / `requestHint`…）原样重现。

**通用教训：黑名单式守卫的维护成本随被守卫集合增长，按状态冻结不会。**

### ★ React state 读不到「当下」，重入闸必须用 ref

`busy` 是 state，事件回调里读到的是**本次渲染时**的旧值——覆盖不了「点击发生在 setState 之后、
重渲染之前」的窗口。`useAgentInterview` 新增 `busyRef` 同步镜像（所有写入统一走 `setBusyBoth`），
暴露 `agentBusy()` 给 `submit` / `jumpToNextQuestion` 入口 `return`。
（`submittingRef` / `continuingRef` 本来就是 ref，所以只有 `busy` 有这个问题。）

两个 UI 入口共用 runtime ⇒ **UI 的 `disabled` 只是提示，不是契约**。`Sender` 的 `disabled`
当时还是字面量 `false`。两层都要做。

### ★ 重入闸必须留逃生口，且不能拦在错的时机上（评审稿在这里是错的）

评审稿建议用 `agentInterview.busy` 拦 `continueAfterFeedback`、并在 `handleSend` 里一律拦。
两条都会造成新 bug，本轮**刻意没照做**，并在代码里写了注释说明原因：

1. **`end_interview` 不能拦**。`finalize()` 会先 `abort()` 再收尾，它是用户主动中止的**逃生口**；
   拦掉 = 把用户锁死在暂停态。
2. **`continueAfterFeedback` 不能用 `busy` 拦**。`awaiting_feedback` 是在 `afterEvaluation`
   **内部**置的，而 `busy` 要到 `agent_end` 才清 ⇒ 用户提交后立刻点「继续」会被**无声吞掉**。
   它的幂等性已由 `continuingRef` + 运行时 `status !== 'awaiting_feedback'` 检查保证。

**通用教训：加「忙判断」前先问「这个忙碌标志与我要保护的状态，生命周期是否一致」。**
不一致时忙判断不是保护，是丢事件。

### ★ 「假成功」比报错更糟：`finalize()` 必须 await `onComplete`

`onComplete` 是 IndexedDB 写入（Promise）。同步调用 ⇒ 写入失败变成 unhandled rejection，
而 UI 已经显示「已保存」。改 `async finalize()` + `try/catch`（失败 `setError`），
两个调用点改 `void finalize()`。

### 存储边界：按「该字段被谁消费」决定校验强度（修正上一轮的结论）

**上一轮我在 MEMORY.md 里写的「session 行刻意不套完整 schema」是过度简化的，本轮修正。**

旧口径只查 `Array.isArray(row.questionResults)` ⇒ `{ questionResults: [{}] }` 能过 ⇒
`result.topic` 是 undefined 被写进 `topicPracticeSessions`，脏形状流进进度页与历史回放。

但**整行套 `sessionRecordSchema` 也会误伤**：该 schema 传递性要求
`questionResults[].evaluation`（完整 `EvaluationResult`）与 `questions`（原题快照），
而这些都是**历史回放才读**的字段。第一次写成严格校验，直接弄坏了
`sessionFromQuiz → updateLearner → save → load` 端到端测试——旧快照形状过时，整行被丢。

正确口径：**`parseSessionRow` 三段式**——严格 → 逐条摘掉不过的 `evaluation`（其余题目评分保留）
→ 整行摘掉 `questions` / `answers` → 仍不过才丢行。

判据是**该字段被谁消费**：
- 核心字段（`overall` / `questionResults` 骨架）被**进度聚合**消费 → 坏了必须丢行；
- 回放快照只在 `SessionReplayDrawer` 读 → 坏了只降级（该组件本就支持退化成「分数 + 解析」视图）。

**为一条过时快照丢掉用户整段历史，代价远大于回放视图降级。**
「不可信边界」不等于「全部严格」——要按消费方分级。

### ★ 评审稿引用的代码 ≠ 文件里的代码时，以文件为准

第 4 项说 `CONTINUE_PATTERN` 仍是前缀匹配并建议加 `$`。**它早在上一轮（ADR-083 收口）
就已经是整句匹配**，末尾保留指代填充（`跳过这道题`）/ 难度修饰（`下一题难一点`）/ 语气词的可选组，
`CONTINUE_EXPLAIN_PATTERN` 也已在位。按稿子的朴素 `$` 写法改会**弄坏**既有的
`detectCommand('下一题难一点')?.difficulty === 'hard'`。

评审稿给的文件路径与代码片段是**线索**，不是事实。落地前必须回文件核对；不一致时要**报告差异**，
而不是照稿执行。

### 其余三项

- `agentSession.status`：`z.string().min(1)` → `z.enum(['running','awaiting_feedback','finished'])`。
  宽泛字符串会把手改的任意值判为合法，续面时恢复出运行时无法理解的状态，后续所有
  `status === '...'` 判断全部落空（症状：「草稿看起来能恢复、但行为莫名其妙」）。
  **不**连带改 `getActiveAgentSession` 的 `!== 'finished'`：宽值比 `'finished'` 失败方向偏保守
  （未知状态当成可恢复），改它没收益。
- `CopilotSidebar`：监听 `phase: running → done`，清空侧栏 `ConversationSession` / `chatQuestion` /
  上下文，**保留** `mode: 'interview'` + `endedAt`（让侧栏显示「面试已结束」，而非静默退回普通问答）。
- `interviewContext.ts` 顶部注释仍在描述「两个独立 runtime」→ 改为描述 ADR-084 的单一 runtime 现状。
  **注释过期会让后来者按错误的心智模型改代码。**


