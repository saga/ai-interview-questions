# 2026-08-29

## 架构/代码/文档一致性全面审查
产出报告：`outputs/architecture-review-2026-08-29.md`。基线：364 tests passed、tsc 通过（问题均不表现为测试失败，属文档/配置漂移）。

### P0 发现
1. **Chrome 并发/超时配置 8 处撕裂**：代码 `chrome.ts:462-466` 是 `concurrency:8/timeoutMs:90_000`；CHANGELOG(08-28) 声称"已纠正为 4/60s"但**代码未改**；ARCHITECTURE.md:316,318 写 2/60s；`chromeComplete` JSDoc(469行) 写 2/60s；测试标题(108行)写 4、注释(123行)写 8、断言(124-125行)要求恰好 =8。
   - 坑：测试断言 `maxActive >= 8` 把当前值锁死，按 CHANGELOG 改成 4 会立刻失败。
   - CHANGELOG 自身摇摆：一处说"纠正为 4"，另一处实测"并发 8 约 90s vs 并发 4 约 250s，建议提到 6~8"。
   - 教训：把并发数抽具名常量、测试从代码读值，勿硬编码断言。
2. **`clone()` 无 fallback**（`chrome.ts:430-434`）：`base.clone?.() ?? Promise.reject(...)`，浏览器不支持 clone 时 Chrome 引擎 100% 不可用，与项目"处处兜底"风格相悖；建议回退到 `lm.create()`。

### P1 发现
3. **ADR-036 漂移**：ADR 要求"finalizeQuestion 移除 try/catch、无兜底回退"，但代码 `interviewEngine.ts:126-140` 仍回退原题（08-28 为修卡死有意改回）；ARCHITECTURE.md 324 行(回退) 与 369-376 行(抛错无回退) 自相矛盾；ADR 未标 Superseded。
4. **架构文档数据过期**：题库 文档 28文件/520题/514双形态 → 实际 **34文件/624题/618双形态**（仅 choice 6 题仍准）；知识点 文档 7文件/74节点 → 实际 **12文件/79节点**。

### P2 发现
5. `src/ai/chromeAgent.ts`（Agent 支持 Chrome）未进架构文档。
6. `graphology` 依赖冗余（package.json 有、src 零引用）——08-28 已报告，仍未清理。
7. `src/data/source.ts` 的 `QuestionSource` 是死代码（无调用方），但架构文档措辞暗示已接入引擎与 Agent。
8. `scripts/` 实际 7 个（含 lint-bias/fix-bias/validate-questions/generate-concept-questions/pilot），文档只描述 2 个。

### 已验证正常
分层边界（ai/ 不越界依赖 learner|adaptive|quiz；domain→schemas 仅 conceptGraph 且在豁免表内）；ChromeAIExecutor 的超时/取消/重试/销毁机制**确实已实现**；"未作答返回 null 不记 0 分"红线全链路一致；Agent→chromeAgent 接线完整；364 tests + tsc strict 通过。

### 待用户拍板
Chrome 并发目标数（2/4/6/8）—— 需先定一个值再全局对齐（代码+注释+JSDoc+测试标题+测试断言+ARCHITECTURE+CHANGELOG）。

## P0-1 已修复：Chrome 并发/超时统一为 4 / 90s（用户拍板）
用户选定：**concurrency=4、timeoutMs=90_000**（retries 保持 1）。

### 改动
- `src/ai/chrome.ts`：新增导出常量 `CHROME_AI_CONCURRENCY=4` / `CHROME_AI_TIMEOUT_MS=90_000` / `CHROME_AI_RETRIES=1`；`chromeAI` 单例引用之；**`ChromeAIExecutor` 构造函数兜底默认值（原 `?? 2`/`?? 90_000`/`?? 1`）也改引用常量**——全模块这三参数只剩一处数字。同步修正 461 行注释、469 行 JSDoc。
- `src/ai/chrome.test.ts`：并发用例改为**从常量取值**，任务数取 `2×CHROME_AI_CONCURRENCY`，断言 `maxActive <= 常量` 且 `== 常量`。这样调整并发数测试自动跟随，又能捕捉"退化成串行"。
- `docs/ARCHITECTURE.md:316,318`：数值改 4 / 90s + 加"单一出处"警示。
- `docs/CHANGELOG.md`：08-28 条目的"默认并发"行加勘误（该"纠正"当时未落地）；"建议 6~8"句后加 08-29 复核；顶部新增「2026-08-29 统一并发/超时配置」条目。

### 验证
tsc 通过、全量 **364 passed**。

### 关键经验（可复用）
- **测试硬编码断言会把错误值"锁死"**：原 `expect(maxActive).toBeGreaterThanOrEqual(8)` 导致"改代码就红、不改又与文档矛盾"。解法：测试从被测代码的导出常量取值，断言相对常量而非绝对数字。
- **配置类数值必须有单一出处**：分散在代码/注释/JSDoc/测试/CHANGELOG/架构文档的同一参数，必然漂移。抽具名常量 + 文档注明"勿再写死数字"。
- 审查文档/代码一致性时，**CHANGELOG 是重要信源但也可能是"承诺未兑现"**：本次正是 CHANGELOG 声称"已纠正"而代码未改，暴露了真相。

## 概念面推广（ADR-042）完成度评估：代码做完、数据只做 1/9
用户问"概念面推广做完了么" → 答：**没做完**。三层拆分：

1. **引擎/代码：✅ 全部完成**。`interviewEngine.nextAdaptiveStep` 的 `buildConceptContext`(L60) → `pickNextAdaptive(conceptCtx)`(L172) → Dynamic Probe(L177-199) → `curationSink`(L183) → probe 晋升全链路接线完整，纯函数 `buildConceptStats`/`computeConceptCoverage`/`getCoverageGaps`/`findQuestionForConcept`/`selectNextConcept` 齐备。
2. **节点概念面：⚠️ 9/79 节点（11%）**。transformer(10) + rag 系 4 节点(28) + agentic-ai 系 4 节点(28) = 66 个概念面。
   - ADR-042:39 声称 rag=33、agentic-ai=35，实际均 **28**（各差 5/7），文档数字与数据不符。
3. **题目 tests 标注：❌ 只做了 transformer**（关键缺口）：
   - 624 题中仅 43 题有 `tests`(6.9%)，按 topic 精确统计：transformer 节点 13 题标了 12 题(92%)；**rag/agentic-ai 8 个节点共 80 题，tests 全为 0**。

### 为何 rag/agentic-ai 是"空转"——技术根因
`findQuestionForConcept`（`adaptive.ts:113`）硬依赖 `q.tests`：
```ts
const candidates = pool.filter((q) => (q.tests ?? []).some((t) => t.concept === conceptId));
if (candidates.length === 0) return null;
```
题没标 tests → 永远找不到题 → 返回 null。后果：
- 开 AI（useAI=true）→ 每步都触发 Dynamic Probe，让 LLM 现场生成临时题（慢、费、质量不可控）；
- 关 AI → 直接回退原 topic/angle 路径，**概念优先等于没启用**。

### 剩余工作量
- 让 rag/agentic-ai 真正生效：给 **80 题**补 `tests`（每題 ≤3 概念、primary 唯一，由 `validate:questions` 校验）。
- 其余 70 个未推广节点含 275+ 题（题量最大：aws-genai-developer-pro 50、evaluation 40、system-design 32、reliability 22）。
- 修正 ADR-042 的概念数（28/28 而非 33/35）。

## 已执行：补齐 80 题 tests 标注（用户"直接补上"）
- 做法：逐题读题干+tags，对照 56 个概念面语义判定 primary/supporting（非关键词硬匹配）。分两个一次性脚本落地（`scripts/_annotate-tests.mjs` 主补丁 + `_annotate-tests-append.mjs` 补 3 个漏标 supporting），**均带 dry-run 与校验**（概念 id 合法性、primary 唯一、≤3），用完已删除。
- 结果：4 个文件（agent-fundamentals 31 / rag 26 / tool-calling 17 / search 6）；含 tests 题 43→**123**；该域覆盖率 0%→**100%**；概念覆盖 59/66→**62/66**；validate:questions 通过；全量 364 passed。
- **剩余 4 个真实缺口**（无题覆盖，正是 curation 管线补题输入）：`vdb-params`(ef/M/nprobe 参数调优)、`rag-latency`(端到端延迟吞吐)、`ffn`、`kv-cache`(后两个是 transformer 既有缺口)。
- 交付：标注对照表 `outputs/concept-tests-annotation.md`（80 题 × 概念映射 + 概念速查）。
- 文档：ADR-042 更正概念数 28/28，新增「概念面生效前置依赖」警示（挂 concepts[] 必须配套标 tests，只做前者空转）；CHANGELOG 顶部新增条目。

### 关键教训（复用价值高）
**「配置/元数据推广」类任务要检查整条依赖链，别只看最显眼的那一环**。ADR-042 记录了"零引擎改动、挂上概念面即生效"，这句话本身没错，但隐含前提是"题目已标 tests"——该前提没写进 ADR，导致推广只做了一半却记为"已落地"。今后遇到类似"挂上 X 就生效"的声明，应先追到最底层的数据依赖再判断完成度。

## 用户决策：删除整个概念层（选项 B），覆盖索引统一 topic × angle
用户深度反思后判定 `tests` 是重复建模 + primary/supporting 太主观 + ROI 低（只服务 20% 题库），要求彻底删除。

### 我在决策前给出的纠正（重要，避免同类误判）
1. **`tests` 有真实消费者**，不是"为未来预存"：findQuestionForConcept / 概念优先抽题 / Dynamic Probe / Curation 都在用。删它 = 拆掉 ADR-042 运行时。
2. **用户提的替代方案（subtopic + angle）站不住**：`subtopic` **100% 稀疏**——188 个值对应 188 道题，每个值下恰好 1 题，无聚合能力，不能当概念索引。
3. **用户想要的「Concept × Angle 索引」当时并不存在**：`angle` 虽 100% 覆盖，但只用作兜底排序因子（`adaptive.ts` angleEvidence）与 Agent 追问（`tools.ts` weakAnglesOf）；架构文档 348 行"pickNextAdaptive 据此优先追问缺证据角度"是**夸大描述**（已顺手修正）。
4. **减法 ≠ 加法**：删 tests 是拆维度，建 Concept×Angle 索引是建能力，二者非替代关系。只做减法会导致覆盖能力净下降。

### 执行结果
- 数据：删 `Question.tests`(123) + `Question.transient`(0) + `KnowledgeNode.concepts[]`(9)。
- 整文件删 13 个：probe.ts / curation.ts / infra/curationStore.ts（连带空目录 src/infra/）/ ai/generateQuestion.ts / scripts/generate-concept-questions.ts + 8 个测试。
- 部分删：coverage.ts(7 个概念函数，保留 ADR-032 的 topic×angle 矩阵)、adaptive.ts(概念优先路径+conceptCtx/allowProbe)、blueprint.ts(PR5 概念蓝图，保留 blueprintFromSuggestion)、provider.ts(generateQuestion)、interviewEngine.ts(buildConceptContext/探针/curationSink)、types.ts、schemas/question.ts、schemas/knowledge.ts。
- npm script 删 generate:concept-questions / curation:produce。
- `scripts/validate-questions.ts` 改写为面向新索引的通用校验（topic 有节点、angle 必填且在枚举、answer 不越界）。
- 文档：新增 **ADR-043**（含完整理由 4 条）、ADR-042 标注废弃、ARCHITECTURE 修正夸大描述、CHANGELOG 条目。
- 验证：tsc 通过、**306 passed**（删 58 个概念用例，补 2 个"无概念层后自适应仍正常"）、validate:questions 通过（624 题/79 节点，angle 100%）、代码与数据层零残留。

### 保留未动
ADR-032 的 questionCoverageMatrix(topic×angle) + coverageSuggestions、ADR-037 的 angleCoverage/weakAnglesOf、conceptGraph（与概念层无关）、subtopic 字段。

### 值得记住的方法
删大块功能前先用 tsc 定位断裂引用，比 grep 逐个找更快更准；schema 字段删除后 tsc 不报错（类型仍存在），必须**单独查数据层残留**才算真删干净。

## 删除 Question.rubric（ADR-044），explanation 接替为题目级评分锚点
用户第二道减法，选了"全删 + explanation 顶上"。

### 核查对前提的修正（重要）
- ❌ 用户前提"rubric 与 explanation 高度重合"**不成立**：`explanation` 只用于 UI 展示（ResultPanel / SessionReplayDrawer）与变体生成，**从不参与评分**；评分时 LLM 拿到的是 `open.referenceAnswer`。两者原本零交互。本次是让 explanation **新增**承担锚点，不是"本就重合"。
- 覆盖率数据：`rubric` 493/624 题（79%，高覆盖非边缘字段）；其中 `required` 有 239 题（48.5%）与知识节点 required **逐字相同**（纯副本），254 题（51.5%）逐题定制；`dimensions` 185 题（29.6%）/ 21 种组合。

### 改动
- 数据删 493 处 rubric（28 文件）；`schemas/question.ts` 删 rubricSchema + 字段。
- `domain/knowledge.ts`：`requiredPointsFor` 只取 `KnowledgeNode.required`（回退升级为唯一来源）。
- `ai/provider.ts`：`mergeQuestionRubric` 不再合并 dimensions，返回全局权重副本 + 知识点要点。
- `ai/evaluate.ts`：`buildEvalUser` 新增「题目解析」段注入 `q.explanation`（题目级锚点）。
- `agent/tools.ts`：`evaluateSessionQuestion` 用全局 DEFAULT_RUBRIC。
- 测试：改写 3 个 rubric 用例；新增 2 例（explanation 注入 / 空 explanation 不产生多余段落）；`bank.test.ts` 的 rubric 权重校验改为校验 explanation 非空（锚点新职责）。

### 验证
tsc 通过、**308 passed**（+2）、validate:questions 通过、数据层残留 0、临时脚本已清理。

### 已知代价（已写入 ADR/CHANGELOG）
254 题（51.5%）逐题定制 required 移除后，锚点形态从"结构化要点清单"变为"解析文本"；185 题失去题目级权重定制。

### 模式沉淀
连续两次"减法"任务，用户的前提都部分不准确（概念层的 subtopic 替代方案、rubric 与 explanation 重合），但**删除的大方向正确**——因为"重复建模 + 维护成本 > 收益"的判断成立。做法：先核查并纠正事实前提，再执行删除，并把真实代价写进 ADR。
