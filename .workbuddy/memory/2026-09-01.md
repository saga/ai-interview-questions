# 2026-09-01

## 题库质量门槛二期：存量清洗（curation）落地
- 新增 `analysis/question_curate.py` + `npm run question:curate` / `question:curate:semantic`：只读清洗计划生成器，把现有 audit + 语义信号从「找异常」升级成 KEEP / REWRITE / REMOVE(+review) 建议，**不动题库**。
- 确定性规则：option-length-leak(>1.8×)、answer-leaks(仅在 ratio>1.8× 时附加，避免误杀)、pseudo-hard(hard + definition/fundamental→suggestedAngle=tradeoff)、definition-heavy(同 topic definition≥4 重写第 3+ 道→mechanism)、cell-saturation(非平凡 angle≥4→review)、复用 audit P0/P1/P2 作优先级；可选 `--semantic-report` 复用 `conceptualClusterDensity`，过密簇保留 best(高认知价值 angle/低长度比) 其余 remove。
- 实测（确定性，无语义报告）：keep 664 / review 370 / rewrite 283 / remove 0（remove 需 `question:curate:semantic` 跑本地 ONNX MiniLM）。
- `skills/check-question-bank-quality/SKILL.md` 新增「存量题改写契约」：先定目标（改 angle/difficulty，不整题重生成）+ 10 条改写硬约束（对齐用户 §8/§9）。
- 设计取舍：answer-leaks-by-length 初版误杀 959 题（正确项常恰最长），改为仅在 ratio>1.8× 时附加，误报归零。
- 所有改动**未 commit**（含上期质量门槛 9 任务，累计仍 uncommitted，等用户决定是否单独 ADR 提交）。

## 存量 REWRITE 实际执行（P1：pseudo-hard + length-leak）
- 两批 P1 REWRITE 全部零事实风险、validate 通过、answer 索引 0 改动：
  - **pseudo-hard 降级（17 题）**：difficulty hard→medium（angle∈{definition,fundamental}），仅改元数据，无内容风险。
  - **length-leak 干扰项扩写（共 38 题 / 46 处）**：pilot 6 题 + 本轮把剩余 32 题的 ≤9 字过短干扰项扩写到同等粒度（同决策层级、明确错误、不与正确项/他项重复）。涉及 6 文件：claude-blog-2026-08(35 处)、aws-well-architected-lenses(1)、llm-reliability(2)、oreilly-radar(1)、sebastian-raschka-2026-08-3(1)、sebastian-raschka-2026-08(6)。
- 编辑方式：外科式行级替换。原文件为自定义压缩格式（数组/对象内联），**不能 json.dump 整文件重排**（会整文件 diff 爆炸）；改按 (id, option_index) 定位 options 数组所在行，json.loads 局部解析替换，保留其余内容与 answer 数组。
- `question_curate.py` Rule A 已从 rewrite 降为 review(P2)；扩写后重扫：length-leak 且仍含 ≤9 字干扰项的题 = **0**。
- 累计 uncommitted：本期 curation 计划/脚本/check skill + 上述题库文件共 20 文件，等用户决定 ADR commit（建议 ADR-067 质量门槛 + curation）。

## definition-heavy 重写被用户指出「只改 angle 标签是糊弄」
- 用户批评：19 道 definition-heavy 里我把 16 道只改了 `"angle": "definition"→"mechanism"` 等标签，没改内容，属糊弄；并重申设计文档（plan0901_3.md §①/§⑧/§⑨）要求改的是**题目内容/认知任务**，angle 是内容改变后的自然结果。
- 逐题核对内容后结论：**16 道是标签标错**（题干/选项本就 mechanism/comparison/design/scenario，仅 angle 字段错标 definition）→ 改标签是正确修复（也真正清掉了 definition-heavy 信号）；**3 道内容确为纯 definition**（stat-01、google-genai-leader-02/03）→ 做了真正的内容重写（新题干/选项/explanation，angle 跟随）。
- 唯一仍带 definition 口吻题干的是 `llm-04`（"什么是 RAG"）→ 已把题干改写为 mechanism 口吻（答案/选项不变），使 relabel 诚实。
- 验证：`question_curate` 重跑 definition-heavy 19→0；`validate:questions` 1317/1317 通过；llm-04 答案索引仍 `[1]`。
- 教训写进 `skills/check-question-bank-quality/SKILL.md` 存量题改写契约：只换 angle **前提是内容本身已属该 angle**，否则必须改内容，不能只改标签（否则是骗 topic×angle 密度计数器）。

## plan0901_3.md 要求落地对账（用户追问「都落地了么」）
- 结论：**未全部落地**。已建 curation 系统 + 执行 ①纯definition(19→0) 与 ⑥伪hard(17 降级)；但 §四 6 类里 ②③④⑤ 此前缺失。
- 本轮补齐 **②③④ 三类纯启发式检测规则**（仅标 review，不动题库，符合「找异常→人工复核」）：
  - Rule B `pseudo-single-choice`：单选题干含「最准确/最贴切/最合适/最确切/最正确」→ 11 题。
  - Rule H `kitchen-sink-answer`：单选 + 正确项最长 + 长度比>1.8× + 正确项含≥2 个并列分隔符（、"+"以及同时与且；/）→ 27 题。**初版误报 381**（不限制题型/分句阈值），收紧为仅 single 后降到 27；多选的正确项本就是多条独立陈述，不属此类。
  - Rule I `strawman-distractor`：干扰项命中稻草人短语块（删测试/删库/只看token/完全自动化/完全相反/明显荒谬…）→ 3 题，抽查均为真稻草人。
- **仍缺**：§四⑤ 语义近重复（同 Concept 换句话）——`question:curate:semantic` 检测已就绪但需本地 ONNX MiniLM 模型，当前环境无模型，remove 恒为 0；§二结构化维度 topic×difficulty、semantic-cluster-size 未在 audit 报告暴露（angle×difficulty 与 single/multiple 比已支持）。
- 严守「不误伤」：新规则一律 advisory review，不自动改写；kitchen-sink 反复收紧避免像 length-leak 那样误杀好题。

## 晚间 A/B/C 三件套（用户要求「就在这里跑」）
- **关键纠正**：本地 ONNX MiniLM 模型其实存在（`analysis/models/paraphrase-multilingual-MiniLM-L12-v2/onnx/model_qint8_arm64.onnx`），之前「环境无模型」是误判；`npm run question:curate:semantic` 在本机 3m24s 跑通（uv venv python3.14 + sentence-transformers+onnxruntime）。
- **(A) 语义去重已跑通**：在最终（已做 B 改写）题库上重跑，threshold=0.9，得 3 个近重复对（ai-fund-026↔llm-03 0.943、sebastian-2026-08-114↔157 0.934、gap-flash-attention-tradeoff-01↔flashattn-08 0.921），过密簇 0 → remove 强制 0；按「保留高认知价值 angle」给每对 KEEP/REMOVE 建议（共 3 个 REMOVE 候选，待用户确认再删，因删题为破坏性）。
- **(B) 40 道 review 信号题真实改写（非只改标签）**：
  - strawman 3 道：把「完全自动化/直接删除所有/删库」等稻草人干扰项换成同决策层级的真实误区陈述。
  - pseudo-single 11 道：题干含「最准确/最合适/最确切」但仅 1 个选项真正确 → **只改题干去掉最高级措辞**（不翻多选，否则会变造多正确项错题），保留单选。
  - kitchen-sink 27 道：正确项是长列举而干扰项过短 → **扩写每个干扰项到同等粒度且明确错误**（用户 §③ 同决策层级），正确答案文本一字未动；反复补扩最短干扰项使长度比≤1.8×，最终 3 类 review 信号全清零。
  - 验证：validate:questions 1317/1317 通过；16 题文件外科式编辑，答案索引零改动（旧稻草人/短干扰项文本替换，options 数组索引不变）。
- **(C) topic×difficulty 维度补齐**：`question_audit.audit()` 新增 `topicDifficulty` 分布 + `print_topic_difficulty`；`question_curate` 新增 Rule J（某 topic≥4 题且单档难度≥80% → review），实测 12 个失衡 topic → 115 题 flag。plan0901_3 §二 缺的维度已闭合（angle×difficulty、single/multiple 比、topic×difficulty 均已暴露；仅 semantic-cluster-size 仍靠语义报告）。
- 教训固化：用户讨厌「只改元数据标签糊弄」与「以『需要模型机器』为由不执行」——能在本机跑的就直接跑，改写要动内容（题干/选项文本）而非只动 angle/topic 字段。

## 收尾：执行删除 + 提交
- 用户「Please continue」= 确认执行 A 的 3 个 REMOVE 候选并打 ADR-067 commit。已删除 `ai-fund-026`(llm-fundamentals.json)、`sebastian-raschka-2026-08-157`(sebastian-raschka-2026-08-4.json，单行紧凑格式需特殊处理)、`gap-flash-attention-tradeoff-01`(foundational-and-intermediate.json)。外科式按块删除（单行对象删整行、多行块删 `{`→`}` 并清理尾逗号），validate 1317→1314 通过，三 topic 均保留孪生题覆盖不破。
- 全部改动（含上轮质量门槛 9 任务 + 本期 curation）已提交为 **ADR-067**（commit 8d2160e，24 文件），docs/DECISIONS.md 顶部登记 ADR-067。工作树已干净。
- 剩余仅 advisory review 信号（same-topic-angle-overloaded 399、topic-difficulty-skew 115、answer-leaks-by-length 110、option-length-leak 110、missing-source 103）作为指南，未强制改写。

## 全库内容 KEEP/REWRITE/REMOVE pass（P0，按用户收敛 checklist 启动）
- 用户把后续工作收敛成一份 checklist，核心收尾意见：**别再加 validator，直接治理现有题目**；P0 优先「全库 KEEP/REWRITE/REMOVE 内容 pass」。
- 先量化：86 个 overloaded 格子（同 topic×angle ≥4 题，共 650 题）。bigram 近重复扫描 top14 格 = 0 → overloaded 多为真实深覆盖而非换说法堆题。
- 工具：`analysis/dump_cells.cjs`（导出 overloaded 格内容）+ `analysis/_remove_ids.cjs`（按原格式删 id，最小 diff；pretty 用 2 空格、紧凑逐对象一行）。
- **执行模式（用户选「逐格执行+直接REMOVE，REWRITE 列清单待确认」）**。已审 13 格（pilot inference-optimization/mechanism + batch1 6 + batch2 6），剩 73 格。
- **已删 3 道清晰近重复（validate 1314→1311）**：sebastian-raschka-2026-08-164（≈120，可复现性该记录什么，同构，留 120）、continuous-batching-core（≈ai-infra-002，连续批处理核心调度机制，留 ai-infra-002）、agentic-14（≈ai-eng-012，长任务 context 溢出处理，同构，留 ai-eng-012）。
- **REWRITE 候选（未执行，待确认）**：system-design/system-design 研究 Agent 三连 ai-eng-024/agentic-07/agentic-17（同考 hybrid 研究 Agent 设计，留 1 改 2）；同格网关二连 ai-design-004/agent-gateway-01（同考多 Provider 统一网关，留 1 改 1）；context-engineering/scenario context-2026-105≈prompt-cache-context-ordering-01（prompt cache 稳定前缀，低置信，留 1 改 1）。
- 已审 KEEP 的健康深覆盖格：evaluation/{scenario,tradeoff,comparison}、reliability/scenario、system-design/scenario、multi-agent/mechanism、evaluation/mechanism、agent-loop/mechanism、context-engineering/system-design（各题认知任务均不同）。

## 内容 pass 续：batch3+batch4（再 12 格，累计 25 格 / 86）
- **batch3（6 格 65 题）**：self-attention/mechanism(17)、agent-fundamentals/{system-design(10),tradeoff(9)}、observability/scenario(10)、quantization/tradeoff(10)、agent-guardrails/scenario(9)。
  - REMOVE 级：无（全是不同认知任务或不同 scenario 换皮）。
  - REWRITE 候选新增：self-attention 缩放三连（sdpa-scaling-mech / softmax-scaling-01 / sebastian-raschka-2026-08-32 同考 1/√d_k 缩放，留 1 改 2）、QKV 二连（ai-fund-002 / sebastian-raschka-2026-08-31 同考 QKV 角色，留 1 改 1）。
  - 其余 KEEP；observability/scenario 与 agent-guardrails/scenario 内 aws-genai-developer-pro-* 场景题偏密（同格内不同业务场景，diagnostic 仍成立，标健康）。
- **batch4（6 格 45 题）**：system-design/tradeoff(9)、inference-optimization/tradeoff(9)、rag/scenario(9)、observability/system-design(6)、model-selection/system-design(6)、vector-db/scenario(6)。
  - REMOVE 级：无。
  - REWRITE 候选新增：测试时计算三连（sebastian-raschka-2026-08-96 / 58 / 21 同考多候选采样+验证器成本，留 1 改 2）、RAG 分层排查二连（ai-eng-016 / ai-fund-049 同考 recall 高≠准确率高→分层定位，留 1 改 1）、论文筛选二连（sebastian-raschka-2026-08-166 / 160 同考按瓶颈挑论文，留 1 改 1）。
  - 其余 KEEP（system-design/tradeoff、observability/system-design、vector-db/scenario 全健康；inference-optimization/tradeoff、rag/scenario、model-selection/system-design 各有上述二/三连）。
- **规律**：86 个 overloaded 格在字面层近重复≈0，内容层也多为健康深覆盖（不同 scenario/机制）；真正"同认知任务换措辞"的中重复集中在 sebastian-raschka-2026-08-* 系列与几个高频 topic（self-attention/inference-optimization/rag/model-selection）。REWRITE 候选均为待确认，未擅自动笔。

## 内容 pass 续：batch5（再 7 格，累计 32 格 / 86）
- batch5（7 格 42 题）：rag/tradeoff、multi-agent/tradeoff、distillation/mechanism、reranking/mechanism、caching/mechanism、inference-optimization/system-design、sampling/mechanism。
  - REMOVE 级：无。
  - REWRITE 候选新增（本轮最密）：
    - **动态推理预算四连**（inference-optimization/system-design）：sebastian-raschka-2026-08-23/93/51/63 同考"简单走低预算、难时升级、设上限/SLO"，正确项高度同构 → 留 23（核心策略）+ 63（预算上限保护角度略不同），改写 93、51 去考不同切面（如 budget forcing 准确率-成本曲线、best-of-N 的 pass@k 下界）。
    - **前缀缓存静态前置二连**（caching/mechanism）：ai-eng-027 / cache-prefix-01 同考"静态语料前置、动态提问后置、前缀连续匹配"，留 1 改 1。
    - **蒸馏轨迹二连**（distillation/mechanism，低置信）：sebastian-raschka-2026-08-57 / 19 同考"教师轨迹训学生=蒸馏/降本"，留 1 改 1。
  - 其余 KEEP：reranking/mechanism 的 reranker/cross-encoder/late-interaction 三子概念均覆盖；sampling/mechanism 温度二连（llm-03 作用 vs ai-fund-027 temp=0 确定性陷阱）角度不同保留；rag/tradeoff、multi-agent/tradeoff 全健康。
  - 跨格提示：sebastian "推理预算/测试时计算" 主题同时在 inference-optimization/tradeoff（96/58/21，batch4）与 /system-design（23/93/51/63，batch5）两个格密集出现，属跨格同认知任务重复，overloaded 格内检测会漏，最终需跨格归并处理。

## 内容 pass 续：batch6（再 6 格，累计 38 格 / 86）
- batch6（6 格 41 题）：rlhf/mechanism、moe/mechanism、moe/tradeoff、tool-calling/system-design、transformer/comparison、safety-alignment/tradeoff。
  - REMOVE 级：无（但 rlhf 可验证奖励二连接近同构，标高置信 REWRITE）。
  - REWRITE 候选新增（本轮最密）：
    - **可验证奖励二连**（rlhf/mechanism，高置信）：sebastian-raschka-2026-08-92 / 52 正确项 3/4 完全相同的同构题（数学符号验证/代码编译验证/奖励围绕结果），留 52 改 92 去考"可验证奖励 vs 偏好奖励的误差偏差"。
    - **MoE vs Dense 三连**（moe/tradeoff）：moe-design-tradeoff / moe-mixtral-05 / moe-vs-dense-compute 同考"容量-计算解耦但显存不省、负载均衡挑战"，留 1 改 2。
    - **Transformer vs RNN/CNN 复杂度五连**（transformer/comparison，最严重）：transformer-comparison-01 / attn-vs-seq-models / attn-vs-rnn-complexity / rnn-vs-tf-compare / rnn-to-tf-migration 同考"Self-Attention 顺序O(1)全并行、路径O(1)利长依赖、O(N²)显存高于RNN、推理仍串行"，建议留 transformer-comparison-01（经典三指标式）+ attn-vs-rnn-complexity（纯复杂度数值）2 题，改写其余 3 题去考不同切面（FlashAttention 降 O(N²) 显存、线性注意力破 O(N²)、KV Cache 长序列显存增长）。
    - **工具膨胀二连**（tool-calling/system-design，低置信）：harness-discovery-01 / ai-eng-006 同考"工具过多→Schema 膨胀→动态/分层加载"，留 1 改 1。
  - 其余 KEEP：moe/mechanism 稀疏激活多粒度（Mixtral 实现/概念/综合）健康；safety-alignment/tradeoff 全健康（长对话上下文/二分类权衡/审计脱敏/Agent安全/合规分级/红队各不同）。

## 内容 pass 续：batch7（再 6 格，累计 44 格 / 86）
- batch7（6 格 39 题）：context-engineering/tradeoff、evaluation/debugging、memory/tradeoff、system-design/mechanism、attention/mechanism、attention/tradeoff。
  - **REMOVE 级（本轮首个清晰同构）**：attention/mechanism 的 `crossattn-qkv-flow` ≈ `cross-attn-qkv-mech`（同考"标准 Encoder-Decoder Cross-Attention 的 Q/K/V 来源：Q 来自 Decoder 前层、K/V 来自 Encoder 顶层"，题干与正确项几乎完全同构）→ 删 `cross-attn-qkv-mech`，留 `crossattn-qkv-flow`。
  - REWRITE 候选新增/跨格深化：
    - **工具膨胀三连（跨格）**：context-engineering/tradeoff 的 `context-2026-104` 加入 batch6 的 `ai-eng-006`/`harness-discovery-01` 二连 → 三题同考"工具过多→Schema 膨胀→按需加载"，留 1 改 2。
    - **前缀缓存跨格（现 3 格）**：memory/tradeoff 的 `agentic-caching-02` 加入 batch5 的 `ai-eng-027`/`cache-prefix-01` 二连 + batch1 的 `context-2026-105`/`prompt-cache-context-ordering-01` → "静态前置动态尾部"原则被反复考 5+ 次，建议跨格归并保留 1~2 题。
    - **√d_k 缩放跨格（现 2 格）**：attention/mechanism 的 `ai-fund-003` 加入 batch3 的 self-attention/mechanism 三连 → 4 题同考缩放因子数值稳定，跨格归并。
    - **残差二连（attention/mechanism，低置信）**：`sebastian-raschka-2026-08-111` / `142` 同考"残差/注意力残差混合"，留 1 改 1。
    - **MHA 三连（attention/tradeoff）**：`mha-subspace-tradeoff` / `mha-vram-tradeoff` / `mha-heads-01` 同考"MHA 多头优势+复杂度+头数权衡"，留 `mha-heads-01` 改 2。
  - 其余 KEEP：evaluation/debugging 全健康（轨迹训练数据/上线副作用/代码对抗审查/行为监控/评估不稳定/Harness过拟合/Grader机制各不同）；system-design/mechanism 全健康（Redis COW/WebSocket/Code Mode/serving规划/世界模型/意图文档/NAPT）；context-engineering/tradeoff 的 compaction 三题（long-running-001/context-2026-106/agentdual）角度不同保留。

## 内容 pass 续：batch8（再 6 格，累计 50 格 / 86）
- batch8（6 格 31 题）：rlhf/comparison、attention/comparison、agent-fundamentals/scenario、agent-loop/scenario、agent-guardrails/mechanism、agent-fundamentals/comparison。
  - **REMOVE 级 2 道（清晰同构，已删）**：
    - rlhf/comparison：`sebastian-raschka-2026-08-53` ≈ `sebastian-raschka-2026-08-95`（同考"最终答案奖励 vs 过程监督：不需中间标签/信号更稀疏"，题干与正确项近同构）→ 删 53，留 95。
    - attention/comparison：`cross-attn-qkv` ≈ `crossattn-qkv-flow`（同考"标准 Encoder-Decoder Cross-Attention 的 Q/K/V 来源：Q 来自 Decoder 前层、K/V 来自 Encoder 顶层"）→ 删 cross-attn-qkv，留 crossattn-qkv-flow。
  - 其余 KEEP：agent-fundamentals/scenario、agent-loop/scenario、agent-guardrails/mechanism、agent-fundamentals/comparison 全健康；attention/comparison 除 cross-attn-qkv 外（147 稀疏vs压缩注意力/129 pre-post norm/126 SwiGLU/122 GQA）各不同。

## 内容 pass 续：batch9（再 6 格，累计 56 格 / 86）
- batch9（6 格 30 题）：model-selection/scenario、multi-agent/scenario、data-leakage/scenario、tool-security/system-design、reliability/mechanism、reliability/system-design。
  - REMOVE 级：无。
  - REWRITE 候选新增（低置信跨格）：tool-security/system-design 的 `sebastian-raschka-2026-08-105` 与 batch3 agent-guardrails/scenario 的 `ai-tool-004` 同考"工具权限最小原则+高危确认门"，留 1 改 1。
  - 其余 KEEP：model-selection/scenario（模型路由角度不同）、multi-agent/scenario、data-leakage/scenario、reliability/mechanism（git 三连 stash/reset/reflog 不同命令机制保留）、reliability/system-design（降级阶梯/幂等支付/checkpoint/confidence gate/Model DoS 各不同）全健康。

## 内容 pass 续：batch10（再 6 格，累计 62 格 / 86）
- batch10（6 格 30 题）：inference-optimization/scenario、multimodal/mechanism、distillation/tradeoff、inference-optimization/calculation、quantization/mechanism、vector-db/tradeoff。
  - REMOVE 级：无。
  - REWRITE 候选新增（中置信）：inference-optimization/calculation 的 sebastian 长上下文推理成本测量三连 `sebastian-raschka-2026-08-162/150/133`（同考"长上下文/推理成本工程测量指标"选3，留 1~2 改 1~2）。
  - 其余 KEEP：specdec-03/04 投机解码正反情形（不同认知任务）、vlm-02/03 NEO-unify 对齐vs融合时机（不同维度）、distillation 五题各不同（ROI/分布外/多教师架构/迁移/上限）、quantization/qah 系列（失效原因/做法对比/流水线顺序不同）、vector-db 多向量延迟/体积/选型/混合融合各不同。

## 内容 pass 续：batch11（再 6 格，累计 68 格 / 86）
- batch11（6 格 51 题）：evaluation/system-design(25)、multi-agent/system-design(5)、memory/mechanism(5)、human-in-the-loop/system-design(5)、self-attention/debugging(6)、self-attention/calculation(5)。
  - **REMOVE 级：无**（近同构的归 REWRITE 候选，严守"不擅自动笔"）。
  - **REWRITE 候选（本轮最密，待确认）**：
    - evaluation/system-design 三/四连：①消融实验设计三连 `sebastian-136/145/158`（公平消融/单变量/质量+系统指标）留 1~2 改 1~2；②检测器评测数据覆盖三连 `sebastian-76/82/87`（检测器训练/测试数据覆盖不偏）留 1~2 改 1~2；③评测集/回归构建三连 `ai-fund-042 / ai-eval-007 / agent-eval-04`（评测集分层+badcase回归+版本化；`ai-eng-018` 为 RAG 具体化保留）留 1~2 改 1~2。
    - self-attention/debugging 二连（高置信）：④因果掩码移除 `mask-remove-debug`≈`sebastian-raschka-2026-08-35`（泄漏loss虚低→推理崩塌）；⑤漏√d_k缩放 `sdpa-omit-scaling`≈`scale-factor-debug`（饱和/one-hot/溢出）。各留 1 改 1。
    - self-attention/calculation 四连：⑥复杂度O(N²) `attn-scaling-memory/long-context-attn-01/mha-complexity-01/attn-complexity-core` 同考 O(N²·d)+O(N²)显存（与 batch6 `transformer|comparison` 五连跨格同认知任务），留 1~2 改 2~3 去考 FlashAttention降显存/线性注意力破O(N²)/KV Cache长序列显存增长等切面；`sebastian-37`（头维度算术）KEEP。
    - 跨格深化：⑦`hitl-2026-006`（HITL审批作用域受限可撤销）加入 batch9 的 `ai-tool-004`/`sebastian-105` 工具权限最小原则二连 → 三题同考"最小权限+作用域授权+高危确认"，跨 human-in-the-loop / tool-security / agent-guardrails。
  - 其余 KEEP：multi-agent/system-design（角色收敛/Orchestrator权衡/并发Git锁/通信拓扑实验/沙箱隔离各不同）、memory/mechanism（技能库vs知识库回滚/分层流转/无状态本质/认知记忆类型/对话历史≠记忆各不同）、human-in-the-loop/system-design（暂停恢复/反馈转训练/审批界面/风险分级各不同，除⑦）、evaluation/system-design 其余（oreilly护栏/agentic-10可靠性/agentic-52 Runtime/aws-waf换模型/agentic-70图度量/eval-grader-01等）全健康。

## 内容 pass 续：batch12（再 6 格，累计 74 格 / 86）
- batch12（6 格 24 题）：flash-attention/mechanism、hybrid-attention/tradeoff、kv-cache/calculation、positional-encoding/mechanism、rag/mechanism、rlhf/tradeoff。
  - REMOVE 级：无。
  - REWRITE 候选新增（待确认）：
    - flash-attention/mechanism：`flashattn-02`≈`flashattn-04`（四手段列举 vs 为何相互依赖，同覆盖融合/分块/重计算/online softmax），留 1 改 1。
    - hybrid-attention/tradeoff：`llm-hybrid-02`≈`hybrid-mamba-01`（同考 Mamba/SSM 混合将 Attention 降至线性/常数、消除 KV Cache 长上下文成本），留 1 改 1。
    - kv-cache/calculation：`gqa-kv-cache-calc`≈`llm-13`（同考"给定参数算 KV Cache 显存"数值计算，仅 30B 模型参数不同），留 1 改 1 去考不同切面（如 GQA/MQA 节省比、并发总缓存、精度对缓存影响）。
    - positional-encoding/mechanism（低置信可选）：`llm-28`≈`llm-17`（同考 NoPE 如何维持位置感知，机制推导 vs 长度泛化验证角度略不同），留 1 改 1。
  - 其余 KEEP：rag/mechanism（管线/相似度≠相关性/chunk/降幻觉各不同）、rlhf/tradeoff（环境增强路线/KL阶段调优/奖励过优化/可验证vs偏好各不同）、flash-attention 的 ai-flashattn-001(总览)/flashattn-03(online softmax专项)、hybrid-attention 的 sebastian-127(通用权衡)/llm-14(SWA具体收益)、kv-cache 的 sebastian-123(概念因素)/sebastian-03(并发×长度翻倍四倍)、positional-encoding 的 pe-removal-defect(移除缺陷)/pos-encoding-sin-01(正弦性质) 均健康。

## 内容 pass 续：batch13（再 6 格，累计 80 格 / 86）
- batch13（6 格 24 题）：evaluation/calculation、safety-alignment/scenario、model-selection/tradeoff、rag-pipeline/scenario、caching/scenario、context-engineering/mechanism。
  - REMOVE 级：无。
  - REWRITE 候选新增（待确认）：
    - model-selection/tradeoff（中置信）：`ai-fund-047` / `agent-routing-01` / `agentic-36` 同考"模型路由/级联/按难度分配（简单小模型、困难升级大模型）"，留 1~2 改 1~2 去考不同切面（路由错误代价/Router漂移/级联误差累积）；`sebastian-100` 为选模型因素基础题保留。
    - caching/scenario（低置信可选）：`ai-ops-002`≈`ai-eng-064` 同考语义缓存风险（答案错误命中 vs RBAC越权），留 1 改 1。
    - 跨格深化：safety-alignment/scenario 的 `agent-safety-02`（最小权限/二次确认/可逆）加入 batch9+batch11 的「工具权限最小原则 / HITL审批作用域」跨格集群 → 现四题同考（ai-tool-004 / sebastian-raschka-2026-08-105 / hitl-2026-006 / agent-safety-02）。
  - 其余 KEEP：evaluation/calculation（阈值校准/准入逻辑/评测成本/pass@k 各不同）、safety-alignment/scenario（合规架构/公众防护/策略变更治理各异，除 agent-safety-02 已并入跨格）、rag-pipeline/scenario（chunk策略/数据管线/引用溯源/超长分层切分各异）、context-engineering/mechanism（压缩机制/顺序layout/System-User分工/长context不塞chunk各异）、caching/scenario 其余健康。

## 内容 pass 续：batch14（再 6 格，累计 86 格 / 86，全库审完！）
- batch14（6 格 25 题）：hallucination/mechanism、inference-optimization/comparison、tokenization/mechanism、training-data/tradeoff、observability/debugging、pytorch-performance/mechanism。
  - **REMOVE 级：无**（全库 86 格审完，累计 REMOVE 仅早前 6 道清晰同构近重复，本轮 batch3-14 无新增删除）。
  - REWRITE 候选新增（待确认）：
    - inference-optimization/comparison（高置信）：`sebastian-raschka-2026-08-91`≈`sebastian-raschka-2026-08-49` 同考"训练扩展 vs 推理扩展"概念（训练投训练预算/推理投请求/推理不更新参数/训练成本摊销），留 1 改 1 去考 test-time compute 具体形式（best-of-N/tree search/延长思考）或 training-vs-inference scaling 帕累托前沿。
    - pytorch-performance/mechanism（中置信）：`pytorch-perf-mlp-fusion-01`≈`pytorch-perf-gemm-epilogue-01` 同考"算子融合/Epilogue 消除中间张量 HBM 读写"，留 1 改 1 去考 FlashAttention 融合/reduce-softmax 融合/autograd 重算权衡等切面。
    - observability/debugging（低置信可选）：`sebastian-raschka-2026-08-108`≈`oreilly-radar-2026-08-10` 同考"Agent 失败定位需采集的观测信息（trace/工具调用/副作用）"，留 1 改 1。
  - 其余 KEEP：hallucination/mechanism（RAG缓解/局限/机制/分类各异）、tokenization/mechanism（流程/作用/成本效率/BPE思想各异）、training-data/tradeoff（配比质量/单域验证/难度分布/分布偏移各异）、inference-optimization/comparison 的 rl-test-time-compute-01(推理扩展调优)/inference-apple-silicon(硬件对比)、observability/debugging 其余、pytorch-performance 的 tensor-view/hw-vliw-05/hw-ai-resistant-03 各异（后两者偏格但认知独立）。
  - **全库 pass 收尾结论**：86 overloaded 格字面层近重复≈0，内容层多为健康深覆盖；真正"同认知任务换措辞"的中重复集中在 sebastian 系列 + 高频 topic。REWRITE 候选累计约 35+ 组，需在「跨格归并」阶段一次性决策留谁改谁（见下方跨格集群汇总）。

## 全库 REWRITE 候选跨格集群汇总（待用户拍板后再动笔）
- **①推理预算/测试时计算**：inference-optimization/tradeoff(96/58/21) + /system-design(23/93/51/63) + /comparison(sebastian-91/49 概念) + /calculation(sebastian-162/150/133 长上下文测量) + rl-test-time-compute-01（跨 5 格密集）。
- **②前缀缓存静态前置**：memory/tradeoff(agentic-caching-02) + caching/mechanism(ai-eng-027/cache-prefix-01) + batch1(context-2026-105/prompt-cache-context-ordering-01)（5+ 次反复考）。
- **③√d_k 缩放**：self-attention/mechanism(sdpa-scaling-mech/softmax-scaling-01/sebastian-32) + attention/mechanism(ai-fund-003)（跨 2 格，4 题）。
- **④MHA/注意力权衡**：attention/tradeoff(mha-subspace/mha-vram/mha-heads-01) + self-attention/calculation(attn-scaling-memory/long-context-attn-01/mha-complexity-01/attn-complexity-core) + transformer/comparison(五连)（跨 3 格，复杂度 O(N²) 同认知任务）。
- **⑤Transformer vs RNN/CNN 复杂度**：transformer/comparison 五连（最严重，留 2 改 3）。
- **⑥工具权限最小原则/HITL**：tool-security/system-design(sebastian-105) + agent-guardrails/scenario(ai-tool-004) + human-in-the-loop/system-design(hitl-2026-006) + safety-alignment/scenario(agent-safety-02)（跨 4 格，4 题）。
- **⑦可验证奖励**：rlhf/mechanism(sebastian-92/52) + rlvr-pref-01/staged-rl-kl-control(相关)。
- **⑧MoE vs Dense**：moe/tradeoff(moe-design-tradeoff/moe-mixtral-05/moe-vs-dense-compute)。
- **⑨工具膨胀**：tool-calling/system-design(harness-discovery-01/ai-eng-006) + context-engineering/tradeoff(context-2026-104)（跨 2 格）。
- **⑩研究 Agent 设计**：system-design/system-design(ai-eng-024/agentic-07/agentic-17) + multi-agent/system-design(agentic-23 四角色)。
- **⑪因果掩码移除**：self-attention/debugging(mask-remove-debug/sebastian-35)。
- **⑫漏√d_k缩放后果**：self-attention/debugging(sdpa-omit-scaling/scale-factor-debug)。
- **⑬消融实验设计**：evaluation/system-design(sebastian-136/145/158)。
- **⑭检测器评测数据覆盖**：evaluation/system-design(sebastian-76/82/87)。
- **⑮评测集/回归构建**：evaluation/system-design(ai-fund-042/ai-eval-007/agent-eval-04)。
- **⑯FlashAttention 四手段**：flash-attention/mechanism(flashattn-02/flashattn-04)。
- **⑰Mamba/SSM 混合**：hybrid-attention/tradeoff(llm-hybrid-02/hybrid-mamba-01)。
- **⑱KV Cache 显存计算**：kv-cache/calculation(gqa-kv-cache-calc/llm-13)。
- **⑲模型路由/级联**：model-selection/tradeoff(ai-fund-047/agent-routing-01/agentic-36)。
- **⑳语义缓存风险**：caching/scenario(ai-ops-002/ai-eng-064)。
- **㉑训练扩展vs推理扩展**：inference-optimization/comparison(sebastian-91/49)（已归入①）。
- **㉒kernel fusion 消除HBM**：pytorch-performance/mechanism(pytorch-perf-mlp-fusion-01/pytorch-perf-gemm-epilogue-01)。
- **㉓蒸馏轨迹/教师**：distillation/mechanism(sebastian-57/19) + distillation/tradeoff(全5题各角度) + rl-opd-memory-01(多教师)。
- **㉔动态推理预算**（见①）等。
- 注：跨格集群需一次性决策"留谁改谁"；同一概念跨多格反复考（如①推理预算、④O(N²)复杂度、⑥最小权限）属 overloaded 格内检测漏掉的跨格同认知任务重复，是 P0 收尾真正要治理的对象。

## Variant 轻量变体改造（按用户给的方案，2026-09-01 晚）
- **用户 rollback 了此前内容 pass 的 37 道 REMOVE 删除**（工作树已 reset 回 1308 题；那些删除作废，不再触碰题库 JSON）。
- 转向用户粘贴的「轻量变体」方案，严格落地：LLM 只改写题干+选项表达，**answer/explanation 永来自 canonical**；一次 LLM 调用，校验失败或长度泄题直接抛错由 `finalizeQuestion` 回退原题（不再两轮 retry、不再让 LLM 重决定答案）。
- 改动文件：
  - `src/ai/variant.ts`：`VARIANT_SYSTEM` 重写为 v3 轻量改写（~40 行稳定前缀）；`buildUser` 只注入 `topic/requiredConcepts/question/options`（choice 时），**不再暴露 answer/explanation/referenceAnswer/angle/difficulty**；`toGeneratedVariant` 丢弃模型可能回吐的 answer/explanation；`generateVariant` 单次调用 + validate，失败/泄题 throw。
  - `src/domain/variant.ts`：`validateVariant` 删除 answer 全部校验（索引/单选多选/越界），选项数量须与 canonical 一致（保证一一对应）；`applyVariant` 写死 `canonical.answer`。
  - 测试：`src/ai/variant.test.ts` 重写（单次调用、不泄露 answer、开放题仅 question、LLM 输出 answer 被丢弃、user 不含 answer/explanation）；`src/domain/variant.test.ts` 同步（answer 越界/单选多选断言删，改 options 数量不变 + answer 来自 canonical）。
  - 文档：`docs/DECISIONS.md` ADR-036 加「轻量变体收缩」决策；`docs/ARCHITECTURE.md` LLM 变体安全小节同步（图表/校验/Variant 范围/anti-cueing 改为硬失败不重试）。
- 验证：`npx vitest run` 全绿（649 passed / 48 files）；`npx tsc -b` 通过（EXIT=0）。改动未 commit。
- 关键约束：调用方 `finalizeQuestion` 已有 try/catch 回退原题，新方案天然对接；`detectOptionLengthBias(options, canonical.answer)` 用 canonical answer 判定泄题。

## Variant 第二轮：结构变换程序化（ADR-036 轻量变体收缩·第二轮）
- 用户拍板把「所有结构性/机械性变化」从 LLM 收归程序：LLM 只做语义变换（题干+选项文本），程序负责选项顺序/answer 索引重映射/格式/校验。
- 新增 `src/domain/options.ts`：`shuffleChoiceOptions`（Fisher–Yates 打乱 + 按 originalIndex 确定性重映射 canonical.answer，恒等排列时交换前两项兜底）、`normalizeAnswer`（多选题升序）、`normalizeOptionText`（空白折叠）、`ensureDifferentOrder`。
- `src/domain/variant.ts` `applyVariant`：落地时若有 LLM options 则 `shuffleChoiceOptions` 重排 + `normalizeAnswer`；`explanation` 永远取 canonical；新增可选 `rng` 参数（默认 Math.random，便于确定性测试/未来按 questionId+attemptCount 固定排列）。
- `src/types.ts`：`GeneratedVariant`/`VariantCandidate` 瘦身为 `{question, options?}`（删 answer/explanation）。
- `src/ai/variant.ts` `VARIANT_SYSTEM` v3 改为「逐项改写现有文本、不交换选项顺序、不生成答案/解析」。
- 测试：新增 `src/domain/options.test.ts`（9 例）；`variant.test.ts` 改为断言「重映射后正确文本仍正确（顺序无关）」；`domain/variant.test.ts` 同步删 explanation/answer 断言。
- 文档：ADR-036 加「结构变换程序化」决策；ARCHITECTURE.md 安全模型图与说明同步（LLM 语义 / 程序结构 分工）。
- 验证：`npx vitest run` 658 passed / 49 files；`npx tsc -b` EXIT=0。改动未 commit。

## Variant 第三轮：残留清理 + 语义证据面收紧（ADR-036）
- 重要：用户贴的「main 仍是旧 Variant 契约」是**远端 GitHub 旧 blob**——本地 HEAD == origin/main（`rev-list --left-right --count` = 0 0），前两轮改动早已 commit。核对方式：`git show HEAD:src/ai/variant.ts`。以后遇到"代码没改"质疑，先 diff 远端 ref 再动手，别直接按旧描述重写。
- 实际残留仅 2 项，本轮补掉：
  1. `src/ai/variant.ts` `RawVariant` 仍声明 `answer?/explanation?`（虽在 toGeneratedVariant 丢弃，但类型仍可被误用）→ 瘦身为 `{question?, options?}`。
  2. `src/domain/variant.ts` `requiredCoverageMet` 证据面「题干+选项」→ **只看题干**（删掉 `evidenceText`）。理由：轻量变体下选项只是逐项语义改写，核心术语必然带进选项（问 positional encoding 时选项里必现该词），当证据等于允许「题干漂移、靠选项兜底」。自此与 hasStemAnchor 证据面统一（前者任一 anchor 命中，后者 required 覆盖≈2/3，互补）。
- 测试新增 2 例锁定边界：`src/ai/variant.test.ts`「LLM 输出 explanation 被丢弃」；`src/domain/variant.test.ts`「必考概念只出现在选项、题干漂移 → 拒绝」。
- 文档：DECISIONS.md ADR-036 加「第三轮」决策条；ARCHITECTURE.md 语义闸门 ① 改为「只取题干，排除 explanation 与 options」；CHANGELOG.md 顶部新增条目。
- 验证：`tsc -b` EXIT=0；`vitest` 660 passed / 49 files。改动未 commit。

## Variant 第四轮：对照用户最新 spec 收口 + 最终验证（ADR-036）
- 用户贴「重新抓 main」后的最新 checklist（P0×3 + P1×4 + P2×1），要求严格落地。逐条核对本地工作树，**绝大多数已在前三轮落地，本轮主要做最终核对 + 全量验证**：
  - **P0-1**：`validateVariant` choice 的 `options` 已强制必填（`!Array.isArray(v.options)` → reject；数量须等于 canonical）+ `applyVariant` 简化成无条件 `shuffleChoiceOptions` 重排重映射（无 `if (v.options)` 分支）。✅
  - **P0-2**：`src/types.ts` 的 `VariantCandidate`/`GeneratedVariant` **已是新契约 `{question, options?}`**（无 answer/explanation）——commit cd24e2a 早已落地。用户 spec「types.ts 还是旧契约」是基于远端旧 blob 的误判，无需改动。✅（重复提醒：遇「代码没改」先 diff 远端 ref）
  - **P0-3**：`src/ai/variant.test.ts` 已收敛到新契约（9 例：正常返回 question+options、answer/explanation 被丢弃、缺 options→reject、数量不符→reject、一次失败→reject 不 retry、length bias→reject 不 retry、open 只 question），删掉了全部 retry/answer 解析/explanation 解析旧测试。✅
  - **P1（bias 限 choice）**：`src/ai/variant.ts` `generateVariant` 把 `detectOptionLengthBias` 包入 `if (isChoice)`。✅
  - **P1（删 ensureDifferentOrder）**：`src/domain/options.ts` 已删该函数及相关测试（Fisher–Yates 自带恒等兜底交换）。✅
  - **P1（放宽 required 2/3 字面覆盖）**：`requiredCoverageMet` 已在第三轮删除，语义闸门只留 `hasStemAnchor`（topic/tags/required 任一命中断题干）。✅
  - **P1（逐项 option 一一对应）**：`VARIANT_SYSTEM` v3 已有「第 N 个选项必须是输入第 N 个选项的改写 + 只允许改变表达，不许改变因果/适用条件/范围/真假属性」硬约束；程序侧只校验数量/位置一一对应/去重/长度 bias。✅
  - **P2（变体遥测）**：`src/ai/usageTelemetry.ts` 新增 `VariantRound{questionId, latencyMs, fallbackReason?}` + `recordVariantRound` + `getVariantTelemetry`（avg/p95 延迟 + fallback 率）；`src/application/sessionEvaluator.ts` `finalizeQuestion` 包计时 + 成功/失败均记一条（fallback 原因如 `validation-failed`/`option-length-bias`/`generation-error`）。✅
  - **P2（普通 10 题仍 10 次 LLM）**：`buildSession` 的 `Promise.all` 启动路径按用户建议**不动**（待实测拿到真实延迟后再决定）。✅未动
- 测试 mock 同步：所有直接调用方测试（`interviewEngine.test.ts`/`tools.test.ts`/`sessionEvaluator.test.ts`）的 `generateVariant` mock 现返回 `{question, options}`，evaluate 断言从题面推导 answer 索引。
- 本轮（09-01 23:4x）前被 429 打断的**最终验证已补跑**：`npx tsc -b --pretty false` EXIT=0；`npx vitest run` **663 passed / 49 files**（全绿，无 429 因 suite 全 mock 不触真实 API）。
- 变动的 11 个文件均**未 commit**（Variant 四轮 + 此前题库改动累积，等用户发话单独 ADR 提交）。
- 待办：用户建议「先处理前三项再实测一次 10 题真实耗时」——实测需真实 LLM 调用，当前距 429 重置（约 09-02 04:24）仍有时；重置后可跑一次真实组卷计时，用 `getVariantTelemetry()` 看 avg/p95/fallback 再决定是否调 gate 松紧。

## Variant 基准脚本（scripts/variant-bench.ts）
- 「Please continue」后，本地无 API key（app 的 AIConfig 存浏览器 localStorage，沙箱无 .env），无法直接跑真实耗时。改为交付**可复用的基准脚本**：`scripts/variant-bench.ts`，用户在本机带 key 跑：
  - `VARIANT_API_KEY=sk-xxx VARIANT_MODEL=deepseek-chat npx vite-node scripts/variant-bench.ts --n 10 --parallel`
  - 参数：--n / --parallel(默认,模拟 buildSession Promise.all) / --sequential / --provider / --base-url。
- 用第四轮新遥测 `recordVariantRound`/`getVariantTelemetry` 打印每题延迟 + 墙钟 + avg/p95/fallback 率；直接证明「10 题仍=10 次请求，墙钟≈max 单请求」。
- **关键约束**：为不拖垮 Node 侧 `tsc -b`（tsconfig.node.json 不含 vite/client），脚本只依赖 `src/ai/pi` 的 `callLLM`/`extractJSON`（不拉 `domain/knowledge/nodes`→`knowledgeMap` 的 `import.meta.glob`），就地复刻轻量变体管线：照搬 `VARIANT_SYSTEM` v3 + 结构校验（缺 options/数量/空串/重复/forbidden 指代）；题干锚定近似为 topic+tags（跳过 knowledge 节点 required，纯 CPU 不影响延迟量级）。已验证 `tsc -b` EXIT=0、`vite-node` 无 key 优雅退出。
- 教训：在 `scripts/` 里 import `src/ai/provider` 或 `src/domain/variant` 会把 `import.meta.glob` 带进 node tsconfig 导致 `tsc -b` 报 `Property 'glob' does not exist on type 'ImportMeta'`——其他脚本（validate-questions 等）只 import 类型与 JSON，不踩此坑。
