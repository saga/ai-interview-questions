# 2026-09-01 · 题库试点清理（rag / mcp / multi-agent）

## 背景
用户基于 `main` HEAD 重新 review，给出题库侧 6 点改进意见（P1 选项粒度一致 / P2 def+fund 软帽 25–30% / P3 难度=认知复杂度 / P4 知识价值治理 / P5 同概念密度 / P6 质量记分卡）。
用户选「试点清理」：挑 rag/mcp/multi-agent 三节点做真实内容编辑，产出可 review 补丁。

## 方法
- 一次性分析脚本放 `/tmp`（不进仓库）：筛 topic∈{rag,mcp,multi-agent} 共 69 题，按节点聚合 angle/difficulty 分布、def+fund 占比、难度↔认知映射不符、同概念密度。
- 逐题人工通读全部 69 题（dump 到 /tmp/pilot_dump.txt）找真实问题，而非依赖噪声探测器。

## 关键发现（诚实负面结果）
- **P1 粒度**：用户描述的「累加型坏题（X / X+Y / X+Y+Z，C 因最全而正确）」在三个节点中**基本不存在**。探测器初筛 4 个候选全是误报——正确选项是「合法的组合答案」（如「本机用 stdio、远程用 Streamable HTTP 并配鉴权」），干扰项是明显错误项，不是子集项。无需改。
- **P2 def+fund 占比**：mcp 20% / rag 16% / multi-agent 0%，全部远低于 30% 软帽。无需压。
- **P5 密度**：multi-agent 34 题散在 8 个文件、rag 25 题散在 4 文件、mcp 10 题散在 3 文件，无单概念过密。无需控。
- 结论：三个节点已是题库「示范节点」，主要可改进项是 P3 难度↔认知不一致。

## 实际改动（P3，6 处难度重标，跨 3 文件）
认知映射：definition/fundamental→easy、mechanism/comparison/calculation→medium、tradeoff/scenario/debugging/system-design/design→hard。仅改「方向明显错」的：
- rag.json `llm-04`（definition, hard→easy，纯定义被误标 hard）
- rag.json `ai-fund-036`（definition, medium→easy）
- rag.json `ai-fund-034`（mechanism, easy→medium，机制题被误标 easy）
- mcp.json `agentic-11`（definition, medium→easy）
- mcp.json `ai-tool-005`（definition, medium→easy）
- foundational-and-intermediate.json `foundation-mcp-discovery-01`（mechanism, easy→medium）

## 校验
- `npm run validate:questions` → 通过（1317 题 / 123 节点，无错）
- `src/data/bank.test.ts` → 10 passed
- 6 处改动经脚本回读确认全部落地、JSON 可解析。

## 待办 / 备注
- 改动仍 **uncommitted**，等用户决定是否提交（建议单一个「题库试点清理」commit）。
- P6 质量记分卡（6 维 keep/revise/reject）本次未建——用户选的是试点清理而非立标准；记分卡可作为下一阶段单独任务。
- P4 知识价值治理：知识节点已有 `priority: P0/P1/P2`（src/schemas/knowledge.ts:11），可后续用于 coverage 加权，本次未动。
