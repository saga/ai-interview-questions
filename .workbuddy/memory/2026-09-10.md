# 2026-09-10 工作日志

## 全库质量体检（只读审计，未改题目/规则）

基线：1409 题 / 148 节点 / 53 个 variant 池文件（1261 变体，覆盖 647 题 45.9%）。
门禁：validate:questions ✓（100% angle）· vitest 890/890（61 files）· validate-variants EXIT 0（0 stale/0 dup/0 sanity）· tsc ✓。

**结构面无错误（已独立复算）**
- 重复 id 0；题干规范化后完全重复 0 组；语义近重复最高 Dice 仅 0.711（跨 topic 短题干假阳性），**无实质孪生题**。
- open 与 choice 答案字母**不一致 0 条**（放宽 `正确答案[：:\s]*` 逐题复算 1409 道）。
- 解析与答案冲突嫌疑 2 条（`skillevo-skill-module-spec-01`、`flash-attn-io-mechanism`）经人工复核均为
  **否定式题干（"下列说法错误的是"）误报**，非真错。全库否定式题干仅 5 道。

**发现的真问题（按严重度）**
1. **P0 工具漂移**：`analysis/question_audit.py` 的 `VALID_ANGLES` 仍只有 11 个值（缺 ADR-077 后新增的
   causal/diagnosis/prediction/architecture/boundary/misconception/quantitative/implementation/synthesis），
   → 报 **16 条假 P0「invalid-angle」**（dit/moe/clip/multimodal/llm-pretraining/multi-agent/knowledge/wemm 全中），
   且把缺口数从真实 **20 虚报为 26**（未识别的 angle 不计入 covered_cells）。
   即：`npm run question:audit` 的 P0 与 gap 数字当前**不可信**，会把所有新入库批次显示成坏的。
2. **P1 门禁盲区**：`src/data/bank.test.ts:77` 正则 `正确答案\s*([A-Z]…)` 不匹配全角冒号，
   全库 **645/1409（45.8%）** 写作 `正确答案：A、B`，命中 `if (answerMatch)` 才校验 → 这些题的
   choice/open 一致性**被静默跳过**。当前数据恰好一致（0 冲突），但唯一自动护栏对近半库失明。
   运行时不解析该标记（open 走 LLM 按 referenceAnswer 评分），故属**测试盲区而非判分 bug**。
   另有 477 道 referenceAnswer 无任何 `正确答案` 标记（rubric 写法，CHANGELOG 记录过这是刻意迁移方向）。
3. **P1 长度泄题**：140 道选项长度比 >1.8×（AGENTS.md 要求的硬阈值），其中 **49 道 strong**
   （正确项全局最长 + 明显过短干扰项）。均为历史存量，新批次（transformer/wemm/multi-agent/knowledge）0 命中。
4. **P1 单选占比 42.0%**（592/1409），高于 §4.2 目标 ≤33.3%；全库门禁按设计默认关闭，等存量清理后再开。
5. **P1 覆盖缺口 20**（402 期望格，全部 P1 级）：LLM 核心 7 / LLM 应用 8 / Agent 工程 5。
   其中 5 格来自我建的 multi-agent-* 4 节点——**节点 `angles` 声明了 4 个角度但只写了 2 道题**，
   期望格由节点 angles 推导 ⇒ 自造缺口。要么补题要么收窄 angles。
6. **P1 治理缺口**：无 `source.materialId` 1037/1409（73.6%）→ 无法溯源/复核；
   `concepts` 缺失 1311/1409（93%）→ assessment 去重能力实际不可用；
   `assessment` 缺失 3（全在 `dit-2026-09.json`）。
7. **P1 难度倾斜**：hard 717（50.9%）/ medium 605（42.9%）/**easy 仅 87（6.2%）**——入门梯度垫太薄。
8. **P2 过载格 51 个**（≥4 题）：evaluation×scenario 27、evaluation×system-design 26、
   context-engineering×system-design 12、agent-loop×mechanism 12 等。
9. **P2 内容缺陷（我自己的题）**：`audit-question-quality` ④探测器命中 3 道多选"正确项互为复述"，
   其中 2 道是我出的：`scaled-dot-product-scaling-calculation-canonical`（A"方差≈64"与 B"标准差≈8"
   数学等价，A⇒B，等于同一判断点考两次）、`multi-head-attention-subspace-calculation-canonical`（相似度 78）。
   答案没错，但多选失去区分度、部分给分失真。

**未改任何文件**（skill 约定：默认只读，修复需用户明确要求）。已向用户报 P0/P1/P2 清单并待其指定修复范围。

---

## 修复轮（用户批准）：P0 + P1-正则 + 2 道等价正确项

**① `analysis/question_audit.py`（P0 工具漂移，已修）**
- 原来硬编码 11 个 VALID_ANGLES → 改为 `_load_angles_from_schema()`：正则解析
  `src/schemas/common.ts` 的 `questionAngleSchema`（先 `re.sub(r'//[^\n]*','')` 去行注释，
  再抓 `z.enum([...])` 里的 `'xxx'`），解析失败回退到 19 值硬编码副本。
  **以后 schema 加 angle 会自动同步，不会再漂移。**
- 效果：P0 16 → **0**；覆盖 376/402（缺口虚报 26）→ **382/402（缺口 20）**，与 TS
  `question-coverage.ts` 完全一致。

**② `src/data/bank.test.ts`（P1 护栏盲区，已修）**
- 正则 `/正确答案\s*([A-Z]…)/i` → `/正确答案[：:\s]*([A-Z](?:\s*[,、和]\s*[A-Z])*)/i`，
  提为模块级常量 `OPEN_ANSWER_RE` 复用。
- 新增回归用例：「全角冒号 / 半角冒号 / 空格 / A 和 C」四种写法都能解析（防再次静默跳过）。
- 效果：可校验题数 154 → **799**（占 open 形态 62.6%，+645）；全量 vitest 890 → **891/891**。
  477 道 rubric 式（无「正确答案」标记）按 CHANGELOG 的迁移方向保持跳过。

**③ 2 道正确项互为复述的计算题（已改写）**
- `scaled-dot-product-scaling-calculation-canonical`：原 A「方差≈64」与 B「标准差≈8」数学等价
  （相似度 85）。改为三个独立判断点：方差=d_k / **缩放因子 1/√d_k=1/8 而非 1/d_k=1/64** /
  缩放后标准差≈1 且与 d_k 无关；干扰项同步换成「方差≈8」与「应取 1/d_k 归一化更彻底」，
  新增 misconception「以为缩放越彻底越好，把 1/√d_k 误用成 1/d_k」。
- `multi-head-attention-subspace-calculation-canonical`：A×C 相似度 78。C 改为
  「缩放因子取 1/√d_k，依据的是每个头各自的维度」（去掉重复的 64/512 数字）；
  B 改为「头数越多每头越窄，h × d_k 的乘积始终等于 d_model」（与干扰项 E 形成正反对照）。
- **相似度实测**（fuzzball token_set_ratio，阈值 70）：Q1 85→**64**、Q2 78→**61**；
  长度比 1.35 / 1.46（安全）。④ 探测器命中 3 → **1**（仅剩存量 `transformer-comparison-01`=70 卡线）。

**⚠️ 关键连带坑：改 canonical 选项会让变体池 stale**
改完这 2 题后 `validate-variants` 立刻报 **stale 2**（`sourceHash` 覆盖 options）。
无 API key 时的手动重生成路径（已验证可行）：
1. 用**新** canonical 选项写只含这 2 题的草稿（沿用原变体题干）；
2. `assemble-variants.ts <草稿> _tmp-xxx.json <原slug>` → 临时池文件；
3. 脚本按 `kind` 就地替换回原池文件（**不要用 assemble 直接写原名，它会整文件覆盖、
   丢掉同池其余 10 条变体**）；
4. 删临时文件，跑 `validate-variants` 确认 0 stale。
（`question-variants.ts --stale` 是 LLM 路径，需要 VARIANT_API_KEY，无 key 时走不了。）

**修复后门禁**：validate 1409/148 ✓ · vitest **891/891** · tsc 0 · validate-variants **EXIT 0 池健康**
（0 stale/0 近重复/0 语言质量）· lint:bias+length 对 transformer-attention **0 命中** ·
python audit **P0=0 / P1=0 / P2=243**。

**仍未处理**：③ 覆盖缺口 20（其中 5 格是我建 multi-agent 节点时声明 angles 多于落题数造成的，
补题 or 收窄待定）；④ 49 道 strong 长度泄题（历史存量，需按文件分批）。
