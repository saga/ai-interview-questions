# 2026-08-30

## Cloudflare 部署命令坑（Pages vs Workers）
- 本项目是 Cloudflare **Pages** 项目：`wrangler.toml` 含 `pages_build_output_dir = "dist"`，无 `main` Worker 入口。
- 部署平台若把 Deploy command 配成 `npx wrangler deploy`（**Workers** 模式），会报 `Missing entry-point to Worker script or to assets directory`；Wrangler 也会警告 "run wrangler deploy on a Pages project, wrangler pages deploy should be used instead"。
- **正确命令**：`npx wrangler pages deploy dist`（仓库 `npm run deploy` 已是对的写法）。`pages_build_output_dir` 已设时，也可把 Deploy command 留空让 Pages 自动发布 `dist/`。
- 教训：`wrangler deploy` ≠ `wrangler pages deploy`。Pages 项目必须用 `pages deploy`。该命令是在 Cloudflare Pages 控制台 / 部署工具里配的，不在仓库文件中（仓库里 `package.json` 的 deploy 脚本已正确）。

## 剔除 Cloudflare 构建期的 Python 安装
- 触发：仓库根目录的 `pyproject.toml` + `uv.lock` 让 Cloudflare Pages 框架探测把本仓库识别成 Python 项目，于是构建期安装 Python（浏览器构建 `vite build` 根本用不到）。
- 修复：把 Python 工程清单 `pyproject.toml` + `uv.lock` 从根目录 `git mv` 到 `analysis/`。
- 后续（同次会话）：用户要求把相关 Python 脚本也迁到子目录 → `git mv scripts/question_analysis.py analysis/`、`git mv scripts/question_audit.py analysis/`、`git mv models analysis/models`（Git LFS 指针随 git mv 保留）。`scripts/` 下只剩 TS 脚本 + `pilot/`（.mjs，不动）。
- 路径修正：`analysis/question_analysis.py` 的 `LOCAL_MODEL_DIR` 由 `ROOT/"models"/...` 改为 `ROOT/"analysis"/"models"/...`（ROOT 仍为仓库根 = `parents[1]`）。
- 同步更新所有引用：`package.json`（`question:audit`/`question:analysis`）、README.md（路径×2、模型路径、发布范围段）、AGENTS.md、wrangler.toml 注释、docs/ARCHITECTURE.md（3 处）、skills/check-question-bank-quality/SKILL.md、skills/add-question-to-bank/SKILL.md。
- `docs/CHANGELOG.md` 第 63/68 行仍保留旧 `scripts/question_*.py` 写法——属历史变更记录，按约定不改动。
- 校验：`uv tree --project analysis` 解析成功（68 包）；两 .py 语法 OK；根目录已无 pyproject.toml/uv.lock/requirements.txt/models → Cloudflare 不再识别为 Python 项目。

## wrangler.toml 部署模式定案（2026-08-30 决策）
- 用户选 **方案 1：定为 Workers + Assets**，部署命令保持 `npx wrangler deploy`。
- 已删 `pages_build_output_dir = "dist"`，保留 `[assets] directory = "./dist"` + `[observability]`。冲突警告消除，`wrangler deploy --dry-run` 干净通过（读 dist/ 414 文件，退出码 0）。
- 顶部注释改为 "Cloudflare Workers + Assets 部署配置"，并写明部署命令为 `npx wrangler deploy`。
- ⚠️ 重要副作用：`wrangler deploy`（Workers 模式）会新建一个**并列的 Workers 项目**，不会更新 Cloudflare 控制台里**已存在的同名 Pages 项目**。已绑定到老 Pages 项目的自定义域名/预览需重新绑定到新 Workers 项目。若不想新建并列项目、想继续更新原 Pages 项目，应改用 `wrangler pages deploy dist`（方案 2）。
- 结论：仓库侧 Deploy command 应保持 `npx wrangler deploy`；若部署平台控制台仍配的是 `npx wrangler deploy`，现在已能跑通。
