# 2026-08-30

## Cloudflare 部署命令坑（Pages vs Workers）
- 本项目是 Cloudflare **Pages** 项目：`wrangler.toml` 含 `pages_build_output_dir = "dist"`，无 `main` Worker 入口。
- 部署平台若把 Deploy command 配成 `npx wrangler deploy`（**Workers** 模式），会报 `Missing entry-point to Worker script or to assets directory`；Wrangler 也会警告 "run wrangler deploy on a Pages project, wrangler pages deploy should be used instead"。
- **正确命令**：`npx wrangler pages deploy dist`（仓库 `npm run deploy` 已是对的写法）。`pages_build_output_dir` 已设时，也可把 Deploy command 留空让 Pages 自动发布 `dist/`。
- 教训：`wrangler deploy` ≠ `wrangler pages deploy`。Pages 项目必须用 `pages deploy`。该命令是在 Cloudflare Pages 控制台 / 部署工具里配的，不在仓库文件中（仓库里 `package.json` 的 deploy 脚本已正确）。

## 剔除 Cloudflare 构建期的 Python 安装
- 触发：仓库根目录的 `pyproject.toml` + `uv.lock` 让 Cloudflare Pages 框架探测把本仓库识别成 Python 项目，于是构建期安装 Python（浏览器构建 `vite build` 根本用不到）。
- 修复：把 Python 工程清单 `pyproject.toml` + `uv.lock` 从根目录 `git mv` 到 `analysis/`。
- 后续（同次会话）：用户要求把相关 Python 脚本也迁到子目录 → `git mv scripts/question_analysis.py analysis/`、`git mv scripts/question_audit.py analysis/`、`git mv models analysis/models`（Git LFS 指针随 git mv 保留）。`scripts/` 下只剩 TS 脚本 + `pilot/`（.mjs，不动）。
- 路径修正：`analysis/question_analysis.py` 的 `LOCAL_MODEL_DIR` 由 `ROOT/"models"/...` 改为 `ROOT/"analysis"/"models"/...`（ROOT 仍为仓库根 = `parents[1]`）。
- 同步更新所有引用：`package.json`（`question:audit`/`question:analysis`）、README.md（路径×2、模型路径、发布范围段）、AGENTS.md、wrangler.toml 注释、docs/ARCHITECTURE.md（3 处）、skills/check-question-bank-quality/SKILL.md、skills/add-question-to-bank/SKILL.md。
- `docs/CHANGELOG.md` 第 63/68 行仍保留旧 `scripts/question_*.py` 写法——属历史变更记录，按约定不改动。
- 校验：`uv tree --project analysis` 解析成功（68 包）；两 .py 语法 OK；根目录已无 pyproject.toml/uv.lock/requirements.txt/models → Cloudflare 不再识别为 Python 项目。

## wrangler.toml 部署模式定案（2026-08-30 决策）
- 用户选 **方案 1：定为 Workers + Assets**，部署命令保持 `npx wrangler deploy`。
- 已删 `pages_build_output_dir = "dist"`，保留 `[assets] directory = "./dist"` + `[observability]`。冲突警告消除，`wrangler deploy --dry-run` 干净通过（读 dist/ 414 文件，退出码 0）。
- 顶部注释改为 "Cloudflare Workers + Assets 部署配置"，并写明部署命令为 `npx wrangler deploy`。
- ⚠️ 重要副作用：`wrangler deploy`（Workers 模式）会新建一个**并列的 Workers 项目**，不会更新 Cloudflare 控制台里**已存在的同名 Pages 项目**。已绑定到老 Pages 项目的自定义域名/预览需重新绑定到新 Workers 项目。若不想新建并列项目、想继续更新原 Pages 项目，应改用 `wrangler pages deploy dist`（方案 2）。
- 结论：仓库侧 Deploy command 应保持 `npx wrangler deploy`；若部署平台控制台仍配的是 `npx wrangler deploy`，现在已能跑通。

## Chrome 内置 AI 状态条一直显示「下载中」修复
- 症状：模型已下载完，但设置页黄色 Alert 一直显示「本地模型正在下载中」。
- 根因：`SettingsPanel` 只在组件 mount 时查一次 `LanguageModel.availability()`，且 Chrome Prompt API 在模型刚下完时 often 仍返回 `downloading`（状态未及时刷新）。没有轮询，状态拿到后不再更新。
- 修复：`src/components/settings/SettingsPanel.tsx` 把一次性检测改成轮询——状态为 `downloading` 时每 4s 复查 `chromeAvailability()`，直到变为 `available`/`unavailable`/`downloadable` 才停；卸载时 clearInterval。typecheck 通过。
- 注意：改动在源码，线上 `dist/` 仍是旧构建；需重新 `npm run build` + `npx wrangler deploy` 才生效。

## Cloudflare Workers AI 报 "Provider is not configured" 修复
- 症状：配置好 cloudflare-workers-ai（apiKey + accountId + 模型 `@cf/moonshotai/kimi-k2.6`）后调用仍报 `⚠️ 模型调用失败：Provider is not configured: cloudflare-workers-ai`。报错文案来自 `CopilotSidebar.tsx:74`，根因在 pi-ai 鉴权解析。
- 根因：`src/ai/pi.ts` 的 `callLLM` 与 `CopilotSidebar.tsx` 都调用 `models.complete(model, ctx, entry.apiKey.trim() ? { apiKey: entry.apiKey } : {})`。一旦传 `apiKey` 作为 override，pi-ai 的 `resolveProviderAuthWithSignal`（`auth/resolve.js:33`）会走「合成 credential」分支，构造 `{ type:'api_key', key: overrides.apiKey, env: overrides.env }`，而 `overrides.env` 为 `undefined` → 直接丢弃了内存 CredentialStore（`createCredentialStore`）里存好的 `CLOUDFLARE_ACCOUNT_ID`，导致 `resolveCloudflareEnv` 拿不到 accountId 返回 undefined → applyAuth 抛错。
- 关键细节：Cloudflare 的 accountId 只存在于 CredentialStore 的 `env`，且 cloudflare 模型 baseUrl 模板 `.../accounts/{CLOUDFLARE_ACCOUNT_ID}/ai/v1` 依赖该 env 填充。其他 provider（deepseek/openrouter/google/local）无此 env 依赖，所以之前没暴露。
- 修复：两处都改为 `models.complete(model, ctx, {})`，鉴权完全交给 CredentialStore（已按 provider 注入 apiKey/accountId）。`runtime.ts` 的 Agent 主流程本就用 `streamSimple` 不传 override，所以面试主流程之前就能调通，受损的是变体生成/开放题评分/挑战题（provider.ts→callLLM）和 Copilot 侧栏。
- 验证：写临时脚本拦截 fetch（不发真实请求），确认修复后请求 URL 为 `.../accounts/<accountId>/ai/v1/chat/completions`，accountId 正确模板化；apiKey 经 openai-completions 客户端 SDK 自动加 Authorization 头（与 deepseek 等一致）。typecheck 通过，ai+agent 测试 89 个全过。

## SettingsPanel 隐藏无用的 Base URL 字段
- Cloudflare（及 deepseek/openrouter/google）都不读 `entry.baseUrl`，只有 `local` 引擎用。之前 UI 对所有非 chrome 引擎都渲染 Base URL 输入框，易误导。
- 改动：`src/components/settings/SettingsPanel.tsx` 第 473 行渲染条件由 `provider.id !== 'chrome'` 改为 `provider.id === 'local'`，且 placeholder 直接用 `DEFAULT_LOCAL_BASE_URL`（已 import）。typecheck 通过。

## 本机 Vite "Failed to fetch dynamically imported module" 调试
- 现象：编辑 pi.ts/CopilotSidebar 后，浏览器报 `Failed to fetch dynamically imported module: .../.vite/deps/openai-completions-*.js?v=...`。
- 根因：Vite 改文件后重新预打包依赖，浏览器仍持有旧的 `?v=` 哈希引用 → 404。
- 处理：停掉旧 vite（pid 65507/65524）、`rm -rf node_modules/.vite` 清缓存、后台 `npm run dev -- --host` 重启（新 pid 62410，监听 5173）。用户浏览器硬刷新即可。属 Vite 开发期缓存问题，非代码 bug。

## Cloudflare 浏览器直连报 "Connection error."（CORS）
- 现象：修好 "Provider is not configured" 后改报 `⚠️ 模型调用失败：Connection error.`。文案来自底层 OpenAI SDK 的 `APIConnectionError`（fetch 网络层失败）。
- 根因（已 curl 验证）：本项目是纯前端 SPA，无后端代理，AI 请求由浏览器直打 `api.cloudflare.com`。Cloudflare REST API **不返回 CORS 头**（OPTIONS 405 无 `access-control-allow-origin`；带 Origin 的 POST 401 也无 ACAO）→ 浏览器跨域拦截 fetch → "Connection error."。服务端 curl 不受 CORS 限制故能通（返回 401 鉴权错）。
- 决策：用户选 **AI Gateway** 方案。AI Gateway 地址（`https://gateway.ai.cloudflare.com/v1/<account_id>/<gateway_id>/workers-ai`）支持浏览器 CORS（控制台开启后）。
- 代码改动：
  1. `src/ai/pi.ts` 的 `getModel` 改为接收 `entry`，当 `entry.baseUrl` 非空时用它覆盖模型目录默认 baseUrl（去掉尾部斜杠）。
  2. 三个调用点（pi.ts `callLLM`、CopilotSidebar.tsx、agent/runtime.ts）改传 `getModel(models, entry)`。
  3. `SettingsPanel.tsx` 重新对 `cloudflare-workers-ai` 显示 Base URL 输入框（与 `local` 一起），placeholder 提示填 AI Gateway 地址（开启 CORS）。
- 鉴权：`cloudflareWorkersAIProvider` 用 `Authorization: Bearer <token>`，AI Gateway 接受该头（也支持 `cf-aig-authorization`）。accountId 仍需填写（auth.resolve 要求 apiKey+accountId 双字段，否则 "Provider is not configured"），但 gateway URL 已含 accountId 于路径、env 占位符不再使用。
- 用户侧操作：Cloudflare 控制台建 AI Gateway + 开启 CORS → 把 gateway URL 填进 Cloudflare 引擎的 Base URL → 保留 apiKey/accountId → 浏览器硬刷新。
- ⚠️ 安全：用户最早在对话里贴了真实 Cloudflare API Token，建议去控制台轮换/吊销。
- typecheck 通过，ai+agent+copilot 测试 89 个全过。

## AI Gateway 方案实测不可行（针对 Workers AI 模型）
- 用户粘贴了 `https://gateway.ai.cloudflare.com/v1/<account>/default/compat/chat/completions`。实测**不通**：
  - `gateway.ai.cloudflare.com` 支持 CORS（OPTIONS 对 openai provider 返回 `access-control-allow-origin` 等头），但其 provider 列表**只含第三方模型**（openai/anthropic/google/bedrock/azureopenai 等），**不含 `workersai`**，所以 `compat`/`workersai` 路径都返回 `400 Invalid provider`。
  - 真正的 Workers AI 端点 `api.cloudflare.com/client/v4/accounts/{account}/ai/v1/chat/completions`（即便带 `cf-aig-gateway-id` 头）**仍不返回 CORS 头**（405 无 ACAO）。
- 结论：**`@cf/*` 原生模型无法从浏览器直连，AI Gateway 也救不了（无 CORS 友好路径）**。用户放弃 AI Gateway 方案。

## 改用 Cloudflare 专用同源 Worker 代理（最终方案，已实施）
- 约束（用户明确要求）：**代理只服务于 cloudflare-workers-ai 这一个 provider，且该 provider 未启用时不应产生任何代理流量。**
- 实现：
  1. 新增 `worker/index.ts`：仅拦截 `/api/ai/*`，重写路径前缀后在服务端转发到 `https://api.cloudflare.com`（host 改、去掉 `/api/ai` 前缀、`host`/`connection` 头删除、非 GET 读 body 文本转发）；其它请求 `env.ASSETS.fetch(request)` 回退静态 SPA。绝不代理第三方。
  2. `wrangler.toml`：加 `main = "./worker/index.ts"`，`[assets]` 加 `not_found_handling = "single-page-application"`。
  3. `src/ai/pi.ts` 的 `getModel`：cloudflare 且未手动填 baseUrl 时，baseUrl 自动设为同源代理 `/api/ai/client/v4/accounts/{accountId}/ai/v1`（注意必须含 `/client/v4`，否则漏段 → 404）；SDK 会再追加 `/chat/completions`；Worker 重写后精确等于真实端点。其它 provider 不变。
  4. `vite.config.ts`：dev 也加 `/api/ai` proxy（target api.cloudflare.com, changeOrigin, rewrite 去 `/api/ai`），本地调试同生产行为。
  5. `SettingsPanel.tsx`：cloudflare Base URL placeholder 改为「留空则自动走同源代理…」。
- 范围保证：代理流量仅在 cloudflare 启用且经 `entry` 调用时产生（createLLMProvider 已按 enabled 过滤），未启用则无 `/api/ai` 请求。
- 验证：`npm run typecheck` 通过；ai+agent+copilot 测试 89 全过；`npx wrangler deploy --dry-run` 干净通过（main 被识别，414 文件）；node 模拟确认最终 URL == `https://api.cloudflare.com/client/v4/accounts/{account}/ai/v1/chat/completions`；`npm run build` 成功（修复了 pi.ts 误留的未用 `ProviderId` 导入导致 TS6196）。
- 上线步骤：`npm run build` + `npx wrangler deploy`。dev server 改了 vite.config 后会自动重启，浏览器硬刷新即可。
- ⚠️ 注意：worker/index.ts 不在 src/，不被 `tsc -b` 类型检查，由 wrangler(esbuild) 部署时打包；proxy 转发浏览器的 `Authorization: Bearer <token>`（token 来自 UI，同源 HTTPS 传输）。

## 回撤 AI Gateway 的无效修改（2026-08-30 末尾）
- 背景：AI Gateway 方案已实测不可行（见上），但当时为支持「用户粘贴 Gateway 地址」留下两处残留，现回撤（Worker 代理方案不受影响、保留）。
- 回撤内容：
  1. `src/components/settings/SettingsPanel.tsx`：Base URL 输入框恢复为**只给 `local` 显示**（之前为 AI Gateway 把 Cloudflare 也显示出来）。Cloudflare 走同源 Worker 代理自动路由，不再需要手动 Base URL 字段（呼应早期「没用就去掉它」）。
  2. `src/ai/pi.ts` 的 `getModel`：删除通用的「`entry.baseUrl` 手动覆盖」分支（那正是 AI Gateway 让粘贴地址生效的机制，会让用户填失效的 Gateway URL）。Cloudflare 现固定返回 `/api/ai/client/v4/accounts/{accountId}/ai/v1`，其它 provider 仍走各自目录默认 baseUrl。
- 顺带修了一个**与此无关、早先就存在**的测试断言不一致：`src/storage/settings.ts` 校验文案原为小写 `accountId`，测试期望含 `Account ID`（与设置页 UI 标签一致）。改为「Cloudflare 引擎必须填写 model、API Token 与 Account ID」。
- 验证：`npm run typecheck` 通过；ai+agent+copilot+storage 测试 **122 全过**；`npm run build` 成功。Worker 代理（worker/index.ts、wrangler.toml、vite.config.ts proxy）与 auto-proxy 逻辑均未动。

## 新增可选 Express server（nodejs server 为可选、非强依赖）
- 用户已把 `express@^5.2.1` 加入 dependencies，要求建一个 nodejs server，定位为**可选**组件：仅用于解决 CORS（或未来可能的服务端需求），大原则「nodejs server 不是强依赖，而是 optional」。
- 新增 `server/index.js`（ESM，因 package.json `"type":"module"`）：
  - 同源代理 `/api/ai/*` → `https://api.cloudflare.com`，逻辑与 `worker/index.ts` 保持一致（剥 `/api/ai` 前缀、原样转发 method/headers[含 Authorization]/body、回传 upstream 响应、502 兜底）；**仅代理 cloudflare-workers-ai，不代理任何第三方**。
  - serve 构建产物 `dist/`（含 SPA fallback 到 index.html）；dist 不存在时给出提示但不阻塞启动（代理仍可用）。
  - 端口 `PORT` 环境变量，默认 3000；顶部注释明确「可选、非强依赖」原则。
- `package.json` 加脚本 `"server": "node server/index.js"`。`deploy`/`deploy:cloudflare` 仍为 `wrangler pages deploy dist`（注：wrangler.toml 实际是 Workers+Assets 含 main，与 deploy 脚本存在不一致，待用户确认，本次未改）。
- 设计要点：前端始终调同源 `/api/ai`，故 Express 只是第三条可选通道（生产=Worker、dev=Vite proxy、本地预览=Express serve dist + 同源代理）。前端零改动。
- 验证：`PORT=3000 node server/index.js` 启动成功；POST `/api/ai/.../chat/completions` 真实账号+假 token 返回 `401 Authentication error`（证明路径正确、Authorization 已转发、响应回传正常，且无 CORS）；GET `/` 返回 index.html(200)。typecheck + build 通过，server 文件不在 src 故不参与 tsc/vite 构建，也不进 Cloudflare 部署。

## 合并代理实现：删除 Cloudflare Worker，统一用 Node server（本会话最新）
- 用户指令：「cloudflare worker 就使用新加的 nodejs server，不要再搞一套」。
- 动作：**删除 `worker/` 目录（`worker/index.ts`）**——不再维护 Cloudflare 专用的那套独立代理代码。
- `wrangler.toml`：移除 `main = "./worker/index.ts"`，退回**纯静态 Assets**（`[assets] directory = "./dist"` + `not_found_handling = "single-page-application"`）；更新顶部注释说明 Cloudflare 仅作静态托管、CORS 由可选 Node server 解决。
- `package.json`：`deploy` 脚本改为 `wrangler deploy`（Workers+Assets 纯静态推送）；删除重复的 `deploy:cloudflare`。
- `server/index.js`：更新头部注释与第 31 行注释，去掉对 `worker/index.ts` 的引用，明确它是「Cloudflare Workers AI 浏览器直连 CORS 问题的唯一服务端解法」，dev 用 vite proxy、生产用 Cloudflare 静态 + 可选 Node server。
- `src/ai/pi.ts` 的 `getModel` 注释：同源代理来源由「worker/index.ts」改为「可选的 Node.js server：server/index.js（dev 用 vite.config.ts proxy）」。
- 现在的部署/代理模型：
  - 生产 = Cloudflare **纯静态**托管 dist/（无 Worker）；如需 Cloudflare provider 调用，运行可选 Node server（`npm run server`）作同源后端，或把 Node server 部署到自有主机/放 Cloudflare 前作源站。
  - 本地 dev = Vite proxy（`vite.config.ts` 的 `/api/ai`）。
  - 本地预览 = `npm run build` + `npm run server`（Node serve dist + 同源代理）。
- 验证：typecheck 通过；无 `worker/index` 残留引用；`wrangler deploy --dry-run` 干净通过（读 414 静态文件、无 main）；`PORT=3000 node server/index.js` 启动正常，POST `/api/ai/.../chat/completions`（真实账号+假 token）返回 `401 Authentication error`（路径正确、Authorization 已转发、无 CORS），GET `/` 返回 index.html(200)。
- 结论：代理逻辑现在**只有一份**（`server/index.js` + `vite.config.ts` dev proxy 指向同一 `/api/ai` 路径约定），无重复维护。

## npm run dev 同时启动前端与 Node server
- 用户要求：`npm run dev` 一条命令同时拉起 Vite 前端（5173）与可选的 Node server（3000）。
- 改动：
  1. `package.json` 的 `dev` 脚本改为：`trap 'kill $VITE_PID $SERVER_PID 2>/dev/null' EXIT INT TERM; vite & VITE_PID=$!; node server/index.js & SERVER_PID=$!; wait`。不引入新依赖（未装 concurrently/npm-run-all，沿用 shell 内建）。
  2. `vite.config.ts` 的 `/api/ai` proxy target 由 `https://api.cloudflare.com` 改为 `http://localhost:3000`（去掉 rewrite），使 dev 里的请求也走本地 Node server，从而**代理只有一份实现**（dev 与生产可选 server 行为一致，不再直连 cloudflare）。
- 清理可靠性：曾用 `trap 'kill 0'` 但后台任务终止会遗留孤儿进程；改为显式记录两个子 PID 并在 EXIT/INT/TERM 时精确 kill，单测验证 SIGTERM 下两个子进程均被清理。真实终端 Ctrl+C 会把 SIGINT 发到整个前台进程组，trap 正常触发。
- 验证：`npm run dev` 后 5173/3000 均监听；POST `/api/ai/.../chat/completions` 经 Vite(5173)→Node(3000)→Cloudflare 返回 `401`（路径正确、无 CORS）；`GET /` 返回 200；精确 PID kill 后两端口释放无孤儿。

## 反转：重新加回 Cloudflare Worker 代理 + 修复 Invalid URL + 部署文档
（用户确认「vite proxy 在 CF 部署不生效、GH Pages 无 server 会出错」后，要求同时做 CF Worker 代理与部署文档。）
- **反转上一轮「删除 worker」决定**：重新创建 `worker/index.ts`（Cloudflare 生产同源代理），`wrangler.toml` 设 `main = "./worker/index.ts"`，注释同步更新为 Workers+Assets。
  - 原因：vite proxy 与可选 Node server（`server/index.js`）只存在于本地 dev / 自有 Node 主机；Cloudflare Workers 部署无法运行 Node server，所以生产要能用 cloudflare provider 必须有一份 Worker 代理。Worker 与 Node server 转发逻辑一致、但服务不同部署目标（不是重复维护）。
  - Worker 行为：`/api/ai/*` 剥前缀转发 `api.cloudflare.com`；其它路径 `env.ASSETS.fetch(request)` 提供静态 SPA（`not_found_handling = single-page-application`）。
- **修复本地 "Failed to construct 'URL': Invalid URL"**：根因是 pi-ai 底层 OpenAI SDK 对 `baseUrl` 执行 `new URL()`，而我们原来传的是相对路径 `/api/ai/...`，浏览器 `new URL(相对)` 抛错。
  - 修正 `src/ai/pi.ts` 的 `getModel`：**不再手写整条路径**，而是只把 pi-ai 内置 `baseUrl`（`https://api.cloudflare.com/client/v4/accounts/{CLOUDFLARE_ACCOUNT_ID}/ai/v1`）的源替换成 `${location.origin}/api/ai`。保留 pi-ai 的 `{CLOUDFLARE_ACCOUNT_ID}` 占位符与路径拼接（accountId 由 credential store 的 env 注入）。baseUrl 必须是绝对 URL。
- 新增 `DEPLOYMENT.md`：说明 CORS 同源代理需求、各部署方式对照表（本地 dev ✅ / CF Workers ✅ / 自有 Node ✅ / GitHub Pages ❌）、GitHub Pages 纯静态无法提供 /api/ai 代理故 cloudflare provider 不可用及外接代理方案、/api/ai 前缀约定。
- 验证：`tsc --noEmit` 通过；`wrangler deploy --dry-run` 干净通过（worker 编译、读 414 静态文件）；`vitest run src/ai src/storage` 99 passed。

## build 失败修复（tsc -b 报 Cannot find name 'location'）
- 现象：`npm run build`(= `tsc -b && vite build`)报 `src/ai/pi.ts: Cannot find name 'location'`。`tsc -p tsconfig.app.json --noEmit` 单独编译 src 却通过——说明是 build 模式（app+node 两个项目）才触发。
- 根因：`scripts/fix-bias.ts` 第 34 行 `import { callLLM, extractJSON } from '../src/ai/pi.ts'`，把浏览器模块 pi.ts 拉进了 **node 项目**（tsconfig.node.json，lib=ES2023、无 DOM）。pi.ts 里直接用 DOM 全局 `location` → node 项目编译它时找不到 `location`。
  - 附带坑：`tsc --noEmit`（根 tsconfig.json 是 `files:[]`+references）在非 -b 模式下忽略 references、等于啥都没查，会假「通过」；必须以 `tsc -b` 为准。
- 修法：pi.ts 不直接引用 DOM 全局，改为 `const g = globalThis as unknown as { location?: { origin: string } }; const origin = g.location?.origin ?? '';`。两种编译环境（DOM / 纯 ES2023）都通过，运行时浏览器仍拿到正确 origin。
- 结论/约定：**src 下的浏览器模块若被 scripts/ (node 构建) 引用，绝不能裸用 `location`/`window`/`document` 等 DOM 全局**，统一走 `globalThis` 结构性读取。

## Cloudflare 调用 403 / Failed to fetch 的调试定案（实测）
- 用户报 `⚠️ 模型调用失败：403 Failed to fetch`（配置 model=`@cf/moonshotai/kimi-k2.6`）。
- **实测定位（关键方法：先绕开浏览器、直接 curl 真实 API 带 token）**：
  - 直接 `curl https://api.cloudflare.com/client/v4/accounts/{account}/ai/v1/chat/completions` + `Authorization: Bearer {token}` → **403**：`AiError: Model @cf/moonshotai/kimi-k2.6 is not available on the Workers Free plan` → 是**套餐/模型限制，与代码无关**。
  - 同源代理链路（vite:5173 proxy → node server:3000 → Cloudflare）curl 同样返回 403 + 可读 JSON；换成可用模型则 **200**（实测返回正常回答）。**证明代理、vite proxy、node server 全部正常，无代码 bug**。
- **调试顺序铁律**：遇到 cloudflare 调用失败，**第一步就直接 curl 真实 API**（curl 不受 CORS 限制，能拿到真实 status+body），先区分是「Cloudflare 侧拒绝（401/403 套餐或权限、404 模型名错）」还是「本地代理/CORS 问题」。别一上来怀疑代理链路。
- **"Failed to fetch" 的含义**：这是浏览器 fetch **传输层**失败（CORS 拦截或代理目标不可达），不是 HTTP 层 403 的错误体。服务端所有路径实测都回传 403+JSON（非 Failed to fetch），所以浏览器侧出现 "Failed to fetch" 基本是**旧 bundle 仍在直连 api.cloudflare.com 被 CORS 拦**（需硬刷新清 Vite 缓存、确认 `npm run dev` 确实带起了 node server:3000）。换用可用模型后该现象也会消失。
- **Cloudflare Workers Free plan 实测可用模型**（同一 token/account，2026-08-30 验证）：
  - ✅ `@cf/meta/llama-3.3-70b-instruct-fp8-fast` → 200
  - ✅ `@cf/mistral/mistral-7b-instruct-v0.1` → 200
  - ❌ `@cf/moonshotai/kimi-k2.6` → 403（不在 Free plan）
  - ❌ `@cf/qwen/qwen3-0.6b-thinking`、`@cf/google/gemma-2-9b-it` → 400 No such model（名字错）
  - ❌ `@cf/meta/llama-3.1-8b-instruct` → 410 已废弃
  - 完整可用清单见 Cloudflare 模型目录：https://developers.cloudflare.com/workers-ai/models/
- 结论给用户：403 不是 bug，把配置的 model 换成 Free plan 支持的名称即可；kimi-k2.6 需升级 Cloudflare 付费套餐才可用。

## Agent 面试「面试官的推理与决策」面板布局修复
- 症状：工具调用记录（🔧 搜索题目 / getCoverageGaps）与面试官 agent 回复混在一起，只有简单 flex 行，视觉上像一段话被拆碎，图标和文字对齐杂乱。
- 修复 `src/components/agent/AgentInterviewPage.tsx` 的 transcript 渲染：
  - agent 回复：左侧蓝色机器人头像圆圈 + 右侧浅灰气泡块（圆角、边框、内边距），看起来像聊天消息。
  - 工具调用：改用 Ant Design `Tag`（带图标、成功/失败颜色、紧凑圆角），并左侧缩进对齐到 agent 头像中心，视觉层级清晰。
  - 行间距由 `10` 加大到 `14`。
- 验证：`npm run build` 通过（exit 0）。

## Agent 面试 transcript 去噪：合并连续同名工具调用
- 症状：用户反馈「搜索题目 搜索题目 搜索题目 搜索题目 选定题目 搜索题目 搜索题目」这种重复刷屏，是无效提示信息。根因：agent 每轮都先调 `searchQuestions`（开头常连发 4 次），而 `onEvent` 的 `tool_execution_end` 每次都把该工具记一条 transcript，于是堆出大量内容相同的标签。
- 修复 `src/components/agent/AgentInterviewPage.tsx`：新增 `collapseToolRuns()`，把**连续、同名、同状态（ok 也相等）、同 detail** 的 tool 条目合并成一条带 `count` 的记录；渲染时 `count>1` 显示 `搜索题目 ×N`，`count===1` 才显示 `· detail`。agent 文本行保持原样。开头 4 连发 → `搜索题目 ×4`，不再刷屏。
- 说明：这是纯 UI 去噪，不动 agent 行为（连发 search 仍发生在逻辑层，但显示被折叠）。若用户仍嫌「搜索题目」本身无用，可改为直接不把 searchQuestions 记入 transcript（仅保留 选定题目 / 评估作答 等决策信息）。
- 验证：`npm run build` 通过（exit 0）。

## Copilot 切 deepseek 报 `assistantMsg.content.flatMap is not a function`
- 症状：provider 改为 deepseek，打开 Copilot 发 "nihao" 即报错 `⚠️ 模型调用失败：assistantMsg.content.flatMap is not a function`。
- 根因（pi-ai 契约）：pi-ai 的 `transform-messages.js:71` 对 assistant 消息执行 `assistantMsg.content.flatMap(...)`、`openai-completions.js:960` 对 assistant 消息执行 `msg.content.filter(...)`——**都要求 content 是「内容块数组」**；`transform-messages.js:49` 只把 null/undefined 规整成 `[]`，string 不处理。我们的 `CopilotSidebar.chatCopilot` 把历史消息(含 assistant 回复)的 `content` 以** string** 直接传入 `models.complete`，于是任何含 assistant 历史的调用都崩。user 消息用 string 是兼容的（`openai-completions.js:922` 专门处理），但 assistant 不行。
- 修复 `src/components/copilot/CopilotSidebar.tsx` 的 `chatCopilot`：新增 `toBlocks(c)` 把 string 规整成 `[{ type:'text', text:c }]`（空串→`[]`），历史与 newContent 的 content 全部走数组形式再传给 pi-ai。`callLLM`（pi.ts）只发单条 user 消息、string 兼容，无需改；Agent 主流程用 pi-agent-core 自己构建消息，未受影响。
- 教训：凡是把多轮历史喂给 pi-ai `models.complete` 的地方，assistant 消息 content 必须是数组，不能传 string。
- 验证：`npm run build` 通过（exit 0）；`vitest run src/ai` 66 passed。

## Agent 面试：选择题"正在检查回答"长时间转圈修复
- 症状：Agent 面试页提交**选择题**后，`正在检查回答…` 遮罩转很久（deepseek/cloudflare 下尤其明显，像"一直转"）。
- 根因：选择题评分本身是确定性的（`sessionEvaluator.ts:45` → `gradeChoice`，不触 LLM/网络，瞬时）。但原 `submitAnswer` 对选择题也走完整 `agent.continue()` **LLM 决策循环**：观察→决策调 evaluateAnswer→观察→决策调 getQuestion，至少 2 次 LLM 往返。deepseek/cloudflare 单次 ~5–9s，故一道选择题被拖成 15–30s；若免费模型工具循环不收敛还可能逼近 90s 看门狗才被掐。
- 修复 `src/agent/interviewAgent.ts`：新增 `choiceAdvance()`（确定性判分 `evaluateSessionQuestion` + 确定性选题 `fallbackNextQuestion`/`pickNextAdaptive`），并在 `submitAnswer` 入口加 `if (session.currentQuestion?.format === 'choice') return choiceAdvance(answer);`——**选择题跳过 LLM 循环，瞬时出结果**。开放题仍走 LLM（评分必须联网）。
- 关键设计点：`choiceAdvance` 调用 `fallbackNextQuestion()` 但**不翻转 `usingFallback`**，以免误杀后续开放题的 LLM 评分路径与 Agent 自适应决策（与兜底 `fallbackAdvance` 的区别）。
- 测试用例 `interviewAgent.test.ts` 的 `scriptedResponses` 需同步更新：选择题回合不再消费 LLM 脚本（原 7 条→现 4 条，去掉选择题回合的 evaluateAnswer/getQuestion/ask），与确定性快路径对齐。
- 验证：`npm run build` 通过；`vitest run`（全量 334 passed）。


## 修复：切 tab（如去设置）导致进行中的 session 消失
- 根因：路由是单个 `App`（HashRouter）包所有页，**切 tab 不会重挂 App**；但 Agent 面试的全部会话状态（phase/currentQuestion/answer/questions/transcript/busy/submitting/summary/error/sessionRef/handleRef）原本都在 `AgentInterviewPage` 本地 useState/useRef，切到其它页时该组件卸载即销毁。训练 session 在 `useTrainingSession`（App 层）本就存活，但导航菜单仅在 `phase==='home'` 显示，进入训练/面试后菜单隐藏，切走后回不来，观感像"消失"。
- 修复：新增 `src/hooks/useAgentInterview.ts`（把 Agent 面试状态机从组件抽出，与 `useTrainingSession` 同思路在 App 层调用），`AgentInterviewPage` 改为纯展示组件（props 注入）。Agent 在后台继续运行，切 tab 不 dispose（dispose 仅发生在 restart / App 卸载）。训练 quiz/result 渲染加 `page==='train'` 限定，避免与其它页叠显；导航菜单改为常驻，进入 session 后也能切 tab 再切回。
- Props 变更：`AgentInterviewPage` 不再接收 `profile`/`onComplete`（profile 由 App 透传给 hook 的 `useAgentInterview`，onComplete 经 hook.finalize 调用）。
- 验证：`npm run build` 通过；`vitest run` 全量 334 passed；vite dev 启动 200 无转译错误。

## 修复：切 tab 丢失设置页未保存草稿（与 Agent/训练同思路）
- 背景：用户问「其它类似的切换时需要保留状态的，是不是也做类似修改」。盘点后：训练（`useTrainingSession` 已在 App 层）、Agent（`useAgentInterview` 已在 App 层）、Copilot（`CopilotSidebar` 在 `page` 条件外常驻挂载）均已保留；**唯一缺口是设置页 `SettingsPanel` 内部的 `draft`/`text`/`promptDraft`/`activeTab` 等编辑态（组件内 useState）**，切到其它 tab 再切回，没点保存的改动全丢。
- 修复：新增 `src/hooks/useSettingsDraft.ts`（把设置草稿编辑态 + `updateProvider`/`moveProvider`/`updateProficiency`/`handleSave`/`handleFormSave`/`handlePromptSave` 抽到 App 层），`SettingsPanel` 改为纯展示组件（props 注入 draft/setDraft/text/setText/promptDraft/setPromptDraft/activeTab/setActiveTab + 各 handler + `onResetLearner`）。`useEffect([config])` 留在 hook 内——仅在主动保存（`config` 变化）后同步草稿，切 tab 不触发，草稿得以保留。`message` 实例由 App 经 `AntdApp.useApp()` 传入 hook（类型用 `ReturnType<typeof AntdApp.useApp>['message']`，避免 antd 未导出 `MessageInstance`）。
- App：`const settings = useSettingsDraft(config, handleSaveConfig, message);` 后向 `SettingsPanel` 透传 `{...settings}`（`config`/`onSave` 不再传给 `SettingsPanel`，改由 hook 持有）。
- 验证：`npx tsc -b` 0 错误；`npm run build` 通过；`npm test` 全量 334 passed。
