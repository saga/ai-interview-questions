
########## inference-optimization|scenario   (n=5) ##########

• sebastian-raschka-2026-08-144  [hard]  concepts=
   Q: KV Cache 压缩后平均吞吐提高但 P99 延迟变差，哪些原因值得调查？（请选择三个）
     0 ✓ 压缩和恢复操作增加了单请求计算
     1 ✓ 不同长度请求造成排队和缓存碎片
     2 ✓ 内存节省带来的并发提升改变了尾部负载
     3   只要平均吞吐提高就可以忽略 P99
   A: 压缩可能增加解码、同步或 kernel 开销，并在长短请求混合时放大尾部排队；平均吞吐不能代表用户体验。

• sebastian-raschka-2026-08-59  [hard]  concepts=
   Q: 比较低、中、高推理努力度模式时，哪些指标应同时记录？（请选择三个）
     0 ✓ 按任务难度分桶的正确率或验证通过率
     1 ✓ 每请求推理 token、采样数或费用
     2 ✓ 首 token 和端到端延迟
     3   只看平均输出字数而不看任务质量
   A: 努力度是质量、成本和延迟的联合权衡，因此应记录任务正确率或通过率、推理 token/成本以及 TTFT 或端到端延迟。

• p0-inference-optimization-scenario-01  [hard]  concepts=
   Q: 线上观测到 TTFT 很高、TPOT 正常，GPU 利用率在短请求下偏低；增加输出长度并未明显改变首 token 延迟。优先排查哪类瓶颈？
     0 ✓ 优先拆分排队、prefill 和网络耗时，排查调度或短请求利用率问题，而不是先优化 decode
     1   直接把 temperature 调低，因为采样参数决定首 token 的主要延迟
     2   只增加最大输出 token 数，因为 TPOT 正常代表 prefill 一定过慢
     3   立即把模型换成更大的版本，因为更大模型一定能提高短请求 GPU 利用率
   A: TTFT 高而 TPOT 正常，说明瓶颈更可能在请求排队、调度、prefill 输入处理或网络/序列化，而不是 decode 阶段。应先拆分排队、prefill、首 token 和网络耗时，再选择 batching、缓存或并行化手段。

• specdec-04  [medium]  concepts=
   Q: 在哪些情形下，投机解码可能无明显收益甚至变慢？
     0 ✓ 输出很短，省下的前向次数很少，难以摊薄草稿模型开销
     1 ✓ 采样温度很高，草稿模型频繁猜错、接受段很短
     2 ✓ 草稿模型本身很慢，其开销抵消了跳过目标前向的收益
     3   草稿与目标使用完全相同的 tokenizer，且草稿小一个数量级
   A: 输出很短时省下的前向次数少，难以摊薄草稿开销；高温开放生成时草稿频繁猜错、接受段很短；草稿模型本身很慢则会抵消跳过目标前向的收益。共享同 tokenizer 且草稿小是『有利』条件，不是有害条件。

• specdec-03  [medium]  concepts=
   Q: 在哪些情形下，投机解码最可能带来明显的推理加速？
     0 ✓ 草稿模型比目标小一个数量级以上，且任务偏输入驱动（转录、翻译、摘要）
     1 ✓ 目标模型放不下 GPU、需靠内存卸载时，加速比可达约 10x
     2 ✓ 草稿与目标同架构仅规模更小，且采样温度接近 0
     3   草稿模型质量很差但很大，能保证每次生成很长的候选
   A: 当草稿模型比目标小一个数量级以上、任务偏输入驱动（转录/翻译/摘要等事实生成）、同架构仅规模更小、且采样温度接近 0 时接受率高；当目标模型需内存卸载（offload）时加速最显著，可达约 10x。草稿差且大反而无益。

########## multimodal|mechanism   (n=5) ##########

• vlm-07  [medium]  concepts=
   Q: 把视觉语言模型用于『文档检索』（如 ColPali）体现了多模态的哪种能力？
     0 ✓ 把页面图像与文本查询做跨模态 late interaction（MaxSim），免 OCR 即可利用表格图表等线索
     1 ✓ 模型同时理解图像与语言两种模态，在统一相似度空间对齐页面 patch 与查询 token
     2   它只处理纯文本内容，图像仅作为装饰性内容，完全不参与任何检索打分
     3   必须先把整页 OCR 识别转换成文本，再退化为纯单模态的文本检索
   A: ColPali 把页面图像与文本查询做跨模态 late interaction（MaxSim），无需 OCR 即可利用表格/图表等视觉线索；模型同时理解图像与语言两种模态，并在统一相似度空间里对齐页面 patch 与查询 token。它并非只处理纯文本，也不先把整页 OCR 再退化为单模态检索。

• vlm-03  [medium]  concepts=
   Q: 关于多模态融合时机（早融合 vs 晚融合），结合 NEO-unify 的设计，下列哪些描述正确？
     0 ✓ NEO-unify 的近无损视觉接口让图像像素在输入侧就进入统一序列，属早融合思路
     1 ✓ 早融合使模态在主干内部即混合，避免传统晚融合需后期跨系统对齐的问题
     2   晚融合（先各自编码再拼接）总比早融合精度更高，因此 NEO-unify 选择晚融合
     3   融合时机对跨模态表征的最终质量其实没有任何影响
   A: NEO-unify 的近无损视觉接口让图像像素在输入侧就进入统一序列，属于早融合思路；早融合使模态在主干内部即混合，避免了传统晚融合需要后期跨系统对齐的问题。NEO-unify 并非选择晚融合，融合时机也显著影响跨模态表征质量。

• vlm-02  [medium]  concepts=
   Q: NEO-unify 如何把图像像素与文本词元对齐到同一序列？
     0 ✓ 通过『近无损视觉接口』直接处理像素，无需 VAE 离散 tokenizer
     1 ✓ 文本用自回归交叉熵、视觉用像素流匹配，在同一 MoT 主干内协同
     2   依靠外部共享 tokenizer 把图像和文本映射到同一词表后再拼接
     3   先把图像编码成单一稠密向量再与文本向量相加
   A: 通过『近无损视觉接口』直接处理像素，无需 VAE 离散 tokenizer；文本用自回归交叉熵、视觉用像素流匹配（pixel flow matching），二者在同一 MoT（Mixture-of-Transformer）主干内协同演化。它打破外部共享 tokenizer 依赖，并非把图像编码成单一稠密向量再与文本向量…

• google-genai-leader-09  [medium]  concepts=
   Q: 扩散模型（Diffusion Model）做什么？（单选）
     0 ✓ 通过把噪声细化为结构化数据来生成高质量内容。
     1   便于结构化数据的存储与管理。
     2   分析数据并预测未来趋势与模式。
     3   优化企业业务流程与内部资源分配决策，从而提升整体运营效率。
   A: A 正确：扩散模型从随机噪声出发，经多步去噪逐步细化出结构化高质量内容（图像等）。B 错：那是数据存储。C 错：那是预测/分析。D 错：那是流程优化。

• foundation-multimodal-mechanism-01  [medium]  concepts=
   Q: 视觉语言模型常用一个 projector 或适配层连接视觉编码器与语言模型，主要目的是什么？
     0 ✓ 把视觉特征映射到语言模型可处理的表示空间
     1   把所有图像像素直接改写成真实文字标签
     2   替代语言模型的 tokenizer 并删除文本输入
     3   只负责压缩模型文件而不参与前向计算
   A: 视觉编码器产生视觉特征，projector 将其映射到语言模型可接收的表示空间或 token 序列，使语言模型能结合图像特征生成文本。

########## distillation|tradeoff   (n=5) ##########

• ai-eng-042  [hard]  concepts=
   Q: 客户每月 $50K 的旗舰模型支出中，90% 流量来自稳定的代码评审类任务。团队提议蒸馏一个专用学生模型接管这部分流量（一次性投入约 $120K，上线后月推理成本降至约 $8K）。关于该项目的 ROI 判断与持续维护，下列说法正确的是？
     0   一次性投入 $120K、月省 $42K，3 个月回本；但蒸馏是一次性的，上线后无需再投入，后续教师模型升级也不影响学生模型的表现，因此不必再安排任何维护预算，所有再蒸馏都应彻底取消
     1   $120K 投入相对 $50K 月支出太高，任何情况下都不应做蒸馏，因为节省的成本永远追不回初始投入，且小模型精度必然不如教师，只会拖累业务指标
     2 ✓ 约 3 个月回本、一年净省近 $40 万，但必须把“再蒸馏”当作周期性维护项：教师模型升级后限期跟进再蒸馏，质量漂移超阈值随时触发，每轮 $25-35K 摊入预算；负载定义不稳定、变化快于再蒸馏周期的工作流不适合蒸馏
     3   学生模型一旦上线就应立即停用教师模型，否则成本不会下降，且保留教师只会增加推理延迟，对业务指标没有任何正面作用，应当彻底下线以最大化节省
   A: 静态账：月省约 $42K，$120K 投入约 3 个月回本，一年净省 $380K+——稳定高量任务上蒸馏是正确的 CapEx→OpEx 转换。但蒸馏不是一次性买卖：教师模型升级（如 Opus 4.x → 下一代）后需 30 天内再蒸馏以继承质量提升；评测集分数漂移超阈值（如 -2%）触发计划外再蒸馏，每轮成本 $25-…

• sebastian-raschka-2026-08-20  [hard]  concepts=
   Q: 学生模型在蒸馏数据上表现很好，但遇到不同题型就明显退化，最合理的改进方向是什么？（单选）
     0   只增加同分布样本的重复次数
     1 ✓ 扩展任务分布并用独立分布外集验证
     2   删掉教师模型所有的推理轨迹数据
     3   只提高学生模型输出温度
   A: 蒸馏只保证学生学习到教师在蒸馏分布上的行为，分布外泛化仍受数据覆盖和验证限制。应扩大任务多样性并用独立分布外集评估。

• rl-opd-memory-01  [hard]  concepts=
   Q: 在长文本 / 长链条任务的 Agent 强化学习中，引入 Off-Policy Distillation (OPD) 或多教师模型（Multi-Teacher）指导能显著提升策略学习效率。但若为每个教师模型建立常驻（Long-running）的独立推理服务，会导致显存与计算资源消耗过高。针对该架构瓶颈，哪些系统设计方案能在控制显存开销的同时维持较高的蒸馏吞吐？
     0 ✓ 利用本地 Host 内存与 NVMe 存储作为分层缓存（Hierarchical Cache），在训练端对教师模型状态进行预取（Prefetching）与动态切换
     1   将所有教师模型统一量化至 1-bit 权重并强行常驻显存，完全牺牲教师模型推理精度以消除显存加载延迟
     2 ✓ 在 Rollout 与训练的数据流中引入统一的 Data Buffer，使教师模型推导与学生模型采样在同一数据流控制下实现异步流水线重叠
     3   废弃所有 Off-Policy 教师信号，强制要求所有教师模型在学生模型的每个 Rollout Step 中进行同步单步对齐推导
     4 ✓ 针对不同 Rollout 环境的请求特性，采用工作负载感知的路由调度，实现 Prefill 与 Decode 阶段动态资源比例调节
   A: 正确：①用 Host RAM + NVMe 构建多级缓存，并在计算下一 Batch 前预取（Prefetch）目标教师模型权重，能在有限 GPU 显存下实现多教师动态切换（A 对）；②统一的数据流 Buffer 解耦 Sampling、Teacher Inference 与 Policy Training 的速率差异，…

• sebastian-raschka-2026-08-98  [medium]  concepts=
   Q: 将高预算教师模型的推理样本蒸馏给小模型时，哪些判断合理？（请选择三个）
     0 ✓ 可以把部分教师解题行为迁移给学生
     1 ✓ 学生在线推理成本可能下降
     2 ✓ 教师样本质量和任务覆盖会限制学生泛化
     3   蒸馏保证学生在所有分布外任务等价于教师
   A: 蒸馏可降低在线成本并迁移教师行为，但学生能力受样本覆盖、教师错误和分布外泛化限制。

• distill-05  [medium]  concepts=
   Q: 相比从头训练同尺寸小模型，知识蒸馏带来哪些典型收益与限制？
     0 ✓ 学生在远少于教师参数量下仍能保留教师的大部分能力，部署更便宜
     1 ✓ 学生的能力上限被教师锚定，无法超越教师（除非教师本身在持续更新）
     2   学生一定比教师更准，因为学了更多数据
     3   蒸馏不需要任何教师模型，学生可自行达到教师水平
   A: 学生在远少于教师参数量下仍能保留教师的大部分能力，部署更便宜；但学生的能力上限被教师锚定，无法超越教师（除非教师本身在持续更新）。学生不会比教师更准，蒸馏也离不开教师模型。

########## inference-optimization|calculation   (n=5) ##########

• sebastian-raschka-2026-08-162  [hard]  concepts=
   Q: 论文声称长上下文复杂度更低时，工程验证应包括哪些内容？（请选择三个）
     0 ✓ 不同序列长度下的显存和吞吐曲线
     1 ✓ 首 token、端到端和尾延迟
     2 ✓ 长上下文检索与任务质量
     3   只在最短序列上测一次平均速度
   A: 复杂度声明需要转化为实际长度、硬件和批处理下的内存、延迟、吞吐和质量测量。

• sebastian-raschka-2026-08-150  [hard]  concepts=
   Q: 拆解长上下文推理成本时，哪些部分应分别测量？（请选择三个）
     0 ✓ Prefill 的计算和首 token 延迟
     1 ✓ Decode 的每 token 计算与内存带宽
     2 ✓ KV Cache 显存、压缩恢复和并发排队
     3   只测短输入的平均 tokens/s
   A: 需要区分预填充、解码、KV 存储和访存、压缩/恢复操作以及并发排队，单个平均 token 成本不够。

• sebastian-raschka-2026-08-133  [hard]  concepts=
   Q: 解释“每 token 激活计算较低”时，哪些额外指标仍必须测量？（请选择三个）
     0 ✓ 内存带宽和缓存访问量
     1 ✓ 设备间通信与专家负载均衡
     2 ✓ TTFT、端到端延迟和尾延迟
     3   只用理论 FLOPs 就能推断用户体验
   A: FLOPs 不是端到端性能，内存带宽、通信、kernel 实现、首 token 延迟和尾延迟都会决定实际吞吐。

• spec-decode-accept-probability  [hard]  concepts=
   Q: 投机采样利用草稿模型一次性自回归生成 K 个 Token 候选再由目标模型并行验证。为保证最终输出与直接使用目标模型逐 Token 采样完全一致当 p(x) < q(x) 时接受概率应设定为多少？若拒绝该如何重新采样？
     0 ✓ 接受概率 α(x)=p(x)/q(x) 拒绝后从残差分布 normalize(max(0,p(x)-q(x))) 中重新采样
     1   接受概率 α(x)=p(x)-q(x) 拒绝后直接从原目标模型分布中重新采样，因为概率差直接刻画了草稿与目标之间的偏离度，用减法比用比值更能稳定衡量该偏差
     2   接受概率 α(x)=1.0 拒绝后直接终止整条轨迹的生成，因为一旦拒绝就说明草稿完全不可信，继续采样只会浪费算力且必然产生错误token
     3   接受概率 α(x)=q(x)/p(x) 拒绝后从草稿模型分布中重新采样，因为用草稿概率作分子更能反映草稿自身的置信度从而加快收敛
   A: 正确答案 A。修正的拒绝采样算法：p(x)≥q(x) 时接受概率为 1.0；p(x)<q(x) 时以 α(x)=p(x)/q(x) 概率接受。被拒绝后从截断残差分布 normalize(max(0,p(x)-q(x))) 中采样新 Token。该推导可严格证明无偏等价于目标模型独立采样。

• specdec-06  [hard]  concepts=
   Q: 在理想情况下（草稿模型几乎零误差），投机解码把逐 token 自回归生成的延迟复杂度从约 O(n) 降到接近？
     0 ✓ O(1)，因为一次前向即可验证整条候选序列
     1   O(log n)，因为采用二分验证
     2   O(n²)，因为要存储完整注意力矩阵
     3   不变，仍是 O(n)，只是常数更小
   A: 理想情况下每次迭代用一次前向即可验证并产出整条候选序列，等效把逐 token 生成的延迟从 O(n) 降到接近 O(1)；这依赖于内存带宽瓶颈而非计算瓶颈，因此跳跃式跳过多个前向传播。

########## quantization|mechanism   (n=5) ##########

• prune-quant-compound-degrade  [hard]  concepts=
   Q: 在 LLM 高效部署管线中通常先通过结构化剪枝降低参数量随后将残余权重进一步量化至 4-bit。这种组合压缩策略在代码生成数学推导与 CoT 任务上性能剧烈下降其底层机制包括哪些？（多选）
     0 ✓ 结构化剪枝删除了部分层或注意力头降低了模型的表征容量与冗余度使得剩余权重对量化噪声的容忍度大幅下降
     1 ✓ 4-bit 量化引入的截断误差与舍入误差会在自回归生成过程中沿序列时间轴逐 Token 累积形成误差放大效应
     2 ✓ 剪枝打破了原模型内部各层之间的激活值分布平衡使得基于原始分布设计的静态量化 Scaler 发生失真
     3   4-bit 量化会导致 Transformer 的 Softmax 注意力矩阵彻底退化为单位矩阵失去上下文关联能力
   A: A 正确 剪枝降低表征容量使剩余权重对量化噪声容忍度下降。B 正确 4-bit 离散化误差在自回归生成中逐 Token 累积放大。C 正确 剪枝改变激活分布使静态量化 Scaler 失真。D 错误 Softmax 仍能计算注意力权重只是精度受干扰。

• quant-qah-03  [hard]  concepts=
   Q: 为什么在『结构压缩之后』，常规的 Quantization-Aware Distillation（QAD）会失效，而 QAH 仍能生效？
     0 ✓ QAD 需同架构全精度教师；结构压缩后唯一教师是退化 bf16 检查点，学生上限被锚定
     1 ✓ QAH 直接以压缩前的原始模型为冻结教师跨架构蒸馏，不被退化检查点限制
     2 ✓ QAD 仅在只做量化（未结构压缩）时有效，因为存在同架构全精度教师
     3   QAH 与 QAD 在做法上完全等价，只是把学生换成了更大的模型
   A: QAD 需要同架构的全精度教师来对齐 logits；结构压缩后，唯一可做的教师是已退化的 bf16 检查点（它本身是对原模型的蒸馏近似），学生上限被锚定在退化目标。QAH 直接以压缩前的原始模型为冻结教师做跨架构蒸馏，不被退化检查点限制；而 QAD 在『仅做量化、未结构压缩』时才是有效手段。

• quant-qah-02  [hard]  concepts=
   Q: Quantization-Aware Training（QAT）与本文的 Quantization-Aware Healing（QAH）在做法与特性上有何不同？
     0 ✓ QAT 在前向插入 fake-quantization 并用任务损失微调，使权重适应低精度
     1 ✓ QAH 以压缩前原始全精度模型为冻结教师，用 KL 散度做跨架构蒸馏
     2 ✓ QAT 在 9B 模型 MXFP4 实验约 step700 达峰后到 step1200 暴跌近 19 点，不稳
     3   QAH 在整体耗时上比 QAT 慢约 7 倍，但峰值精度反而更高
   A: QAT 在前向中插入 fake-quantization 并用任务损失微调，使权重适应低精度；但其成本高且不稳定——9B 模型 MXFP4 实验中约 step 700 达峰后到 step 1200 暴跌近 19 个百分点。QAH 以压缩前的原始全精度模型为冻结教师，用 KL 散度仅依赖教师输出分布做跨架构蒸馏，达到峰值…

• quantization-mem-bandwidth-gguf-01  [medium]  concepts=
   Q: 在本地硬件运行大语言模型（LLM/SLM）进行单 Token 自回归生成（Autoregressive Generation）时，推导性能通常受限于内存带宽（Memory Bandwidth Bound）而非纯粹的计算峰值（Compute Bound）。关于 GGUF 量化（如 Q4_K_M）提升本地推理吞吐量的物理机制与影响，下列哪些说法是正确的？（请选择以下 3 个正确选项）
     0 ✓ 将 FP16 权重压缩为 4-bit 整数后，显著减少了每生成一个 Token 需要从显存/内存传输到计算核心的数据体积，从而直接降低了内存带宽瓶颈。
     1 ✓ 4-bit 量化大幅降低了模型权重的显存占用，使原本超出显存容量的模型能够完全驻留于 GPU 显存内，避免了跨 PCIe 总线向系统内存（RAM）Offload 带来的严重性能损耗。
     2   量化算法在推理时会顺带把 KV Cache 也压缩为 4-bit 表示，并跳过注意力矩阵的存储开销，从而从根本上消除了长上下文推理时的显存增长问题。
     3 ✓ 量化能够提升 Token 生成速率（Tokens/sec），主要得益于显存数据传输时延的降低，但在计算端需要付出微小的解量化（Dequantization）额外开销。
   A: 自回归生成是典型的 Memory-bound 任务，每生成一个 Token 都需将全量权重加载进计算单元。GGUF 4-bit 量化将权重体积缩小至原 FP16 的 ~25%，极大减轻了显存带宽压力，并能避免跨 PCIe Offload（选项 A、B、D 正确）。量化压缩的是模型权重和标量数据，并不直接消除 KV Ca…

• quant-qah-01  [medium]  concepts=
   Q: 工业界部署大模型时常采用『结构压缩 → 量化 → 修复（healing）』流水线。关于这一步步，下列哪些描述正确？
     0 ✓ 先移除 layer/head/neuron 等结构削减参数量，再把剩余权重量化到 4-bit（如 MXFP4）
     1 ✓ 压缩与量化会系统性降低推理、数学、代码等能力，部署前需修复步骤恢复
     2   修复（healing）指的是把模型重新从头预训练一遍来恢复精度
     3   量化必须在结构压缩之前完成，否则无法得到可用的 4-bit 权重
   A: 标准做法是先移除 layer/head/neuron 等结构以削减参数量，再把剩余权重量化到 4-bit（如 MXFP4）；压缩与量化会系统性降低推理、数学、代码等能力，因此部署前需一个轻量的修复（healing）步骤恢复能力。healing 是轻量后训练/蒸馏，并非从头预训练；通常顺序是先结构压缩再量化。

########## vector-db|tradeoff   (n=5) ##########

• ai-rag-010  [hard]  concepts=
   Q: 一个 Vector Database 已存储 1 亿条 embedding，内存成本越来越高。你有哪些降低成本的办法？会牺牲什么？
     0 ✓ 按阶梯推进：先做免费的数据治理（去重、过期数据淘汰），再下调 HNSW 的 M/ef 参数，最后评估 PQ/INT8 量化或磁盘索引——各级分别以召回率或查询延迟为代价，取舍由实测曲线决定
     1   首选扩容服务器内存维持全内存 HNSW 结构，因为量化、磁盘索引和数据治理都会造成不可接受的召回损失，不值得冒险去尝试
     2   把索引整体迁移到 SSD 磁盘（DiskANN 类方案）是唯一途径，它能在不增加查询延迟的情况下把内存成本降低一个数量级，别无选择
     3   将所有向量从 FP32 转为 INT8 标量量化即可一步到位，标量量化经过校准后不会带来任何召回率损失，是最简单的降本手段
   A: 省钱阶梯：数据治理 → 索引参数调优 → PQ/INT8 量化 → 磁盘索引；每一级都在用召回率或延迟换内存，须以评测曲线定取舍。

• vectordb-04  [hard]  concepts=
   Q: 关于多向量检索的查询延迟，下列哪些描述符合其权衡？
     0 ✓ 穷举全库多向量在 GPU 上单查询约百毫秒级，比单向量全扫更慢
     1 ✓ 用 PLAID/fast-plaid 等近似索引可把多向量查询降到约 10ms 级
     2 ✓ 只对召回的 top-k 做多向量重排时，延迟几乎可忽略
     3   多向量查询永远比单向量 ANN 快一个数量级，因为它无需扫描
   A: 穷举全库多向量（如 60 万向量）在 GPU 上单查询约百毫秒级，比单向量全扫慢；用 PLAID/fast-plaid 等近似索引可降到约 10ms 级；只对召回的 top-k 做多向量重排时延迟几乎可忽略。多向量『穷举』比单向量 ANN 更慢，并非永远更快。

• vectordb-02  [hard]  concepts=
   Q: 实验表明多向量索引体积可达同语料单向量的数十倍（如 4874 段 NQ：MiniLM 384d 约 7.5MB，LateOn 多向量约 311MB）。下列哪些是有效的缓解手段？
     0 ✓ 用 PLAID 类量化把多向量索引压缩到与稠密向量同量级（如 92MB）
     1 ✓ 对 bi-encoder 召回的 top-k 做多向量 MaxSim 重排，避免全库建多向量索引
     2 ✓ token pooling 把向量数减半，通常仅损失极少的检索性能
     3   将维度从 128 提升到 1024，以换取更小的索引体积
   A: 缓解手段：用 PLAID 类量化把多向量索引压缩到与稠密向量同量级（如 92MB）；对 bi-encoder 召回的 top-k 做多向量重排，避免全库建多向量索引；token pooling 把向量数减半而性能几乎不降。把维度从 128 提升到 1024 会增大存储，反而更糟。

• search-03  [medium]  concepts=
   Q: 在混合检索（向量 + 关键词 BM25）中，为什么需要对两路召回结果进行归一化与加权融合，而非直接取 Top-K 拼接？
     0 ✓ 两路分数分布与尺度不同，需归一化后加权融合以平衡语义与关键词信号
     1   混合检索无需任何融合，直接拼接两路 Top-K 结果即可获得最优效果，简单高效
     2   BM25 分数恒大于向量分数，因此完全不需要做归一化处理，直接相加即可
     3   融合必然会降低整体召回率，所以应当只选择其中一路结果来使用，避免相互干扰
   A: 两路分数尺度与分布不同，直接拼接会导致某一路主导；需归一化后加权融合才能平衡语义与关键词信号。

• vectordb-05  [medium]  concepts=
   Q: 给定『百万级语料、存储与延迟敏感、查询条件单一』的场景，关于单向量与多向量的选型，哪些判断合理？
     0 ✓ 优先单向量 bi-encoder：索引小、查询快，足以覆盖单一条件查询
     1 ✓ 若后续出现多约束或精确标识符查询且质量不达标，可再叠加多向量重排
     2   必须一开始就上多向量全库索引，否则检索质量一定不达标
     3   多向量在此场景的主要优势是显著节省显存，而非提升召回
   A: 该场景优先单向量 bi-encoder：索引小、查询快，足以覆盖单一条件查询；若后续出现多约束或精确标识符查询且质量不达标，可再叠加多向量重排。并非必须一开始就上多向量全库索引，且多向量在该场景是更费存储而非更省显存。
