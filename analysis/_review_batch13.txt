
########## evaluation|calculation   (n=4) ##########

• sebastian-raschka-2026-08-84  [hard]  concepts=
   Q: 部署二分类文本检测器前，阈值校准应关注哪些指标？（请选择三个）
     0 ✓ 不同阈值下的 precision 和 recall
     1 ✓ 人类文本误报率与 AI 文本漏报率
     2 ✓ 目标场景中两类错误的相对代价
     3   只看训练集准确率并固定使用 0.5
   A: 阈值改变 precision、recall、误报和漏报，应根据真实业务分布和错误代价选择 operating point，而非默认 0.5。

• selfharness-acceptance-rule-01  [hard]  concepts=
   Q: 在基于评估迭代的 Harness 自我改进框架中，假设第 t 轮演进产生的候选修改方案为 Δ_j，系统在保留数据集（Held-in Split, D_in）与未留存测试集（Held-out Split, D_ho）上的通过率变化分别为 Δ_in^(j) 与 Δ_ho^(j)。为了防止 Harness 发生过拟合与性能倒退，系统采用的提议准入条件（Acceptance Rule）通常应满足哪项逻辑约束？
     0 ✓ Δ_in 大于等于 0 且 Δ_ho 大于等于 0 且 max(Δ_in, Δ_ho) 大于 0
     1   Δ_in 大于 0 且 Δ_ho 小于 0 且 (Δ_in 加 Δ_ho) 大于 0
     2   Δ_in 等于 0 且 Δ_ho 大于 0 且 (Δ_in 乘 Δ_ho) 等于 0
     3   Δ_in 大于 0 且 Δ_ho 大于等于 0 且 min(Δ_in, Δ_ho) 小于 0
   A: A 正确：准入逻辑必须保证零退化（非负性）且具备净增益，即候选修改在保留集与未留存集上均不能出现性能下降（Δ_in ≥ 0 且 Δ_ho ≥ 0），并且至少在一个集合上有严格正向提升（max(Δ_in, Δ_ho) > 0）。B 错误：允许未留存集下降会导致过拟合与泛化性能倒退。C 错误：限定保留集增益必须为 0 过于…

• ai-eng-051  [medium]  concepts=
   Q: 50 个 PR/周的团队，完整跑一次 golden set 评测成本 $80-$200，若每个 PR 都全量跑每周要 $4K-$10K。请给出把单 PR 成本压到 ~$25、周总成本压到预算内的工程手段与成本账。
     0   把 golden set 缩减到 100 个案例，所有 PR 都全量跑，自然就便宜了；小集合既能覆盖主线路径，又能让每次改动快速得到反馈，规模大的评测集反而拖慢迭代节奏且不必要，而且维护与标注成本也更低，性价比最高，无需分层采样等复杂工程。
     1 ✓ 默认 PR 跑 10-25% 分层样本（每种失败模式保底 1 例），full-eval 需 CODEOWNERS 审批，main 每晚全量兜底；再用 (prompt-hash, model-version) → (output, judge…
     2   取消 LLM judge 只保留代码断言，成本自然为零；确定性断言不需要模型调用，也就不存在 judge 费用，用单元测试式校验完全足以把控 Agent 改动质量，既省钱又更可复现，还能消除 judge 主观噪声，是中小团队的最优解。
     3   让开发者本地自测通过后再手动触发 CI 评测，省掉大部分自动运行；人肉把关能过滤掉明显问题，只有可疑改动才进云端评测，整体开销可压到极低，还能培养工程师的质量 Owner 意识，减少无效流水线占用与排队，把云端算力留给真正需要的变更。
   A: 三板斧：①分层抽样——默认每个 PR 只跑 10-25% 的 golden set，但按失败模式分层保证 12 类失败模式每类至少 1 例，牺牲覆盖但不牺牲代表性；full-eval 标签触发 100%（需 CODEOWNERS 审批防滥用），main 分支每晚全量兜底；②结果缓存——输出由 (prompt-hash,…

• agent-eval-01  [medium]  concepts=
   Q: 在对具备随机性的 AI Agent 进行多轮试炼（Trials）评测时，常用的两个统计指标是 pass@k（k 次尝试中至少成功 1 次的概率）和 pass^k（k 次尝试全部成功的概率）。假设某 Agent 解决某类独立任务的单次执行成功率 p = 0.8。关于这两个指标的计算与工程应用场景，下列哪些说法是正确的？
     0 ✓ 针对该任务进行 3 次独立试验时，pass@3 的值为 99.2%，而 pass^3 的值为 51.2%
     1 ✓ pass@k 随着 k 的增加而递增，适合用于评估探索性生成任务（如代码修复）中 Agent 找出可行解的能力上限
     2 ✓ pass^k 随着 k 的增加而递减，适合用于评估高可靠性业务场景（如自动化客服）中 Agent 的行为稳定性
     3 ✓ 当 k=1 时，pass@k 与 pass^k 的数值相等，均等于 Agent 单次执行的成功概率 p
     4   pass@k 与 pass^k 均用于衡量 Agent 在多轮重试下的平均 Token 消耗效率
   A: A 正确：pass@3 = 1 - (1-0.8)^3 = 0.992（99.2%），pass^3 = 0.8^3 = 0.512（51.2%）。B 正确：pass@k 衡量「k 次中至少成功一次」，适合探索性生成（如代码修复）中取最优解的场景。C 正确：pass^k 要求 k 次全成功，体现系统高可靠性与行为一致性，…

########## safety-alignment|scenario   (n=4) ##########

• aws-genai-developer-pro-41  [hard]  concepts=
   Q: 某金融公司上线用 Bedrock 帮客服代表做个性化投资建议的 GenAI 应用，须负责任的 AI 治理并满足监管：检测并防止推荐幻觉、对客户交互加安全控制、实时监控模型行为漂移、保留所有 prompt-响应对审计。须 60 天内上线、集成既有合规仪表盘、响应 <200ms。哪种方案以最低运维开销满足？（单选）
     0   配 Bedrock Guardrails 自定义内容过滤与毒性检测，用 Model Evaluation 检幻觉，prompt-响应对存 DynamoDB 加 TTL，CloudWatch 自定义指标接合规仪表盘。
     1   用 PrivateLink 安全访问 Bedrock，Lambda 做自定义提示校验，prompt-响应对存 S3 配生命周期，自建 CloudWatch 仪表盘监控。
     2 ✓ 用 Bedrock Agents + Knowledge Bases 接地响应，Guardrails 保内容安全，OpenSearch 存储索引 prompt-响应对，集成 QuickSight 生成合规报告并检测行为漂移。
     3   用 SageMaker Model Monitor 检漂移，WAF 过滤内容，交互存加密 RDS，API Gateway 建自定义 HTTP API 接合规仪表盘。
   A: Bedrock 知识库提供托管 RAG 工作流，使响应基于企业数据并附来源归因，降低幻觉风险。Guardrails 按配置安全策略评估输入与响应。将 prompt-响应对存于 OpenSearch 支撑可搜索的合规审计轨迹，QuickSight 可用收集数据做合规报告与行为趋势分析。该托管服务组合最小化快速部署所需的自…

• eth-03  [hard]  concepts=
   Q: 在部署面向公众的大模型对话产品时，应采取哪些安全与对齐措施来降低滥用与有害输出风险？
     0 ✓ 上线输入/输出内容审核与敏感词过滤，并对代码执行、外部工具调用等高风险能力实施权限隔离
     1 ✓ 通过系统提示与护栏（guardrails）约束行为边界，并结合 RLHF 与定期红队测试持续提升对齐水平
     2 ✓ 配置速率限制与身份认证防止滥用，同时建立用户反馈、人工复核与事故响应的闭环机制
     3   预训练语料已经足够干净，上线时应关闭全部线上内容审核与护栏，避免损害模型能力
     4   默认拒绝一切涉及医疗、法律等专业领域的提问，仅保留闲聊功能即可彻底消除有害输出的风险
   A: 考察面向公众产品的多层安全防护设计。核心措施包括输入/输出内容审核、系统提示护栏约束行为边界、RLHF 与红队测试提升对齐水平、速率限制防滥用、高风险能力权限隔离、以及用户反馈与事故响应闭环。仅靠模型自律不足以防范系统性风险。

• agent-safety-02  [hard]  concepts=
   Q: 一个具备自动执行权限的 Agent 在处理用户请求“帮我清理一下邮箱”时，删除了用户近 3 年的所有邮件且未做确认。关于 Agent 安全中的过度委托（Over-delegation）风险，下列设计正确的有？（多选）
     0 ✓ 对删除、支付等高风险操作需引入二次确认或 Human-in-the-loop
     1 ✓ 遵循最小权限原则，仅授予完成任务必需的权限
     2   为提升自主性，应授予 Agent 所有操作的自动执行权限
     3 ✓ 操作应可逆或可审计，支持回滚与追溯
   A: 需遵循最小权限、敏感操作需二次确认与可逆设计，避免过度委托。

• oreilly-radar-2026-08-17  [medium]  concepts=
   Q: 一个原本稳定的 Agent 因模型安全策略变化而突然拒绝合法任务。上线治理最需要补充什么？（单选）
     0   接受行为变化，因为安全策略不可测试
     1 ✓ 版本化模型和护栏，进行回归、灰度和回滚并定义边界
     2   把所有合法请求改写成更模糊的词
     3   只增加重试次数直到请求成功
   A: 应把模型和护栏版本纳入变更管理，维护版本化回归集，明确允许和拒绝边界，执行灰度、回滚和人工升级。依赖供应商策略且无行为基线会使生产行为不可预测。

########## model-selection|tradeoff   (n=4) ##########

• sebastian-raschka-2026-08-100  [hard]  concepts=
   Q: 为本地 Coding Agent 选择开放权重模型时，哪些因素应联合评估？（请选择三个）
     0 ✓ 代码任务上的真实评估质量
     1 ✓ 量化后的显存占用和 token 延迟
     2 ✓ 上下文长度、工具调用和长任务稳定性
     3   只按参数量排序而不运行任务评估
   A: 模型大小和量化影响显存与速度，代码任务质量、上下文能力和工具调用稳定性决定实际可用性，还需考虑许可证和维护成本。

• ai-fund-047  [hard]  concepts=
   Q: 一个 7B 模型和一个 70B 模型摆在面前，你会如何为业务做选型？请说明决策依据。
     0   一律选旗舰模型：参数更大必然带来更高的业务收益，成本与延迟差异可以忽略，因为规模优势在多数任务上都能直接转化为质量
     1   一律选 7B 模型：小模型响应更快，准确率差距总能靠 prompt 工程弥补到位
     2 ✓ 先用评测集确认旗舰模型在该任务上相对 7B 的准确率增益，再权衡成本、延迟与并发增量；必要时简单请求路由给小模型、困难请求升级到大模型
     3   按每百万 token 报价更低者选择：推理成本是决定业务价值的唯一因素，更低的单价必然对应更优的整体收益与回报
   A: 选型依据 = 任务准确率增益 × 业务价值 vs 显存/成本/延迟/并发增量；必要时用模型路由混合部署，而非直觉二选一。

• agent-routing-01  [hard]  concepts=
   Q: 在构建 LLM 动态路由系统（Model Router）以降低 API 成本时，通常有两种决策策略：一种是“基于已有信号路由”（Signal-based Routing），另一种是“基于请求文本预测路由”（Predictive Routing）。关于这两种路由策略的工程权衡，下列说法正确的有？（多选）
     0 ✓ 基于已知信号路由（如系统上游明确标记当前请求为 Planning、Code Edit 或 Commit Summary 等模式）通常是一个静态查表过程，运行延迟极低且结果可预测
     1 ✓ 基于预测的路由（Predictive Router）可以在缺少上游任务分类信号时，直接根据输入 Prompt 的文本特征预测任务难度并分发给性价比最高的模型
     2 ✓ 基于预测的路由由于引入了额外的分类器（Classifier），不仅增加了请求延时，还需要持续维护分类模型以适应底层 LLM 价格与性能的迭代
     3 ✓ 基于已知信号路由的准确率完全取决于上游传入信号的可靠性，如果任务模式划分过粗，可能会将复杂的子任务误判为简单任务并路由至弱模型
   A: 已知信号为静态查表极低延迟、预测需分类器增延迟与维护成本、准确率依赖上游信号可靠性。

• agentic-36  [medium]  concepts=
   Q: Agent 应该用一个强模型贯穿始终，还是多个弱模型协作？给出决策依据。
     0   应始终用一个最强模型贯穿所有步骤，任何级联方案都会不可避免地劣化最终质量，因为模型能力是决定输出的唯一因素
     1   多个弱模型协作没有任何代价，链条越长误差越会被相互抵消从而提升整体准确率，因此应把流程拆成更多更细的环节
     2   模型选择只需比较单价高低，与该环节的错误代价和任务难度无关，选最便宜的模型即可实现最优的成本收益
     3 ✓ 采用级联路由并用 A/B 实测对比级联与全强的成功率与成本曲线：格式化、初筛等简单步骤用便宜的小模型，错误会被下游放大的关键推理节点保留强模型。
   A: 考察级联路由的成本-误差权衡。级联将简单步骤交给小模型、关键推理保留强模型，可大幅降低成本；风险是误差在链条中累积放大。决策需 A/B 实测对比级联 vs 全强的成功率与成本曲线，而非拍脑袋。多弱模型协作的协调开销与责任归因困难也是隐性代价。

########## rag-pipeline|scenario   (n=4) ##########

• ai-eng-034  [hard]  concepts=
   Q: 你要为一个企业文档知识库搭建 RAG 的切分（Chunking）管线，语料混合了技术手册、法律合同、FAQ 和带大量表格的财务报告。请给出你的分块策略设计与理由。
     0   所有文档统一按 256 token 定长切分即可，重叠设为 0 以避免冗余索引，这样实现最简单、索引最紧凑，且对技术手册、合同、表格等所有类型都能达到一致的召回效果。
     1 ✓ 按文档类型分别处理：技术手册递归切分加 overlap、合同按条款边界、FAQ 按问答对整块、表格抽取结构后序列化或独立索引；高精度场景叠加 parent-child（小块检索、大块返回）
     2   直接把每篇文档作为单个 chunk 存入向量库，靠模型的长上下文能力消化，既避免了切分带来的语义断裂，也省去了为不同文档类型设计差异化策略的复杂工程。
     3   先用 LLM 给每个 chunk 写摘要替代原文入库，原文本不再参与检索，这样既能压缩索引体积，又能让检索阶段直接命中高层语义，而不必再处理冗长的原始段落。
   A: 没有万能切分策略：技术文档用递归切分（如 512 token + 50 overlap）；法律合同按段落/条款边界保留语义完整；FAQ 按问答对整体成块；表格先抽取结构再序列化为 Markdown 或单独建表索引。精度要求高的场景用 parent-child：小块用于检索命中、返回大块补充上下文。策略选择由文档类型与下…

• aws-genai-developer-pro-08  [hard]  concepts=
   Q: 开发者做基于 RAG 的客服应用，用 Bedrock FM，需处理 S3 中 50 GB 历史对话 JSON 作为检索语料。数据处理流程要抽取相关字段、移除 PII、生成嵌入向量，且要在 4 小时内低成本完成。哪个方案以最低运维开销满足？（单选）
     0   用 Lambda + Comprehend 并行处理、去 PII、调 Bedrock 生成向量，配并发与内存优化
     1   Glue ETL 跑 PII 检测脚本，SageMaker Processing 用 HuggingFaceProcessor 生成嵌入，存入 OpenSearch
     2   EMR 集群跑 Spark UDF 调 Comprehend 去 PII，Bedrock 生成向量，存入 Aurora PostgreSQL(pgvector)
     3 ✓ 用 Step Functions 编排：Comprehend 去 PII、Bedrock 生成嵌入，直接对接 OpenSearch Serverless 存向量并做相似度检索
   A: D 正确：Step Functions 可编排托管服务集成（Comprehend 去 PII、Bedrock 生成嵌入），无需运维计算集群；OpenSearch Serverless 存向量并支持相似度检索，整体无服务器、运维最省。A 大规模 50GB 用 Lambda 有时长/内存限制；B/C 需运维 EMR/Sag…

• anthropic-cca-f-09  [hard]  concepts=
   Q: 生产中终稿频繁出现无出处的声明。排查发现网页搜索与文档分析代理的输出都正确带了引用，但合成代理在合并多源发现时弄丢了“哪些来源支撑哪些结论”。最有效的架构改动是？（单选）
     0 ✓ 要求所有子代理输出结构化“声明-来源映射”，合成代理合并多源时必须保留并合并这些映射。
     1   保留全部子代理交互转写，并加一个引用解析代理在分析日志后确定归因再生成报告。
     2   加一步核验：报告生成器用语义相似度对原始来源匹配，重建声明来自哪些文档。
     3   协调器在每次交接前把来源标识前缀注入文本，报告生成时解析这些前缀重建引用。
   A: A 正确：要求所有子代理输出“结构化声明-来源映射”，合成代理在合并多源时必须保留并合并这些映射——溯源随数据走，报告生成器可直接用。B 错：完整转写+事后解析日志既重又易错。C 错：语义相似度回填引用不可靠且贵。D 错：手工前缀注入在多次合并中脆弱、难解析。

• aws-genai-developer-pro-05  [medium]  concepts=
   Q: 某企业应用用 Bedrock FM 处理分析 50–200 页技术文档。当用户处理超过 FM 上下文窗口的文档时，出现响应不一致与输出被截断。哪个方案解决该问题？（单选）
     0   固定 4,000 token 分片、20% 重叠，应用层把多分片顺序拼到 200,000 token 上下文上限后再推理
     1 ✓ 分层切分：父块 8,000 token、子块 2,000 token，用 Bedrock Knowledge Bases 内置检索按查询上下文自动选相关父块，配置重叠 token 保持语义连续
     2   语义切分：断点百分位 95%、缓冲 3 句，用 RetrieveAndGenerate 按嵌入相似度动态选最相关分片
     3   用 Lambda 借 FM tokenizer 分析 token 数，把文档切成不超上下文窗口 80% 的等长段，分段独立处理后再聚合
   A: B 正确：Bedrock Knowledge Bases 分层切分以小子块做精确检索、再以更广的父块喂给 FM，既保持每次推理在上下文窗口内又提供更完整相关上下文。Bedrock 语义切分不支持“缓冲 3 句”（有效范围为 0–1），故 C 错；A 把多个分片拼到 200k 仍可能超窗口且应用层逻辑脆弱；D 等长切分会…

########## caching|scenario   (n=4) ##########

• ai-ops-002  [hard]  concepts=
   Q: “What is ESG?” 和 “Can you explain ESG?” 语义相似但字符串不同。如何设计 Semantic Cache？它有什么风险？
     0   语义缓存通过字符串精确匹配历史 query，天然不存在误命中问题，主要风险只是存储成本随 query 规模线性增长。
     1   相似度阈值应设置得尽可能高，高阈值既能保证命中的答案绝对正确，又能同步提升命中率与节省的调用成本。
     2 ✓ 最大风险是「相似不等于可复用」：仅年份不同的两个问题高度相似但答案迥异，错误命中后用户很难察觉，因此阈值必须结合业务容忍度实测校准，不能拍脑袋设定。
     3 ✓ 应限于事实型、无状态、答案稳定的查询启用，按意图或领域分区缓存，命中附加来源标记，并对时效敏感内容设置 TTL 兜底。
     4   语义缓存适合开放对话场景，因为对话历史不影响回答内容，相同 query 在任何上下文中都可以复用同一个答案。
   A: Embedding 相似度 + 阈值直返历史答案；最大风险是“相似不等于同答”的错误命中且难以察觉——限定事实型无状态 query、分区校准阈值、TTL 兜底。

• ai-eng-064  [hard]  concepts=
   Q: 企业级 RAG 引入精确缓存（Query 哈希）与语义缓存（Embedding 相似度匹配）来降低成本与延迟。在多租户 + 严格 RBAC 场景下，缓存层会引入什么新的安全风险？应如何设计才能既省钱又不越权？（多选）
     0 ✓ 仅以 Query 文本哈希或语义向量作 Cache Key 时，低权限用户可能命中高权限用户的缓存答案，造成未经检索层过滤的越权泄露
     1 ✓ Cache Key 的生成或语义缓存的候选过滤必须绑定权限上下文（部门/用户 ID/ACL 角色）
     2   语义缓存相似度阈值设置过高会破坏 RBAC 权限隔离，因此必须把阈值固定为极低值并强制关闭所有语义匹配，改用纯关键词哈希来规避任何越权风险
     3 ✓ 即使语义缓存命中，若 ACL 无法编码进 Key/向量空间，仍须对缓存结果引用的原始文档做二次权限校验
   A: 风险：若 Cache Key 只由 Query 文本哈希或语义向量构成，低权限用户可能命中高权限用户留下的缓存答案——ACL Leakage，越权数据根本没经过检索层过滤就直达用户。设计：①权限上下文强绑定进 Key（部门/用户 ID/ACL 标签参与哈希或作为语义缓存的预过滤条件）；②语义缓存无法把 ACL 编进向量…

• ai-ops-001  [medium]  concepts=
   Q: 你的 LLM 应用存在大量重复 Query，API 成本很高。你会如何设计 LLM Response Cache？哪些请求不适合缓存？
     0   缓存键只需使用原始查询字符串本身的哈希，文本完全一致即可复用响应，模型版本与温度等参数不会影响输出的一致性，因为相同输入在任意配置下都映射到同一个确定性答案
     1 ✓ 缓存键应在大小写空白归一化后，把 prompt 版本、模型、温度等所有影响输出的变量一并纳入；带用户私有上下文的对话、实时行情类数据和有副作用的调用都不能进共享缓存；失效策略与 prompt 版本绑定并监控命中率。
     2   所有重复请求都应进入共享缓存以最大化命中率，个性化与实时性问题可以通过命中后的后处理过滤解决，因为先缓存再过滤比直接绕过缓存的成本更低且延迟更可控
     3   失效策略采用固定 TTL 定期清理即可，与 prompt 版本脱钩，因为 prompt 修改通常不会影响已缓存响应的正确性，模型对同一问题的输出在版本间保持稳定因此无需随版本失效，跨版本复用缓存只会轻微降低新鲜度而不会引入任何一致性或正确性…
   A: Key = 归一化 query + 全部影响输出的变量；私有上下文、实时数据、创意发散、副作用调用都不该进缓存；版本绑定失效策略。

• aws-genai-developer-pro-17  [medium]  concepts=
   Q: 某公司用 Bedrock 建客服助手。分析显示 40% 咨询用不同措辞问相同问题。公司要降低冗余模型调用、让语义等价问题得到一致回答且低延迟。哪个方案满足？（单选）
     0   部署 DAX 内存缓存，查询文本作分区键、响应文本作排序键，用 LIKE 过滤表达式查
     1   用 Bedrock 生成查询嵌入，MemoryDB for Valkey 存哈希集向量与响应，用 RANGE 查相似
     2 ✓ 用 OpenSearch k-NN 存查询-响应对，用近似 k-NN 找相似查询与响应
     3   用 DynamoDB 对规范化查询文本建 GSI，对入查询做词干还原后查缓存
   A: C 正确：向量嵌入表达语义，使不同措辞的等价问题可按相似度匹配；OpenSearch 支持向量 k-NN，近似 k-NN（如 HNSW）在大数据集上更快，缓存最近语义匹配避免冗余调用且返回一致缓存响应。A 的 LIKE 仅字面匹配；B 的 Valkey 哈希集+RANGE 非向量检索；D 词干还原仍偏字面，抓不住语义等…

########## context-engineering|mechanism   (n=4) ##########

• rl-context-compaction-01  [hard]  concepts=
   Q: 在针对长流程 Agent 进行强化学习（RL）训练时，随着 Agent 与环境交互轮数增加，上下文（Context Window）急剧膨胀，导致计算复杂度呈二次方增长且注意力容易分散。在策略优化中引入“上下文压缩与选择性注意力”（Context Compaction / Selective Attention）机制的核心作用是？
     0   移除所有历史 Bash 执行输出与报错日志，仅保留最初始的 System Prompt 与最新的代码差异（Git Diff）
     1 ✓ 动态提炼与压缩历史轨迹中的关键决策状态与环境反馈，在保留长跨度因果依赖的同时减少无效 Token 对模型注意力与算力的占用
     2   将长上下文直接剪裁为固定长度的滑动窗口（Sliding Window），完全放弃超出窗口范围的所有早期历史信息
     3   在推理阶段关闭所有注意力层，改用递归神经网络（RNN）单元对上下文进行无损线性记忆
   A: 正确：上下文压缩（Context Compaction）通过智能提取或总结历史交互轨迹中的关键状态（错误根因、尝试过的错误方案、关键变量定义），在保持长跨度因果依赖的同时减少无效 Token 对注意力与算力的占用（B 对）。错误：盲目移除所有历史报错日志会让 Agent 丢失迭代调试依据，易陷入重复错误死循环（A 错）…

• context-2026-109  [hard]  concepts=
   Q: 同样的一组信息，为什么仅仅改变 context 中的信息顺序，也可能影响模型表现？工程上你会如何利用这一现象设计 context layout？
     0 ✓ 注意力与 recency 效应使位置影响权重，应将最关键信息放在高注意力区并结构化布局
     1   Transformer 对所有位置一视同仁，顺序完全不影响输出，layout 仅关乎可读性
     2   只要按字母序排列就能保证模型同等关注，顺序本身无需专门设计
     3   顺序仅影响首字延迟，对生成内容的质量起不到任何实质作用
   A: A 正确：LLM 的注意力与 recency 效应使信息位置影响权重，把最关键决策信息放在高注意力区（靠近任务或重复出现）并用结构化布局，可提升利用率。B 错误：Transformer 并非对所有位置一视同仁，顺序会影响注意力分布与输出。C 错误：按字母序排列不能保证模型同等关注，与任务重要性无关。D 错误：顺序会影响…

• foundation-context-hierarchy-01  [medium]  concepts=
   Q: 在大多数大模型 API（如 OpenAI / Anthropic）中，System Prompt 与 User Prompt 的设计分工是什么？
     0 ✓ System Prompt 设定全局角色与不可违背的规则约束，User Prompt 提供当前任务输入
     1   User Prompt 拥有最高执行权限，可以随意重置 System Prompt 中的安全设定
     2   System Prompt 只能用来填写模型版本号，不能写任何自然语言规则
     3   两者在底层机制上没有任何区别，仅仅是显示名称不同
   A: System Prompt 用于设定模型的角色定位、核心行为规范、安全红线和全局约束，具有更高的指令优先级；User Prompt 则是终端用户在当前会话中提供的具体指令或待处理数据。

• ai-eng-013  [medium]  concepts=
   Q: 一个模型支持 1M token context。你的 RAG 系统可以把检索得到的 100 个 chunk 全部放进去。为什么仍然不应该简单地把所有 chunk 都塞进 context？
     0   可行，长上下文模型经过对齐训练能均匀利用全部输入内容，无关 chunk 既不会增加显著成本也不会影响答案质量。
     1 ✓ 应先经 reranking 精排、去重与压缩，只将与问题真正相关的少量 chunk 送入 context：无关内容会推高成本与延迟，还会作为噪声干扰模型定位关键证据。
     2   必须裁剪到前 10 个 chunk 以内并丢弃其余，因为长上下文模型只能有效利用输入开头位置的信息，中后部内容等于浪费。
     3   100 个 chunk 已逼近模型物理输入上限，继续塞入会直接触发长度报错，正确做法是缩短每个 chunk 的长度来腾出空间。
   A: 长上下文不等于有效利用；无关 chunk 带来成本、延迟与噪声，应 rerank/dedup/compress 后再进入 context。
