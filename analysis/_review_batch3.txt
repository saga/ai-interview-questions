
########## self-attention|mechanism   (n=17) ##########

• sebastian-raschka-2026-08-33  [easy]  concepts=
   Q: 对 QK 分数沿 Key 位置使用 softmax 后，得到的注意力权重具有什么性质？（请选择两个）
     0 ✓ 同一查询位置对所有 Key 位置的权重和为 1
     1 ✓ 权重通常非负，可作为 Value 的加权系数
     2   每个位置的权重都必须完全相同
     3   softmax 后的权重仍然是未归一化的任意实数
   A: softmax 将同一查询对应的分数转成非负且和为 1 的权重，因此输出是 Value 向量的加权和。

• causal-mask-cross-attn  [hard]  concepts=
   Q: 在 Transformer Decoder 的自注意力层中使用因果掩码保持自回归特性。关于该机制的运行逻辑与工程处理方式包括哪些？（多选）
     0 ✓ 在 Softmax 计算之前将注意力得分矩阵中未来位置对应的值设置为 -∞（或非常小的负数如 -1e9）
     1 ✓ 经过 Softmax 之后未来位置的注意力权重归零确保当前位置 i 只能聚合位置 ≤ i 的上下文信息
     2   该掩码机制使得 Decoder 在训练阶段必须退化为逐 Token 的串行计算无法进行全序列并行训练
     3 ✓ 该掩码仅施加在 Decoder 的自注意力层；在交叉注意力层中 Decoder 可以关注 Encoder 的整个源序列
   A: A 正确：Softmax 前 Score 设为负无穷使未来权重为零。B 正确：当前位置只聚合 ≤i 的上下文信息。C 错误：因果掩码恰好使 Decoder 可以全序列并行训练而非退化为串行。D 正确且常被忽视：因果掩码仅施加在 Decoder 自注意力层；Cross-Attention 中 Decoder 可以关注 E…

• transformer-perm-01  [hard]  concepts=
   Q: 假定输入序列由 N 个 Token 向量组成，表示为矩阵 X ∈ R^{N×d}。在不添加任何位置编码的前提下，将 X 经过任意置换矩阵 P（正交置换矩阵）变为 X' = PX。设 Standard Scaled Dot-Product Self-Attention 为 softmax(QKᵀ/√d_k)V，其中 Q=XW_Q, K=XW_K, V=XW_V。下列关于变换后输出的说法，正确的是？
     0   输出完全保持不变，即 Attention(X') = Attention(X)
     1 ✓ 输出与原输出保持相同的行置换，即 Attention(X') = P · Attention(X)
     2   输出的 Softmax 注意力权重矩阵保持不变，但值向量 V 的顺序被打乱
     3   由于 QKᵀ 包含转置操作，置换矩阵 P 无法被抵消，导致输出变为完全无规律的随机数值
   A: 正确答案 B。设 Q'=PXW_Q=PQ, K'=PK, V'=PV。由于 P 是正交矩阵（P^T=P⁻¹），softmax(PAP^T)=P·softmax(A)P^T，代入推导可得 Attention(X')=P·Attention(Q,K,V)，即 Self-Attention 具有行置换等变性。这证明没有位置编…

• dl-04  [hard]  concepts=
   Q: 简述 Transformer 中自注意力（self-attention）机制的计算过程，并说明它相比 RNN 的优势。
     0 ✓ 以 Q、K 点积衡量 token 间相关性，除以 sqrt(d_k) 缩放后经 softmax 得到权重，再对 V 加权求和，各位置可并行计算
     1   以 Q、K 的逐元素相乘衡量相关性，结果不经任何归一化直接作为权重对 V 加权求和
     2   先对 V 做 softmax 归一化得到分布，再用它对 Q、K 加权，且必须沿时间步顺序串行计算
     3   用 Q 与 K 之间的余弦相似度构造权重矩阵，sqrt(d_k) 的作用是把序列长度归一到 0 到 1 之间
   A: 考察自注意力机制的计算流程与相比 RNN 的优势。Q·Kᵀ/√d 经 softmax 得权重后对 V 加权求和；每个位置的输出可直接关注任意距离的 token（O(1) 路径长度），且训练时可全并行无需按时间步展开。RNN 的序列化依赖使其无法并行且长程信息逐步衰减。

• sebastian-raschka-2026-08-42  [hard]  concepts=
   Q: 关于自注意力中投影矩阵的作用，哪些说法正确？（请选择两个）
     0 ✓ Wq 和 Wk 共同决定用于匹配的表示空间
     1 ✓ Wv 决定被注意力权重汇聚的内容表示
     2   投影矩阵只负责保存 token 的原始整数 id
     3   训练投影矩阵不会改变注意力模式
   A: 不同投影矩阵把输入映射到用于匹配和内容汇聚的不同子空间。训练这些矩阵使模型学会任务相关的相似性和信息读取方式。

• sdpa-scaling-mech  [medium]  concepts=
   Q: 在自注意力计算中缩放因子 1/√d_k 具有关键作用。假设 q 和 k 的各分量独立同分布且均值为 0、方差为 1。下列关于该缩放因子的分析，正确的有哪些？（多选）
     0 ✓ 点积 q·k = Σq_i·k_i 的均值为 0，方差随着 d_k 的增加而呈线性增长（方差等于 d_k），这是独立同分布分量的基本统计性质
     1 ✓ 若不使用缩放因子，当 d_k 较大时点积结果的绝对值数量级变大会导致 Softmax 函数进入梯度极小的饱和区，进而引发梯度消失
     2 ✓ 乘以缩放因子 1/√d_k 能将点积结果的方差重新调整归一化为 1，维持 Softmax 输入在合理的数值范围内，避免数值溢出
     3   引入缩放因子的主要目的是为了降低矩阵乘法 QKᵀ 的浮点计算复杂度，从而让大维度下的注意力计算在同等算力预算下跑得更快，并显著减少显存峰值占用
   A: A 正确：独立变量点积的方差为各分量方差之和即 d_k。B 正确：不缩放时大方差使 Softmax 进入饱和区引发梯度消失。C 正确：乘以 1/√d_k 将方差归一化回 1。D 错误：缩放因子是标量除法不改变 FLOPs 量级。

• mask-parallel-training  [medium]  concepts=
   Q: 在 Transformer Decoder 的 Self-Attention 层中，训练阶段必须引入因果掩码（Causal Mask）。关于该机制的工作原理及其工程作用，下列说法正确的有哪些？（多选）
     0 ✓ 在计算注意力 Softmax 前将掩码矩阵中未来 Token 位置的 Score 设置为 -∞
     1 ✓ 经过 Softmax 运算后未来位置的注意力权重精确变为 0，防止模型在预测第 i 个 Token 时偷看后续 Token
     2 ✓ 使得 Decoder 在训练阶段无需像推理时那样逐字自回归循环生成，而是可以一次性并行输入全序列进行高效训练（Teacher Forcing）
     3   该因果掩码的主要目的是屏蔽 Padding Token，以保证 Batch 内不同长度序列的计算对齐
   A: A 正确：Softmax 前将未来位置 Score 设为负无穷使 e^{-inf}=0。B 正确：Softmax 后未来位置权重精确为零防偷看。C 正确：因果掩码使得 Teacher Forcing 并行训练成为可能——全序列一次输入而非逐字自回归循环。D 错误：因果掩码屏蔽的是未来 Token 而非 Padding …

• pe-perm-equivariance  [medium]  concepts=
   Q: 在标准 Self-Attention 中，若不对输入 Token 序列叠加位置编码，将输入的 Token 序列顺序随机打乱后再送入该模块，其输出会发生怎样的变化？
     0 ✓ 输出的 Token 向量集合数值保持对应关系，但其排列顺序会随输入的打乱而相应打乱，即具有置换等变性（Permutation Equivariance）
     1   输出的每一个位置的向量数值及排列顺序都完全保持不变，即具有置换不变性（Permutation Invariance），模型天然能记住原始词序
     2   输出结果完全失效，各个向量数值随机变化且不再保留任何语义关联，这说明 Self-Attention 在面对乱序输入时会彻底崩溃
     3   Attention 权重矩阵退化为单位矩阵（Identity Matrix），意味着每个 token 只能看到自己而完全无法与上下文交互
   A: 正确答案 A。Self-Attention 具有置换等变性（Permutation Equivariance）：输出的 Token 向量集合数值保持对应关系，但排列顺序随输入打乱而相应打乱。这意味着无位置编码时模型无法区分 Token 在序列中的绝对或相对位置——这正是需要显式注入位置编码的根本原因。

• softmax-scaling-01  [medium]  concepts=
   Q: 在注意力机制中若移除缩放因子 1/√d_k（即直接使用未缩放的点积 QKᵀ），当 d_k 较大时网络在训练初期极易出现梯度消失。以下哪些数学或工程解释是合理的？（多选）
     0 ✓ 假设 q 和 k 的各分量为独立均值 0 方差 1 的随机变量，则点积 q·k 的均值为 0，方差为 d_k
     1 ✓ 当 d_k 很大时，点积的数值方差随之大幅增加，导致输入到 Softmax 函数的元素极大或极小
     2 ✓ Softmax 在输入值绝对值较大且极不均匀时输出趋近 one-hot 向量，饱和区的梯度接近于 0，使得反向传播时梯度严重衰减
     3 ✓ 乘以缩放因子 1/√d_k 可以将点积结果的方差重新归一化为 1，从而维持 Softmax 处于梯度敏感区
   A: 四项均正确。A：q,k 各分量独立均值0方差1，则点积方差为 d_k。B：d_k 大时点积方差大幅增加导致输入到 Softmax 的值极大或极小。C：Softmax 在输入绝对值大且不均匀时输出趋近 one-hot 且饱和区梯度接近零。D：乘以 1/√d_k 将方差归一化回 1 维持梯度敏感区。

• ai-fund-004  [medium]  concepts=
   Q: Multi-Head Attention 为什么需要多个 Head？单个 Attention 不够吗？
     0   多头通过把总参数量增加数倍来换取表达能力，计算成本随 Head 数成比例上升
     1 ✓ 多个 Head 在不同的表示子空间中学习不同类型的关系，弥补单头只能输出一种加权平均的局限
     2   每个 Head 负责处理序列中互不重叠的一段，各段输出拼接后构成完整表示
     3   多头机制的作用是抑制过拟合，Head 数量越多模型的泛化能力单调提升
   A: 多头 = 多个表示子空间并行建模不同关系（语法/语义/位置等），单头只有一种混合视角；代价近似不变而表达能力更强。

• ai-fund-002  [medium]  concepts=
   Q: 请解释 Self-Attention 中 Query、Key、Value 分别代表什么，以及 Attention 最终是如何计算出来的。
     0 ✓ Q、K、V 由输入经三组不同的线性投影得到，attention 输出为 softmax(QK^T/sqrt(d_k))V，即用归一化后的权重对 V 进行加权求和
     1   Q、K、V 是同一个输入矩阵的简单三份复制，attention 直接对原始输入序列做等价加权平均，完全跳过了任何独立的投影与归一化计算步骤
     2   Q 与 V 的点积经 sigmoid 归一化得到权重，再用该权重对 K 加权求和，且全程不引入任何缩放因子以保持数值简单
     3   Q 与 K 计算相似度后不做任何缩放直接取最大值索引，输出为对应的 V 向量，认为这样能够避免 softmax 带来的计算开销
   A: Q/K/V 来自三组线性投影；Q·K 打相似度 → 缩放 → softmax 归一化 → 对 V 加权求和。

• sebastian-raschka-2026-08-47  [medium]  concepts=
   Q: 注意力输出是 Value 的加权和时，以下哪些性质通常成立？（请选择两个）
     0 ✓ 不同查询位置可以得到不同的上下文向量
     1 ✓ 权重决定每个 Value 对当前输出的贡献大小
     2   输出必然等于某一个 Value 的未经加权复制
     3   所有查询位置必须共享完全相同的上下文向量
   A: 每个查询位置得到一个由所有可见 Value 按权重汇聚的上下文表示；权重分布不同会产生不同的信息读取结果。

• sebastian-raschka-2026-08-43  [medium]  concepts=
   Q: 把单个序列的注意力实现扩展到 batch 时，哪些变化通常是必要的？（请选择两个）
     0 ✓ 让 Q/K/V 带有 batch 维并进行批量矩阵乘法
     1 ✓ 对 padding token 使用 padding mask 以避免其影响权重
     2   把不同样本的 token 直接拼成一个无边界序列
     3   batch 维会让每个样本自动共享同一条注意力分布
   A: 需要在张量中加入 batch 维并保持矩阵乘法按每个样本独立进行；如果序列有 padding，还需要相应的 padding mask。

• sebastian-raschka-2026-08-36  [medium]  concepts=
   Q: 多头注意力相对单头注意力的主要优势有哪些？（请选择两个）
     0 ✓ 不同头可以在不同表示子空间中关注不同关系
     1 ✓ 各头输出通常拼接后再由输出投影整合
     2   多头注意力保证每个头关注完全不同的 token
     3   多头注意力把序列复杂度自动降为线性
   A: 不同头使用不同的投影子空间，可以同时捕捉不同类型的关系；各头结果拼接后再经过输出投影整合。

• sebastian-raschka-2026-08-34  [medium]  concepts=
   Q: 自回归语言模型使用因果注意力掩码时，以下哪些行为是正确的？（请选择两个）
     0 ✓ 位置 i 可以关注自己和所有更早位置
     1 ✓ 未来位置的分数应在 softmax 前被屏蔽
     2   因果掩码让训练时每个位置都能读取完整答案
     3   因果掩码只用于编码器而不用于自回归解码器
   A: 位置 i 只能关注位置 i 及其之前的 token，不能读取未来 token。实现通常在 softmax 前把未来位置的分数设为负无穷。

• sebastian-raschka-2026-08-32  [medium]  concepts=
   Q: 缩放点积注意力中除以 $\sqrt{d_k}$ 的主要作用有哪些？（请选择两个）
     0 ✓ 控制点积分数随 Key 维度增长的尺度
     1 ✓ 帮助 softmax 避免过早饱和并保留有效梯度
     2   把注意力复杂度从平方级变成线性级
     3   让 Value 向量自动包含位置信息
   A: 当 Key 维度增大时，QK 点积的方差会随维度增大。缩放可以让 softmax 输入保持在更适合梯度学习的范围，减少过度尖锐导致的梯度问题。

• sebastian-raschka-2026-08-31  [medium]  concepts=
   Q: 关于自注意力中的 Query、Key 和 Value，以下哪些说法正确？（请选择两个）
     0 ✓ Query 与 Key 的点积用于计算相关性分数
     1 ✓ Value 是按注意力权重汇聚的内容表示
     2   Query、Key、Value 必须来自三个不同序列
     3   注意力权重直接等于 Value 向量本身
   A: Query 表示当前位置要查找什么，Key 表示每个位置可被匹配的特征，Value 是被加权汇聚的内容。它们通常由同一输入经过不同线性投影得到。

########## agent-fundamentals|system-design   (n=10) ##########

• sebastian-raschka-2026-08-99  [hard]  concepts=
   Q: 在本地运行 Coding Agent 时，哪些系统边界必须显式设计？（请选择三个）
     0 ✓ 限制文件系统和命令执行范围
     1 ✓ 为网络访问和凭证使用设置边界
     2 ✓ 为写入和部署动作配置审批或回滚
     3   本地运行意味着不需要沙箱和审计
   A: 本地模型并不自动安全，需要限制文件、网络和命令权限，设置预算与审批，同时记录执行轨迹。

• long-running-agent-003  [hard]  concepts=
   Q: 在 Agent 自动化开发系统的环境脚手架中，通常需要维护一份包含数百项功能的断言清单（Feature List）。关于该清单的数据结构选型与约束机制，下列哪些说法是合理的？
     0 ✓ 采用结构化 JSON 格式比 Markdown 文本更具严格的 Schema 约束，能有效降低 LLM 误删或篡改测试项的概率
     1 ✓ 清单中每项功能需包含明确的端到端步骤与布尔断言字段，以便 Agent 严格按照单一粒度推进并客观更新状态
     2   为防止 Agent 强行修改字段以规避失败测试，应严格禁止 Agent 具备写入该清单文件的任何文件系统权限
     3 ✓ 将所有测试初始状态均设为未通过，可为后续接替的 Agent 实例提供全局拓扑路线与明确的终止条件依据
   A: A、B、D 正确。结构化 JSON 可通过 Schema 降低误删和格式破坏；每项应包含可验证步骤与布尔状态，以支持原子推进；全部初始化为未通过能为后续会话提供全局进度和终止条件。C 错误：编码 Agent 在验证通过后需要更新状态字段，正确做法是限制可修改字段、用校验器验证变更，而非禁止其写入整个清单。

• long-running-agent-002  [hard]  concepts=
   Q: 在多上下文窗口的 Agent Harness 架构中，设计专门的初始化 Agent（Initializer Agent）与后续的编码 Agent（Coding Agent）进行分阶段协作，其核心工程考量包括哪些？
     0 ✓ 在初始阶段解耦需求拆解与代码实现，避免编码 Agent 因缺乏清晰的任务边界而试图一次性构建整个系统
     1 ✓ 由初始化 Agent 统一生成确定性的环境启动脚本与测试脚手架，降低后续编码 Agent 搭建运行环境的 token 开销
     2   将系统提示词完全隔离，使初始化 Agent 仅拥有文件系统读写权限，而编码 Agent 仅拥有代码编译与运行权限
     3 ✓ 预先构建结构化的全量功能清单并标记初始状态，防止后续 Agent 因局部进度完成而提前误判整体项目已结束
   A: A、B、D 正确。初始化阶段应拆解需求、准备环境脚本并建立带状态的全量功能清单，使后续编码 Agent 以小粒度、可验证任务推进。C 错误：编码 Agent 仍需读写源码、配置和进度文件，不能简单剥离文件系统写权限；更合理的做法是按工具和路径实施最小权限。

• skillevo-scaling-complementarity-01  [hard]  concepts=
   Q: 在评估“模型参数规模（Model Scale）”与“演进技能（Evolved Skills）”对 Agent 系统综合性能的提升效果时，关于两者的系统设计取舍与互补关系，以下哪些结论是正确的？
     0 ✓ 演进技能与模型规模具有互补性，通常基座模型能力越强，从高质量演进技能中获得的性能增益越大
     1 ✓ 引入高质量演进技能可以在一定程度上弥补模型规模不足，使较小模型配备技能后超越无技能的更大模型
     2   随着基座模型参数量扩大，模型泛化能力会自动涵盖领域工作流，因而超大模型完全不需要演进技能
     3   技能演进仅在小模型上产生效果，大型模型由于存在严重指令过拟合，加载外部技能反而导致性能下降
   A: A 正确：模型本身推理能力越强，遵循和发挥高质量技能指令的能力就越强，增益随之扩大。B 正确：技能显式补充了领域特定流程与避坑指南，使配备技能的小模型可以在特定场景下击败没有流程提示的大模型。C 错误：超大模型仍需领域流程知识，泛化能力不会自动涵盖具体工作流。D 错误：技能演进对大模型同样有效，不存在“指令过拟合导致加…

• selfharness-vs-optimizer-01  [hard]  concepts=
   Q: 假设某团队正在规划构建一套自动化的 AI Agent 研发迭代系统。在评估“基于强模型（如 Teacher LLM）的 Meta-Harness 优化器”与“基于目标模型自我闭环（Self-Harness）”两种系统架构方案时，下列哪项分析切中了前者的主要系统性缺陷？
     0 ✓ 强模型生成的 Harness 优化策略往往基于其自身优异的理解力，容易忽略目标基座模型特有的行为偏好与能力短板
     1   强模型在处理并发 Trace 分析时会导致系统吞吐量剧烈下降，无法支撑大规模软件工程基准测试的并行评估
     2   强模型优化器需要对目标基座模型的内部参数权重进行实时梯度反向传播，带来了不可接受的算力开销与显存瓶颈
     3   强模型生成的系统提示词无法适配目标模型的 Tokenizer 编码规则，容易在运行过程中引发严重的解码异常
   A: A 正确：Meta-Harness 的核心痛点在于“能力不匹配”。强模型作为优化器时，倾向于假设目标模型具备与其相当的指令遵循和隐式逻辑推理能力，其设计的 Harness 可能在较弱或不同架构的目标模型上失效，无法精准修复目标模型独特的工具调用错乱或循环停滞等瓶颈。B 错误：吞吐量瓶颈可通过工程并发扩容解决，并非架构范…

• agentdual-harness-simplicity  [hard]  concepts=
   Q: 在 Agent 系统的演进过程中，开发者需要根据底层大语言模型（Base Model）能力升级来重新审查并精简 Harness（脚手架）复杂度。关于“模型基座能力提升对 Agent Harness 设计的影响”，下列哪些说法是合理的？
     0 ✓ 当新一代基座模型原生具备更强的长上下文保持与自我纠错能力时，可以适当精简或移除频繁的强制上下文重置机制。
     1 ✓ 即使基座模型的基础编码能力大幅提升，高层的需求规划（Planner）与独立的质量评估（Evaluator）依然能带来显著提升。
     2 ✓ Harness 中设计的每一个复杂约束都暗示了底层模型能力的某种缺陷，应随模型迭代持续进行简化验证与“减法测试”。
     3   基座模型的上下文窗口扩大后，Multi-Agent 协作与工具调用架构将被完全淘汰，单一 Prompt 即可解决所有复杂工程问题。
   A: A、C 项（脚手架减法原则）：Agent Harness 本质是对当前模型缺陷的补偿（如上下文焦虑、遗忘、自我盲目）。当基座模型原生修正这些缺陷（如长上下文逻辑推演与自我审查能力升级）后，过多过度拆解（微型 Sprint 分解、频繁强制 Reset）会引入不必要的 Token 开销、系统延迟与编排复杂度。团队应遵循“做…

• oreilly-radar-2026-08-06  [medium]  concepts=
   Q: 为什么 Agent 连接多个服务的主要价值通常来自跨应用工作流，而不是把单个应用改成聊天界面？（请选择两个）
     0 ✓ 跨应用工作流可以传递状态并编排连续动作
     1 ✓ 价值在于完成单一系统无法独立完成的任务
     2   只需把一个应用的按钮改成自然语言
     3   连接越多服务就一定越可靠
   A: 跨应用任务需要在不同系统间传递状态、权限和结果，组合式 Agent 可以完成单一应用无法独立完成的流程；单应用问答往往只是替换成熟 UI，收益有限。

• oreilly-radar-2026-08-01  [medium]  concepts=
   Q: 企业部署大量自主 Agent 后，哪些问题应成为每个非人类身份的最小治理记录？（请选择两个）
     0 ✓ 确认该身份代表的资产和责任归属
     1 ✓ 记录权限范围以及生命周期失效条件
     2   只记录模型名称和调用次数
     3   只记录最终输出，不记录执行主体
   A: 最小记录应回答‘它是什么、谁负责、能访问什么、何时应失效’。缺少身份归属和生命周期，权限审查、轮换和撤销都会依赖猜测。

• aws-waf-lens-2026-08-01  [medium]  concepts=
   Q: 一个 Agent 同时负责检索、审批、发信和修改生产数据，错误很难定位。哪些架构改进最关键？（请选择两个）
     0 ✓ 拆成原子能力并为输入、输出建立可测试契约
     1 ✓ 为不同能力分别配置权限和遥测边界
     2   继续扩大一个 Agent 的职责以减少通信开销
     3   只增加 system prompt 中的安全提醒
   A: 应按单一、原子的能力拆分任务，为每个能力定义输入校验和结构化输出契约，并分别配置权限、遥测和失败处理。扩大上下文或只加一句谨慎提示不能降低组合式失败面。

• agent-arch-model-harness  [medium]  concepts=
   Q: 关于 Agent 的 Model（模型）与 Harness（外部编排代码）的边界，以下哪些说法是正确的？
     0 ✓ Harness 负责把模型输出解析为结构化动作、校验参数，并维护状态与重试
     1 ✓ 副作用（写库、调用外部 API）必须由 Harness 落地，模型本身不应直接执行
     2 ✓ 工具选择的决策可以由 Harness 结合策略与状态在代码中完成，不必硬编码在模型权重里
     3   模型应当直接执行工具副作用（如写数据库），Harness 只负责把结果展示出来
     4   Model 与 Harness 只是同一事物的两种叫法，没有实质区别
   A: 模型负责理解意图、生成下一步动作与工具参数；Harness 负责把模型输出解析为结构化动作、校验参数、执行副作用、维护状态与重试。副作用（写库、调用外部 API）必须由 Harness 落地，模型本身不应直接执行。工具选择的决策可以、也经常由 Harness 结合策略与状态在代码中完成，并非必须在模型权重里硬编码。模型…

########## agent-fundamentals|tradeoff   (n=9) ##########

• ai-eng-001  [easy]  concepts=
   Q: 一个系统需要依次执行“读取 PDF → 提取文本 → 清洗文本 → 写入数据库 → 生成摘要”，每一步的输入和输出都已经确定，没有运行时决策。你会把它设计成 Agent，还是普通 Workflow？为什么？
     0 ✓ 采用确定性 Workflow 编排五步流水线：步骤与顺序固定、无运行时决策时引入 Agent，只会徒增不可预测性、成本与调试负担
     1   实现成自主 Agent，由模型在每次运行时自行决定下一步动作，以最大灵活度应对未来可能的流程变化
     2   两种形态并无实质差别，只要最终输出正确，架构不会影响可靠性与排查效率，因此用自主 Agent 替换固定流水线不会带来任何可观测性或回归测试上的损失，二者在工程上完全等价，选择哪种只是风格偏好
     3   做成 Agent 并辅以人工抽查兜底，因为固定流水线无法对每步的输入输出契约做单元测试验证
   A: 流程步骤与顺序固定、无运行时决策时应选确定性 workflow；Agent 的自由度在此只会带来不可预测性、成本与调试负担。

• agentic-53  [easy]  concepts=
   Q: 以下哪种场景最不应该使用 Agent，而应该使用传统固定 workflow？
     0   B 线舆情摘要：主题虽不可预测，但可以通过预设模板穷举所有分析路径
     1   C 线工单分流：语义分支多变，只有交给自主 Agent 才能保证分流准确
     2 ✓ A 线银行对账：流程固定、步骤可枚举，高合规场景要求可靠、可测试、可审计，Agent 的自由度在这里反而构成风险与成本
     3   D 线研究报告：问题空间开放、分支无法预先穷举，应固化为一套静态脚本逐章执行
   A: 流程固定且高合规的场景，workflow 更可靠、可测、可审计且便宜；Agent 的灵活性在此反而是风险。Agent 适合开放、动态、分支不可预知的任务。

• hil-vs-auto-loop  [hard]  concepts=
   Q: 在高吞吐量的 AI Agent 辅助软件工程体系中完全无人工干预的自动化循环往往会导致代码库快速退化与代码垃圾堆积。为了在保持高交付并发的同时维持生产级代码质量以下哪些工程取舍是合理的？（多选）
     0 ✓ 人类工程师需主导关键设计与技术品味不将核心架构决策完全委托给 Agent
     1 ✓ 在 Spec 制定与代码实现阶段引入独立的、不同模型族的 Agent 进行对抗性审查
     2   放弃人类对 Pull Request 的控制与合并权限由 Agent 相互审查并直接全自动合入主干
     3 ✓ 要求 Agent 在真实测试环境中校验动态运行行为而非仅通过检查静态文本或验证 Mock 来断言任务成功
   A: A 正确 人类主导关键设计与技术品味不将核心架构决策完全委托给 Agent。B 正确 引入不同模型族进行对抗性审查交叉消除单一模型的系统性偏差。C 错误 放弃人类合并权限会导致未经人工审核的代码直接进入主干风险极高。D 正确 要求在真实测试环境中校验动态运行行为而非仅通过静态检查或 Mock 断言。

• agentic-43  [hard]  concepts=
   Q: Agent 是否应该被允许修改自己的 system prompt？请从能力与风险两方面论证，并给出你认为合理的折中方案。
     0   允许 Agent 直接修改任意条款并立即生效，这样自我改进速度最快、收益最大。
     1 ✓ 修改先写入提案区，经规则校验（安全条款列为不可触碰的白名单禁区）与人工审批后版本化发布，全程保留回滚与审计记录，可修改范围之外一律锁定。
     2   持久化注入的危害仅限于当前对话轮次，对话结束后恶意写入的指令会自动消失，因此无需把提示词修改纳入任何审计或版本管理流程，也无需设置白名单禁区，提示词本身并没有持久化风险。
     3   系统提示词是运行时可随意调整的普通配置，修改它不会影响安全约束的有效性。
   A: 考察自指修改的风险与受控折中方案。风险：一次错误修改被永久固化并放大（提示词是 Agent 的'基因'）；攻击者可通过注入诱导持久化恶意指令。折中：修改写入提案区经规则校验（安全条款白名单禁区）与人工审批后版本化发布，全程保留回滚与审计记录。

• agent-arch-localized  [hard]  concepts=
   Q: 关于 "Localized Harness"（把控制逻辑本地化到编排代码）的做法，以下哪些是它的主要动机或特征？
     0 ✓ 把循环、重试、权限校验、状态管理等确定性控制流放在代码侧，而不是全部委托给模型自由发挥
     1 ✓ 让 Agent 行为可预测、可测试、可观测，并降低对模型「自觉遵守规则」的依赖
     2 ✓ 与之相反的做法（让模型自行决定何时结束、何时调用工具、把业务逻辑都塞进 System Prompt）往往更难调试与保障可靠性
     3   让模型自己决定何时结束循环、何时调用工具以减少代码量，并认为模型天然比确定性代码更可靠，因此应将重试、权限校验与状态管理等控制流全部下放给模型自由发挥，代码侧只负责把最终结果展示出来
     4   Localized Harness 会降低系统可靠性，应尽量避免
   A: Localized Harness 主张把循环、重试、权限校验、状态管理等确定性控制流放在代码侧，而不是全部委托给模型自由发挥。动机是让 Agent 行为可预测、可测试、可观测，并降低对模型「自觉遵守规则」的依赖。与之相反的做法（让模型自行决定何时结束、何时调用工具、把业务逻辑都塞进 System Prompt）往往更…

• claude-blog-2026-08-02  [medium]  concepts=
   Q: 一个 Agent 会从用户对话中自动提炼长期记忆，而记忆错误可能导致后续错误决策。上线前应增加哪些控制？（请选择两个）
     0 ✓ 对高影响记忆启用人工或独立审核
     1 ✓ 为记忆更新保留版本化和回滚能力
     2   只把采样温度设置为零
     3   只更换长期记忆的存储引擎
   A: 高影响的记忆写入应经过人工审核或独立验证，并保留版本和回滚能力；只依赖模型自我判断不能提供足够的治理边界。

• aws-waf-lens-2026-08-03  [medium]  concepts=
   Q: 为什么不应让所有 Agent 动作都采用同一种人工审批策略？哪些设计更合理？（请选择两个）
     0 ✓ 低风险且可逆的动作可以在规则内自动执行
     1 ✓ 高影响或不可逆动作应审批并设置超时和升级
     2   所有动作都人工批准，才能保证系统可靠
     3   由模型自行决定是否需要人工，不设外部规则
   A: 审批等级应匹配动作的风险、影响和可逆性。低风险可逆操作可以自动化，高影响或不可逆操作需要审批、超时和升级路径；统一全审批会形成瓶颈，统一全自动则放大事故。

• ai-eng-002  [medium]  concepts=
   Q: 一个客服系统有 20 个固定步骤，其中 3 个步骤需要根据用户意图选择不同分支。你会把整个系统做成 Agent，还是 Workflow + LLM 决策节点？为什么？
     0   整体改造为自由 Agent，二十个步骤全部交由模型在运行时自主规划调度，一步到位获得灵活性
     1   三处分叉点继续沿用关键词规则并扩充词典，任何 LLM 参与决策都会破坏系统的确定性
     2 ✓ 保持 Workflow 显式编排主干流程，仅在三个语义分叉点嵌入受限的 LLM 决策节点（限定可选分支集合），兼顾可靠性与灵活性
     3   将二十个步骤压缩为一次端到端的 LLM 调用，彻底消除多步编排带来的额外开销
   A: 大多数步骤确定时保持显式编排，只在需要语义判断的分叉点引入 LLM 决策节点，兼顾可靠性与灵活性。

• ai-agent-001  [medium]  concepts=
   Q: 一个客服系统主要回答企业知识库中的固定问题，偶尔需要查询订单状态。你会设计成纯 RAG、Workflow 还是 Agent？为什么？
     0 ✓ Workflow 显式编排 + RAG 节点 + 受控工具的混合架构：高频 FAQ 走固定的检索生成流程，订单与工单查询注册为参数受限的结构化工具，前端配轻量意图分流，仅复杂场景进入受控多步处理
     1   打造完全自主的自由 Agent，让模型对每个请求自主规划多步行动，以最大灵活度应对一切变化
     2   把订单与工单库切片向量化并入 RAG 语义召回，用检索替代专用的结构化查询工具
     3   单一纯 RAG 管线覆盖全部需求，意图分流与工具注册在此规模下属过度设计，因为所有客服问题最终都可归结为相似度召回，用一套静态向量库就能同时解决 FAQ 与订单查询，无需引入任何受控工具或编排逻辑，RAG 本身已具备完整能力，拆分路由只会制…
   A: 高频 FAQ 走固定 RAG 流程、低频查单注册成受控工具，Workflow 显式编排兜底；自由度只在流程无法枚举时才值得买。

########## observability|scenario   (n=10) ##########

• obs-06  [hard]  concepts=
   Q: 在 LLM 应用中，仅监控 Token 消耗与延迟等技术指标，是否足以评估业务质量？还需补充哪些质量维度的可观测性？
     0 ✓ 需补充基于黄金集的离线评估，衡量回答正确性与召回，离线评测不可或缺
     1 ✓ 需引入 LLM-as-Judge 或人工抽检，评估回答的有用性与安全性，质量需主动度量
     2   仅监控 Token 与延迟已足够，质量可通过用户投诉被动发现，技术指标即业务质量
     3   质量指标无需与技术指标关联，可独立监控互不影响，二者无需打通
   A: 技术指标无法反映回答是否正确、有用、安全；需补充基于黄金集的离线评估、在线 LLM-as-Judge 与人工抽检的质量指标。

• agent-observability-01  [hard]  concepts=
   Q: 在建立 LLM Agent 系统的可观测性（Observability）与成本监控体系时，运维团队最初仅按“API 请求次数（Request Counts）”和“业务功能模块”对成本进行监控，结果发现请求次数极少的某辅助功能模块却贡献了全系统 70% 的 Token 支出。关于 LLM 系统的成本归因与指标设计，下列说法正确的有？（多选）
     0 ✓ LLM API 的成本核心驱动因素是单次请求的 Token 数量（尤其是由长 Context 导致的 Input Token），而非单纯的 API 调用次数
     1 ✓ 成本监控指标必须深入到 Token 粒度，记录每次请求的 Input/Output Token 数，并结合任务类型（Task Type）和功能标签进行聚合分析
     2 ✓ 调用频次低但单次运行时间长、上下文极大的 Agent 任务（如全库代码规划或长文档分析），其 Token 消耗量可能远超高频但短上下文的交互任务（如拼写检查）
     3   为了准确评估成本，只要监控输出 Token（Output Tokens）的数量即可，因为各大供应商的 Output Token 单价比 Input Token 更高，决定了绝大部分账单金额
   A: 成本驱动为Token尤其长Context、需Token粒度按Task聚合、长上下文低频远超高频短上下文、D称仅监控输出错误。

• claude-blog-2026-08-28  [hard]  concepts=
   Q: 一个监控系统按异常程度分三档：低级异常只记录，中级异常允许只读诊断，高级异常才能提出受审批约束的修复。这样设计的主要目的是什么？（单选）
     0   所有异常都立即自动生产部署
     1 ✓ 按严重度逐步增加权限，修复行动仍需经过既有门禁
     2   严重异常只记录不诊断
     3   所有阈值都执行同一动作
   A: 分级把检测和行动权限解耦：确定性规则先识别异常，较低等级只积累证据，中等级允许诊断，只有高等级才允许通过预批准 runbook 或 PR 提出行动，从而控制误报和自动化风险。

• aws-genai-developer-pro-16  [hard]  concepts=
   Q: 某公司推荐系统跑在 EC2 上，调用 Bedrock FM 分析用户行为生成个性化推荐。系统间歇性出问题：部分推荐不符合用户偏好。公司要可观测方案监控运维指标、对照基线识别性能劣化模式，并在 FM 行为偏离预期时 10 分钟内产出含关联数据的告警。哪个方案满足？（单选）
     0   配 CloudWatch Container Insights，设延迟告警，用 EMF 加自定义 token 指标，建看板
     1   用 X-Ray 追踪，CloudWatch Logs Insights 查错误模式，CloudTrail 监控 Bedrock 调用，QuickSight 看板
     2 ✓ 启用 CloudWatch Application Insights，用 EMF 发布推荐质量/token/延迟自定义指标，开异常检测，Logs Insights 做日志模式分析
     3   用 OpenSearch Observability 插件，Kinesis 摄入指标日志，PPL 查询分析行为模式，实时看板
   A: C 正确：CloudWatch Application Insights 监控应用与底层 EC2 资源，持续分析指标/日志、基于历史模式检测异常并把指标异常与日志错误关联成“问题”事件；EMF 发布推荐质量/token/延迟自定义指标，异常检测识别偏离基线的偏差，并可生成含关联诊断信息的 SNS 通知（<10 分钟）。…

• sebastian-raschka-2026-08-69  [medium]  concepts=
   Q: 推理模型上线后，为了定位成本和延迟回归，哪些指标值得分开监控？（请选择三个）
     0 ✓ 按努力度和任务难度分桶的推理 token
     1 ✓ 采样、验证和重试次数
     2 ✓ TTFT、端到端延迟与验证耗时
     3   只记录请求总数而不记录推理预算
   A: 总请求数不能解释推理模式变化，应拆分思考 token、候选/重试次数、验证耗时以及不同努力度和任务难度的质量。

• ai-engineering-003  [medium]  concepts=
   Q: 一个 Agent 线上出现错误回答，你需要哪些 Trace 信息才能判断问题发生在哪一步？
     0   继续只记录最终的用户可见回答，中间调用过程属于性能损耗；不应采集和存储，留白即合规且最省资源，少存即安全，过程无关紧要，无需留痕
     1   只需记录每次调用的耗时数字即可满足排障需求，prompt 内容与工具返回值可以直接丢弃以规避合规风险；数字即足够，内容无用，耗时即真相，任何进一步的上下文留存都只会增加存储开销与合规负担，而排障真正需要的仅仅是知道哪一步慢了，其余细节毫无价…
     2   在故障发生后由当事用户手动补充描述当时情形，系统本身维持现状、无需预先埋点；事后补录更加灵活轻便，补录即溯源，埋点多余，事后亦可
     3 ✓ 以统一的 request/session id 串联完整链路并采集：原始输入与拼装后的最终 prompt 及版本、检索命中的 chunk 与分数及来源、每次 LLM 出入参、工具调用的名称参数与返回值、Agent 关键决策点；敏感字段脱敏并…
   A: Trace 五要素：最终 prompt+版本、检索命中与分数、每次 LLM 调用出入参、工具调用明细、决策点与反馈；用统一 id 串联并平衡脱敏与存储。

• claude-blog-2026-08-10  [medium]  concepts=
   Q: 排查多 Agent 任务为何选择了错误路径时，哪些遥测字段最有价值？（单选）
     0   仅比较最终答案的字数多少，据此判断路径是否正确
     1 ✓ 关联 Agent、动作顺序、工具结果摘要和委派原因
     2   仅依据所用模型的名称来判断哪条路径更可靠
     3   删除中间事件后再凭印象重新猜测，丢弃可追溯的调试线索
   A: 应关联任务、Agent、工具调用、时间顺序、输入输出摘要和委派原因，才能判断是路由、执行还是汇总阶段出错；只看最终文本无法定位链路。

• aws-genai-developer-pro-29  [medium]  concepts=
   Q: 某医疗公司用 Bedrock 生成临床文档摘要，响应质量不稳定且偶发事实幻觉，月度成本比预期高 40%。需近实时监控方案检测幻觉、识别异常 token 消耗、对成本异常预警，且自定义开发最少。哪种方案满足？（单选）
     0   配置 CloudWatch 告警监控 InputTokenCount/OutputTokenCount 检测异常，调用日志存 S3，用 Glue+Athena 识别潜在幻觉。
     1   运行 Bedrock 评估作业（LLM 评判）检测幻觉，用 CloudWatch 跟踪 token 用量，Lambda 处理指标并通知。
     2 ✓ 配置 Bedrock 将调用日志存 S3 并开启文本输出日志；启用 Guardrails 上下文接地检查检测幻觉；对 token 用量指标建 CloudWatch 异常检测告警。
     3   用 CloudTrail 记录所有 Bedrock API 调用，QuickSight 自建仪表盘可视化 token，用 SageMaker Model Monitor 检测质量漂移。
   A: Amazon Bedrock Guardrails 的上下文接地检查通过评估生成内容是否事实性地基于所提供参考源（含摘要）来检测并过滤幻觉。Bedrock 提供 InputTokenCount/OutputTokenCount 等 CloudWatch 指标，CloudWatch 异常检测告警可识别指标模式的异常偏离，…

• aws-genai-developer-pro-11  [medium]  concepts=
   Q: 某电商用 FM 生成商品描述。工程团队用 CloudWatch 监控 token/延迟/错误率等技术指标，营销团队在另一系统看转化率/营收等业务指标。公司要统一可观测性把技术与业务关联，指标劣化自动告警，并同时可见两类指标。哪个方案满足？（单选）
     0 ✓ 建 CloudWatch 仪表盘同时展示技术与导入的业务指标，配 CloudWatch 复合告警组合两类数据，用 SNS 通知干系人
     1   用 Amazon Managed Grafana 可视化 CloudWatch 技术与外部业务指标，配 Grafana 告警触发 Lambda 自动修复
     2   把 CloudWatch 指标经指标流送到 S3，用 QuickSight 看板，EventBridge 规则超阈值时通知
     3   配 CloudWatch 自定义看板整合指标，配带异常检测的复合告警，SNS 通知
   A: A 正确：业务指标可发布/导入为 CloudWatch 自定义指标，与运维指标同看板展示；指标告警监控各自指标，复合告警用布尔逻辑表达“技术与业务相关”的条件，状态变化经 SNS 通知干系人。D 的异常检测复合告警也可行但不如 A 直接“组合两类数据并 SNS”；B 的“Lambda 自动修复”超出“告警”需求；C 走…

• aws-genai-developer-pro-03  [medium]  concepts=
   Q: 某公司开发 GenAI 应用，从内外部数据源生成内容，需保证生成结果完全可溯源：注册数据源并打元数据标签以归因到原始来源，且保留数据访问与使用的审计日志。哪个方案满足？（单选）
     0   用 Lake Formation 编目并管控访问，直接在 S3 打元数据标签，用 CloudTrail 监控 API 活动
     1   用 Glue Data Catalog 注册并打标签，用 CloudWatch Logs 监控访问模式
     2   数据存 S3 并用对象标签归因，用 Glue Data Catalog 管 schema，用 CloudTrail 记录 S3 访问
     3 ✓ 用 Glue Data Catalog 注册所有数据源并打元数据标签归因，用 CloudTrail 记录跨服务的访问与活动
   A: D 正确：Glue Data Catalog 集中注册数据源并管理可保留来源归因的元数据标签；CloudTrail 记录跨 AWS 服务的 API 活动，提供数据访问与流水线的审计轨迹。A 的 Lake Formation 侧重访问管控而非归因标签；B/C 仅覆盖部分或只用 CloudWatch Logs，不如 Clo…

########## quantization|tradeoff   (n=10) ##########

• sebastian-raschka-2026-08-146  [hard]  concepts=
   Q: 对压缩 KV 或共享表示做量化时，哪些风险应单独验证？（请选择三个）
     0 ✓ 重构后的注意力分数误差
     1 ✓ 长上下文累积误差和检索质量
     2 ✓ 不同硬件 kernel 下的速度与数值行为
     3   只要缓存字节数下降就无需质量回归
   A: 低精度误差可能在重构、长序列累积或特殊 token 上放大，应比较不同长度、任务和数值稳定性。

• sebastian-raschka-2026-08-114  [hard]  concepts=
   Q: 比较低精度预训练和训练后量化时，哪些变量应分别评估？（请选择三个）
     0 ✓ 训练过程中的收敛和数值稳定性
     1 ✓ 部署后的显存、带宽和吞吐
     2 ✓ 不同任务和长上下文上的质量回归
     3   只比较权重文件大小即可推断生产性能
   A: 低精度训练影响整个优化过程，训练后量化主要改变部署表示；两者都需要测量吞吐、显存、数值稳定和质量。

• ste-gradient-mismatch  [hard]  concepts=
   Q: 在进行量化感知训练时离散量化函数的导数处处为 0 导致标准反向传播失效。工程上使用直通估计器 STE 近似梯度。关于 STE 在实际训练中的工程局限与应对策略下列说法正确的有？（多选）
     0 ✓ STE 引入了梯度不匹配问题当连续浮点主权重严重超出截断区间时仍会向其传递梯度可能导致浮点权重无休止漂移
     1 ✓ 对于落入截断区间外的权重可以通过梯度裁剪或对超出界限的权重施加惩罚防止浮点主权重膨胀
     2 ✓ 伪量化节点仅作用于前向传播以计算量化后的损失反向传播时根据 STE 规则直接将梯度传递给未量化的连续浮点主权重进行参数更新
     3   STE 能够精确计算离散量化操作的二阶 Hessian 矩阵因此反向传播得到的梯度与真实梯度完全一致
   A: STE 前向离散化反向恒等传导（C）。恒等传导是粗暴近似在截断区外产生梯度不匹配导致主权重越界漂移（A），需 Weight Decay 或 Bound Clipping 约束（B）。D 错误 STE 是启发式近似不可能精确计算二阶 Hessian。

• ptq-reasoning-llm  [hard]  concepts=
   Q: 在后训练完成的 CoT 推理大模型进行量化部署到生产环境时关于 FP8、NVFP4/MXFP4 和 GGUF 等量化格式的工程权衡以下哪些说法是正确的？（多选）
     0 ✓ 基于 Dynamic Per-Channel Weight 与 Per-Token Activation 的 FP8 动态量化在绝大多数场景下几乎无损推理精度适合高吞吐生产部署
     1 ✓ 推理模型的 CoT 思考链对 Activation 中的 Outlier 较为敏感过度的 FP4 极低比特量化可能会导致长链条逻辑推演中出现累积误差或死循环
     2 ✓ GGUF 格式支持 CPU/GPU 混合离线加载与多阶量化非常适合边缘端和单卡显存受限设备的本地部署
     3   对推理模型进行 FP8 量化前必须经过数万条数据的校准否则模型的逻辑推理能力会立刻降级为随机猜答，因为 FP8 的动态范围极窄，缺少校准集就无法确定任何缩放因子
   A: D 错误。目前先进的 FP8 方案支持无需校准数据的动态 per-channel 权重与 per-token 激活量化即可直接转换且依然保持极高的逻辑推理精度。

• quant-ptq-qat-01  [hard]  concepts=
   Q: 在将大语言模型部署至内存受限设备（如手机、边缘设备或高并发服务节点）时，权重量化（Quantization）是核心压缩技术。关于训练后量化（PTQ）与量化感知训练（QAT）的技术特性及工程权衡，下列说法正确的有？（多选）
     0 ✓ PTQ（Post-Training Quantization）无需重新训练模型，实施成本低，但在极低比特（如 INT4）下更容易引发明显的模型精度衰退
     1 ✓ QAT（Quantization-Aware Training）在训练或微调阶段引入量化噪声模拟，使权重适应低精度表示，因此在低比特下通常比 PTQ 表现出更好的精度保持度
     2 ✓ 量化主要降低模型权重的显存/内存占用，并提高显存带宽利用效率，属于内存密集型推理优化的重要手段
     3   参数量化与注意力机制的 KV Cache 结构优化（如 GQA）属于不同层面的技术，在实际部署中无法同时使用
   A: PTQ免训练但低比特易退化、QAT引入噪声适应精度好、量化降显存带宽为内存密集型优化、与GQA可叠加。

• quant-pref-decode-01  [hard]  concepts=
   Q: 对 LLM 进行低比特量化（如从 FP16 量化为 INT8/INT4）是常见的推理优化手段。关于量化在 Prefill 与 Decode 阶段的加速机理以及模型不同组件对量化的敏感度，下列说法正确的有？（多选）
     0 ✓ 在 Prefill 阶段，量化主要通过降低浮点计算精度、使用专用低精度计算单元来加速大矩阵乘法
     1 ✓ 在 Decode 阶段，量化减少了模型权重的体积，降低了每次前向传播从显存加载权重的带宽压力，从而提升 TPS
     2 ✓ 注意力层（Attention Layer）通常比线性层（Linear Layer）对量化误差更敏感，因为注意力层的舍入误差会随序列生成在多步中逐步累积放大
     3 ✓ 生产环境中常见的工程折中方案是将注意力层保持全精度（如 FP16），而仅对线性权重（Linear Weights）等敏感度较低的部分进行量化
   A: Prefill以低精度算单元加速、Decode以减权重体积降带宽、Attention更敏感需保精度、实践Attention保FP16仅量化Linear。

• ai-quant-fp4-01  [hard]  concepts=
   Q: 在深度学习大规模预训练中，采用低精度数值格式（如 4-bit / FP4）是降低显存吞吐与计算功耗的重要手段。以下关于低精度预训练与软硬件协同设计的说法，正确的有？（多选）
     0 ✓ 直接使用 4-bit 进行预训练通常依赖硬件体系结构（如专用的 FP4 张量计算单元）的原生支持
     1 ✓ 相比于高精度预训练后量化（Post-Training Quantization），从预训练阶段直接引入低精度计算能让模型权重适应低精度的数值范围，减少后量化的精度损失
     2 ✓ 4-bit 预训练的主要优势在于减少内存带宽需求和数据传输开销，加快矩阵乘法计算
     3   采用 4-bit 预训练会显著增加模型训练时的显存消耗，但能提升推理吞吐量，因为低精度权重在反向传播时需要额外的高精度影子副本与补偿累加器，这部分开销远大于省下的空间
   A: FP4需硬件FP4张量单元、预训练低精度适配减少后量化损失、优势在带宽与矩阵乘加速、D称显存增加错误。

• sebastian-raschka-2026-08-101  [medium]  concepts=
   Q: 对本地 Coding Agent 使用量化模型时，哪些权衡需要实测？（请选择三个）
     0 ✓ 显存占用和可运行的并发规模
     1 ✓ 代码修复、测试生成等任务质量
     2 ✓ 长上下文和工具调用下的稳定性
     3   量化位数越低就必然在所有硬件上越快
   A: 量化通常降低显存和带宽需求，但可能影响代码生成质量、长上下文稳定性和某些算子性能，收益依赖硬件与实现。

• ai-eng-039  [medium]  concepts=
   Q: 自部署 LLM 时需要在 FP16、INT8（LLM.int8）、GPTQ/AWQ 4-bit、GGUF Q4_K_M 之间做部署选型。关于各方案的取舍，下列说法正确的是？
     0   GPTQ 和 AWQ 都是 4-bit 且效果完全相同，选哪个只看社区热度，因为二者在算法层面只是同一思想的不同实现，量化后的权重分布与推理质量没有任何可测差异，工程上可以任意互换而不必重新评估，所以无论用 GPTQ 还是 AWQ 部署同一…
     1 ✓ AWQ 利用激活分布识别并保护高影响权重，4-bit 质量优于朴素 GPTQ，是 GPU 部署的默认起点；INT8 近乎无损但只省一半显存；GGUF Q4_K_M 服务于 CPU/边缘场景；FP16 用于质量敏感的基准对照
     2   量化必然导致模型能力大幅下降，生产环境永远不应使用 4-bit，只有在离线批处理且对质量零要求的场景里才勉强可用，因为所有低比特格式都会无差别地抹平权重中的重要信号，4-bit 与 2-bit 在质量上只是程度差异而非性质差异
     3   INT8 量化能把显存占用降到 FP16 的 1/4，是省显存最多的方案，因此任何显存受限的服务都应优先选择 INT8 而非 FP8
   A: AWQ 是激活感知量化：按激活分布保护高影响权重，4-bit 下质量损失小于朴素 GPTQ，是多数 GPU 部署的首选；INT8 折中——几乎无感损失但只省一半显存；GGUF Q4_K_M 面向 CPU/llama.cpp 场景；FP16 作为质量基准。经验法则：先试 AWQ 4-bit，质量不够升 INT8，纯 CP…

• quant-qah-05  [medium]  concepts=
   Q: 尽管 4-bit 量化能在多数基准追平甚至超过 16-bit，但在哪些情形下 4-bit 仍可能微幅落后？
     0 ✓ 在 MMLU-Pro、SciCode 等知识类基准上，4-bit 仍比同尺寸 16-bit 低不到 1.5 个点
     1 ✓ 若量化时未做 healing 修复，能力退化会更大，落后的幅度也更多
     2   4-bit 在任何基准上都绝对优于 16-bit，不存在落后情形
     3   4-bit 量化只影响推理速度，对模型的最终精度其实没有任何影响
   A: 在 MMLU-Pro、SciCode 等知识/科学类基准上，4-bit 仍比同尺寸 16-bit 低不到 1.5 个点；若量化时未做 healing（修复），能力退化会更大、落后更多。4-bit 并非绝对优于 16-bit，也不是对精度毫无影响。

########## agent-guardrails|scenario   (n=9) ##########

• aws-genai-developer-pro-47  [hard]  concepts=
   Q: 某金融公司建 AI 助手帮客户做投资规划，识别高风险对话模式（如具体股票推荐、保证收益）以免违规。须确保助手不提供不适当投资建议、不产出竞品内容、不编造无获批指引支撑的断言；用 Bedrock Guardrails。哪三项组合满足？（请选择 3 个）
     0 ✓ 将高风险对话模式加入 denied topics guardrail。
     1   配置内容过滤器 guardrail 过滤含高风险模式的提示。
     2   配置内容过滤器 guardrail 过滤含竞品名称的提示。
     3 ✓ 将竞品名称加入自定义词过滤器，输入与输出动作均设为 block。
     4   设较低的接地分数阈值。
     5 ✓ 设较高的接地分数阈值。
   A: Bedrock denied topics 可拦截提示与响应中的不良上下文主题，适合高风险投资建议模式。自定义词过滤器屏蔽精确词或短语（含竞品名），且可同时拦截输入与输出。上下文接地检查评估响应是否事实性基于所提供获批源材料；高阈值要求更强置信度才放行内容。

• aws-genai-developer-pro-36  [hard]  concepts=
   Q: 某金融客服助手须不提供投资建议、拦截有害内容、掩码 PII、保留审计轨迹；须对用户输入与模型响应都按敏感度做内容过滤；需以最少误杀生效，且支持多种敏感内容的多种处理策略。哪种方案满足？（单选）
     0   配单个 guardrail，所有类别内容过滤器设 high，denied topics 设投资建议并加样例短语，敏感信息过滤器对所有 PII 实体 block，应用到所有推理调用。
     1   配多个分级 guardrail：一个 high 过滤并对公开交互 block PII，一个 medium 并对内部 mask PII，多个主题 guardrail 拦截投资建议并加接地检查。
     2 ✓ 配 guardrail 对有害内容过滤器设 medium；denied topics 设投资建议并配清晰定义与样例短语；敏感信息过滤器对响应 mask PII、对输入 block 金融信息；启用输入/输出双向评估并自定义拦截消息供审计。
     3   为每个用例建独立 guardrail：一个有害内容过滤、一个主题过滤投资建议、一个敏感信息过滤 block PII；用 Step Functions 顺序串联并按分类条件路由。
   A: Bedrock Guardrails 可在单一配置中组合中等强度有害内容过滤器、带定义与样例短语的 denied topics、以及敏感信息过滤器。敏感信息策略可对输入/输出施加不同动作：响应中脱敏 PII，输入中拦截金融信息。Guardrails 同时评估提示与模型响应，中等过滤相比高强度有助于减少误杀。

• aws-genai-developer-pro-25  [hard]  concepts=
   Q: 某媒体公司发布平台，每小时数千用户上传图文，用 Bedrock 处理并生成创意合成。公司要确保平台既不处理也不产出不当内容、不在合成中泄露 PII，且要与现有 S3 存储工作流集成。哪个方案以最低基础设施管理开销满足？（单选）
     0   启用 Enhanced Monitoring，用 CloudWatch 告警过滤流量，Comprehend PII 预处理，Step Functions 含 Rekognition 图像审核
     1   API Gateway HTTP API 配请求校验模板在存 S3 前筛查，SageMaker 自建内容审核模型处理后送 Bedrock
     2   Cognito 用户池用预认证 Lambda 做审核，Textract 过滤文本、Rekognition 过滤图像后才允许上传
     3 ✓ Step Functions 工作流用内置 Bedrock Guardrails 过滤内容，Comprehend 做 PII 预处理，Rekognition 做图像审核
   A: D 正确：Bedrock Guardrails 提供托管的文本/图像有害内容过滤与 PII 敏感信息控制；Comprehend 预处理文本 PII、Rekognition 评估图像，Step Functions 把这些托管检查编排进 S3 工作流，无需自建审核模型或请求筛查基础设施，运维最省。A 的 Enhanced …

• aws-genai-developer-pro-24  [hard]  concepts=
   Q: 某咖啡公司移动 app 用 Bedrock + 三段式提示链生成个性化烘焙曲线：把用户输入转结构化元数据、检索烘焙日志、生成推荐。多地用户反映相同输入推荐不一致、检索步骤推理慢、且出现不安全建议（如过高温度冲泡）。公司要提升重复输入的一致性（99.5%）、性能（<1 秒）、并用已验证安全控制拦截不安全/幻觉建议。哪个方案满足？（单选）
     0 ✓ Bedrock 配 Provisioned Throughput 稳定延迟，加 Guardrails 拦不安全输出，用 Prompt Management 审批管理提示
     1   用 Bedrock Agents 管理链式调用，输入输出日志到 CloudWatch，用日志做提示版本 A/B 测试
     2   结果缓存到 ElastiCache，Lambda 预处理元数据并追踪端到端延迟，X-Ray 定位性能瓶颈
     3   用 Kendra 提升检索准确度，归一化元数据存 DynamoDB，Step Functions 编排多步提示
   A: A 正确：Provisioned Throughput 提供专用容量使推理延迟更可预测；Guardrails 通过 denied-topic 与上下文接地检查拦截不安全/幻觉建议；Prompt Management 支持版本化提示配置，使一致获批的提示版本跨环境部署，提升重复输入一致性。B/C/D 均未同时覆盖“一致性…

• aws-genai-developer-pro-02  [hard]  concepts=
   Q: 某公司用已开启全部功能的 AWS Organizations 管理多账号，员工在多个账号使用 Bedrock。公司必须：禁止在提交给 Bedrock 模型的提示中出现特定主题与专有信息；员工只能用获批的 Bedrock 模型；员工 IAM 角色由公司集中管理。哪些方案组合满足？（请选择以下 2 个正确选项）
     0   为每个员工 IAM 角色建权限边界，要求用获批 guardrail 标识符才能调用 Bedrock；另建 SCP 只允许用获批模型
     1   建 SCP 只允许用获批模型，并要求调用获批模型时必须指定 guardrail 标识符
     2 ✓ 建 SCP：未指定集中部署的 guardrail 标识符则拒绝调用模型；并对每个员工 IAM 角色设权限边界，只允许调用获批模型
     3 ✓ 用 CloudFormation 建带 block（拦截）过滤策略的 Bedrock guardrail，并用 StackSets 部署到各账号
     4   用 CloudFormation 建带 mask（掩码）过滤策略的 Bedrock guardrail，并用 StackSets 部署到各账号
   A: C、D 正确：Bedrock Guardrails 的 block 策略可拦截被禁主题与敏感信息（mask 仅脱敏，不能阻止处理）；用 CloudFormation StackSets 集中部署到各成员账号。SCP 可显式拒绝未带必需 guardrail 标识符的调用，权限边界限制员工角色只能用获批模型。A 错在权限边…

• anthropic-cca-f-08  [hard]  concepts=
   Q: 生产评审发现终稿对不确定性的处理不一致：有时把冲突发现合成成一句过于自信的陈述（丢失细节），有时又过度铺垫（变得无用）。例如网页搜索返回“行业分析估 $50B（方法各异）”、文档分析返回“同行评审估 $35B（±$7B, 95% CI）”，协调器要么武断选一、要么含糊写“可能在 $35B-$50B 视因素而定”。最系统的应对方法是？（单选）
     0   配置子代理只上报达到高置信阈值的发现，在到达协调器前过滤不确定信息。
     1   加一个核验子代理跨源交叉比对，只把至少两个独立来源佐证过的声明交给合成。
     2 ✓ 指示合成代理把报告结构化为显式分区，区分确证发现与有争议发现，保留原始来源刻画与方法学上下文。
     3   实现置信校准层，把子代理的不确定性表达归一为 0.0-1.0 标准分，再按校准置信做加权平均。
   A: C 正确：让合成代理把报告结构化为“确证发现”与“有争议发现”显式分区，保留原始来源的方法学刻画与置信上下文——既不失真也不含糊。A 错：过滤掉不确定信息会丢信号。B 错：要求两源互证会系统性丢弃单源但有效的发现。D 错：把不确定性归一化为 0-1 概率再加权平均，既不可靠又抹掉方法学差异。

• ai-tool-004  [hard]  concepts=
   Q: 一个 Agent 可以调用发送邮件、删除数据和查询数据库的工具。你会如何设计工具权限和高风险操作的确认机制？
     0   三类工具赋予同等操作权限，信任模型在参数中附带“用户已同意”标记即放行各类危险动作
     1 ✓ 按风险分级授权：查询默认放行，邮件外发限制收件域名与频率，删除等破坏类操作默认禁止自动执行
     2   删除类操作直接物理执行以追求效率，软删除与独立审批流只会无谓拖慢 Agent 响应
     3 ✓ 高危操作设置人工确认门，执行前提供 dry-run 预览，并采用软删除以便回滚
     4 ✓ 权限判定一律在服务端基于会话身份完成而不采信模型自述，并对全部敏感调用留存审计日志
   A: 风险分级 + 确认门 + dry-run 预览 + 软删除 + 最小 scope 凭证 + 审计日志；权限判定在服务端做，防注入伪造“已同意”。

• ai-security-002  [hard]  concepts=
   Q: 如何设计一个 LLM Application 的 Input Guardrail 和 Output Guardrail？哪些检查应该在 LLM 之前完成，哪些应该在 LLM 之后完成？
     0   全部安全检查都应在 LLM 之后集中执行，因为只有看到完整输出才能全面评估风险，前置检查反而会浪费一次调用机会；输入侧的注入与越权在模型生成阶段自然会被对齐能力消解，无需在 LLM 之前做任何拦截，把护栏全部后置既统一又省成本，这也是多数成…
     1   注入检测属于深层语义理解任务，必须依赖 LLM 自身判断输入意图，因此只能放在模型调用之后基于完整对话历史进行分析。
     2   Guardrail 触发拦截时应静默丢弃请求且不给任何反馈；为照顾用户体验应尽量放行边界情况，误杀留待事后审计处理。
     3 ✓ 注入检测、PII 脱敏、主题与权限过滤等廉价且确定性高的检查放在 LLM 之前拦截恶意输入；grounding 校验、毒性合规审查等需要语义理解的检查放在 LLM 之后；两层都保持低延迟、fail-safe（宁可拒绝不放行高危内容），并持续…
   A: 前置拦输入（注入/PII/权限，便宜确定），后置审输出（grounding/合规/schema，需语义理解）；两层低延迟、fail-safe、误杀率监控。

• agentic-12  [hard]  concepts=
   Q: 为防止 Agent 在执行中失控（无限循环、误用高危操作），下列哪些是有效的工程护栏？（多选）
     0 ✓ 为每次运行设置最大迭代步数与超时上限，并对参数高度相似的重复工具调用做检测与告警。
     1   在系统提示词中写明“请谨慎操作、避免重复”，依靠模型的内建安全对齐作为主要防线。
     2 ✓ 对删除资源、变更配置等不可逆高危动作引入人工确认门或独立的权限隔离层。
     3   取消所有人工确认环节以保持全自动执行效率，改为事后逐日审计全部操作日志。
     4 ✓ 把代码执行类动作放进隔离沙箱运行，并对工具调用实施速率限制。
   A: 生产级 Agent 必须有循环护栏（步数上限 + 重复检测）、高危操作的确认/最小权限、沙箱与限流；仅依赖模型自身对齐不足以防范系统性风险，失控的代价是真金白银与数据破坏。
