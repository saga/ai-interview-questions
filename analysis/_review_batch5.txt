
########## rag|tradeoff   (n=6) ##########

• slm-rag-ceiling-01  [hard]  concepts=
   Q: 由于参数量限制，小语言模型（SLM）存在隐式事实记忆容量受限的“知识天花板”（Knowledge Ceiling）。关于克服小模型知识天花板的架构设计与技术权衡，下列说法正确的有？（多选）
     0 ✓ 模型参数充当参数化记忆（Parametric Memory），缩小参数量必然会降低模型隐式存储的宽泛世界知识与偏门事实总量
     1 ✓ 针对事实性问答场景，通过引入 RAG（检索增强生成）机制将非参数化知识注入上下文，是避免无限扩大模型参数量的重要架构解法
     2 ✓ 在 RAG 架构中，小模型通常对检索上下文中的噪声和不相关信息更加敏感，因此相比大模型更依赖高质量的 Context 剪裁与重排序（Reranking）
     3   借助高质量合成数据（Synthetic Data）训练，可以彻底取消小模型的参数量限制，使其无限量记忆所有互联网事实
   A: 参数为参数化记忆、小则知识少、RAG非参数化注入、SLM对噪声更敏感需剪裁重排、合成数据不消参数限制。

• ai-rag-012  [hard]  concepts=
   Q: 通用 Embedding Model 在你的金融领域检索效果只有 75%。你会优先更换模型、Fine-tune Embedding Model，还是增加 Reranker？为什么？
     0   应直接微调专属 embedding 模型，因为它领域专用性最强、收益上限最高，理应作为第一步投入，早微调早收益，能最快把领域召回拉到可接受水位。
     1 ✓ 按投入产出比走阶梯：先零训练成本地更换更强的通用或领域模型并用评测集对比，再加 Reranker 对召回结果精排（代价是延迟上升），两者仍不达标且错误分析显示需要细粒度语义区分时，才考虑投入微调 embedding。
     2   加 Reranker 是唯一正确选择，cross-encoder 无需训练即可解决所有领域检索问题，而且几乎不增加额外的延迟开销；无论召回质量多差，重排都能在末端把正确答案顶上来，因此任何场景都应直接上重排而非换模型或微调。
     3   三种手段互斥只能择一：换模型与加 Reranker 不能叠加使用，因此必须一步到位选择收益上限最高的微调方案，混合堆叠只会让效果相互抵消。
   A: 升级阶梯：换模型（零训练）→ 加 reranker（零训练换延迟）→ FT embedding（高上限高成本）；由错误分析决定走到哪一级。

• ai-fund-048  [hard]  concepts=
   Q: 公司希望模型掌握 10 万条内部 FAQ。你会选择继续 Pre-training、Fine-tuning 还是 RAG？请说明理由和实施路径。
     0 ✓ 优先 RAG：FAQ 需要持续更新、引用出处且可审计，正是检索方案的强项而预训练无法增量更新；先建成高质量检索库，风格格式不达标再用 LoRA 做行为层适配
     1   优先继续 Pre-training：把 FAQ 训进参数掌握最牢固，后续知识更新只需再做一轮预训练即可同步生效，且参数内的知识天然带有权威性与一致性。
     2   优先 Fine-tuning 把十万条 FAQ 写入权重：存进参数的知识永远不会过时，并且天然支持逐条引用溯源，是最贴合企业知识沉淀诉求的路线。
     3   三种方案效果等价，按团队成员的技术栈熟悉程度任选其一即可，不必考虑更新频率与审计要求，因为无论哪种方式最终都能让模型答对同样的维护问题。
   A: FAQ = 会更新、需溯源的可检索知识 → RAG 优先；pretrain 不可能，FT 仅在行为/格式不达标时作为补充（LoRA+SFT）。

• ai-eng-017  [medium]  concepts=
   Q: 你需要为企业 ESG 报告建立 RAG 系统。PDF 中同时包含长文本、表格、脚注、多级标题和图表。你会选择固定长度 chunk、semantic chunk，还是基于文档结构进行 chunking？为什么？
     0   继续使用固定长度切分，表格与多级标题在向量空间中没有语义意义，切分方式的差异不会影响召回质量，所以无需为结构化文档引入任何特殊的边界处理策略。
     1   把整份 PDF 作为单个超大 chunk 注入，依靠模型的长上下文能力直接通读全文，彻底避免切分造成的信息损失，同时还能省去所有分块与重排的工程开销。
     2   完全依赖纯 semantic chunking 按语义相似度自动聚类切分，它对所有内容类型都是最优解，无需针对文档结构定制，也不必为表格或脚注单独设计任何处理路径。
     3 ✓ 以文档结构为主导切分：按章节标题层级保持段落语义完整，表格沿行列边界整体保留并与所属章节关联，脚注单独建块，再结合 token 上限对过长部分二次切分，对不同内容类型采用不同策略。
   A: 结构化文档优先按章节/标题/表格边界切分，再结合 token 长度；不同内容类型采用不同 chunking 策略。

• agentic-41  [medium]  concepts=
   Q: RAG 应该放在 Agent 外部（前置管线）还是作为 Agent 内部的一个 Tool？两者各适合什么场景？
     0 ✓ 前置管线适合查询意图单一、每次都需要检索的确定性场景（低延迟、行为可预测）；Tool 化适合是否检索、检索几轮都需判断的场景（多轮迭代、跨源验证）；按查询分布可预测性与延迟预算为两类流量分别选择形态。
     1   RAG 作为 Tool 适合查询意图单一、每次都必须检索的场景，前置管线则用于需要多轮迭代的复杂问题——这种划分把两者职责完全颠倒，既让简单查询被迫承担多轮决策开销，也在复杂链路上丢失了前置检索的低延迟优势。
     2   两种形态互斥不可混合使用，高频简单意图与长尾复杂问题必须走同一条链路，且一旦选定架构便无法按流量分层切换，也无法在演进中把两类意图分别升级。
     3   RAG 放置位置决定一切效果，分块、混合检索与重排等检索质量工程无关紧要，架构师只需关心检索发生在管道还是工具内部即可保证质量。
   A: 考察 RAG 放置位置对系统行为的影响。前置管线适合查询意图单一、每次都需要检索的确定性场景（低延迟、行为可预测）；Tool 化适合是否检索、检索几轮都需判断的场景（多轮迭代、跨源验证）。决策依据主要是查询分布的可预测性与延迟预算，两种形态也可按流量分层混用。

• aws-waf-lens-2026-08-10  [medium]  concepts=
   Q: RAG 应用延迟超出目标，且上下文中包含大量无关文档。最合理的优化顺序是什么？（单选）
     0   直接把所有文档放入最大上下文
     1 ✓ 测量检索链路并改进过滤、分块、重排和上下文选择
     2   只调高生成 temperature
     3   删除引用以减少生成 token
   A: 应分别测量检索召回、重排、上下文构建和生成的耗时与质量，优化查询、过滤、分块和重排，使上下文更小但保留证据。盲目扩大上下文会增加成本和延迟，并可能降低有效注意力。

########## multi-agent|tradeoff   (n=6) ##########

• ma-swarm-tradeoff-01  [hard]  concepts=
   Q: 在分布式软件漏洞挖掘系统设计中，架构师需要选择“独立并行搜索（Uncoordinated Brute-force）”与“协调式智能体集群（Coordinating Swarm with Peer Review & Arbiter）”两种架构之一。假设系统面临代码库庞大、漏洞类型多样且存在跨模块逻辑依赖的场景，以下关于这两种架构的技术权衡与机制分析，哪些项是正确的？（多选）
     0 ✓ 协调式集群通过共享通信总线与仲裁节点机制，使智能体能够根据中间反馈自发形成工具制作与漏洞类型分工，在全局覆盖度上表现更佳
     1 ✓ 独立并行架构在限定核心代码目录内进行固定路径检索时，单位 Token 产出的漏洞发现效率（Tokens per vulnerability found）通常不亚于协调式集群
     2   协调式集群能彻底杜绝各智能体之间的重复劳动，因此其总 Token 消耗量必定显著低于完全解耦的独立并行搜索架构
     3 ✓ 独立并行架构由于缺乏智能体间的动态交互，更容易受限于预设的检索路径，难以自发拓展至核心目录之外的潜在风险区域
     4 ✓ 协调式集群在处理高度解耦且可完全并行化的子任务时，其通信、评审与共识开销可能成为额外负担，降低资源利用效率
   A: A正确：协调式集群通过共享通信与仲裁允许自发分工拓展全局覆盖。B正确：受限核心目录内独立并行无额外通信评审开销，单位Token效率不亚于集群。C错误：集群的讨论、评审与仲裁开销极大且无法彻底杜绝重复，Token总量通常更高。D正确：静态预分配的独立智能体无法根据中间线索动态调整范围。E正确：高度解耦可完全并行任务上引入…

• agentic-67  [medium]  concepts=
   Q: 在构建 Agent 系统架构评估测试集（Benchmark）时，为了消除大语言模型（LLM）API 调用带来的高成本、网络延迟及随机波动，架构师采用确定性规则策略（如基于 TF-IDF 提炼并共享未重复的最新事实）代替真实 LLM。关于这种评估设计的工程权衡（Trade-offs），以下说法正确的有？（多选）
     0 ✓ 能够实现 100% 可审计和比特级（Bit-for-bit）完全可复现，且测试运行零 API 开销
     1 ✓ 能够准确评估拓扑几何分布、消息路由规则及网络链路利用率等纯结构性与工程性能指标
     2   其测试结论可以无缝推广至所有生产环境，因为真实 LLM 在复杂拓扑中的行为模式与确定性规则完全一致
     3 ✓ 无法捕捉真实 LLM 面对复杂上下文时的推理噪声、指令遵循偏差及幻觉生成对网络通信的影响
   A: 确定性规则实现100%可审计/比特级复现、零API开销（A），能准确评估拓扑几何与链路利用率等结构性指标（B），但丢失LLM推理噪声/指令偏差/幻觉（D）；C称可无缝推广至生产是过度承诺。

• agentic-63  [medium]  concepts=
   Q: 关于多智能体（Multi-Agent）系统的架构模式与适用边界，下列说法正确的是？
     0   多智能体系统一定优于单智能体，因为更多 LLM 意味着更高智能；模型数量即能力上限，应当尽量拆分，堆量即智能，规模决定一切，无需评估协调开销与调试复杂度，拆分越多收益必然单调递增
     1   无论任务类型如何，Manager-Worker 模式都应是默认最优架构；即使任务不可分解也应强制套用层级编排，因为统一拓扑能简化运维并天然提升质量
     2 ✓ 架构模式应匹配任务形态：层级式适合可分解任务分派（注意 Manager 瓶颈）、流水线适合阶段加工、Critic 用对抗审查提质量；启用前提是任务天然可分解且需不同工具或专长——单 Agent 能解决时应坚持单 Agent，协调开销是真实代…
     3   多智能体共享同一完整上下文窗口是标准做法，能自动消除信息差；因此无需设计消息边界或权限隔离，集中式上下文即最佳协作范式，透明即最高效，合一即无需考虑状态同步与失败隔离
   A: 四种主流拓扑各有适配场景：Hierarchical（Manager 分解派发）适合可分解复杂任务但 Manager 易成瓶颈；Pipeline 顺序交接适合阶段化处理但无并行；Peer-to-peer 协作自然但协调复杂；Critic/Verifier 一个生成一个挑刺能有效提质。启用多智能体的前提是任务天然分解、各子…

• agentic-39  [medium]  concepts=
   Q: 什么情况下 Multi-Agent 是过度设计？你会用什么信号来判断该退回单体 Agent？
     0   角色数量越多系统越智能，Multi-Agent 不存在过度设计的问题，加角色总能带来额外收益与更高智能
     1   判断标准是代码行数，单体实现超过一定行数就必须拆分为 Multi-Agent，行数即架构取舍的唯一依据
     2   协调开销是否超过分工收益难以度量，因此不应与单体基线做实证对比，凭架构直觉拆分即可，经验优于数据
     3 ✓ 当任务放进单个上下文即可完成、角色间只是转发消息而无独立视角或并行收益、协调成本超过分工收益时就是过度设计；应先用精心设计的单体跑基线对比，成功率相当则退回单体
   A: 考察以基线对比判断架构复杂度是否值得。过度设计信号：任务放单上下文即可完成、角色间只是转发消息而无独立视角、协调成本超过分工收益。判断方法是同任务跑精心设计的单体基线对比成功率——若相当则 Multi-Agent 是负资产。从单体开始，出现扩展瓶颈再加角色。

• ma-reputation-09  [medium]  concepts=
   Q: 在人类社会中，信任与合作依赖于法庭、声誉机制和同行评审等社会制度；而在无状态、可随时销毁与重启的 AI 智能体网络中，缺失这些制度会导致合作效率低下。若要在多智能体计算网络中引入类似机制以改善信任与协作，以下架构考量正确的有哪些？（多选）
     0 ✓ 引入基于加密身份（Cryptographic Identity）的持久化声誉系统，使智能体的历史欺诈或恶意对抗行为产生跨会话的长期成本
     1   依靠智能体单次会话中的自我反思（Self-reflection）提示词，即可完全替代跨会话的声誉与惩罚机制
     2 ✓ 设计经济质押（Staking）与违约罚没（Slashing）机制，在智能体执行关键操作或提交数据时要求质押资源
     3 ✓ 建立可追溯的独立审计日志（Audit Trail），使得第三方仲裁智能体或人类管理员能够对争端进行事后追责与状态复原
     4   确保每个智能体节点能够在没有任何身份标识与历史记录的情况下，完全依赖零知识推理实现最高效的长期市场合作
   A: A正确：加密身份持久化声誉使欺诈产生跨会话成本。B错误：单次会话自我反思无法替代跨会话声誉与惩罚。C正确：质押与罚没使欺诈付出实质代价。D正确：审计日志支持事后追责与复原。E错误：无身份与历史记录致道德风险无法高效合作。

• claude-blog-2026-08-08  [medium]  concepts=
   Q: 为什么多 Agent 编排适合同时调查部署历史、错误日志、指标和支持工单？（单选）
     0   所有数据都必须复制到同一个 prompt
     1 ✓ 独立调查可以由专业 Agent 并行执行
     2   并行 Agent 不需要共享任何结果
     3   并行的唯一作用是降低模型温度
   A: 这些调查维度相互独立且需要不同专业工具，Lead Agent 可以将它们分给专业 Agent 并行处理，再汇总结果。

########## distillation|mechanism   (n=6) ##########

• ai-eng-044  [hard]  concepts=
   Q: 在"用前沿模型蒸馏学生模型接管高频任务"的项目中，关于数据工程的三个关键决策，下列说法正确的有？（多选）
     0 ✓ 训练数据应使用真实生产 trace 分层采样，而非 LLM 生成的合成 prompt——后者会让学生在合成题上虚高、在真实流量上退化数个点
     1 ✓ 对教师输出做拒绝采样：随机抽约 5% 由领域专家核对，不一致的 pair 直接丢弃，防止教师错误固化为学生的训练分布
     2   思维链蒸馏应无条件应用于所有任务类型，因为带理由的标签总是优于纯答案标签，且推理链越长模型学到的泛化能力就越强，无论任务是否需要推理都应强制加入
     3   为扩大训练集规模，可以用温度调高的教师模型大量生成合成变体来替代真实 trace，温度越高生成的样本多样性越好，模型在真实流量上的表现也会随之线性提升
   A: ①蒸馏数据必须来自真实生产 trace 而非 LLM 合成 prompt：合成数据训出的模型在合成题上表现好、在真实流量上掉 4-7 个点，因为生产 trace 才携带分布偏移、长尾与怪异输入；②教师标签需要拒绝采样兜底：92% 精度的教师若全量照单全收，会把错误写进学生的训练分布——对随机样本做约 5% 人工抽查并丢…

• distill-04  [hard]  concepts=
   Q: 本文提出的 Quantization-Aware Healing（QAH）在蒸馏设定上与 QAD 有何不同？
     0 ✓ 以压缩前的原始全精度模型为冻结教师，做跨架构蒸馏
     1 ✓ 仅使用教师的输出分布（logits 上的 KL），不依赖学生与教师同架构
     2   学生上限被锚定在退化的 bf16 检查点上，因此精度必然更差
     3   QAH 与 QAD 完全一样，只是把学生换成了更大模型
   A: QAH 以压缩前的原始全精度模型为冻结教师，做跨架构蒸馏，仅使用教师的输出分布（logits 上的 KL），不依赖学生与教师同架构；这使它不被结构压缩后退化检查点限制。QAD 在结构压缩后的问题（学生上限锚定退化目标）正被 QAH 规避。两者并非只是学生大小的差别。

• sebastian-raschka-2026-08-19  [medium]  concepts=
   Q: 将一个高成本推理模型产生的高质量轨迹用于训练小模型，主要属于哪种优化思路？（单选）
     0 ✓ 知识蒸馏
     1   只做 KV cache 清理
     2   只做输入分词
     3   只做网络负载均衡
   A: 这是知识蒸馏：学生模型学习教师产生的答案或推理轨迹，以较低推理成本近似教师在目标分布上的行为。

• sebastian-raschka-2026-08-57  [medium]  concepts=
   Q: 把高预算教师模型的推理样本用于训练低成本学生模型时，哪些目标合理？（请选择两个）
     0 ✓ 降低相同任务上的推理成本
     1 ✓ 让学生学习教师示例中的解题行为
     2   保证学生在所有分布外任务都等价于教师
     3   不需要验证教师输出，因为教师不会出错
   A: 蒸馏可以把教师在目标任务上的答案或轨迹行为迁移给学生，降低部署成本；学生能力仍受数据覆盖和教师错误限制。

• distill-03  [medium]  concepts=
   Q: 在『量化感知蒸馏（QAD）』中，关于教师与对齐方式，下列哪些符合文中描述？
     0 ✓ 冻结一个同架构的全精度教师，用学生与教师输出 logits 的 KL 散度蒸馏到量化学生
     1 ✓ QAD 仅在『只做量化、未做结构压缩』时有效，因为存在同架构全精度教师
     2   QAD 不需要教师，直接让学生随机初始化后自我训练
     3   QAD 必须把教师和学生拼成同一个单一模型做联合前向
   A: QAD 冻结一个同架构的全精度教师，通过学生与教师输出 logits 的 KL 散度把知识蒸馏到量化学生；它仅在『只做量化、未做结构压缩』时有效，因为此时存在同架构全精度教师可做对齐。QAD 并非无教师随机训练，也不把师生拼成单模型联合前向。

• distill-02  [medium]  concepts=
   Q: 典型的知识蒸馏损失如何构成？
     0 ✓ 在教师软化后的输出分布上算 KL 散度（学生 ‖ 教师），迫使学生匹配软目标
     1 ✓ 同时保留对真实硬标签的交叉熵，蒸馏损失常为 α·KL + (1−α)·CE 的加权组合
     2   只用硬标签的交叉熵损失，完全不使用教师提供的输出分布
     3   先训练学生再用学生当教师去训练另一个，如此循环无限次
   A: 蒸馏损失在教师软化后的输出分布上计算 KL 散度（学生 ‖ 教师），迫使学生匹配教师的软目标；同时保留对真实硬标签的交叉熵，常见形式为 α·KL(学生‖教师) + (1−α)·CE(学生, 硬标签) 的加权组合。完全不用教师输出分布就只是普通监督训练，不是蒸馏。

########## reranking|mechanism   (n=6) ##########

• ai-fund-038  [medium]  concepts=
   Q: 在 RAG 流程中，Reranker 解决的是什么问题？为什么有了向量检索还需要它？
     0   把 top-k 从 10 扩大到 30，塞入更多候选内容让模型在生成时自行甄别取舍
     1 ✓ 在召回之后增加 cross-encoder 重排环节，对少量候选做联合编码精排，让真正相关的段落占据 top-k 再进入 context
     2   针对本领域从头训练一个专用 embedding 模型，重建全量索引以提升向量检索本身的精度
     3   先让模型为每个候选段落生成内容摘要，再把摘要拼接进 prompt 以节省 token 开销
   A: bi-encoder 快但粗（独立编码），cross-encoder 准但慢（联合编码）→ 召回后对少量候选精排，提升进入 context 的 precision。

• ai-fund-037  [medium]  concepts=
   Q: Hybrid Search 为什么通常比单独使用 Vector Search 更可靠？通常会怎么组合？
     0   整体替换为纯关键词检索，彻底摆脱对 embedding 模型的依赖，从根源解决精确符号匹配问题，因为关键词路在型号与错误码上的命中率天然高于任何向量方案。
     1   在用户查询后自动追加大量同义词扩展词再做向量检索，以覆盖编号、型号一类的表达，同义词越多召回越全，向量路便能完全替代关键词检索的职责。
     2 ✓ 改造为混合检索：并行保留向量路与关键词路，利用两路互补的失败模式稳住召回，并用 RRF 融合或 cross-encoder 重排统一排序
     3   要求用户提问前手动选择“语义搜索”或“精确匹配”模式，把两类查询分流处理，由用户自己承担路由责任，系统便无需再做任何自动融合与重排。
   A: 双路失败模式互补 → recall 更稳；典型做法：双路召回 → RRF 融合或 cross-encoder 重排 → top-k 进生成。

• ai-eng-063  [medium]  concepts=
   Q: 混合检索用 RRF（Reciprocal Rank Fusion，按排名倒数加权）合并向量检索与 BM25 的结果。相比于直接对两路相似度分数做加权求和，RRF 的核心优势是什么？
     0   RRF 能自动学习向量维度的最佳权重，无需人工设定任何参数，因为融合过程本身会根据两路召回的分布自适应地调整每条结果的贡献比例，并在训练阶段收敛到最优融合系数。
     1 ✓ 向量相似度（如 Cosine ∈ [0,1]）与 BM25 分数（无上界正实数）的分布与量级不同，直接加权求和会让数值范围大的一方主导排序；RRF 仅依赖相对排名倒数融合，从机制上消除了量纲不一致问题
     2   RRF 可以在 O(1) 时间完成合并，而分数加权需要 O(N log N)，因此它在大规模候选集上不仅更鲁棒，也拥有数量级层面的性能优势；即便候选数量增长到数万，RRF 的常数级开销依然稳定，是融合的首选方案。
     3   RRF 能提高 LLM 对 Context Window 的利用率并自动去重检索结果，因为它在融合阶段就剔除了语义重复文档，让进入生成的素材既精炼又不遗漏关键信息。
   A: 两路得分量纲不可通约：Cosine 相似度落在 [0,1]，BM25 分数是无上界的正实数且分布随语料库统计漂移——直接加权求和时数值范围大的一方会主导最终排序，权重形同虚设且极难调稳。RRF 只消费各路的相对排名（score = Σ 1/(k+rank_i)，k 通常取 60），彻底剥离原始分数量纲；代价是丢弃了"第…

• lateinter-05  [medium]  concepts=
   Q: ColPali 如何把晚期交互用于『视觉文档检索』，从而跳过传统 OCR/版面分析流水线？
     0 ✓ 把页面截图送视觉语言模型切 patch 并编码为多向量，与查询 token 做 MaxSim
     1 ✓ 直接在图像像素/patch 层面检索，无需先识别成文本，可利用表格图形等线索
     2   先对页面做 OCR 得文本，再对文本做单向量嵌入，与传统文本 RAG 等价
     3   每个页面只生成一个稠密向量，再用 cosine 做相似度检索
   A: ColPali 把页面截图送视觉语言模型切成 patch 并编码为多向量，查询文本 token 与页面 patch 做 MaxSim 匹配；直接在图像像素/patch 层面检索，无需先把页面识别成文本，从而能利用表格、图形等视觉线索。它并非先做 OCR 再单向量嵌入，也不是每页一个稠密向量。

• lateinter-02  [medium]  concepts=
   Q: 相比把整篇文本压成单个稠密向量，多向量（late interaction）表示在哪些匹配场景下更占优？
     0 ✓ 需精确匹配产品编码、人名、函数名等稀有实体时，单向量把它们与全文竞争同一空间
     1 ✓ 多条件查询能逐项与文档 token 对齐，而非被迫在单一向量中折中
     2   单向量在所有情况下都严格优于多向量，只是存储体积更大
     3   多向量必须在线编码文档，无法在索引阶段离线建索引
   A: 单向量把全文压进一个向量，稀有实体（产品编码、人名、函数名）和长文关键句要与全部内容竞争同一空间，多条件查询被迫折中；多向量保留每 token 一个向量，能与查询 token 逐项对齐，更适合精确标识符与多约束查询。多向量可离线建索引，并非必须在线编码文档。

• lateinter-01  [medium]  concepts=
   Q: 在 ColBERT 风格的『晚期交互（late interaction）』检索中，查询与文档的相似度如何计算？
     0 ✓ 对查询每个 token 取其与文档所有 token/patch 的最大相似度，再求和（MaxSim）
     1 ✓ 文档在索引阶段独立编码、可离线存储，评分只在查询时细粒度匹配
     2   把查询和文档拼接后送交叉编码器，得到单一的整体相关性分数
     3   只取文档单个稠密向量与查询向量做点积，等价于单向量检索方式
   A: 对查询中每个 token 嵌入，取它与文档所有 token/patch 嵌入的最大相似度，再对所有查询 token 求和，即 MaxSim：LI(q,d)=Σ max_sim(q_term, doc_patch)。文档在索引阶段就独立编码完毕、可离线存储，评分只在查询时与查询做细粒度匹配。交叉编码器是早交互（拼接联合编…

########## caching|mechanism   (n=6) ##########

• prompt-cache-kv-mechanism-01  [hard]  concepts=
   Q: 在基于 Transformer 架构的大模型 API 服务中，Prompt Caching 被广泛用于降低长上下文处理的首字延迟（TTFT）与计算成本。关于 KV Cache Prompt Caching 的底层工作机制与前缀匹配规则，下列哪些说法是正确的？
     0   缓存系统直接保存的是经过 Tokenizer 处理后的 Token ID 序列，当新请求到达时通过逐字比较 Token ID 实现毫秒级快速匹配与复用
     1 ✓ 缓存系统保存的是模型在前向传播过程中计算出的 KV Tensor，从而避免对已处理前缀进行重复的 Self-Attention 矩阵乘法计算
     2 ✓ 缓存命中严格依赖前缀的完全一致性，若在前缀断点前的任何位置修改工具定义或参数，会导致该断点之后的所有缓存全部失效
     3 ✓ 每次成功读取并复用已有缓存前缀时，不仅能以折扣价格结算输入 Token，还会同步刷新该缓存条目的存活周期（TTL）而无需再次缴纳写入费
   A: 缓存的物理载体是 Transformer 前向传播算出的 KV State（Key-Value Tensors），而非 Token ID，所以 A 错——只存 Token ID 无法跳过注意力的矩阵乘法；B 正确，复用 KV Tensor 正是避免 Self-Attention 重复计算的核心；C 正确，KV Tens…

• paged-attn-block-tradeoff  [hard]  concepts=
   Q: 在 LLM 推理服务框架中 PagedAttention 借鉴 OS 虚拟内存分页机制将连续 KV Cache 切分为固定大小的 Block 进行非连续显存分配。关于内存管理机制与 Block Size 的工程 Trade-off 下列哪些说法是正确的？（多选）
     0 ✓ 较小的 Block Size 能显著减少请求结束或未填满 Block 时的内部碎片提高显存利用率
     1 ✓ 较大的 Block Size 能提高 GPU Tensor Core 的访存连续性与计算吞吐但会增加内部碎片浪费
     2 ✓ PagedAttention 几乎消除了外部碎片因为所有物理块的大小相同可以按需分配给任意序列
     3 ✓ PagedAttention 允许不同序列共享相同的物理 KV 块从而实现零拷贝 Copy-on-Write
   A: PagedAttention 核心优势：将连续逻辑显存映射到非连续物理块。较小 Block 降低内部碎片(A)；大 Block 提高 SRAM/DRAM 连续读取效率但增加碎片(B)；统一尺寸 Block 解决外部碎片(C)；Parallel Sampling 场景下通过页表指针实现物理 KV Cache 零拷贝共享(…

• harness-cache-01  [hard]  concepts=
   Q: 在 LLM Agent 系统中，利用 Prompt Caching（前缀缓存）重用已计算的 KV 状态是降低延迟和成本的关键手段。Prompt Caching 的匹配要求输入 Token 序列必须保持精确的前缀一致。关于 Harness 层在构建 Prompt 时引发缓存失效的原因及优化策略，下列说法正确的有？（多选）
     0 ✓ 在存储工具定义（如 MCP 工具）时使用无序 Hash Map，会导致每次构建请求时 JSON 序列化的工具顺序不一致，从而引发 Prompt 缓存失效
     1   当用户在运行期修改工具的审批策略（如将“询问确认”改为“自动通过”）时，应当直接在 System Prompt 的工具定义文本中更新权限属性，以确保 LLM 获取最新规则
     2 ✓ 应当将易变的运行时配置（如权限策略）移出 Prompt，改由 Harness 在沙箱环境中拦截和校验工具执行，以维持 Prompt 文本的稳定
     3   对历史消息进行中间修改或调整序列化格式，只要总 Token 数量保持一致，就不会破坏 Prompt 缓存的匹配
   A: 无序HashMap序列化致工具顺序抖动破前缀命中；易变权限应移出Prompt由Harness校验；B直接改Prompt文本破稳定、D中改历史仍破前缀。

• cache-prefix-01  [hard]  concepts=
   Q: 为了降低 LLM 推理的 Prefill 计算开销并减少首 Token 延迟，推理引擎普遍引入了 Prefix Caching（前缀缓存）机制。针对该机制的匹配规则与 Prompt 结构设计，下列说法正确的有？（多选）
     0 ✓ Prefix Caching 依赖从序列开头开始的连续 Token 匹配；若两个 Prompt 的第一个 Token 不一致，即使后续大段文本完全相同，也无法命中缓存
     1   只要两个请求中包含完全相同的 System Prompt，无论将其放在 Prompt 的开头还是末尾，Prefix Caching 都能带来相同的加速效果
     2 ✓ 在工程实践中，应将固定的 System Prompt 和静态上下文放在 Prompt 前部，将动态变化的 User Query 或变量放在尾部，以最大化缓存命中率
     3 ✓ 命中 Prefix Caching 后，引擎可以直接读取复用已计算好的 KV Cache，从而显著降低 TTFT 并减少计算开销
   A: 前缀需从头连续匹配、System Prompt置前置提升命中、命中可复用KV显著降TTFT。

• ai-infra-001  [hard]  concepts=
   Q: 在 LLM 高并发服务化部署中，显存（VRAM）碎片与前缀重复计算是吞吐瓶颈。关于分页式 KV Cache（借鉴 OS 分页）与前缀树（Radix Tree）复用机制的原理与权衡，下列说法正确的有？（多选）
     0 ✓ 分页式 KV Cache 借鉴 OS 虚拟内存分页，将逻辑连续的 KV 切为固定大小物理 Block，按需分配以消除预分配导致的显存碎片
     1 ✓ 前缀树复用对 KV Cache 按 Token 序列做基数树管理，跨请求高效匹配重叠前缀并复用其 KV
     2 ✓ 在多轮对话/Agent 场景中，开启前缀复用的系统相较未开启能显著降低首字延迟（TTFT），因避免重复前缀的 Prefill
     3   分页式机制只能在单次请求内动态分配物理块，无法支持跨请求的 KV 块共享，因此前缀复用必须建立在完全独立的另一套显存分配器之上，两套机制互斥不能共存
   A: 分页式将逻辑连续 KV 切为固定物理 Block 解决碎片；前缀树按 Token 序列做前缀匹配跨请求复用；二者在多轮 Agent 场景均显著降 TTFT；D 称分页不能跨请求共享错误（物理块可共享）。

• ai-eng-027  [medium]  concepts=
   Q: 在长上下文 LLM 架构中，为了最大化利用 Provider 的 Prefix Caching 机制降低 Token 计费与首帧延迟（TTFT），提示词（Prompt）结构的构建规范是怎样的？原理是什么？
     0 ✓ 将静态文档语料（Corpus）置于最前，动态提问（Question）置于最后；原理是 Prefix Caching 严格按从首字符开始的连续完全匹配来识别缓存
     1   将动态提问（Question）置于最前，静态文档语料（Corpus）置于最后；原理是注意力机制对 Prompt 头部赋予最高权重
     2   将语料与提问混合输入，通过设置 <cache> 标签标记语义关键块；原理是 LLM 自动识别跨请求的向量语义相似度
     3   提示词顺序无影响，只要单个 Request 的总 Token 数保持不变，网关即会自动触发 KV Cache 命中
   A: Prefix Caching 仅在请求的前缀字符与历史请求完全一致时才能命中。将静态语料置于 Prompt 头部，变动的 Question 置于尾部，可确保多轮不同提问共享相同的前缀缓存。若将提问置于头部，每次提问变动都会破坏前缀匹配，导致 100% Cache Miss。

########## inference-optimization|system-design   (n=6) ##########

• sebastian-raschka-2026-08-23  [hard]  concepts=
   Q: 一个在线系统希望简单请求低延迟、复杂请求获得更多推理步骤，最合理的推理预算策略是什么？（单选）
     0   所有请求固定使用最大推理预算
     1 ✓ 按难度或验证信号动态分配并设置预算上限
     2   所有请求都禁用中间步骤
     3   只根据用户姓名决定预算
   A: 应根据任务难度、置信度或验证结果动态分配推理预算，简单请求提前结束，困难请求增加采样或搜索，并以服务 SLO 约束上限。

• sebastian-raschka-2026-08-93  [hard]  concepts=
   Q: 在线服务按请求动态分配推理预算时，哪些设计合理？（请选择三个）
     0 ✓ 简单任务优先走低预算路径
     1 ✓ 验证失败或难度高时升级预算
     2 ✓ 为每个请求设置延迟和费用上限
     3   所有任务固定使用最高预算以避免路由误差
   A: 系统可先用低预算路径，再依据任务难度、置信度或验证失败升级，并设置 token、延迟和费用上限。

• sebastian-raschka-2026-08-51  [hard]  concepts=
   Q: 为在线服务设计动态推理努力度路由时，哪些策略合理？（请选择三个）
     0 ✓ 先用轻量路径处理明显简单的请求
     1 ✓ 验证失败或难度高时再升级推理预算
     2 ✓ 用 SLO 约束最大思考 token 和端到端延迟
     3   无论请求难度都固定使用最高努力度
   A: 可以根据任务难度、初步置信度或验证失败情况分配预算，同时设置延迟和成本上限，避免所有请求都走最大路径。

• pd-disagg-01  [hard]  concepts=
   Q: PD 分离（Prefill-Decode Disaggregation）架构将 LLM 推理的 Prefill 和 Decode 阶段解耦，分别部署在不同的硬件集群上，并在两者之间通过网络传输 KV Cache。关于 PD 分离架构，下列说法正确的有？（多选）
     0 ✓ Prefill 节点与 Decode 节点可以根据各自阶段的硬件瓶颈（算力受限 vs 带宽受限）分别选用针对性优化的 GPU 配置
     1 ✓ Prefill 节点在生成首 Token 和 KV Cache 后，需要将该请求的 KV Cache 通过高速网络传输给 Decode 节点
     2 ✓ 条件 PD 分离（Conditional Disaggregation）策略允许较短的 Prompt 或已命中缓存的请求跳过解耦流程，直接在 Decode 节点完成，以节省 KV Cache 网络传输开销
     3   由于 Prefill 和 Decode 解耦，PD 分离架构完全消除了跨节点网络通信，显著降低了系统的整体运维复杂度
   A: 按阶段瓶颈选型、需高速传KV、条件分离短请求本地完成省传输、D称消除网络错误。

• sebastian-raschka-2026-08-63  [medium]  concepts=
   Q: 为高努力度推理配置预算上限时，哪些保护措施合理？（请选择三个）
     0 ✓ 限制最大推理 token 或候选数量
     1 ✓ 限制端到端时间和验证重试次数
     2 ✓ 为超限请求定义降级或可观测的失败路径
     3   为了追求准确率允许单请求无限搜索
   A: 预算上限应同时约束 token、时间和重试次数，并在超限时返回可识别的降级结果或切换到较低努力度，避免请求无限占用资源。

• ai-rl-scale-01  [medium]  concepts=
   Q: 在大模型强化学习（RL）后训练阶段，提升模型本身的推理效率和降低单 Token 算力成本对扩展模型能力（RL Scaling）至关重要。关于推理效率与 RL 后训练的关系，下列说法正确的是？
     0   RL 后训练的性能提升仅取决于奖励模型（Reward Model）的准确度，与模型本身的推理吞吐量无关
     1 ✓ 推理速度更快、单 Token 开销更低的模型，能够以更低的算力成本运行大规模并行采样（Rollouts），从而在多样化的环境中积累更丰富的探索经验
     2   RL 阶段要求模型必须保持高精度的慢速推理，因此降低推理延迟会破坏 RL 的收敛性
     3   在 RL 后训练中，模型能力提升的唯一瓶颈是预训练数据量的大小，与 RL 练习环境的多样性无关
   A: RL需大规模并行采样积累探索，推理快则低成本扩rollout；与奖励模型、数据多样性相关，非唯一瓶颈但关键。

########## sampling|mechanism   (n=6) ##########

• sebastian-raschka-2026-08-72  [hard]  concepts=
   Q: 检测 token 水印时，为什么需要使用统计检验而不是寻找某个固定词？（请选择两个）
     0 ✓ 水印信号通常分散在许多 token 的选择偏差中
     1 ✓ 检测显著性会受到文本长度和随机波动影响
     2   固定关键词可以覆盖所有模型和语言的水印
     3   只要发现一个高频词就能证明文本来自模型
   A: 水印通常表现为大量 token 选择上的微小偏差，而不是固定短语；检测器需要根据文本长度、密钥规则和观测偏差评估显著性。

• sebastian-raschka-2026-08-71  [hard]  concepts=
   Q: 生成式文本水印通常可以在不改变模型参数的前提下通过哪些机制植入？（请选择两个）
     0 ✓ 依据密钥和上下文伪随机划分 token 候选集合
     1 ✓ 在采样时对指定集合施加轻微偏置
     2   修改模型全部权重以编码作者身份
     3   只在生成完成后把水印字符串追加到文本末尾
   A: 水印可利用伪随机规则把 token 候选划分为不同集合，并在采样时轻微偏向某一集合；检测器再利用统计偏差判断文本是否带有水印。

• llm-03  [medium]  concepts=
   Q: 在 LLM 推理中，温度（temperature）参数主要影响？
     0   调高温度参数使输出分布更加多样，借助多样性让模型在不同结果中"投票"出正确答案
     1 ✓ 把温度参数调到接近 0，让解码更确定、输出分布更集中，保证相同输入得到稳定一致的抽取结果
     2   扩大模型的上下文窗口长度，窗口变长之后输出自然就不会再发生漂移
     3   扩充模型词表以覆盖更多金融术语，词表越大模型的输出就越稳定
   A: temperature 缩放 softmax 前的 logits，值越高输出越随机多样，值越低越确定集中。

• ai-logits-001  [medium]  concepts=
   Q: LLM 的 logits 是什么？它和最终生成的 token 有什么关系？
     0   Logits 是词表中每个 token 归一化后的概率值，总和为一，可直接作为输出置信度使用；归一化后的分数天然落在 0 到 1 之间，数值高低即代表模型对该 token 的确信程度。
     1 ✓ Logits 是模型最后一层对词表中每个 token 输出的未归一化分数，经温度缩放与 top-k/top-p 截断后 softmax 归一化为分布，再从中采样或取 argmax 得到下一个 token
     2   Logits 是输入文本的隐层表示，与词表维度无关，主要用于向量化检索场景；它承载句子级语义，经池化后可作为 embedding 供相似度检索与聚类任务直接使用。
     3   Logits 是 tokenizer 统计出的词频表，训练结束后固定不变，不会随输入内容变化；它记录各 token 出现频率，是模型先验偏置的来源，推理时直接查表即可得到。
   A: Logits = 词表上的未归一化分数；温度/top-k/top-p/logit bias 全部作用于这一层，再经 softmax 采样出 token；logprobs 可用于置信度类工程。

• ai-fund-027  [medium]  concepts=
   Q: 把 Temperature 设为 0，模型输出一定是确定性的吗？为什么这在生产系统中很重要？
     0 ✓ temperature=0 并不保证严格确定：浮点非结合误差、top-p 等其余采样参数以及服务端模型版本变化都可能引入波动
     1 ✓ 回归判定应改为在固定评测集上核对关键质量指标，放弃逐字一致的快照比对方式
     2   只要把 top-p 一并设为 0，输出就会逐字节稳定，现有快照比对机制可以继续沿用；双参数归零消除了采样不确定性，确定性解码使结果完全可复现，无需改用指标对比。
     3   任何输出不一致都意味着服务出现故障，应触发自动回滚并安排人工复盘；非确定性在推理服务中属于异常信号，稳定复现才是健康状态，波动即代表系统出错需立即处置。
   A: temp=0 ≠ 保证确定：浮点非结合/MoE 抖动/top-p 等参数仍在/服务端版本变化都会引入波动；生产要以评测集而非逐字比对做回归。

• llm-arch-thinking-sampling-01  [medium]  concepts=
   Q: 部署兼具思考模式（Thinking Mode）与指令模式（Instruct/Non-Thinking Mode）的思考型 LLM 时，两种模式需配置不同采样参数。下列关于采样参数配置最佳实践的说法中，正确的是？（单选）
     0 ✓ 思考模式通常推荐较高的 Temperature（如 1.0）与较宽松的 Top-p，以鼓励推理路径的多样性与自我探索
     1   指令模式通常推荐极低的 Temperature（如 0.0）与最小的 Presence Penalty，以强制触发链式思考输出
     2   无论是否开启思考模式，Presence Penalty 都应保持在最高值（如 2.0 以上），以完全避免生成重复 Token
     3   禁用思考模式后，必须将 Top-k 设为 1 且启用 Greedy Decoding，否则模型无法输出合规的格式化文本
   A: A 正确：Thinking 模式下较高 Temperature（如 1.0）有助于推理阶段探索多条思维分支、避免思维定势。B 错误：指令模式用于快速确切输出，通常用较低 Temperature（如 0.7）与较高 Presence Penalty 防重复，而非强制触发链式思考。C 错误：Presence Penalty…
