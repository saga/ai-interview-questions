
########## evaluation|system-design   (n=25) ##########

• sebastian-raschka-2026-08-177  [hard]  concepts=
   Q: 在分析一个新模型前记录架构假设，有哪些好处？（请选择三个）
     0 ✓ 把待验证问题变成明确检查项
     1 ✓ 区分报告中的事实与自己的推断
     2 ✓ 便于发现配置、代码和图表之间的不一致
     3   记录假设后就可以不再查看实现
   A: 假设清单能明确待核验事实、定位证据来源、区分推断与观察，并在代码验证后更新结论。

• sebastian-raschka-2026-08-172  [hard]  concepts=
   Q: 从代码绘制架构图后，哪些验证步骤有助于避免误读？（请选择三个）
     0 ✓ 沿前向路径核对张量形状和模块顺序
     1 ✓ 确认残差、归一化和注意力缓存的位置
     2 ✓ 检查 MoE、共享 KV 或特殊位置编码等分支
     3   只要图画得像常见 Transformer 就不必验证
   A: 应追踪一批张量的形状和顺序，核对残差与归一化，检查特殊层及输出头，并与配置匹配。

• sebastian-raschka-2026-08-158  [hard]  concepts=
   Q: 阅读论文消融表时，哪些信息有助于判断组件贡献？（请选择三个）
     0 ✓ 确认对照模型和训练预算是否匹配
     1 ✓ 确认每行是否只移除或替换目标组件
     2 ✓ 比较多个任务和资源指标的变化
     3   只挑对作者最有利的一列作为结论
   A: 要看基线是否公平、一次改变哪些变量、不同任务是否一致以及资源指标是否同时变化。

• sebastian-raschka-2026-08-145  [hard]  concepts=
   Q: 评估 KV Sharing、压缩注意力或 mHC 等新架构时，哪些对照更有说服力？（请选择三个）
     0 ✓ 相同模型规模和训练预算的基线
     1 ✓ 只改变一个组件的消融实验
     2 ✓ 不同上下文长度下的质量、显存和延迟
     3   只展示新方法最优长度下的平均速度
   A: 应包含相同规模基线、组件消融、目标长度和真实服务指标，避免只展示最有利的设置。

• sebastian-raschka-2026-08-136  [hard]  concepts=
   Q: 要验证某个架构组件是否真正带来收益，消融实验应如何设计？（请选择三个）
     0 ✓ 尽量固定数据、训练预算和优化配置
     1 ✓ 只改变待研究的架构组件
     2 ✓ 同时报告目标任务质量与资源指标
     3   同时改变模型规模、数据和架构以放大差异
   A: 应保持数据、训练预算和评估一致，只改变目标组件，并同时报告质量和系统指标。

• sebastian-raschka-2026-08-117  [hard]  concepts=
   Q: 团队想把论文中新的长上下文技术引入生产，哪些步骤更稳妥？（请选择三个）
     0 ✓ 先用 profiling 证明当前瓶颈与技术目标匹配
     1 ✓ 在代表性长上下文任务上比较质量和系统指标
     2 ✓ 通过受控消融和灰度测试验证收益
     3   看到论文宣称线性复杂度就直接替换生产模型
   A: 应先明确瓶颈和验收指标，再做受控原型、架构消融和真实负载测试，最后通过灰度验证质量与成本。

• sebastian-raschka-2026-08-104  [hard]  concepts=
   Q: 评估 Coding Agent 是否真的提升软件工程效率时，哪些指标应联合使用？（请选择三个）
     0 ✓ 真实任务的完成率和测试通过率
     1 ✓ 人工返工、回滚和缺陷率
     2 ✓ 端到端延迟、资源成本和工具失败率
     3   只统计模型生成的代码行数来衡量效率
   A: 仅看模型生成代码数量会掩盖返工和回归，应同时看测试通过率、任务完成率、人工修改量、延迟和成本。

• sebastian-raschka-2026-08-87  [hard]  concepts=
   Q: 评估 AI 文本检测器是否具备生产价值时，哪些实验设计更有区分度？（请选择三个）
     0 ✓ 测试训练未见模型和领域的文本
     1 ✓ 加入人类编辑、改写和混合来源样本
     2 ✓ 分别报告不同长度和写作群体的误报漏报
     3   只在训练分布上报告总体准确率
   A: 需要独立测试、多模型和多领域泛化、编辑后的文本以及按长度和群体分桶报告，不能只看单一总体准确率。

• sebastian-raschka-2026-08-82  [hard]  concepts=
   Q: 构建 AI 文本检测器训练集时，哪些数据策略更可靠？（请选择三个）
     0 ✓ 覆盖多种人类写作来源和体裁
     1 ✓ 覆盖多个模型、提示和生成任务
     2 ✓ 独立保留未参与训练的测试数据
     3   只用一个模型生成同一主题的正样本即可代表所有 AI 文本
   A: 训练集应同时包含多来源人类文本和多模型、多任务生成文本，并隔离评估集，避免检测器只记住主题、来源或固定模型风格。

• sebastian-raschka-2026-08-79  [hard]  concepts=
   Q: 将水印检测用于教育或内容审核时，哪些治理措施合理？（请选择三个）
     0 ✓ 将检测结果作为风险信号并保留人工复核
     1 ✓ 记录误报、漏报和申诉结果以校准阈值
     2 ✓ 测试不同语言、长度和编辑条件下的公平性
     3   只要检测分数超过阈值就自动处罚且不提供申诉
   A: 检测结果应作为调查信号而非唯一裁决，需提供人工复核、申诉、阈值监控和对不同语言与群体的误报评估。

• sebastian-raschka-2026-08-76  [hard]  concepts=
   Q: 评估文本水印检测器时，哪些测试集组成更合理？（请选择三个）
     0 ✓ 包含带水印和无水印的模型文本
     1 ✓ 包含人类文本以估计误报率
     2 ✓ 按文本长度、采样参数和改写程度分桶
     3   只用一批长的带水印样本并报告准确率
   A: 需要覆盖带水印与未带水印文本、不同长度和生成参数，还应包含改写文本与人类文本，以评估检测能力和误报。

• sebastian-raschka-2026-08-65  [hard]  concepts=
   Q: 设计推理任务验证器时，哪些做法有助于避免把验证器本身当成真实能力？（请选择三个）
     0 ✓ 使用训练未见的隐藏测试或变体
     1 ✓ 覆盖边界条件而不是只检查一个样例
     2 ✓ 用独立评估或人工抽检验证通过结果
     3   把验证器通过率直接当作无需审计的真实准确率
   A: 验证器应覆盖隐藏测试和边界情况，使用与训练不同的测试，并通过人工或第二种验证方式抽检，监控模型是否针对规则投机。

• oreilly-radar-2026-08-19  [hard]  concepts=
   Q: 要评估一套面向安全和生产可用性的护栏，测试集至少应同时包含什么？（单选）
     0   只放明确恶意样本追求最高拒绝率，拒绝越多护栏即越有效
     1 ✓ 同时覆盖危险、合法边界、多步上下文和表达变体，分别评估误报漏报与恢复
     2   只测试模型回复是否礼貌得体，礼仪正确即代表护栏可靠可用
     3   让模型自生成并自评安全样本，用模型自我打分作为唯一度量标准
   A: 需要危险样本、合法但易误判的安全研究或技术内容、边界样本、多步上下文和不同语言/表达变体，并分别报告漏报、误报、可用性和恢复指标。

• eval-grader-01  [hard]  concepts=
   Q: 在构建复杂 AI 系统时，不同形态的系统（纯 LLM、RAG 系统、Coding Agent、Multi-Agent 系统）其评测（Evaluation）架构的关注点与 Grader（评估器）选择存在显著差异。下列关于 AI 系统评测设计的说法正确的有？（多选）
     0 ✓ 对于 RAG 系统，评估不能仅看最终回答的准确性，必须将评估器解耦拆分为“检索阶段”（评估 Document Recall/Relevance）与“生成阶段”（评估 Faithfulness/Hallucination）
     1 ✓ 对于 Coding Agent 等具备 Tool Calling 与 Loop 调度的自主 Agent，最客观高效的 Grader 是针对其产出的 Patch 代码运行单元测试/集成测试，而非单纯依赖 LLM-as-Judge 评估文本
     2 ✓ 对于 Multi-Agent 编排系统，评测核心除了最终任务完成度之外，还需包含对任务路由准确性、角色契约遵从度（Role Adherence）以及节点间协作效率的评估
     3   只要最终输出的文本经过 LLM-as-Judge 打高分，就足以说明 RAG、Coding Agent 或 Multi-Agent 系统的架构设计没有任何缺陷
   A: RAG需拆检索/生成、Coding Agent以测试为Grader、Multi-Agent需路由/角色/协作评估、D称LLM打高分即无缺陷错误。

• ai-fund-042  [hard]  concepts=
   Q: 如何为一个上线中的 LLM Application 设计 Evaluation Dataset？请说明数据构成与持续维护机制。
     0   评测集建成后立即冻结不再修改，以保证不同版本之间的指标绝对可比；变更评测集会破坏历史可比性，冻结是最严谨的做法，线上分布漂移不应反向影响离线基准集。
     1 ✓ 分层覆盖正常主流程、边界输入（超长极短、多语言、格式异常）与对抗样本（注入、误导性前提），每条尽量附参考答案或评分要点
     2 ✓ 将线上真实失败案例固化为回归 case，并持续从生产日志抽样补充，保持与真实流量分布一致
     3 ✓ 对评测集做版本化管理，每次 prompt、模型或流水线变更都触发全量回归评测
     4   用程序自动生成百万级样本追求规模，数据量越大覆盖越全，无需人工筛选；规模本身即代表质量，机器生成的海量问答天然优于人工精心挑选的小而精评测集。
   A: 分层构成：正常/边界/对抗/回归/生产抽样 + 参考答案；版本化 + 变更触发回归 + 线上 badcase 持续回流，作为长期资产运营。

• ai-eval-007  [hard]  concepts=
   Q: 你修改了 Agent Prompt，但无法保证所有历史场景仍然正常。如何建立 LLM Regression Test？
     0   回归集只需覆盖新功能的主路径场景，历史 badcase 修复确认后即从用例库删除，以免陈旧用例拖慢回归执行速度。
     1 ✓ 把线上 badcase 固化为带判定标准的用例持续沉淀，测试集本身版本化管理，并定期从线上新增样本防止用例陈旧失效。
     2   判定只看整体平均分的变化即可，个别用例从通过变为失败属于正常波动，无需在报告中单独列出。
     3 ✓ 任何影响行为的变更（prompt、模型版本、工具定义、检索配置）都触发全量回归，结果与基线做 diff 并重点报告由过变挂的用例。
     4 ✓ 将回归套件接入 CI 成为合并门禁，核心场景零失败才允许发布，让行为回归像单元测试一样拦截在合并之前。
   A: 回归 = badcase 沉淀的场景库 + 变更触发的自动化执行 + 与基线的 diff 报告；接入 CI 成为合并门禁，用例库持续版本化补给。

• ai-eng-018  [hard]  concepts=
   Q: 你负责一个企业 RAG 系统，希望上线前证明系统质量。你会如何设计 offline evaluation？请说明 evaluation dataset、retrieval metrics、answer metrics 和 LLM-as-a-Judge 各自解决什么问题。
     0   先把线上流量日志收集起来留作日后分析，各项评估指标可以等系统上线积累数据后再补建；早期以功能交付为主，评测体系可在稳定后逐步补齐，不必在立项阶段投入建设，先上线再观测更符合敏捷节奏。
     1 ✓ 构建覆盖真实用户问题分布的 evaluation dataset，涵盖不同险种、文档类型与难度层级，并为每题标注 golden evidence 和参考答案，作为所有指标的计算基础。
     2   安排几位内部同事每人随手编写几十道简单问答题跑通流程即可，无需刻意覆盖真实用户的提问分布；内部视角足够代表典型用法，小规模样例能快速验证链路通畅，覆盖度问题上线后自然会暴露并补齐。
     3 ✓ retrieval 层用 recall@k、MRR、nDCG 评估证据找得准不准；answer 层用 correctness、faithfulness 等指标评估答得对不对，并以 LLM-as-a-Judge 批量打分，但要先与人工标注子集…
     4   直接把 LLM-as-a-Judge 的打分作为唯一质量结论，其评分已足够权威，无需确定性指标交叉验证或人工抽检；judge 模型能力超过普通标注员，单一信号即可直接用于发布决策，交叉校验属于冗余成本。
   A: offline evaluation 四件套：覆盖真实场景的数据集、retrieval 指标、answer 指标、LLM judge（需校准并结合人工）。

• agentic-52  [hard]  concepts=
   Q: 你认为未来 Agent Runtime 最重要的基础设施是什么？可以选：身份与权限体系、支付与结算、可验证的执行环境、统一记忆协议、评测基础设施……请选一到两项并深入论证。
     0   最重要的是无限扩大的上下文窗口，只要窗口足够大，身份、权限与结算等信任问题都会自然消解
     1 ✓ 身份与权限体系是最重要的地基：可信身份是授权委托、最小权限锚点与跨组织信任传递的前提，支付、工具市场与审计都构建在其上
     2   评测基础设施最重要，但它只在单个应用内部有价值，跨组织协作不需要任何共同的信任基础
     3   Agent 的瓶颈将长期停留在模型能力本身，责任与信任类基础设施可以等到能力完全饱和后再建
   A: 考察对信任基础设施的深层理解。身份与权限体系是地基——可信身份是授权委托、最小权限锚点与跨组织信任传递的前提；支付、工具市场与审计都构建在其上。类似互联网的价值建立在 TCP/IP 与 PKI 之上而非单个应用。

• agentic-10  [hard]  concepts=
   Q: 如何评估一个 AI Agent 的可靠性？请讨论常见的失败模式与对应的缓解措施。
     0   聚焦最终回答的语言流畅度与格式规范度做评估，中间轨迹与工具调用过程不必纳入考察；因为终端用户只感知最终输出，过程指标既难以量化也不影响业务结果，把评测资源集中在文本质量打分上即可。
     1   先做一轮小样本人工抽测，若全部通过即可认定系统可靠，无需再建评测集做回归；小样本通过说明主干逻辑健全，偶发长尾问题可由线上监控兜底，不必投入持续评测工程。
     2   循环卡死与上下文溢出属于运维故障而非可靠性问题，不应纳入评估与缓解的设计范围；这类故障由基础设施与超时配置负责，与评测集和失败模式分析无直接关联。
     3 ✓ 从任务成功率、轨迹有效性与工具调用准确率等维度构建评测集做统计化评估，并针对规划偏差、工具误用、循环卡死等失败模式分别配套校验、重试与步数上限等缓解手段
   A: 考察评估维度与失败模式的系统性思考。核心维度包括任务成功率、工具调用准确率、轨迹有效率与鲁棒性；常见失败模式有规划偏差、工具误用、幻觉、循环卡死与上下文溢出。缓解手段需配套：schema 校验防参数错、步数上限防死循环、检索+引用降幻觉。仅看最终回答流畅度会漏掉'表面正确实际错误'的案例。

• aws-waf-lens-2026-08-11  [hard]  concepts=
   Q: 更换基础模型后，团队只看到平均质量分数上升就准备全量发布。还缺少什么关键步骤？（单选）
     0   平均分上升即可发布，无需重新评估安全与成本，历史指标能代表新模型在所有子群上的表现
     1 ✓ 只让模型基于新结果自评新模型更好，由模型自行决定是否可以全量发布，无需独立评估集
     2   只让模型自评新模型更好
     3   删除旧模型评估结果避免被拿来比较，仅保留新模型 favorable 指标即可用于发布决策
   A: 应对固定评估集重新执行质量、安全、事实性、延迟和成本测试，检查关键子群和边界用例，并记录模型、prompt、检索和护栏版本；平均分不能覆盖回归风险。

• sebastian-raschka-2026-08-120  [medium]  concepts=
   Q: 复现论文中的模型或消融实验时，哪些信息应版本化记录？（请选择三个）
     0 ✓ 代码、数据和模型权重或版本指纹
     1 ✓ 训练、推理、随机种子和评估配置
     2 ✓ 硬件、软件依赖和运行日志
     3   只保存最终分数，不保存实验配置
   A: 应记录代码、数据和模型版本，训练与推理参数、硬件及随机性设置，才能解释结果差异。

• agentic-70  [medium]  concepts=
   Q: 在一个基于图结构驱动的多 Agent 系统中，若要对“静态图几何结构”与“动态运行态流量”之间的差异进行度量和优化，以下哪些指标或方法构成了有效的架构监控与诊断手段？（多选）
     0 ✓ 边利用率（Edge Utilization）：统计系统运行周期内实际发生消息传输的边数占总配置边数的比例
     1 ✓ 关系效率（Relationship Efficiency）：统计所有传输的消息中，能够向全局共享状态中引入全新事实/有效信息的离散消息比例
     2 ✓ TF-IDF 重复度（Lexical Redundancy）：计算新增消息与历史已共享信息库之间的词法相似度，用于评估信息轰炸与无效重发
     3   静态图配置密度（Relationship Density）：仅凭有向图的静态边数与最大可能边数比值，直接代替对运行态消息流量的性能监测
   A: A边利用率、B关系效率（有效消息比例）、C TF-IDF重复度（词法冗余）均为运行态流量诊断指标；D仅用静态密度代替流量监测是典型误用，未度量实际行为。

• aws-waf-lens-2026-08-24  [medium]  concepts=
   Q: 传统欺诈检测模型上线后，为什么需要同时监控数据质量、模型质量、偏差和特征归因变化？（单选）
     0   模型部署成功后只需监控 CPU 和内存
     1 ✓ 多维监控能区分输入漂移、质量退化、公平性变化和逻辑变化
     2   只监控总体准确率即可代表所有维度
     3   模型一旦训练完成就不会因数据变化而退化
   A: 输入分布、真实效果、公平性和模型依赖特征可能分别漂移；只看 endpoint 健康或总体准确率会遗漏业务退化、群体影响和数据管道问题。监控还应能关联模型与数据 lineage。

• aws-waf-lens-2026-08-19  [medium]  concepts=
   Q: 团队只邀请模型开发者定义 AI 风险，遗漏了受影响的客户群体。Responsible AI Lens 建议如何改进？（单选）
     0   只让开发者决定风险以提高效率
     1 ✓ 识别上下游和脆弱群体，邀请不同职能参与并登记潜在伤害
     2   只用历史平均准确率推断风险
     3   等上线出现投诉后再寻找利益相关者
   A: 应识别下游受影响者、上游贡献者和组织内的法务、隐私、风险、领域专家等利益相关者，并用多种场景识别潜在伤害，形成可持续维护的风险登记。

• agent-eval-04  [medium]  concepts=
   Q: 在 AI Agent 的持续迭代和部署生命周期中，开发团队需要区分能力评估（Capability Evals）与回归评估（Regression Evals）。关于两者的定义、演进关系及在 CI/CD 中的应用，下列哪些说法是正确的？
     0 ✓ 能力评估（Capability Evals）旨在探索 Agent 的能力边界，初始通过率通常较低，用于指导系统的攻坚演进
     1 ✓ 回归评估（Regression Evals）旨在确保系统在升级或 Prompt 调整后不发生既有能力退化，目标通过率通常接近 100%
     2 ✓ 随着 Agent 能力的提升，通过率已达标且稳定的能力评估测试用例可以逐步「毕业」（Graduate）并转入回归评估套件
     3 ✓ 当更换底层基础大模型时，回归评估套件能够帮助团队快速验证新模型是否会引起特定边缘场景的性能下滑
     4   为最大化评测效率，能力评估应在每次代码提交（Git Push）时全量运行，而回归评估仅需在季度发布前运行一次
   A: A 正确：能力评估探索能力边界，初始通过率通常较低，用于指导攻坚演进。B 正确：回归评估是防退化防线，目标通过率近 100%。C 正确：通过率已达标且稳定的能力用例可「毕业」转入回归套件。D 正确：更换底层模型或改动 Scaffold 时，回归套件能第一时间暴露边缘场景下滑。E 错误：回归评估应频繁运行（进 CI pi…

########## multi-agent|system-design   (n=5) ##########

• agentic-23  [hard]  concepts=
   Q: 设计一个 Multi-Agent 研究系统：Planner、Researcher、Critic、Writer 四角色。如何划分职责、传递信息并避免互相扯皮？
     0   四个角色之间应以自由对话形式充分讨论，讨论轮数越多观点碰撞越充分、成稿质量越高，无需约束
     1   允许 Writer 在成稿时补充自己已知的外部知识，以保证文章的可读性与信息完整性，自由发挥更好
     2   Critic 对稿件不满意时可以无限次打回重做，直到所有角色对最终版本达成一致为止，民主即质量
     3 ✓ 四角色通过共享黑板交换结构化的任务与评审状态而非自由对话；Writer 只能基于通过 Critic 审核的事实卡片成稿；同一稿件打回超过次数上限就升级人工处理
   A: 考察多角色分工与收敛机制。核心设计：通过共享黑板交换结构化状态而非自由对话（消息带 schema）；每轮有明确准入标准决定推进或打回；打回次数超上限则升级人工。缺乏收敛机制的多 Agent 系统常陷入几十轮互踢仍无结论的死循环，token 成本失控。

• agent-multi-01  [hard]  concepts=
   Q: 在处理复杂多步骤业务时，关于单 Agent 与多 Agent（Multi-Agent）架构以及 Orchestrator-Worker 模式的工程权衡，下列哪些说法是正确的？（多选）
     0 ✓ 多个平级 Agent 之间直接进行点对点（Peer-to-Peer）自由通信的架构，容易因各个子 Agent 的独立决策冲突而导致整体系统行为不可控
     1 ✓ 由单一中央协调者（Orchestrator）掌控全局上下文并分发任务给短生命周期（Short-lived）、单一职责的 Worker Agent，能有效减少 Agent 间的决策冲突
     2 ✓ 将复杂任务拆解为多个单一职责（Narrow Scope）的小型 Agent，能够缩小单个 Agent 的故障表面（Failure Surface），提升可测试性与排查效率
     3 ✓ 多 Agent 架构在提升复杂推理解决能力的同时，通常会显著增加 Token 消耗量与系统响应延迟
   A: 点对点自由通信易冲突、Orchestrator集中分发短Worker减冲突、单职责缩小故障面、多Agent增Token与延迟。

• multi-agent-locking-01  [hard]  concepts=
   Q: 在无中心化 Orchestrator 的轻量级多 Agent 团队协同开发模式中，多个 Agent 实例并行挂载同一 Git 代码库易引发重复劳动与合并冲突。为在最小化通信开销下实现任务抢占与同步，下列哪种机制最为合理？
     0 ✓ Agent 在开始特定子任务前，向共享 Git 仓库提交带有任务标识的独占锁文件，利用 Git 的原子提交与版本冲突校验完成任务认领
     1   由中心化 API 网关对代码文件进行行级排他锁定，禁止其他 Agent 在锁期间读取或修改该文件的任何内容
     2   各 Agent 独立在本地分支开发，开发完成后强制执行 git push --force 覆盖远程主干，由后续 Agent 负责重新同步
     3   放弃任何前置任务锁定机制，完全依赖后续 Git Merge 阶段由 LLM 自动解析并合并并发修改冲突
   A: A 正确：分布式 Agent 架构中，利用 Git 原子提交锁文件（如 current_tasks/ 下新建任务文件）作为分布式锁，是轻量可靠的并发控制手段；若 Push 冲突，后到 Agent 即知任务被抢占，转而选其他任务。B 错误：行级排他锁限制过死，且需复杂中心化锁服务，违背分布式灵活并行读写需求。C 错误：强…

• agentic-66  [medium]  concepts=
   Q: 在评估 Agent 协同网络的通信密度对系统性能的影响时，传统做法若直接对比链式（Chain）、星型（Star）与全连接（Mesh）结构，会导致“拓扑中心性/结构形状”与“边数量（关系密度）”两个混淆变量同时变化。为了在固定节点数（如 N=8）前提下严格隔离并独立评估“关系密度”这一单一变量，以下哪些实验设计是合理的？（多选）
     0 ✓ 保持节点数不变，通过 Erdős–Rényi 随机图模型均匀采样生成边，并强制剔除不连通的样本
     1 ✓ 锁定全局随机种子（Random Seed），保证同一密度配置下的图拓扑生成与消息路由算法完全可复现
     2   随着关系密度的增加，同步按比例扩充 Agent 节点数量，以保持全局平均度数（Average Degree）恒定
     3 ✓ 在控制 Agent 节点数与任务场景完全一致的前提下，单向梯度递增边数量（如从 20% 密度 sweep 至 100%）
   A: 需固定 N、隔离关系密度单一变量：ER随机图均匀采样+剔除不连通样本（A）、锁定随机种子保证可复现（B）、单向梯度扫边数（D）均正确；C 扩充节点改变了 N，破坏控制变量。

• ma-infra-isolation-08  [medium]  concepts=
   Q: 系统架构师在设计一个支持高自治度多智能体（Multi-Agent）协同工作的生产级基础设施时，为了防止智能体在共享资源池中陷入“地盘争夺战（Turf Wars）”并保障宿主系统稳定性，以下哪些架构设计方案是合理且必要的？（多选）
     0 ✓ 实施严格的最小权限原则（Least Privilege）与资源沙箱隔离，禁止智能体具备修改系统级账户、全局网络规则或跨容器提权的能力
     1 ✓ 建立中心化的服务发现与任务意图注册表（Intent Registry），使智能体在变更共享组件前必须声明并校验目标兼容性
     2   移除所有的速率限制（Rate Limit）与心跳监控，防止智能体因请求被阻断而误判环境异常
     3 ✓ 引入不可被智能体直接篡改的第三方仲裁节点（Arbiter）与人类介入（Human-in-the-loop）中断机制
     4   允许智能体在检测到任何未预期变更时，自动获取宿主机的根用户（Root）权限以进行紧急修复与环境重置
   A: A正确：最小权限与沙箱是安全基石。B正确：意图注册表显式化目标冲突。C错误：移除限流会导致资源耗尽崩溃。D正确：不可篡改的仲裁节点与人类介入中断机制必要。E错误：赋予Root权限放大提权与破坏风险。

########## memory|mechanism   (n=5) ##########

• skillevo-persistence-asymmetry-01  [hard]  concepts=
   Q: 在基于反思与迭代优化的 AI Agent 技能演进（Skill Evolution）架构中，系统通常包含用于指导 Agent 执行的具体“技能库（Skills Layer）”与记录历史经验模式的“知识库（Wiki Layer）”。当系统对新生成的候选技能进行验证评估（Validation Gating），发现性能下降并触发技能回滚（Rollback）时，关于系统的状态更新机制，下列哪种设计最为合理？
     0 ✓ 技能库回滚至上一轮验证通过的版本，而知识库保留本轮提取出的失败模式与日志记录
     1   技能库与知识库同步回滚至上一轮状态，彻底清除本轮尝试产生的所有数据与临时修改
     2   技能库保留本次修改但降低调用权重，知识库仅在后续验证通过后才补充记录相关模式
     3   技能库回滚至上一轮验证通过的版本，同时重置知识库中涉及该技能的所有归纳索引
   A: A 正确：技能库控制 Inference Agent 的执行逻辑，验证集表现衰退时必须回滚以维持系统 Baseline 性能；而知识库保存的是高层抽象的失败归纳与历史决策记录（如提案 Diff 和准入结果）。知识库保持单调递增、不断积累（Never rolled back），能确保后续优化器在产生新提案时具备长期的历史…

• mem-hierarchy-01  [hard]  concepts=
   Q: 生产环境中的 AI Agent 架构常借鉴操作系统（OS）在 RAM 与 Disk 之间进行页面调度（Paging）的思想，设计分层记忆架构（包含 Context Window、Session Memory、Long-term Store、Cold Archive）。关于信息在该分层体系中的动态流转与调配机制，下列描述正确的是？
     0   所有历史交互在产生时直接同时写入 Long-term Store 与 Context Window，无需进行冷热分层，这样能最大化保留完整信息且省去检索开销
     1 ✓ 当新的用户消息到达时，系统从底层的 Long-term Store 检索相关的持久事实，并将其提升（Promote）装载回 Context Window 中，会话结束再把上下文压缩降级到冷层
     2   Session 结束时，Context Window 中的原始 Token 必须无损降级写入 Cold Archive，严禁进行任何形式的压缩或总结以免丢失细节
     3   Context Window 处于分层体系最高层，其 Token 访问成本最低且存储容量最大，应把尽可能多的数据放进去以获得最佳性能
   A: 新消息到达时从Long-term Store检索提升至Context，A全量直写、C无损降级、D Window成本低容量大均误。

• mem-stateless-01  [medium]  concepts=
   Q: 在开发 Agent 产品时，用户往往感觉“模型记住了昨天的对话”。从 Transformer 模型底层的 API 合约与系统架构的角度来看，这种记忆连续性的本质是？
     0   商业 LLM API 在服务端默认保留了跨请求的模型隐层状态（Hidden States）及 Key-Value Cache
     1 ✓ LLM 本身是无状态的（Stateless），每一轮 API 调用都基于全新空白状态；连续性完全由外围 Agent 平台系统通过状态管理、记忆检索与 Prompt 动态组装工程来实现
     2   LLM 在 Fine-tuning / Continual Pre-training 过程中实时将用户的对话记录更新到了模型权重参数中
     3   只要 Prompt 长度不超过模型宣称的最大 Context Limit，LLM 就能在无外部数据库辅助的情况下自动跨 Session 持久化所有状态
   A: LLM无状态，每轮全新空白，连续性由平台状态管理与检索组装实现。

• mem-cog-01  [medium]  concepts=
   Q: AI Agent 的记忆架构通常借鉴认知科学，将其划分为不同功能的记忆类型。现有一个智能编程/运维 Agent，在其运行过程中记录了以下数据：1. 当前正在针对某堆栈报错及对应代码段进行 Debug 分析；2. 记录“三天前与用户排查了 CI/CD 流程报错，并修改了部署脚本”；3. 记录“用户团队在生产环境采用 GitHub Actions 部署，且偏好使用 Python”；4. 记录“在生成代码 Review 总结时，用户要求固定采用三段式结构”。关于上述数据与 Agent 记忆类型的对应关系，下列分析正确的是？（多选）
     0 ✓ 数据 1 属于 Working Memory（工作记忆），仅驻留于当前任务的 Context Window 中，任务结束即被清空
     1 ✓ 数据 2 属于 Episodic Memory（情境记忆），是带有时间戳和特定交互事件背景的事件记录
     2 ✓ 数据 3 属于 Semantic Memory（语义记忆），是从多次交互中抽象出的独立于特定对话的持久事实与知识
     3 ✓ 数据 4 属于 Procedural Memory（程序记忆），固化了 Agent 针对特定任务或工作流的行为范式与偏好规则
   A: 1工作记忆随任务清、2情境带时戳、3语义持久事实、4程序固化范式。

• agentic-56  [medium]  concepts=
   Q: 为什么 Agent 的 Memory 不应该简单等同于 Conversation History？
     0 ✓ 对话历史信息密度低、噪声大、随轮次线性膨胀，超出窗口且检索低效；Memory 应是提炼后的结构化状态与知识，支持按相关性召回而非按时间全量回放
     1   因为对话历史在技术上根本无法被任何存储系统持久化与读取，所以只能依赖模型在当次对话中临时维持，外部记忆毫无用处
     2   因为 LLM 在架构上无法理解对话历史的文本格式，任何历史注入都会造成解析失败或输出乱码，根本无法被模型利用
     3   因为对话历史会随着会话自然结束而自动过期消失，系统层面没有任何手段能够将其长期保留，只能随用随弃
   A: 逐字历史是记忆的原料而非记忆本身：大量寒暄、重复与失败尝试会稀释关键信号。有效的 Memory 需要提炼（摘要/结构化/向量化），支持按相关性召回而非按时间全量回放。

########## human-in-the-loop|system-design   (n=5) ##########

• hitl-2026-009  [hard]  concepts=
   Q: 一个长时间运行的 Agent 在第 17 步需要人工批准后才能继续。你会如何实现暂停与恢复，使恢复后的 Agent 不需要重新执行已经完成的工具调用？
     0 ✓ 将 Agent 状态与检查点持久化到外部存储，再安全暂停等待人工批准
     1 ✓ 恢复时从最近检查点加载状态续跑，跳过已完成工具调用而非重放
     2   暂停时只保留最终目标，恢复后从头重新规划并执行全部步骤以保证正确
     3 ✓ 为每步写入幂等可重入的进度记录，使中断点之后的步骤可安全恢复
   A: A 正确：将 Agent 状态（已完成步骤、中间结果、检查点）持久化到外部存储，再安全暂停等待人工批准。B 正确：恢复时从最近检查点加载状态续跑，跳过已完成的工具调用而非重放，避免重复执行与副作用。C 错误：暂停只保留目标、恢复后从头重跑所有步骤，会重复调用已完成工具、产生重复副作用且低效。D 正确：为每步写入幂等可重…

• hitl-2026-007  [hard]  concepts=
   Q: 人工审核 Agent 的 approve、reject、edit 和 override 都会产生反馈。如何把这些行为转化为后续评估、策略优化或训练数据，而又避免把人的随手操作直接当成高质量监督信号？
     0 ✓ 将反馈沉淀为评估信号与策略更新依据，并打标区分高/低置信度样本
     1 ✓ 对编辑与覆盖做差异归因，定位 Agent 失败模式用于迭代优化
     2   把每一次点击 approve 都直接当作高质量标注用于微调无需甄别
     3 ✓ 仅把经确认的低置信或高影响反馈纳入训练，随手操作只进监控
   A: A 正确：将反馈沉淀为评估信号与策略更新依据，并打标区分高/低置信度样本，便于后续筛选。B 正确：对编辑（edit）与覆盖（override）做差异归因，定位 Agent 失败模式用于迭代优化。C 错误：把每一次点击 approve 都直接当高质量标注用于微调，会引入大量噪声与偏见。D 正确：仅把经确认的低置信或高影响…

• hitl-2026-006  [hard]  concepts=
   Q: 为什么"批准 Agent 可以处理这类请求"不应该等价于永久授予它处理该类所有未来请求的权限？如何设计作用域受限、可撤销的审批？
     0 ✓ 审批应限定作用域（本次请求/会话/指定金额）而非 blanket 永久授权
     1 ✓ 授权必须可随时撤销，并设有有效期与审计记录避免权限长期沉淀
     2   一旦批准某类请求即可永久信任 Agent 处理该类所有未来请求以简化
     3 ✓ 采用最小权限与按需再授权，高敏感操作每次或每会话都重新确认
   A: A 正确：审批应限定作用域（本次请求/本次会话/指定金额），而非 blanket 永久授权，防止权限被滥用。B 正确：授权必须可随时撤销，并设有有效期与审计记录，避免权限长期沉淀。C 错误：一旦批准就永久信任该类所有未来请求，等于把一次性判断变成永久放权，风险极大。D 正确：采用最小权限与按需再授权，高敏感操作每次或每…

• hitl-2026-004  [hard]  concepts=
   Q: 人工审批 Agent 操作时，审批界面至少应该向审核者提供哪些信息？为什么只展示 Agent 即将执行的动作通常不够？
     0 ✓ 展示动作将造成的实际影响范围与是否可逆，帮助审核者判断风险
     1 ✓ 展示 Agent 提出该操作的理由与上下文，而非只给一个孤立的命令
     2   只需展示即将执行的命令字符串即可，审核者应自行理解其全部后果
     3 ✓ 展示受影响对象、预计结果与历史类似操作记录以支持审计一致性
   A: A 正确：应展示动作将造成的实际影响范围（blast radius）与是否可逆，帮助审核者判断风险。B 正确：应展示 Agent 提出该操作的理由与上下文，而非只给一个孤立的命令，否则审核者无法判断意图。C 错误：只展示命令字符串是不够的，审核者无法仅凭命令理解全部后果，极易误批。D 正确：展示受影响对象、预计结果与历…

• hitl-2026-001  [medium]  concepts=
   Q: 一个 Agent 可以自动执行查询、修改数据和发起退款三类操作。你会如何设计 Human-in-the-loop（HITL）策略，使高风险操作需要人工审批，同时避免所有操作都进入审批队列？
     0 ✓ 按风险分级：查询读取自动执行，修改数据中风险，退款删除为高风险需审批
     1   对三类操作统一强制每次人工确认以追求绝对零风险、不留任何遗漏
     2 ✓ 仅对退款等不可逆高影响操作设人工审批，查询与常规修改默认自动放行
     3 ✓ 依风险等级与可逆性设阈值，超阈值才进入审批队列并支持分级动态调整
   A: A 正确：应按风险分级，查询/读取为低风险自动执行，修改数据为中风险，退款/删除为高风险需人工审批。B 错误：对三类操作统一强制确认会带来审批疲劳、压垮吞吐量，且未必提升安全性。C 正确：仅对退款等不可逆、高影响操作设人工审批，查询与常规修改默认自动放行，是风险分级的核心落地。D 正确：依据风险等级与可逆性设定阈值，超…

########## self-attention|debugging   (n=6) ##########

• mask-remove-debug  [hard]  concepts=
   Q: 在开发基于 Encoder-Decoder 的序列生成模型时不慎移除了 Decoder 内部 Masked Self-Attention 中的上三角掩码矩阵。在 Teacher Forcing 训练及自回归推理阶段模型会表现出哪些现象？（多选）
     0 ✓ 训练阶段的损失函数会异常迅速地下降到接近 0 因为当前位置可以直接看到未来位置的真实 token
     1   训练阶段前向传播会因为注意力矩阵掩码缺失而触发张量形状不匹配的错误
     2 ✓ 推理阶段模型的生成性能会严重崩塌产生大量重复或不连贯的无意义文本
     3   推理阶段模型能够突破单步串行限制实现多 token 的并行生成
   A: A 正确：无掩码时当前位置直接看到未来真实 token，Loss 异常快速降至接近零——这是数据泄漏的典型症状。B 错误：掩码只是数值填充移除后张量形状不变不会报错。C 正确：训练时学到了依赖未来信息的捷径推理时没有未来信息可用生成严重崩塌产生重复或无意义文本。D 错误：推理仍需自回归串行无法并行。

• sebastian-raschka-2026-08-41  [hard]  concepts=
   Q: 实现注意力模块时，输出形状正确但结果明显异常，以下哪些检查最有价值？（请选择三个）
     0 ✓ 确认 QK^T 的维度和 softmax 归一化轴正确
     1 ✓ 确认使用了正确的 sqrt(d_k) 缩放
     2 ✓ 确认因果掩码在 softmax 前作用于正确位置
     3   只要输出 shape 正确就无需检查数值范围
   A: 需要检查 QK 点积的转置维度、缩放因子、softmax 归一化轴以及掩码应用位置；这些错误常不改变最终张量形状。

• sebastian-raschka-2026-08-35  [hard]  concepts=
   Q: 一个语言模型在训练时意外移除了因果掩码，但训练 loss 仍然很低。上线逐 token 生成后性能明显下降，哪些解释合理？（请选择两个）
     0 ✓ 训练期间发生了未来 token 泄漏
     1 ✓ 训练目标与自回归推理的可见上下文不一致
     2   说明模型一定是因为词表太小
     3   说明关闭掩码会让 decode 自动变成双向推理
   A: 训练时模型可能通过读取未来真实 token 获得泄漏信息，导致 loss 虚低；生成时未来 token 不存在，训练和推理分布不一致。

• sdpa-omit-scaling  [medium]  concepts=
   Q: 在实现大隐层维度 Self-Attention 时误删除了缩放因子 √d_k，模型训练过程中最可能出现哪些异常现象？（多选）
     0 ✓ QKᵀ 的点积数值随 d_k 增大而变大导致 Softmax 输出进入极小梯度的饱和区引发梯度消失问题
     1 ✓ Softmax 后的概率分布过度陡峭类似于 one-hot 削弱模型同时关注多个相关 token 的能力
     2   Softmax 后的概率分布趋于均匀分布导致模型无法有效聚焦到关键 token
     3   矩阵乘法 QKᵀ 与 V 的维度不匹配前向传播直接抛出 RuntimeError
   A: A 正确：点积随 d_k 增大导致 Softmax 进入饱和区梯度消失。B 正确：概率过度陡峭削弱多 token 关注能力。C 错误：大方差导致分布更尖锐（接近 one-hot）而非均匀。D 错误：维度匹配不受缩放因子影响。

• scale-factor-debug  [medium]  concepts=
   Q: 在计算 Self-Attention 权重时通常会将 QKᵀ 除以 √d_k。当 d_k 很大时若遗漏了该缩放因子，会导致模型训练出现哪些典型问题？（多选）
     0 ✓ QKᵀ 的点积方差随 d_k 线性增大，导致数值送入 Softmax 后进入极小梯度的饱和区，引发梯度消失
     1 ✓ Softmax 输出的概率分布过于趋近于极端的高峰（接近 One-Hot），注意力集中在极少数 Token 上，降低模型表征能力
     2 ✓ 点积极值过大容易造成指数运算中的浮点数溢出（Overflow）或下溢（Underflow）
     3   导致 Attention 机制失去并行计算能力，反向传播必须退化为逐字串行求解
   A: A 正确：点积方差随 d_k 线性增大导致 Softmax 进入饱和区。B 正确：概率过于集中降低表征能力。C 正确：极值容易造成浮点溢出。D 错误：缩放因子与并行计算能力无关。

• mask-debug-01  [medium]  concepts=
   Q: 在手动实现 Causal Self-Attention 时，某工程师将被 Mask 的位置填充为 0.0（scores.masked_fill(mask, 0.0)），测试发现注意力分布完全没有起到遮掩未来 Token 的作用。请分析根本原因：
     0   masked_fill 中填入的数值应为 0.0 是正确的做法，只是 Softmax 维度设错成了 dim=-1，应改为 dim=-2 就可以正确屏蔽未来 token
     1 ✓ 将被 Mask 的位置填充为 0.0，经过 softmax 后 e^{0.0}=1.0，被遮挡的位置依然会分得很大的注意力权重，导致无法真正屏蔽未来信息
     2   torch.triu(..., diagonal=1) 生成的是下三角矩阵，导致遮挡方向完全颠倒，变成了遮挡历史 Token 而保留未来 Token，与因果掩码意图相反
     3   Q 和 K 乘积前没有对 K 进行转置，导致矩阵维度不匹配并引发隐式广播错误，使得注意力分数矩阵的形状与预期不一致
   A: 正确答案 B。Softmax 包含指数运算 e^x。若被掩码的 Score 填为 0.0，则 e^0=1，这些位置在 Softmax 后仍会获得非零概率权重（甚至高于负分值位置）。正确做法是填充负无穷（-inf 或 -1e9）使 e^{-inf}=0，从而在 Softmax 后权重恰好为零。

########## self-attention|calculation   (n=5) ##########

• attn-scaling-memory  [hard]  concepts=
   Q: 在标准 Full Self-Attention 中设序列长度为 N 隐藏维度为 d。当扩充上下文窗口时关于其计算复杂度与显存占用的分析哪些项是正确的？（多选）
     0 ✓ 计算 Queries 和 Keys 点积生成注意力得分矩阵的浮点运算复杂度为 O(N² · d)
     1 ✓ 在反向传播时保存未经过 Softmax 的原始 Attention Matrix 需要占用 O(N²) 的激活显存空间
     2 ✓ 在隐藏维度 d 保持不变的情况下将序列长度 N 扩大到原来的 4 倍标准 Self-Attention 计算得分矩阵所需的计算量和显存消耗将分别扩大到原来的 16 倍
     3 ✓ 引入检索增强机制或线性注意力机制的主要工程动机之一就是将对长序列的全局计算复杂度从 O(N²) 降低到子二次级别
   A: A 正确 Score 矩阵浮点运算 O(N²·d)。B 正确 反向传播保存原始 Attention Matrix 占 O(N²) 激活显存。C 正确 N 扩大4倍→N²扩大16倍计算量和显存均变为16倍。D 正确 检索增强或线性注意力的动机就是将全局计算从 O(N²) 降至亚二次级别。

• long-context-attn-01  [hard]  concepts=
   Q: 将 Standard Full Self-Attention 应用于超长文本处理（如 N=128,000）时需要评估资源瓶颈。设 N 为序列长度，D 为隐藏维度（D≪N），Batch Size=1，不使用 KV Cache。下列关于资源消耗的计算与判断哪些是正确的？（多选）
     0 ✓ 计算注意力得分矩阵 QK^T 的时间复杂度为 O(N² · D)，随序列长度 N 呈二次方增长
     1 ✓ 存储完整注意力权重矩阵的空间复杂度为 O(N²)，在 N=128k 时单头仅注意力权重的 FP16 显存占用就高达数十 GB 极易触发 OOM
     2   由于 QK^T 的瓶颈主要源自序列长度 N，在 N≫D 的场景下 Feed-Forward Network 层的显存占用会显著超过 Self-Attention 层
     3 ✓ FlashAttention 等算子融合技术的核心思路是利用 Tile 划分在 GPU SRAM 中分块计算 Softmax，将 HBM 的中间矩阵显存占用从 O(N²) 降低至 O(N)
   A: A 正确：QK^T 时间 O(N²·D) 随 N 二次方增长。B 正确：N=128K 时单头 FP16 注意力矩阵占 128000²×2 bytes ≈ 30+ GB 极易 OOM。D 正确：FlashAttention 通过 Tile 划分在 SRAM 中分块计算将 HBM 中间矩阵从 O(N²) 降至 O(N)。C…

• mha-complexity-01  [hard]  concepts=
   Q: 在标准 Multi-Head Self-Attention（MHA）中，假设输入序列长度为 N，隐层维度为 d_model，头数为 h，每个头的维度 d_k = d_model/h。针对单层 MHA 的计算量与中间激活显存开销，下列分析正确的有？（多选）
     0 ✓ 线性投影计算 Q,K,V 及最终输出投影 W_O 的总计算复杂度为 O(N·d_model²)
     1 ✓ 计算所有头的 Attention Score 及其与 V 相乘的总计算复杂度为 O(N²·d_model)
     2 ✓ 在没有 FlashAttention 等融合技术时，反向传播必须保存完整注意力权重矩阵，显存空间复杂度为 O(h·N²)
     3   当 N≫d_model 时，整体瓶颈主要由投影矩阵的参数量决定而非序列长度的二次方项
   A: A 正确：投影矩阵总复杂度为 O(N·d_model²)。B 正确：所有头的 Score 计算合计为 O(N²·d_model)。C 正确：保存完整注意力权重矩阵用于反向传播的空间为 O(h·N²)。D 错误：当 N≫d_model 时，二次方项 O(N²·d_model) 和空间项 O(h·N²) 占据绝对主导。

• attn-complexity-core  [medium]  concepts=
   Q: 假设一个标准 Transformer Encoder 层的输入序列长度为 N，隐藏层维度为 D。在计算 Self-Attention 矩阵（即 QKᵀ 以及对 V 加权）时，其时间复杂度和主要注意力矩阵的显存空间复杂度随序列长度 N 的增长关系分别是？
     0   时间复杂度 O(N · D²)，空间复杂度 O(N)
     1 ✓ 时间复杂度 O(N² · D)，空间复杂度 O(N²)
     2   时间复杂度 O(N² · D²)，空间复杂度 O(N² · D)
     3   时间复杂度 O(N · D)，空间复杂度 O(N · D)
   A: 正确答案 B。计算 QK^T 需要 N² 次点积、每次 D 维乘加，时间复杂度 O(N²·D)；存储完整 N×N 注意力权重矩阵的空间复杂度为 O(N²)。这是长上下文场景的核心瓶颈，也是 FlashAttention 等优化技术的出发点。

• sebastian-raschka-2026-08-37  [medium]  concepts=
   Q: 模型隐藏维度为 768，使用 12 个等宽注意力头时，关于每头维度的说法哪些正确？（请选择两个）
     0 ✓ 每头的 Query/Key/Value 投影宽度可为 64
     1 ✓ 12 个头拼接后恢复为 768 维
     2   每头必须使用完整的 768 维且拼接后为 9216 维
     3   头数增加会让每头维度自动保持 768 不变
   A: 等宽多头通常令每头维度为 768/12=64。各头输出拼接后恢复到 768 维，才能接入后续残差路径。
