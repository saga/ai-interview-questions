# Build output & caches
/dist/
/coverage/

# Node modules and package-manager caches
/node_modules/
/yarn.lock

.zvec-grep/

# Environment files
/.env

# TypeScript cache
*.tsbuildinfo

# Python cache
__pycache__/
*.py[cod]

# Local embedding model cache (large binary artifacts; never commit)
.cache/
.hf-cache/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 本地 LLM Provider 配置（含密钥，绝不提交）
data/provider.local.json

# 批量修复长度泄题的候选产物（review 用的中间态，不提交）
biasfix-report.json
src/data/questions.biasfix/
