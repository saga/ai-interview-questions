# AGENTS.md

本文件供在本仓库工作的 AI Agent（WorkBuddy 等）阅读，明确协作与代码约定。**只放原则性约定**，具体命令见 `README.md`，架构与踩坑见 `docs/`。

## 大原则

### 1. 不要over design，计划然后行动

- 设计要符合用户需求，如果需求不明确，应该与用户确认，而不是无脑的发散
- 不要over design，但是要保留创新性，对设计进行批判性思考
- 不要突然进行产品设计方向性的转变，对于大的架构修改变化，一定要确认
- 大修改大变化，一定要“计划 -> 写设计文档 -> 用户确认 -> 执行修改“

### 2. 代码更新、文档与架构决策必须一致

- 任何影响设计/架构的代码变更（新增模块、分层边界、数据契约、关键依赖引入），必须**在同一批改动里**同步更新文档：架构变化写 `docs/ARCHITECTURE.md`，变更记录追加到 `docs/CHANGELOG.md` 顶部，有取舍的决策补 ADR 到 `docs/DECISIONS.md`。
- 不允许出现"代码已改、文档还停在旧形态"的状态。文档不是事后补记，而是变更交付的一部分。
- 踩坑（依赖解析、构建配置、平台兼容等）必须沉淀进 `docs/ARCHITECTURE.md` 的「技术栈注意点」，让后续 Agent 不重复踩。
- 反向也成立：修改文档中的架构描述时，确认代码确实如此；发现不一致时以代码为准并立即修正文档。

### 3. 不要搞向后兼容，没用的代码直接删掉

- 修改接口、类型、函数签名时，**直接改成目标形态**，不要保留旧参数/旧分支/`deprecated` 兼容层。
- 重构后确认无人引用的导出、类型、文件，**立即删除**，不要标记 TODO 留着。
- 删除比加 `if (legacy) ...` 更干净。宁可让调用方一次性跟着改，也不要让代码背上历史包袱。
- 例外：对外已发布的 JSON 题库结构与 `localStorage` 配置 key 属于用户数据契约，改动需显式说明，不属于"可随手删的内部死代码"。

### 4. 添加关键测试，确保逻辑正确

- 纯逻辑（抽题、判分、题型判定、评分聚合、变体变换的 JSON 解析等）**必须有测试覆盖**，不要只靠 `npm run build` 过关。
- 测试框架用 **Vitest**（与 Vite 同生态，零额外配置成本）。新增逻辑时在同目录放 `*.test.ts`。
- 测试要断言"正确"而不仅是"不崩"：边界值、空输入、乱序答案、LLM 返回残缺 JSON 的兜底都要覆盖。
- 涉及 LLM 的用例一律 mock，**不要**在单测里真发网络请求：`pi-ai` 一次性调用可注入假 config（见 `src/ai/evaluate.test.ts` 的纯函数测法）。
- 提交前跑 `npm run test`（若未配置则先 `npm i -D vitest` 并建立脚本）。

### 4.1 文章转题的内容质量门槛

- 文章是事实来源，不是题目模板。题目必须脱离原文和产品名仍然自包含，考察可迁移的机制、权衡、场景设计、故障排查或治理原则。
- 不得把厂商功能名、内部术语、文章标题或产品流程本身做成主要考点。专有名词只能作为来源背景；若删掉产品名后题目失去技术意义，题目必须重写或删除。
- 来源框架、Lens、认证考纲和文章标题不能成为答题前提。禁止“哪种做法符合某 Lens/框架”“某框架建议什么”这类需要先知道来源分类才能作答的题；必须把来源原则改写成给出目标、约束和验收标准的通用工程场景，来源名称只能保留在 `source` metadata。
- 禁止“某产品中的 X 是什么”“X 主要做什么”式名词记忆题，除非 X 是行业通用技术概念且题目进一步考察其机制或边界。
- 每道题只能考一个明确知识点。题干必须给出足够的目标、约束和验收标准；不能依赖“文中提到”“上述方法”等上下文指代。
- 干扰项必须代表真实、可解释的工程误区，并与正确答案互斥；不得用“换模型、删测试、只看 token、完全自动化”等荒谬稻草人选项凑数。
- 出题完成后必须逐题人工审查：删去产品名仍可作答、答案可由工程原则推导、解析解释了权衡与边界。结构校验通过不等于内容质量通过。

### 5. web抓取注意

- 不要多线程抓取！不要并发抓取！模拟人的行为，不要有规律，不要无任何延迟，人阅读网页需要时间！
- 不要反复读取刷新页面，可以保存html到本地然后分析

#### 5.1 真实教训：2026-08-28 examcademy.com 账号被 disable

本次抓取 AWS AIP-C01 题库时，因以下行为触发站点反爬/滥用风控，导致账号被禁用：

- **批量自动化"Show Answer"**：用脚本一次性 `click()` 当前页所有"Show Answer"按钮、无任何间隔。真人是一题一题展开阅读，机器人特征极明显——这是最可能直接触发封禁的动作。
- **对同一页面反复 reload / 重复抽取**：为调试选择器，多次重新 `page.goto` 同一 URL 并重复 `page.evaluate` 抽取（page1 至少 3 次、page2 多次）。违反"不要反复读取刷新页面"。正确做法：第一次加载后**把渲染后的 DOM/HTML 存到本地**，之后只在本地分析。
- **机械固定延迟**：等待写死 400/800/1200ms，无随机抖动，节奏规律。应加随机延迟（如 1–4s 随机），模拟人读题速度。
- **猛刷付费墙页面**：为满足"后续页面"，循环 `goto` 了 /3 /4 /5，而这些都是 PLUS 付费内容。对无权限页面高频请求既越权又极易被风控标记。
- **瞬时大批量导出**：几秒内经本地服务 POST 出 50 道完整 Q&A，远超人类阅读速度。

**正确做法（下次务必遵守）**：
1. 只抓**有权限**的免费内容，绝不主动撞付费墙。
2. 每页只加载一次，立刻 `page.content()` 存为本地 HTML 后离线解析；需要多次试验选择器时，在本地 HTML 上做，而不是反复回源。
3. 展开答案/滚动逐题进行，每次操作间加**随机**延迟（人读一题约数秒），绝不 for 循环无间隔点全部。
4. 控制节奏与总量：模拟一个人一晚能看的题量，不要短时间拉全量。
5. 若发现页面异常（验证码/空白/跳转登录），立即停手，不要重试硬刚。

### 6，杂项

- 目录回答“这是哪个能力领域？”；topic 回答“具体考什么？”；tags 回答“它还和什么有关？”

